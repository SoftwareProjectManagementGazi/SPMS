---
phase: 12
plan: 04
plan_id: "12-04"
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - Frontend2/components/lifecycle/mini-metric.tsx
  - Frontend2/components/lifecycle/mini-metric.test.tsx
  - Frontend2/components/lifecycle/overview-subtab.tsx
  - Frontend2/components/lifecycle/overview-subtab.test.tsx
  - Frontend2/components/lifecycle/history-subtab.tsx
  - Frontend2/components/lifecycle/history-subtab.test.tsx
  - Frontend2/components/lifecycle/history-card.tsx
  - Frontend2/components/lifecycle/lifecycle-tab.tsx
  - Frontend2/components/my-tasks/task-row.tsx
  - Frontend2/app/(shell)/projects/[id]/page.tsx
autonomous: true
requirements: [LIFE-03, LIFE-04]
must_haves:
  truths:
    - "LifecycleTab now renders a Tabs primitive with 4 sub-tabs (Genel Bakış / Kilometre Taşları / Geçmiş / Artefaktlar) — Plan 12-04 ships Genel Bakış + Geçmiş, Plans 12-05/06 ship the other two"
    - "Overview sub-tab renders 4 MiniMetric cards (Toplam / Tamamlanan / Devam / İlerleme) for the active phase + 'Faz Özeti' + 'Yaklaşan Teslimler' columns; Kanban variant shows 3 metrics (Lead Time / Cycle Time / WIP)"
    - "Zero-task phase MiniMetric values render '---' in mono font + grey color (LIFE-03)"
    - "History sub-tab renders one HistoryCard per closed phase, each with a Collapsible 'Görev Detayları (N)' that lazy-fetches `GET /api/v1/tasks/project/{id}?phase_id=X&status=done` on first expand (LIFE-04)"
    - "Subsequent expands hit the TanStack Query cache (no second network request) — verified via test-mock call count"
    - "MTTaskRow compact (Phase 11 D-32) is reused inside the Collapsible — task-row.tsx accepts a `compact?: boolean` prop or equivalent density flag"
  artifacts:
    - path: "Frontend2/components/lifecycle/mini-metric.tsx"
      provides: "4-cell metric tile with mono `---` zero-task state"
    - path: "Frontend2/components/lifecycle/overview-subtab.tsx"
      provides: "Overview sub-tab content"
    - path: "Frontend2/components/lifecycle/history-subtab.tsx"
      provides: "History sub-tab listing closed phases as HistoryCards"
    - path: "Frontend2/components/lifecycle/history-card.tsx"
      provides: "Per-phase card with metrics + Collapsible task details + (Plan 12-06 will add Rapor button + EvaluationReportCard)"
  key_links:
    - from: "Frontend2/components/lifecycle/history-subtab.tsx"
      to: "Frontend2/hooks/use-tasks.ts"
      via: "useTasks(projectId, { phase_id, status: 'done' }) with `enabled: collapsibleOpen` flag"
      pattern: "useTasks.*phase_id.*enabled"
    - from: "Frontend2/components/lifecycle/lifecycle-tab.tsx"
      to: "Frontend2/components/lifecycle/overview-subtab.tsx + history-subtab.tsx"
      via: "Tabs primitive switching"
      pattern: "Tabs"
---

<objective>
Wire the Lifecycle tab's 4-sub-tab Tabs primitive and ship the first two sub-tabs (Overview + History). LIFE-03 zero-task metric `---` is delivered through the MiniMetric component. LIFE-04 lazy-fetch on Collapsible expand is delivered through HistorySubTab + HistoryCard. The MTTaskRow compact mode (Phase 11 D-32) gets a small `compact` prop extension here.

Purpose: Plan 12-02 shipped the LifecycleTab outer shell with summary-strip + Phase Gate + canvas, but the sub-tab content was a placeholder. This plan replaces that placeholder with the Tabs primitive and the first two real sub-tabs. Milestones (Plan 12-05) and Artifacts (Plan 12-06) are separate plans because each has a distinct vertical slice (CRUD + Timeline integration / CRUD + EvaluationReport).

Output: 7 new files (4 components + 3 test files) + 3 modified files (lifecycle-tab.tsx replaces placeholder with Tabs, task-row.tsx adds compact prop, projects/[id]/page.tsx pre-fetches milestone data for Plan 12-05's Timeline integration).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-CONTEXT.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-SPEC.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-PATTERNS.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-VALIDATION.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-01-SUMMARY.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-02-SUMMARY.md

<interfaces>
From Plan 12-01 + 12-02:
- `useCycleCounters(projectId) → Map<string, number>`
- `computeNodeStates(workflow, phaseTransitions) → Map<string, NodeState>`
- LifecycleTab outer shell already mounts SummaryStrip + PhaseGateExpand + WorkflowCanvas

Phase 11 D-32 MTTaskRow compact prop (to be added if missing):
```tsx
// Frontend2/components/my-tasks/task-row.tsx
interface TaskRowProps {
  task: Task
  compact?: boolean  // NEW in Plan 12-04 if not already present (RESEARCH Open Question 4)
  // ... existing props
}
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /tasks/project/{id} | Filter by `phase_id` + `status=done`; backend already enforces project-membership read access (Phase 9 baseline) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | I (Information Disclosure) | History Collapsible task list | accept | The `/tasks/project/{id}?phase_id=X&status=done` endpoint already enforces project-membership server-side. Frontend lazy-fetch is read-only with no leakage path. |
</threat_model>

<validation_files>
- `Frontend2/components/lifecycle/mini-metric.test.tsx` — value rendering + LIFE-03 zero-task `---` mono branch
- `Frontend2/components/lifecycle/overview-subtab.test.tsx` — default 4-metric variant; Kanban 3-metric variant; zero-task `---` integration
- `Frontend2/components/lifecycle/history-subtab.test.tsx` — first-expand triggers GET; second-expand hits cache (network mock asserts only 1 GET)
</validation_files>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship MiniMetric primitive + Overview sub-tab (4-metric default + 3-metric Kanban + LIFE-03 `---`)</name>
  <files>
    Frontend2/components/lifecycle/mini-metric.tsx,
    Frontend2/components/lifecycle/mini-metric.test.tsx,
    Frontend2/components/lifecycle/overview-subtab.tsx,
    Frontend2/components/lifecycle/overview-subtab.test.tsx
  </files>
  <read_first>
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md lines 798-818 — MiniMetric anatomy + LIFE-03 zero-task variant
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md lines 280-345 — Overview sub-tab copy table (4-metric default + Kanban 3-metric)
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-CONTEXT.md D-43, D-48, D-59 — zero-task handling + Overview port spec + Kanban variant
    - New_Frontend/src/pages/lifecycle-tab.jsx lines 130-204 — prototype OverviewSubTab + MiniMetric (visual ref, ~200 lines port per D-48)
    - Frontend2/components/project-detail/board-tab.tsx lines 50-80 — memoized derived state pattern from useTasks
    - Frontend2/hooks/use-tasks.ts — list query consumed for `phaseStats` derivation
    - Frontend2/lib/lifecycle/graph-traversal.ts (Plan 12-01) — `computeNodeStates` for active-phase detection
  </read_first>
  <behavior>
    - Test 1 (mini-metric value): renders given numeric value at fontSize 18, weight 600
    - Test 2 (mini-metric label): renders given label at fontSize 10.5, color `var(--fg-muted)`
    - Test 3 (mini-metric `---`): when value is the literal string `'---'`, renders in `font-family: var(--font-mono)` and `color: var(--fg-subtle)`
    - Test 4 (overview default): with non-Kanban project + active phase having tasks → renders 4 MiniMetric cards (Toplam / Tamamlanan / Devam / İlerleme)
    - Test 5 (overview Kanban): with `project.methodology === 'KANBAN'` → renders 3 MiniMetric cards (Ortalama Lead Time / Ortalama Cycle Time / WIP)
    - Test 6 (overview LIFE-03): with active phase having `total === 0` → all 4 MiniMetric values render `'---'`
    - Test 7 (overview Yaklaşan Teslimler): renders the upcoming-deliverables column with milestone+task date entries; empty state shows "Yaklaşan teslim yok"
  </behavior>
  <action>
    **Step 1 — Create `Frontend2/components/lifecycle/mini-metric.tsx`** per UI-SPEC §2 lines 798-818. Component: `export function MiniMetric({ label, value, color, mono }: { label: string; value: string | number; color?: string; mono?: boolean })`. Layout per prototype `lifecycle-tab.jsx:199-204`:
    ```tsx
    <div style={{ padding: 10, background: 'var(--surface-2)', borderRadius: 'var(--radius-sm)', textAlign: 'center' }}>
      <div style={{ fontSize: 18, fontWeight: 600, color: value === '---' ? 'var(--fg-subtle)' : (color ?? 'var(--fg)'), fontFamily: (mono || value === '---') ? 'var(--font-mono)' : undefined }}>{value}</div>
      <div style={{ fontSize: 10.5, color: 'var(--fg-muted)', marginTop: 4 }}>{label}</div>
    </div>
    ```
    The `'---'` literal triggers BOTH the mono font AND grey color regardless of `mono` / `color` props (LIFE-03 contract).

    **Step 2 — Create `Frontend2/components/lifecycle/mini-metric.test.tsx`** with 3 cases per `<behavior>` Tests 1-3.

    **Step 3 — Create `Frontend2/components/lifecycle/overview-subtab.tsx`** porting prototype `lifecycle-tab.jsx:130-204` into TypeScript. `"use client"`. Component signature: `export function OverviewSubTab({ project, workflow, activePhase, tasks }: { project: Project; workflow: WorkflowConfig; activePhase: WorkflowNode | null; tasks: Task[] })`.

    Compute `phaseStats` for `activePhase`: `total = tasks.filter(t => t.phaseId === activePhase.id).length`, `done = ...status === 'done'`, `inProgress = ...status === 'in_progress'`, `progress = total === 0 ? 0 : Math.round(done / total * 100)`. When `total === 0` → all metric `value` props become the literal `'---'`.

    Branch on `project.methodology === 'KANBAN'`:
    - Kanban variant: 3 MiniMetrics — Lead Time + Cycle Time + WIP — values derived from tasks if available, else `'—'` (em-dash). Per UI-SPEC line 1408 + CONTEXT D-59. Layout: 3-column grid, gap 12.
    - Default variant: 4 MiniMetrics — Toplam / Tamamlanan / Devam / İlerleme — layout: 4-column grid, gap 12.

    Below the metrics: 2-column grid layout (`1fr 1fr`, gap 16) hosting two Cards:
    - **Faz Özeti** Card: list each `workflow.nodes[i]` with state from `computeNodeStates`; clickable rows update `activePhase` via lifted callback. Use `StatusDot` primitive. Active row: `bg: var(--accent), weight: 600`.
    - **Yaklaşan Teslimler** Card: list upcoming task `due_date`s + milestones (props provided by `lifecycle-tab.tsx`) with date chips. Empty: "Yaklaşan teslim yok."

    **Step 4 — Create `Frontend2/components/lifecycle/overview-subtab.test.tsx`** with 4 cases per `<behavior>` Tests 4-7. Use the shared RTL fixtures from `Frontend2/components/lifecycle/__tests__/setup.tsx`. For Test 5 (Kanban), set `project.methodology = 'KANBAN'`. For Test 6 (zero-task), supply `tasks=[]`.
  </action>
  <verify>
    <automated>cd Frontend2 && npm run test -- mini-metric overview-subtab</automated>
  </verify>
  <acceptance_criteria>
    - `cd Frontend2 && npm run test -- mini-metric` exits 0
    - `cd Frontend2 && npm run test -- overview-subtab` exits 0
    - `grep "value === '---'\\|'---'" Frontend2/components/lifecycle/mini-metric.tsx` finds the LIFE-03 branch
    - `grep "var(--font-mono)" Frontend2/components/lifecycle/mini-metric.tsx` finds the mono font directive
    - `grep "methodology === 'KANBAN'" Frontend2/components/lifecycle/overview-subtab.tsx` finds the Kanban variant branch
    - `grep "Lead Time\\|Cycle Time\\|WIP" Frontend2/components/lifecycle/overview-subtab.tsx | wc -l` returns >= 3
    - `grep "Toplam\\|Tamamlanan\\|Devam\\|İlerleme" Frontend2/components/lifecycle/overview-subtab.tsx | wc -l` returns >= 4
    - `grep "Yaklaşan Teslim" Frontend2/components/lifecycle/overview-subtab.tsx` finds the upcoming-deliverables column copy
  </acceptance_criteria>
  <done>MiniMetric ships with LIFE-03 `---` mono branch. OverviewSubTab ships with default 4-metric + Kanban 3-metric branches. All 7 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship History sub-tab + HistoryCard with lazy-fetch Collapsible + extend MTTaskRow with compact prop</name>
  <files>
    Frontend2/components/lifecycle/history-subtab.tsx,
    Frontend2/components/lifecycle/history-subtab.test.tsx,
    Frontend2/components/lifecycle/history-card.tsx,
    Frontend2/components/lifecycle/lifecycle-tab.tsx,
    Frontend2/components/my-tasks/task-row.tsx,
    Frontend2/app/(shell)/projects/[id]/page.tsx
  </files>
  <read_first>
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md lines 820-859 — HistoryCard anatomy
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md lines 380-440 — History sub-tab copy table (the `Görev Detayları (N)` Collapsible label)
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-CONTEXT.md D-55, D-56 — lazy-fetch + cache + MTTaskRow reuse decisions
    - New_Frontend/src/pages/lifecycle-tab.jsx lines 280-339 — prototype HistorySubTab + HistoryCard (visual ref)
    - Frontend2/components/my-tasks/task-row.tsx — current MTTaskRow component (Phase 11 D-32). Need to determine whether `compact` prop already exists per RESEARCH Open Question 4
    - Frontend2/hooks/use-tasks.ts:4-10 — parametrized cache-key pattern; `phase_id` + `status` filters create separate cache keys
    - Frontend2/components/primitives/collapsible.tsx — primitive consumed
    - Frontend2/components/lifecycle/lifecycle-tab.tsx (just modified by Plan 12-02) — to be extended with Tabs primitive
    - Frontend2/components/primitives/tabs.tsx — Tabs primitive
  </read_first>
  <behavior>
    - Test 1 (lazy-fetch on first expand): mock `taskService.getByProject` and assert call count starts at 0; click Collapsible → assert call count increments to 1 with params `{ phase_id: 'planning', status: 'done' }`
    - Test 2 (cache on second expand): close Collapsible, reopen → call count stays at 1 (cache hit)
    - Test 3 (Collapsible label): renders `Görev Detayları ({N})` where N is the count from the fetched data
    - Test 4 (empty state): with `tasks: []` → renders "Bu faz için kayıtlı görev bulunamadı."
    - Test 5 (compact rendering): rendered tasks use the `compact` prop on MTTaskRow (verify by source-grep that history-subtab passes `compact={true}`)
  </behavior>
  <action>
    **Step 1 — Inspect `Frontend2/components/my-tasks/task-row.tsx`** to determine whether a `compact` prop or density mode already exists. If yes, use it directly. If no, **add a `compact?: boolean` prop** that toggles padding + font-size as a 1-line conditional (per RESEARCH Open Question 4): `padding: compact ? '6px 10px' : '10px 14px'`, `fontSize: compact ? 11.5 : 13`. Maintain the existing MTTaskRow API surface unchanged otherwise.

    **Step 2 — Create `Frontend2/components/lifecycle/history-card.tsx`** per UI-SPEC §3 lines 820-859. `"use client"`. Component: `export function HistoryCard({ project, phase, summary }: { project: Project; phase: WorkflowNode; summary: PhaseSummary })` where `PhaseSummary = { closedAt: string; durationDays: number; total: number; done: number; moved: number; successPct: number; note?: string }`.

    Layout: Card padding 14, internal gap 10. Header row: phase.name + Badge mono "{durationDays} gün" + (placeholder for the Plan 12-06 "Rapor" button). Below: 4 MiniMetric strip (Toplam / Tamamlanan / Taşınan / Başarı). Below: optional note.

    Two collapsibles inside (the EvaluationReport collapsible is added in Plan 12-06; this plan only ships the "Görev Detayları" collapsible):
    - "Görev Detayları (N)" Collapsible. State: `[open, setOpen] = useState(false)`. The hook `const { data: closedTasks = [] } = useTasks(project.id, { phase_id: phase.id, status: 'done' }, { enabled: open })`. **Critical: the `enabled` flag prevents the network call until first expansion** (LIFE-04 lazy-fetch). Inside the Collapsible: skeleton 3 placeholder rows when `isLoading`; `closedTasks.map(t => <TaskRow key={t.id} task={t} compact/>)` when loaded; empty state when `closedTasks.length === 0`.

    The N in "Görev Detayları (N)" is `closedTasks.length` once loaded; before first open it's `summary.done` from the precomputed PhaseSummary.

    **Step 3 — Create `Frontend2/components/lifecycle/history-subtab.tsx`.** `"use client"`. Component: `export function HistorySubTab({ project, workflow, activity }: { project: Project; workflow: WorkflowConfig; activity: PhaseTransitionEntry[] })`.

    Compute `closedPhases` = workflow.nodes filtered to those that appear as a `source_phase_id` at least once in the activity feed. For each, compute `PhaseSummary` from the activity entries (closedAt = max created_at where source_phase_id matches; durationDays = closedAt - first created_at involving the phase; counts derived from tasks).

    Render: Section "Kapatılmış Fazlar" (N count). Loop `closedPhases.map(p => <HistoryCard key={p.id} project={project} phase={p} summary={...}/>)`. Empty state: "Henüz kapatılmış faz yok."

    **Step 4 — Create `Frontend2/components/lifecycle/history-subtab.test.tsx`** with 5 cases per `<behavior>`. Mock `taskService.getByProject` via `vi.mock`. For Test 2 (cache), close + reopen the Collapsible and assert `mock.calls.length === 1` (the second open hits TanStack cache).

    **Step 5 — MODIFY `Frontend2/components/lifecycle/lifecycle-tab.tsx`.** Replace the placeholder div from Plan 12-02 with a Tabs primitive containing the 4 sub-tabs:
    ```tsx
    const [subTab, setSubTab] = React.useState<'overview' | 'milestones' | 'history' | 'artifacts'>('overview')
    const tabs = isKanban
      ? [{ id: 'overview', label: T('Genel Bakış','Overview') }, { id: 'milestones', label: T('Kilometre Taşları','Milestones') }]
      : [{ id: 'overview', label: T('Genel Bakış','Overview') }, { id: 'milestones', label: T('Kilometre Taşları','Milestones') }, { id: 'history', label: T('Geçmiş','History') }, { id: 'artifacts', label: T('Artefaktlar','Artifacts') }]
    ```
    Below the canvas: `<Tabs tabs={tabs} active={subTab} onChange={setSubTab}/>` then conditionally render:
    - `subTab === 'overview' && <OverviewSubTab .../>`
    - `subTab === 'milestones'` → placeholder "Plan 12-05 lands here." (Plan 12-05 replaces this with `<MilestonesSubTab .../>`)
    - `subTab === 'history' && <HistorySubTab .../>`
    - `subTab === 'artifacts'` → placeholder "Plan 12-06 lands here."

    The Kanban methodology hides History + Artifacts per CONTEXT D-59.

    **Step 6 — MODIFY `Frontend2/app/(shell)/projects/[id]/page.tsx`.** Pre-fetch milestones via `useMilestones(project.id)` from Plan 12-01 and pass them as a `milestones` prop to `<ProjectDetailShell>`. ProjectDetailShell forwards to `<TimelineTab>` (Plan 12-05) and `<LifecycleTab>` (this plan, the milestone column in Overview's Yaklaşan Teslimler). For now, the milestones are only consumed by overview-subtab.tsx if available; the Timeline integration lands in Plan 12-05.
  </action>
  <verify>
    <automated>cd Frontend2 && npm run test -- history-subtab task-row</automated>
  </verify>
  <acceptance_criteria>
    - `cd Frontend2 && npm run test -- history-subtab` exits 0
    - `cd Frontend2 && npm run test -- task-row` exits 0 (existing Phase 11 test still passes; compact prop addition is non-breaking)
    - `grep "compact" Frontend2/components/my-tasks/task-row.tsx` finds the prop addition (or pre-existing density flag confirmed)
    - `grep "compact" Frontend2/components/lifecycle/history-card.tsx` finds the prop forwarded to TaskRow
    - `grep "enabled:.*open\\|enabled:\\s*open" Frontend2/components/lifecycle/history-card.tsx` finds the lazy-fetch guard
    - `grep "phase_id:\\|phase_id\\s*:" Frontend2/components/lifecycle/history-card.tsx` finds the phase_id filter
    - `grep "status: 'done'\\|status:\\s*['\"]done['\"]" Frontend2/components/lifecycle/history-card.tsx` finds the status filter
    - `grep "Görev Detayları" Frontend2/components/lifecycle/history-card.tsx` finds the Collapsible label
    - `grep "Tabs" Frontend2/components/lifecycle/lifecycle-tab.tsx` finds the Tabs primitive
    - `grep "OverviewSubTab\\|HistorySubTab" Frontend2/components/lifecycle/lifecycle-tab.tsx | wc -l` returns >= 2 (both sub-tabs wired)
    - `grep "isKanban\\|methodology === 'KANBAN'" Frontend2/components/lifecycle/lifecycle-tab.tsx` finds the Kanban branch (D-59)
  </acceptance_criteria>
  <done>HistoryCard + HistorySubTab ship with lazy-fetch Collapsible. MTTaskRow has compact prop. LifecycleTab Tabs primitive routes between 4 sub-tabs (Overview + History live in this plan; Milestones + Artifacts placeholder for Plans 12-05/06). All 5 tests green.</done>
</task>

</tasks>

<verification_criteria>
The plan-checker confirms this plan by:
1. Running `cd Frontend2 && npm run test -- mini-metric overview-subtab history-subtab task-row` and observing exit 0
2. Smoke test: open `/projects/{id}?tab=lifecycle&sub=history` (or via the Tabs UI) → see 1 card per closed phase; click Collapsible → tasks load
3. Verify second open of the same Collapsible does NOT re-fetch (cache hit)
4. Smoke test on a project with zero tasks for active phase → MiniMetric shows `---` mono
5. Smoke test on a Kanban project → 3-metric Overview variant + History/Artifacts hidden
</verification_criteria>

<success_criteria>
- LIFE-03 zero-task `---` mono delivered
- LIFE-04 lazy-fetch + cache reuse delivered (network mock asserts only 1 GET on repeat expand)
- 4-sub-tab Tabs primitive shipped (Overview + History live; Milestones + Artifacts placeholders)
- Kanban variant honored per CONTEXT D-59
- MTTaskRow compact prop added without breaking Phase 11 callers
- All RTL test cases green
</success_criteria>

<output>
After completion, create `.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-04-SUMMARY.md` documenting:
- Whether MTTaskRow already had a compact/density prop or it was added (RESEARCH Open Question 4 resolution)
- Confirmation that lazy-fetch test mocks correctly assert call counts (TanStack cache reuse semantics)
- Notes on any deviations from prototype OverviewSubTab port (CONTEXT D-48 ~200-line port)
</output>
