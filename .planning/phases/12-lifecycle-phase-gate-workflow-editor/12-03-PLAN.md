---
phase: 12
plan: 03
plan_id: "12-03"
type: execute
wave: 1
depends_on: ["12-01"]
files_modified:
  - Frontend2/components/lifecycle/criteria-editor-panel.tsx
  - Frontend2/components/lifecycle/criteria-editor-panel.test.tsx
  - Frontend2/components/project-detail/settings-tab.tsx
  - Frontend2/components/project-detail/settings-general-subtab.tsx
autonomous: true
requirements: [LIFE-01]
must_haves:
  truths:
    - "Settings > Yaşam Döngüsü sub-tab renders a left-side phase picker iterating `workflow.nodes` (skipping archived) and a right-side criteria editor for the selected phase (LIFE-01)"
    - "Auto-criteria editor exposes 3 Toggle primitives (`all_tasks_done`, `no_critical_tasks`, `no_blockers`)"
    - "Manual-criteria editor exposes a dynamic list with Input + Add button + per-row delete X"
    - "A sibling block at the top of the sub-tab exposes the project-level `enable_phase_assignment` Toggle that PATCHes independently of the per-phase criteria"
    - "Save → PATCH /projects/{id} with `process_config.phase_completion_criteria[phase_id] = { auto: {...}, manual: [...] }`"
    - "Deep-link `?phase={id}` auto-selects that phase via Next.js useSearchParams + scrollIntoView"
    - "Settings > General `methodology` field is read-only display + Tooltip; cannot be PATCHed via the UI (CONTEXT D-60)"
    - "The `sub === 'lifecycle'` AlertBanner stub in settings-tab.tsx is replaced with `<CriteriaEditorPanel/>`"
  artifacts:
    - path: "Frontend2/components/lifecycle/criteria-editor-panel.tsx"
      provides: "Settings > Yaşam Döngüsü body — 2-column phase picker + editor"
  key_links:
    - from: "Frontend2/components/project-detail/settings-tab.tsx"
      to: "Frontend2/components/lifecycle/criteria-editor-panel.tsx"
      via: "Replaces the sub === 'lifecycle' AlertBanner stub"
      pattern: "CriteriaEditorPanel"
    - from: "Frontend2/components/lifecycle/criteria-editor-panel.tsx"
      to: "Frontend2/hooks/use-criteria-editor.ts"
      via: "Local-state hook from Plan 12-01 (drafts + dirty + save)"
      pattern: "useCriteriaEditor"
    - from: "Frontend2/components/lifecycle/criteria-editor-panel.tsx"
      to: "Next.js useSearchParams"
      via: "Reads ?phase={id} for deep-link auto-scroll"
      pattern: "useSearchParams|searchParams\\.get\\(['\"]phase['\"]\\)"
---

<objective>
Replace the Phase 11 D-11 Settings > Yaşam Döngüsü AlertBanner stub with the real criteria editor panel (LIFE-01) and convert Settings > General's methodology field to a read-only display + Tooltip per CONTEXT D-60.

Purpose: LIFE-01 is the data source for the Phase Gate panel's manual checkbox list (Plan 12-02 reads `process_config.phase_completion_criteria[phase_id]`). Without this plan the Phase Gate is functionally complete but the criteria themselves cannot be authored. The deep-link `?phase={id}` from Plan 12-02's "Kriterleri düzenle →" link lands here.

Output: 2 new files (component + test), 2 modified files (settings-tab.tsx replaces stub, settings-general-subtab.tsx converts methodology to read-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-CONTEXT.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-SPEC.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-PATTERNS.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-VALIDATION.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-01-SUMMARY.md

<interfaces>
From Plan 12-01:
- `useCriteriaEditor(project) → { activePhaseId, setActivePhaseId, drafts, setDraft, dirty, save, reset }`
- `WorkflowConfig`, `WorkflowNode` types

Backend Phase 9 D-06 phase_completion_criteria shape:
```ts
type PhaseCompletionCriteria = Record<string /* phase_id */, {
  auto: { all_tasks_done: boolean; no_critical_tasks: boolean; no_blockers: boolean }
  manual: string[]
}>
```

PATCH `/projects/{id}` body:
```ts
{ process_config: { ...currentProcessConfig, phase_completion_criteria: PhaseCompletionCriteria, enable_phase_assignment?: boolean } }
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → PATCH /projects/{id} | Project update endpoint validates body via Pydantic + RPTA authorization (Phase 9 D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | E (Elevation of Privilege) | Save button | mitigate | Editor disabled + AlertBanner "Yetki yok." when `useTransitionAuthority(project)` returns false (UI-SPEC line 1416). Backend re-validates RPTA authorization on every PATCH. |
| T-12-03-02 | T (Tampering) | Methodology field PATCH | mitigate | Settings > General methodology field is read-only display + Tooltip per CONTEXT D-60. The editable input is REMOVED from the DOM, eliminating client-side tampering at the UI surface. Backend Phase 9 D-29 also enforces no-op behavior. |
| T-12-03-03 | T (Tampering) | Manual criteria string contents | accept | Manual criteria are free-text strings displayed as-is in the Phase Gate; XSS surface is the same as Phase 9 audit log free-text fields, accepted as v2.0 baseline. Future hardening: DOMPurify or strict whitelist rendering. |
</threat_model>

<validation_files>
- `Frontend2/components/lifecycle/criteria-editor-panel.test.tsx` — phase picker + auto toggles + manual add/delete + save round-trip + deep-link `?phase={id}` selection + `enable_phase_assignment` Toggle independent persist
</validation_files>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship criteria-editor-panel.tsx with phase picker + auto/manual editor + enable_phase_assignment Toggle + deep-link support</name>
  <files>
    Frontend2/components/lifecycle/criteria-editor-panel.tsx,
    Frontend2/components/lifecycle/criteria-editor-panel.test.tsx
  </files>
  <read_first>
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md lines 928-986 — CriteriaEditorPanel anatomy + click semantics + auto-scroll on deep-link
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md lines 441-479 — Settings > Yaşam Döngüsü copy table
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-CONTEXT.md D-37 — full criteria editor spec
    - New_Frontend/src/pages/settings.jsx — prototype Settings sidebar layout (visual ref)
    - Frontend2/components/project-detail/settings-columns-subtab.tsx lines 95-148 — canonical draft + commit pattern (the per-phase draft mirrors per-column draft)
    - Frontend2/components/primitives/toggle.tsx — used for the 3 auto-criteria toggles + the `enable_phase_assignment` toggle
    - Frontend2/components/primitives/input.tsx — used for the manual-criterion add input
    - Frontend2/components/primitives/card.tsx — wrapper for both the Toggle row Card and the 2-column editor
    - Frontend2/components/projects/confirm-dialog.tsx — fallback for any save-confirmation needs
    - Frontend2/hooks/use-criteria-editor.ts (Plan 12-01)
    - Frontend2/lib/api-client.ts — axios PATCH helper
  </read_first>
  <behavior>
    - Test 1 (phase picker render): with `workflow.nodes = [planning, execution, closure]`, picker shows 3 rows; archived nodes appear with `(Arşiv)` suffix in `--fg-subtle`
    - Test 2 (auto Toggle persist): toggle `all_tasks_done` ON, click Save → PATCH body contains `process_config.phase_completion_criteria.execution.auto.all_tasks_done === true`
    - Test 3 (manual criterion add): type "QA imza" + click Ekle → manual list now contains "QA imza"; click Save → PATCH body contains `manual: ['QA imza']`
    - Test 4 (manual criterion delete): existing entry "Paydaş onayı alındı" + click X → row removed locally; save → PATCH omits the deleted entry
    - Test 5 (deep-link auto-scroll): mount with `?phase=execution` query → picker auto-selects "execution" + calls `scrollIntoView({ behavior: 'smooth' })` on the row
    - Test 6 (`enable_phase_assignment` independent persist): toggle the top-level Toggle ON → PATCH body contains `process_config.enable_phase_assignment === true` and the `phase_completion_criteria` object is preserved unchanged from prior state
    - Test 7 (round-trip): save → reload component (re-render with new `project` prop) → toggles + manual list reflect saved state
    - Test 8 (Cancel reverts): toggle `all_tasks_done` ON, click İptal → toggle returns to prior value; dirty=false
  </behavior>
  <action>
    **Step 1 — Create `Frontend2/components/lifecycle/criteria-editor-panel.tsx`.** `"use client"`. Imports: React, `useApp`, `Button, Card, Toggle, Input, AlertBanner` from primitives, `useToast` from toast, `useSearchParams` from next/navigation, `useQueryClient` from `@tanstack/react-query`, `useCriteriaEditor` from `@/hooks/use-criteria-editor` (Plan 12-01), `apiClient` from `@/lib/api-client`, `useTransitionAuthority` from `@/hooks/use-transition-authority`, `X, Plus` from lucide-react.

    Component: `export function CriteriaEditorPanel({ project, isArchived }: { project: Project; isArchived: boolean })`.

    **Step 2 — Implement layout** per UI-SPEC §5 lines 928-986.

    Outer Card padding 20 hosting the `enable_phase_assignment` Toggle row:
    ```tsx
    <Card padding={20}>
      <div style={{ display: 'flex', alignItems: 'flex-start', gap: 16 }}>
        <div style={{ flex: 1 }}>
          <h3 style={{ margin: 0, fontSize: 14, fontWeight: 600 }}>{T('Görev–Faz Ataması','Task–Phase Assignment')}</h3>
          <p style={{ margin: '4px 0 0', fontSize: 12.5, color: 'var(--fg-muted)' }}>
            {T('Açıkken görev formuna ve board kartlarına faz alanı eklenir.','When on, the task form and board cards expose a phase field.')}
          </p>
        </div>
        <Toggle on={localEPA} onChange={(next) => { setLocalEPA(next); persistEPA(next) }} disabled={!canEdit}/>
      </div>
    </Card>
    ```
    `localEPA` is `useState(project.processConfig?.enable_phase_assignment ?? false)`. `persistEPA(next)` PATCHes `/projects/{project.id}` with `{ process_config: { ...project.processConfig, enable_phase_assignment: next } }` and invalidates `['project', project.id]`. **The persist call MUST preserve `phase_completion_criteria` from `project.processConfig` to satisfy independent-persist test 6.**

    Below the outer Card: 2-column grid `220px 1fr` gap 24. Left = phase picker; right = active phase editor.

    **Phase picker** iterates `workflow.nodes`, skipping `n.isArchived` only when displaying as un-editable. Each row: `&lt;button onClick={() => setActivePhaseId(n.id)} ref={n.id === activePhaseId ? scrollAnchorRef : undefined} style={{ display: 'block', width: '100%', textAlign: 'left', padding: '8px 12px', background: n.id === activePhaseId ? 'var(--accent)' : 'transparent', borderLeft: n.id === activePhaseId ? '2px solid var(--primary)' : '2px solid transparent' }}&gt;{n.name}{n.isArchived && ` (${T('Arşiv','Archive')})`}&lt;/button&gt;`. Hover: `bg: var(--surface-2)`.

    **Right editor:** if selected phase is archived, render `&lt;AlertBanner tone="info"&gt;{T('Arşivlenmiş fazların kriterleri değiştirilemez.','Archived phase criteria are not editable.')}&lt;/AlertBanner&gt;` and bail. Otherwise:

    Section "OTOMATİK KRİTERLER":
    - Row for `all_tasks_done`: Toggle bound to `drafts[activePhaseId]?.auto.all_tasks_done`, label `T('Tüm görevler tamamlandı','All tasks completed')`.
    - Row for `no_critical_tasks`: Toggle, label `T('Kritik görev kalmadı','No critical tasks open')`.
    - Row for `no_blockers`: Toggle, label `T('Blocker kalmadı','No blockers open')`.

    Section "MANUEL KRİTERLER":
    - List `drafts[activePhaseId]?.manual ?? []` as rows: `&lt;li&gt;• {value}&lt;button onClick={() => removeManual(idx)}&gt;&lt;X size={12}/&gt;&lt;/button&gt;&lt;/li&gt;`.
    - Add row: `&lt;Input value={addInput} onChange={...}/&gt;&lt;Button onClick={addManual} disabled={!addInput.trim()}&gt;{T('Ekle','Add')}&lt;/Button&gt;`.

    Bottom row: `&lt;Button tone="ghost" onClick={reset}&gt;{T('İptal','Cancel')}&lt;/Button&gt;&lt;Button tone="primary" onClick={save} disabled={!dirty || !canEdit}&gt;{T('Kaydet','Save')}&lt;/Button&gt;`.

    **Step 3 — Implement deep-link support** per UI-SPEC line 976-977. `const searchParams = useSearchParams(); const deepLinkPhase = searchParams.get('phase')`. `React.useEffect(() => { if (deepLinkPhase && workflow.nodes.find(n => n.id === deepLinkPhase)) { setActivePhaseId(deepLinkPhase); setTimeout(() => scrollAnchorRef.current?.scrollIntoView({ behavior: 'smooth' }), 50) } }, [deepLinkPhase])`.

    **Step 4 — Implement save** via the `useCriteriaEditor` hook from Plan 12-01:
    ```tsx
    async function save() {
      try {
        const next = { ...drafts }
        await apiClient.patch('/projects/' + project.id, { process_config: { ...project.processConfig, phase_completion_criteria: next, enable_phase_assignment: localEPA } })
        qc.invalidateQueries({ queryKey: ['project', project.id] })
        showToast({ variant: 'success', message: T('Kriterler kaydedildi.','Criteria saved.') })
      } catch (err) {
        showToast({ variant: 'error', message: backendErrorMessage(err) ?? T('Kayıt sırasında hata oluştu.','Save failed.') })
      }
    }
    ```
    `backendErrorMessage` is the helper from `Frontend2/components/project-detail/settings-columns-subtab.tsx:49-53`. Copy it inline at the top of the file (same pattern as PATTERNS.md line 304).

    **Step 5 — Implement permission gate.** `const canEdit = useTransitionAuthority(project) && !isArchived`. When false: render top AlertBanner `tone="warning"` "Bu sekmeyi düzenleme yetkiniz yok. Görüntüleme modunda."; disable all Toggles, Input, Buttons.

    **Step 6 — Empty workflow guard.** When `workflow.nodes.length === 0`: render full-panel `&lt;AlertBanner tone="info"&gt;{T('Bu projede aktif workflow tanımlanmamış.','No active workflow for this project.')}&lt;/AlertBanner&gt;` + "Workflow editörünü aç" link Button.

    **Step 7 — Create `Frontend2/components/lifecycle/criteria-editor-panel.test.tsx`** with all 8 cases from `<behavior>`. Use `vi.mock` for `apiClient.patch` to capture the body. For Test 5 (deep-link), mock `useSearchParams` to return `new URLSearchParams('?phase=execution')`. For Test 6 (independent persist), seed `project.processConfig.phase_completion_criteria = { execution: { auto: { all_tasks_done: true, ...}, manual: ['existing'] } }` and assert that toggling `enable_phase_assignment` does NOT remove `execution` from the saved body.
  </action>
  <verify>
    <automated>cd Frontend2 && npm run test -- criteria-editor-panel</automated>
  </verify>
  <acceptance_criteria>
    - `cd Frontend2 && npm run test -- criteria-editor-panel` exits 0
    - `grep "useSearchParams\\|searchParams\\.get\\(['\"]phase['\"]" Frontend2/components/lifecycle/criteria-editor-panel.tsx` finds the deep-link read
    - `grep "scrollIntoView" Frontend2/components/lifecycle/criteria-editor-panel.tsx` finds the auto-scroll
    - `grep "all_tasks_done\\|no_critical_tasks\\|no_blockers" Frontend2/components/lifecycle/criteria-editor-panel.tsx | wc -l` returns >= 3 (all 3 auto-criteria toggles wired)
    - `grep "enable_phase_assignment" Frontend2/components/lifecycle/criteria-editor-panel.tsx` finds the project-level toggle
    - `grep "phase_completion_criteria" Frontend2/components/lifecycle/criteria-editor-panel.tsx` finds the PATCH key
    - `grep "useTransitionAuthority\\|canEdit" Frontend2/components/lifecycle/criteria-editor-panel.tsx` finds the permission gate
    - `grep -c "it\\(" Frontend2/components/lifecycle/criteria-editor-panel.test.tsx` returns >= 8 (all 8 test cases)
  </acceptance_criteria>
  <done>CriteriaEditorPanel renders the full Settings > Yaşam Döngüsü body. Round-trip persistence works. Deep-link auto-selects + scrolls. `enable_phase_assignment` persists independently of per-phase criteria. Permission gate hides edit controls. All 8 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire criteria-editor into settings-tab.tsx + convert methodology field to read-only in settings-general-subtab.tsx</name>
  <files>
    Frontend2/components/project-detail/settings-tab.tsx,
    Frontend2/components/project-detail/settings-general-subtab.tsx
  </files>
  <read_first>
    - Frontend2/components/project-detail/settings-tab.tsx — current 4-sub-tab shell with the `sub === "lifecycle"` AlertBanner stub (look around lines 100-110)
    - Frontend2/components/project-detail/settings-general-subtab.tsx — current methodology field implementation (look for `<select>` or `<Input>` for `methodology`)
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-CONTEXT.md D-60 — methodology read-only spec
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md lines 1417-1417 — Settings > General methodology field "read-only by design" entry in the State table
    - Frontend2/components/workflow-editor/tooltip.tsx (Plan 12-01) — Tooltip primitive
    - Frontend2/components/lifecycle/criteria-editor-panel.tsx (just created in this plan)
  </read_first>
  <behavior>
    - Test 1 (settings-tab swap): rendering `<SettingsTab project={project}/>` with sub="lifecycle" shows the criteria editor panel, NOT the AlertBanner stub
    - Test 2 (methodology read-only): rendering `<SettingsGeneralSubtab project={project}/>` shows methodology as a non-interactive value with a Tooltip on a small info icon; there is NO `<select>` or editable `<input>` for methodology
  </behavior>
  <action>
    **Step 1 — MODIFY `Frontend2/components/project-detail/settings-tab.tsx`.** Find the `sub === "lifecycle"` branch (current shape per UI-SPEC line 103):
    ```tsx
    {sub === "lifecycle" && (
      <AlertBanner tone="info">
        {lang === "tr" ? "Bu sekme Faz 12'de aktive edilecek." : "This tab will be activated in Phase 12."}
      </AlertBanner>
    )}
    ```
    Replace with:
    ```tsx
    {sub === "lifecycle" && (
      <CriteriaEditorPanel project={project} isArchived={isArchived}/>
    )}
    ```
    Add the import at the top: `import { CriteriaEditorPanel } from "@/components/lifecycle/criteria-editor-panel"`.

    **Step 2 — MODIFY `Frontend2/components/project-detail/settings-general-subtab.tsx`.** Find the methodology field (likely a `<select>` or labeled `<Input>` on a row). Replace the editable element with a read-only display:
    ```tsx
    <div style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
      <span style={{ fontSize: 13, fontWeight: 500 }}>{methodologyLabel(project.methodology, lang)}</span>
      <Tooltip text={T('Metodoloji proje oluşturulduğu an sabittir. Değiştirmek için yeni proje oluşturun.','Methodology is fixed at project creation. Create a new project to switch methodology.')}>
        <Info size={14} aria-label={T('Bilgi','Info')}/>
      </Tooltip>
    </div>
    ```
    Where `methodologyLabel(methodology, lang)` returns the localized label (e.g., `'SCRUM' → 'Scrum'`, `'WATERFALL' → 'Şelale' / 'Waterfall'`). Add the imports: `import { Tooltip } from "@/components/workflow-editor/tooltip"` and `import { Info } from "lucide-react"`.

    Remove any `onChange` handler, dropdown options array, or PATCH call that previously targeted methodology. The field is now strictly informational.

    Verify by source-grep that there is no element with `name="methodology"` or onChange handler updating methodology after the modification.
  </action>
  <verify>
    <automated>cd Frontend2 && npm run test -- settings-tab settings-general-subtab</automated>
  </verify>
  <acceptance_criteria>
    - `cd Frontend2 && npm run test -- settings-tab` exits 0 (existing test from Phase 11 + new criteria-editor branch)
    - `grep "CriteriaEditorPanel" Frontend2/components/project-detail/settings-tab.tsx` finds the import + usage
    - `grep "Bu sekme Faz 12'de aktive edilecek" Frontend2/components/project-detail/settings-tab.tsx | wc -l` returns 0 (stub copy gone)
    - `grep "name=\"methodology\"\\|onChange.*methodology" Frontend2/components/project-detail/settings-general-subtab.tsx | wc -l` returns 0 (no editable methodology field)
    - `grep "Tooltip" Frontend2/components/project-detail/settings-general-subtab.tsx` finds the read-only-with-tooltip pattern
    - `grep "Metodoloji proje oluşturulduğu an sabittir" Frontend2/components/project-detail/settings-general-subtab.tsx` finds the Turkish tooltip copy
  </acceptance_criteria>
  <done>settings-tab.tsx routes lifecycle sub-tab to CriteriaEditorPanel. settings-general-subtab.tsx shows methodology as read-only display + Tooltip. No editable methodology field remains.</done>
</task>

</tasks>

<verification_criteria>
The plan-checker confirms this plan by:
1. Running `cd Frontend2 && npm run test -- criteria-editor-panel settings-tab` and observing exit 0
2. Verifying the AlertBanner stub copy "Bu sekme Faz 12'de aktive edilecek." is gone from settings-tab.tsx
3. Confirming the methodology field is no longer editable in settings-general-subtab.tsx
4. Smoke test: open `/projects/{id}?tab=settings&sub=lifecycle` — should show the phase picker + criteria editor
5. Smoke test: open `/projects/{id}?tab=settings&sub=lifecycle&phase=execution` — should auto-select "execution" phase
</verification_criteria>

<success_criteria>
- LIFE-01 fully delivered
- D-60 methodology read-only delivered
- All RTL test cases green
- `enable_phase_assignment` persistence is independent of per-phase criteria persistence
- Deep-link `?phase={id}` auto-selects + scrolls
</success_criteria>

<output>
After completion, create `.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-03-SUMMARY.md` documenting:
- Confirmation that the deep-link query parameter `?phase={id}` round-trips from Phase Gate → Settings > Yaşam Döngüsü
- Whether Phase 9 D-29 backend no-op behavior was verified (the methodology read-only field is the UI defense; backend is the second defense)
</output>
