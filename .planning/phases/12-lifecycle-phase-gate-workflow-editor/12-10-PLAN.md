---
phase: 12
plan: 10
plan_id: "12-10"
type: execute
wave: 4
depends_on: ["12-01", "12-02", "12-03", "12-04", "12-05", "12-06", "12-07", "12-08", "12-09"]
files_modified:
  - Frontend2/lib/lifecycle/presets.ts
  - Frontend2/lib/lifecycle/presets.test.ts
  - Frontend2/components/workflow-editor/editor-page.tsx
  - Frontend2/components/workflow-editor/editor-page.test.tsx
  - Frontend2/components/workflow-editor/preset-menu.tsx
  - Frontend2/components/workflow-editor/preset-menu.test.tsx
  - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UAT-CHECKLIST.md
autonomous: false
requirements: [EDIT-07]
must_haves:
  truths:
    - "9 preset workflows defined in `lib/lifecycle/presets.ts`: Scrum / Waterfall / Kanban / Iterative / V-Model / Spiral / Incremental / Evolutionary / RAD"
    - "Each preset passes `validateWorkflow()` with zero errors (per EDIT-07 acceptance)"
    - "Editor 'Şablon Yükle' menu shows the 3 NEW preset entries (Artırımlı/Evrimsel/RAD) per EDIT-07 + the 6 existing presets for completeness"
    - "Selecting a preset prompts ConfirmDialog 'Mevcut değişiklikler kaybolacak…' if dirty=true; on confirm, canvas swaps to the new workflow + dirty=false"
    - "Cycle counter badge appears end-to-end (Plan 12-08 wired the visibility; this plan verifies invalidation on phase-transition success closes the loop)"
    - "12-UAT-CHECKLIST.md is created with one row per LIFE-* and EDIT-* requirement (14 total) with click-through steps + pass/fail checkbox"
    - "Visual fidelity polish: any oklch token regressions, accessibility issues, or animation budget overruns identified during Plans 12-02..09 are resolved"
    - "Methodology read-only field (Plan 12-03 D-60) verified end-to-end via UAT checklist row"
  artifacts:
    - path: "Frontend2/lib/lifecycle/presets.ts"
      provides: "9 preset workflow configs + resolvePreset() helper"
      contains: "export const PRESETS_BY_ID"
    - path: "Frontend2/components/workflow-editor/preset-menu.tsx"
      provides: "Şablon Yükle dropdown trigger + ConfirmDialog flow"
    - path: ".planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UAT-CHECKLIST.md"
      provides: "14-row manual UAT checklist"
  key_links:
    - from: "Frontend2/lib/lifecycle/presets.ts"
      to: "Frontend2/lib/lifecycle/workflow-validators.ts (Plan 12-01)"
      via: "Each preset must pass validateWorkflow with zero errors (presets.test.ts asserts this)"
      pattern: "validateWorkflow"
    - from: "Frontend2/components/workflow-editor/preset-menu.tsx"
      to: "Frontend2/components/workflow-editor/editor-page.tsx"
      via: "Mounted in toolbar; on select calls editor's `applyPreset(presetId)` handler"
      pattern: "PresetMenu"
---

<objective>
Ship the 3 new preset templates (EDIT-07: Incremental / Evolutionary / RAD) plus the 6 existing prototypes (Scrum / Waterfall / Kanban / Iterative / V-Model / Spiral) as a single `presets.ts` lookup table. Wire the editor's "Şablon Yükle" menu to swap the canvas via ConfirmDialog. Verify cycle counter end-to-end (Plan 12-08 wired the visibility; this plan verifies invalidation closes the loop). Generate the 14-row manual UAT checklist artifact.

This is the final Phase 12 plan. After this, all 14 LIFE-* + EDIT-* requirements are delivered, the manual UAT artifact exists, and the phase is ready for `/gsd-verify-work`.

Output: 2 new files (presets.ts + preset-menu.tsx + tests) + 1 modified file (editor-page.tsx wires preset-menu) + 1 new manual artifact (12-UAT-CHECKLIST.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-CONTEXT.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-SPEC.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-RESEARCH.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-PATTERNS.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-VALIDATION.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-01-SUMMARY.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-02-SUMMARY.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-03-SUMMARY.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-04-SUMMARY.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-05-SUMMARY.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-06-SUMMARY.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-07-SUMMARY.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-08-SUMMARY.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-09-SUMMARY.md

<interfaces>
From Plan 12-01:
- `validateWorkflow(wf) → { errors, warnings }`
- `WorkflowConfig`, `WorkflowMode`, `WorkflowNode`, `WorkflowEdge`, `WorkflowGroup` types

From `New_Frontend/src/data.jsx`:
- `DEFAULT_LIFECYCLES` (Scrum / Waterfall / Kanban defaults — port to TypeScript)
- `EXTRA_LIFECYCLES` (V-Model + Spiral — port to TypeScript)

NEW (this plan): Incremental / Evolutionary / RAD presets — designed from scratch per CONTEXT D-01 + EDIT-07 acceptance.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User to preset menu | Pure client-side UI swap; backend re-validates via Pydantic on save |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10-01 | T (Tampering) | Preset workflow JSON | mitigate | Each preset must pass `validateWorkflow()` with zero errors at unit-test time. Backend Pydantic re-validates on save (Phase 9 D-54/D-55). Cannot tamper a preset to bypass backend rules. |
</threat_model>

<validation_files>
- `Frontend2/lib/lifecycle/presets.test.ts` — 9 cases: each preset passes `validateWorkflow()` with zero errors
- `Frontend2/components/workflow-editor/preset-menu.test.tsx` — menu opens; clicking entry triggers ConfirmDialog when dirty; onConfirm swaps canvas
- `Frontend2/components/workflow-editor/editor-page.test.tsx` — extended with `preset-replace` end-to-end case (ConfirmDialog flow + canvas swap + Save)
</validation_files>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship presets.ts with 9 entries + presets.test.ts validating each</name>
  <files>
    Frontend2/lib/lifecycle/presets.ts,
    Frontend2/lib/lifecycle/presets.test.ts
  </files>
  <read_first>
    - New_Frontend/src/data.jsx — DEFAULT_LIFECYCLES + EXTRA_LIFECYCLES (visual + structural reference for the 6 existing presets)
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-CONTEXT.md D-01 (sequencing) + REQUIREMENTS.md EDIT-07 (Artırımlı/Evrimsel/RAD spec)
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-PATTERNS.md `presets.ts` row — methodology-matrix.ts lookup-table pattern
    - Frontend2/lib/methodology-matrix.ts lines 17-52 — canonical lookup-table shape
    - Frontend2/lib/lifecycle/workflow-validators.ts (Plan 12-01) — used in test assertions
    - Frontend2/lib/audit-formatter.ts — pure-logic module header convention
  </read_first>
  <behavior>
    - Test 1-9 (each preset valid): for each `presetId in ['scrum','waterfall','kanban','iterative','vmodel','spiral','incremental','evolutionary','rad']`, calling `validateWorkflow(PRESETS_BY_ID[presetId])` returns `errors: []`
    - Test 10 (resolvePreset): `resolvePreset('incremental')` returns the same object as `PRESETS_BY_ID.incremental`
    - Test 11 (no shared references): each preset is a deep-cloneable plain object (no shared `nodes` array references between presets)
  </behavior>
  <action>
    Create `Frontend2/lib/lifecycle/presets.ts`. Open with the pure-logic header per `audit-formatter.ts` convention:
    ```ts
    // Pure preset workflow definitions (Phase 12 Plan 12-10).
    //
    // This module has ZERO React imports so it stays unit-testable in vitest
    // without jsdom. The React consumers live in
    // Frontend2/components/workflow-editor/preset-menu.tsx and
    // Frontend2/components/workflow-editor/editor-page.tsx.
    
    import type { WorkflowConfig } from "@/services/lifecycle-service"
    
    export type PresetId = 'scrum' | 'waterfall' | 'kanban' | 'iterative' | 'vmodel' | 'spiral' | 'incremental' | 'evolutionary' | 'rad'
    ```

    Define each of the 9 preset configs.

    **Scrum** (port from `DEFAULT_LIFECYCLES.scrum`):
    ```ts
    const SCRUM: WorkflowConfig = {
      mode: 'flexible',
      nodes: [
        { id: 'planning', name: 'Planlama', x: 50, y: 100, isInitial: true },
        { id: 'execution', name: 'Yürütme', x: 250, y: 100 },
        { id: 'review', name: 'Gözden Geçirme', x: 450, y: 100 },
        { id: 'closure', name: 'Kapanış', x: 650, y: 100, isFinal: true }
      ],
      edges: [
        { id: 'e1', source: 'planning', target: 'execution', type: 'flow' },
        { id: 'e2', source: 'execution', target: 'review', type: 'flow' },
        { id: 'e3', source: 'review', target: 'closure', type: 'flow' }
      ],
      groups: []
    }
    ```

    **Waterfall** (port from `DEFAULT_LIFECYCLES.waterfall`): mode `sequential-locked`, nodes `req → design → impl → test → maint`.

    **Kanban** (port from `DEFAULT_LIFECYCLES.kanban`): mode `continuous`, single node "Sürekli Akış" with `isInitial: true, isFinal: true`.

    **Iterative**: mode `flexible`, 4 nodes with cyclic flow.

    **V-Model** (port from `EXTRA_LIFECYCLES.vmodel`): mode `flexible`, 7-node V shape with vertical verification edges (`type: 'verification'`). Nodes: `requirements → system_design → module_design → coding`, with verification edges `module_design → unit_test`, `system_design → integration_test`, `requirements → acceptance_test`. Use the prototype's exact node coordinates from `EXTRA_LIFECYCLES`.

    **Spiral** (port from `EXTRA_LIFECYCLES.spiral`): mode `flexible`, with feedback edges (`type: 'feedback'`).

    **Incremental** (NEW per EDIT-07): mode `flexible`, 5 nodes representing sprint-like increments where the same flow is repeated:
    ```ts
    const INCREMENTAL: WorkflowConfig = {
      mode: 'flexible',
      nodes: [
        { id: 'planning', name: 'Planlama', x: 50, y: 100, isInitial: true },
        { id: 'increment_1', name: 'Artırım 1', x: 250, y: 100 },
        { id: 'increment_2', name: 'Artırım 2', x: 450, y: 100 },
        { id: 'integration', name: 'Bütünleştirme', x: 650, y: 100 },
        { id: 'release', name: 'Yayın', x: 850, y: 100, isFinal: true }
      ],
      edges: [
        { id: 'e1', source: 'planning', target: 'increment_1', type: 'flow' },
        { id: 'e2', source: 'increment_1', target: 'increment_2', type: 'flow' },
        { id: 'e3', source: 'increment_2', target: 'integration', type: 'flow' },
        { id: 'e4', source: 'integration', target: 'release', type: 'flow' },
        { id: 'fb1', source: 'increment_2', target: 'increment_1', type: 'feedback', label: 'Geri besleme' }
      ],
      groups: []
    }
    ```

    **Evolutionary** (NEW per EDIT-07): mode `flexible`, prototype-based evolution with feedback loops:
    ```ts
    const EVOLUTIONARY: WorkflowConfig = {
      mode: 'flexible',
      nodes: [
        { id: 'analysis', name: 'Analiz', x: 50, y: 100, isInitial: true },
        { id: 'prototype', name: 'Prototip', x: 250, y: 100 },
        { id: 'feedback_collection', name: 'Geri Bildirim', x: 450, y: 100 },
        { id: 'evolution', name: 'Evrim', x: 650, y: 100 },
        { id: 'release', name: 'Yayın', x: 850, y: 100, isFinal: true }
      ],
      edges: [
        { id: 'e1', source: 'analysis', target: 'prototype', type: 'flow' },
        { id: 'e2', source: 'prototype', target: 'feedback_collection', type: 'flow' },
        { id: 'e3', source: 'feedback_collection', target: 'evolution', type: 'flow' },
        { id: 'e4', source: 'evolution', target: 'release', type: 'flow' },
        { id: 'fb1', source: 'evolution', target: 'prototype', type: 'feedback', label: 'Yeniden prototipleme' }
      ],
      groups: []
    }
    ```

    **RAD** (NEW per EDIT-07): mode `flexible`, rapid application development with parallel tracks:
    ```ts
    const RAD: WorkflowConfig = {
      mode: 'flexible',
      nodes: [
        { id: 'planning', name: 'İş Modelleme', x: 50, y: 100, isInitial: true },
        { id: 'data_modeling', name: 'Veri Modelleme', x: 250, y: 50 },
        { id: 'process_modeling', name: 'Süreç Modelleme', x: 250, y: 150 },
        { id: 'application_gen', name: 'Uygulama Üretimi', x: 450, y: 100 },
        { id: 'testing', name: 'Test ve Devreye Alma', x: 650, y: 100, isFinal: true }
      ],
      edges: [
        { id: 'e1', source: 'planning', target: 'data_modeling', type: 'flow' },
        { id: 'e2', source: 'planning', target: 'process_modeling', type: 'flow' },
        { id: 'e3', source: 'data_modeling', target: 'application_gen', type: 'flow' },
        { id: 'e4', source: 'process_modeling', target: 'application_gen', type: 'flow' },
        { id: 'e5', source: 'application_gen', target: 'testing', type: 'flow' }
      ],
      groups: []
    }
    ```

    Export the lookup table:
    ```ts
    export const PRESETS_BY_ID: Record<PresetId, WorkflowConfig> = {
      scrum: SCRUM,
      waterfall: WATERFALL,
      kanban: KANBAN,
      iterative: ITERATIVE,
      vmodel: VMODEL,
      spiral: SPIRAL,
      incremental: INCREMENTAL,
      evolutionary: EVOLUTIONARY,
      rad: RAD
    }
    
    export function resolvePreset(id: PresetId): WorkflowConfig {
      // Deep clone so consumers can mutate without affecting the preset
      return JSON.parse(JSON.stringify(PRESETS_BY_ID[id]))
    }
    
    export const PRESET_LABELS_TR: Record<PresetId, string> = {
      scrum: 'Scrum',
      waterfall: 'Şelale',
      kanban: 'Kanban',
      iterative: 'Yinelemeli',
      vmodel: 'V-Modeli',
      spiral: 'Spiral',
      incremental: 'Artırımlı',
      evolutionary: 'Evrimsel',
      rad: 'RAD'
    }
    
    export const PRESET_LABELS_EN: Record<PresetId, string> = {
      scrum: 'Scrum',
      waterfall: 'Waterfall',
      kanban: 'Kanban',
      iterative: 'Iterative',
      vmodel: 'V-Model',
      spiral: 'Spiral',
      incremental: 'Incremental',
      evolutionary: 'Evolutionary',
      rad: 'RAD'
    }
    ```

    Create `Frontend2/lib/lifecycle/presets.test.ts` with 11 cases. The first 9 cases iterate the keys of `PRESETS_BY_ID` and assert `validateWorkflow(PRESETS_BY_ID[id]).errors.length === 0`. Test 10 asserts `resolvePreset('incremental')` deep-equals `PRESETS_BY_ID.incremental`. Test 11 asserts `resolvePreset('scrum') !== PRESETS_BY_ID.scrum` (deep-clone enforces no shared reference).
  </action>
  <verify>
    <automated>cd Frontend2 && npm run test -- presets</automated>
  </verify>
  <acceptance_criteria>
    - `cd Frontend2 && npm run test -- presets` exits 0
    - `grep "PRESETS_BY_ID" Frontend2/lib/lifecycle/presets.ts` finds the lookup table
    - `grep "scrum.*waterfall.*kanban.*iterative.*vmodel.*spiral.*incremental.*evolutionary.*rad" Frontend2/lib/lifecycle/presets.ts | wc -l` returns >= 1 (or alternative: count entries in PRESETS_BY_ID equals 9)
    - `node -e "const m = require('./Frontend2/lib/lifecycle/presets.ts'); /* check 9 entries */ "` (alternative grep approach: `grep -E "^\\s*(scrum|waterfall|kanban|iterative|vmodel|spiral|incremental|evolutionary|rad):" Frontend2/lib/lifecycle/presets.ts | wc -l` returns 9)
    - `grep "import.*react" Frontend2/lib/lifecycle/presets.ts | wc -l` returns 0 (pure module)
    - All 11 test cases pass
  </acceptance_criteria>
  <done>9 preset workflows shipped. Each passes validateWorkflow. resolvePreset deep-clones. Test suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship preset-menu.tsx + wire into editor-page.tsx with ConfirmDialog flow</name>
  <files>
    Frontend2/components/workflow-editor/preset-menu.tsx,
    Frontend2/components/workflow-editor/preset-menu.test.tsx,
    Frontend2/components/workflow-editor/editor-page.tsx,
    Frontend2/components/workflow-editor/editor-page.test.tsx
  </files>
  <read_first>
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md lines 1118-1144 — toolbar layout (template dropdown placement)
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-CONTEXT.md D-29 + UI-SPEC dirty-save copy lines 725-734 — ConfirmDialog "Mevcut değişiklikler kaybolacak…"
    - Frontend2/components/projects/confirm-dialog.tsx — Phase 10 D-25
    - Frontend2/lib/lifecycle/presets.ts (just created)
    - Frontend2/components/workflow-editor/editor-page.tsx (Plans 12-07 + 08 + 09 already shipped)
    - Frontend2/components/primitives/badge.tsx — for the current-template Badge
  </read_first>
  <behavior>
    - Test 1 (menu open/close): clicking the "Şablon Yükle" toolbar trigger opens a dropdown listing all 9 presets; clicking outside closes
    - Test 2 (dirty=true triggers ConfirmDialog): with `dirty=true`, clicking "Artırımlı" opens ConfirmDialog with copy "Mevcut değişiklikler kaybolacak…"
    - Test 3 (cancel keeps current canvas): clicking "Vazgeç" closes dialog without changing workflow
    - Test 4 (confirm swaps canvas): clicking "Devam Et" calls `onApplyPreset('incremental')` which swaps the workflow to the Incremental preset; dirty resets to false ONLY after the next save
    - Test 5 (dirty=false skips dialog): with `dirty=false`, clicking a preset entry directly swaps the canvas without ConfirmDialog
    - Test 6 (current-preset Badge): the current workflow's preset id (if matchable) renders as a Badge "Şablon: {label}" in the toolbar
    - Test 7 (editor-page integration): selecting "Artırımlı" from preset-menu, then clicking Save → backend receives the Incremental workflow shape
  </behavior>
  <action>
    **Step 1 — Create `Frontend2/components/workflow-editor/preset-menu.tsx`.** `"use client"`. Component: `export function PresetMenu({ currentPresetId, dirty, onApply }: { currentPresetId: PresetId | null; dirty: boolean; onApply: (id: PresetId) => void })`.

    Layout: trigger Button "Şablon: {label}" + dropdown popover. State: `[open, setOpen] = useState(false)`, `[pendingPreset, setPendingPreset] = useState<PresetId | null>(null)`.

    Trigger Button: `<Button onClick={() => setOpen(o => !o)}>{T('Şablon Yükle','Load Template')}</Button>`. Optionally render the current preset Badge mono next to it: `currentPresetId ? <Badge tone="mono" size="xs">{labelFor(currentPresetId)}</Badge> : null`.

    Dropdown content (when `open`): list each preset:
    ```tsx
    {Object.keys(PRESETS_BY_ID).map((id) => (
      <button key={id} onClick={() => {
        if (dirty) { setPendingPreset(id as PresetId) }
        else { onApply(id as PresetId); setOpen(false) }
      }} style={{ width: '100%', textAlign: 'left', padding: '8px 12px' }}>
        {labelFor(id as PresetId)}
      </button>
    ))}
    ```

    ConfirmDialog when `pendingPreset !== null`:
    ```tsx
    <ConfirmDialog
      open={pendingPreset !== null}
      title={T('Şablon Yüklenecek','Load Template')}
      body={T('Mevcut değişiklikler kaybolacak. Devam etmek istiyor musunuz?','Current changes will be discarded. Continue?')}
      confirmLabel={T('Devam Et','Continue')}
      confirmTone="danger"
      onCancel={() => setPendingPreset(null)}
      onConfirm={() => { onApply(pendingPreset!); setPendingPreset(null); setOpen(false) }}
    />
    ```

    Click-outside dismiss for the dropdown via mousedown listener.

    `labelFor(id)` returns the localized string from `PRESET_LABELS_TR` / `PRESET_LABELS_EN`.

    **Step 2 — Create `Frontend2/components/workflow-editor/preset-menu.test.tsx`** with 6 cases per `<behavior>` Tests 1-6.

    **Step 3 — MODIFY `Frontend2/components/workflow-editor/editor-page.tsx`** to mount `<PresetMenu/>` in the toolbar (replacing the Plan 12-07 placeholder). Wire the apply handler:
    ```tsx
    const applyPreset = React.useCallback((id: PresetId) => {
      const next = resolvePreset(id)
      history.push(workflow)  // record current state for undo
      setWorkflow(next)
      setDirty(true)
      setSelected(null)
    }, [history, workflow])
    
    // In toolbar JSX:
    <PresetMenu currentPresetId={detectCurrentPresetId(workflow)} dirty={dirty} onApply={applyPreset}/>
    ```

    `detectCurrentPresetId(workflow)`: helper that compares the current workflow shape to each preset (compare nodes count + edge count + mode); returns the matching PresetId or null. This is best-effort (custom workflows return null).

    **Step 4 — UPDATE `Frontend2/components/workflow-editor/editor-page.test.tsx`** with the integration case Test 7. Mount editor, simulate preset menu open + select "incremental", confirm dialog, then click Save and assert the PATCH body contains the Incremental workflow shape (`nodes` includes `increment_1`).
  </action>
  <verify>
    <automated>cd Frontend2 && npm run test -- preset-menu editor-page</automated>
  </verify>
  <acceptance_criteria>
    - `cd Frontend2 && npm run test -- preset-menu` exits 0
    - `grep "PresetMenu" Frontend2/components/workflow-editor/editor-page.tsx` finds the wire-up
    - `grep "applyPreset\\|resolvePreset" Frontend2/components/workflow-editor/editor-page.tsx | wc -l` returns >= 2
    - `grep "Mevcut değişiklikler kaybolacak" Frontend2/components/workflow-editor/preset-menu.tsx` finds the Turkish copy
    - `grep "PRESETS_BY_ID\\|presets" Frontend2/components/workflow-editor/preset-menu.tsx` finds the import from Plan 12-10 Task 1
    - `grep "history\\.push" Frontend2/components/workflow-editor/editor-page.tsx | wc -l` returns >= 1 (preset apply pushes to undo stack)
  </acceptance_criteria>
  <done>PresetMenu shipped with dirty-aware ConfirmDialog. Editor-page wires apply handler with undo support. Tests cover 7 cases + presets test suite verifies all 9 preset configs are valid.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Generate 12-UAT-CHECKLIST.md and run manual UAT click-through across all 14 LIFE-* / EDIT-* requirements</name>
  <what-built>
    All Phase 12 deliverables across Plans 12-01..09:
    - LifecycleTab with 4 sub-tabs (Overview/Milestones/History/Artifacts)
    - Phase Gate inline expand with 5-error matrix
    - Settings > Yaşam Döngüsü criteria editor + enable_phase_assignment toggle
    - Settings > General methodology read-only + Tooltip
    - /workflow-editor?projectId=X page with 1024px viewport gate
    - Editor with React Flow canvas + DnD + edge create + 5 group entry points + cloud morph
    - Edge types (flow / verification / feedback) with custom dasharrays
    - All-gate edge + bidirectional pair-wise semantic
    - Cycle counter ×N badge
    - Parallel actives in flexible / sequential-flexible
    - Sequential-flexible mode with feedback transitions
    - Incremental / Evolutionary / RAD presets via Şablon Yükle menu
    - Editor save flow with 200/422/409/429/network error matrix
    - Dirty-save guard (beforeunload + router intercept)
    - Backend WorkflowEdge.bidirectional + is_all_gate fields (additive Pydantic, no migration)
    - Timeline tab Gantt with milestone flag lines + popover
    - PDF download from Evaluation Report
  </what-built>
  <how-to-verify>
    1. **Step 1 — Generate the UAT checklist artifact** at `.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UAT-CHECKLIST.md` with the structure below. The executor agent creates this file as part of this task.
    
    2. **Step 2 — Run all 14 manual UAT rows** by following the click-through steps. Mark each row pass/fail. If any row fails, file a fix ticket as part of this plan's iteration loop.
    
    3. **Step 3 — Sign off** by setting all 14 rows to pass. The plan-checker reads this file to confirm UAT completion.
    
    The executor must create the file with this exact structure:
    
    ```md
    # Phase 12 — Manual UAT Checklist
    
    **Purpose:** End-to-end verification of all 14 LIFE-* + EDIT-* requirements per SPEC Acceptance Criteria + VALIDATION.md Manual-Only Verifications table.
    
    **How to run:** Open the application, follow each row's click-through steps, observe the expected behavior, mark Pass/Fail. Sign off when all 14 rows pass.
    
    | # | Requirement | Surface | Click-through Steps | Expected Behavior | Result |
    |---|-------------|---------|---------------------|-------------------|--------|
    | 1 | LIFE-01 | Settings > Yaşam Döngüsü | (1) Open `/projects/{id}?tab=settings&sub=lifecycle` (2) Click "execution" in phase picker (3) Toggle "all_tasks_done" ON (4) Type "QA imza" + click Ekle (5) Click Kaydet (6) Reload | Saved criteria persist; toggle is ON; "QA imza" appears in manual list | ☐ Pass ☐ Fail |
    | 2 | LIFE-01 (enable_phase_assignment) | Same surface | (1) Toggle "Görev–Faz Ataması" ON (2) Wait for save indicator (3) Reload (4) Verify per-phase criteria are unchanged | EPA toggle persists independently of per-phase criteria | ☐ Pass ☐ Fail |
    | 3 | LIFE-02 | Lifecycle tab + Phase Gate | (1) Open `/projects/{id}?tab=lifecycle` for a Waterfall project with all auto criteria satisfied (2) Click "Sonraki Faza Geç" (3) Verify panel expands (4) Click "Faz Geçişini Onayla" | Toast "Geçiş tamam"; active phase badge updates; activity feed includes new `phase_transition` row | ☐ Pass ☐ Fail |
    | 4 | LIFE-02 (override) | Same surface | (1) On a `sequential-locked` project with unmet criteria (2) Open Phase Gate (3) Verify override checkbox visible (4) Check it (5) Verify button relabels to "Zorla Geç" tone=danger (6) Submit | Audit log records `override_used: true` | ☐ Pass ☐ Fail |
    | 5 | LIFE-03 | Lifecycle Overview + Phase Gate | (1) Open Lifecycle tab on a project where the active phase has 0 tasks (2) Verify MiniMetric shows "---" mono (3) Open Phase Gate (4) Verify auto criteria show "Uygulanamaz" (5) Verify info AlertBanner | All zero-task UI rendered correctly; submit succeeds with note only | ☐ Pass ☐ Fail |
    | 6 | LIFE-04 | History sub-tab | (1) Open Lifecycle > Geçmiş (2) Verify N closed phase cards (3) Click "Görev Detayları (3)" Collapsible (4) Open DevTools Network tab (5) Verify ONE GET /tasks/project/{id}?phase_id=X&status=done request fires (6) Close + reopen Collapsible | Second open does NOT fire a new GET (cache hit); MTTaskRow compact rows render | ☐ Pass ☐ Fail |
    | 7 | LIFE-05 | Milestones sub-tab + Timeline tab | (1) Open Lifecycle > Kilometre Taşları (2) Click Ekle (3) Type "Alpha Launch" + select target date 12 days from now + select "planning" phase (4) Click Kaydet (5) Open Timeline tab | New milestone card appears; Timeline Gantt shows vertical flag line at the target date with label "Alpha Launch · {date}"; clicking the flag opens popover | ☐ Pass ☐ Fail |
    | 8 | LIFE-06 | Artifacts sub-tab | (1) Open Lifecycle > Artefaktlar on a Scrum project (2) Verify 5 default artifact rows (3) Click "Sprint Planı" (4) In expanded panel: select status "Tamam" + click Dosya Ekle + select a single file (5) Click Kaydet (6) Reload | Status persisted; file linked; reload preserves both | ☐ Pass ☐ Fail |
    | 9 | LIFE-07 | History card Rapor button | (1) On a closed phase HistoryCard, click Rapor (2) Verify summary fields prefilled read-only (3) Type "API yanıt süresi gecikti..." into "issues" textarea (4) Click Kaydet (5) Reload (6) Click PDF | Text persists; revision incremented; PDF downloads with filename `Phase-Report-{key}-{slug}-rev{N}.pdf`; opens in OS PDF reader | ☐ Pass ☐ Fail |
    | 10 | EDIT-01 | Workflow Editor edge type | (1) Open `/workflow-editor?projectId={id}` (2) Click an edge (3) In right panel, switch type to "verification" via SegmentedControl (4) Verify stroke updates immediately (dashed `6 3`) (5) Click Kaydet (6) Reload | Edge type persists; stroke pattern unchanged after reload | ☐ Pass ☐ Fail |
    | 11 | EDIT-02 + EDIT-04 + EDIT-05 + EDIT-06 | Workflow Editor groups + canvas | (1) Open the editor on a V-Model project (2) Verify both "Module Design" + "Unit Test" render with active ring (parallel actives EDIT-05) (3) Verify cycle counter ×N badge appears on a node closed >=2 times (EDIT-06) (4) Multi-select 2 nodes + click "Grup" (5) Verify cloud-shaped path encloses both (6) Drag one node, verify cloud morphs in real-time (7) Drag a node OUTSIDE the cloud — verify parent association drops + cloud reshrinks (8) Click "Grubu Çöz" — verify ungrouped | All 4 EDIT-* requirements visually verified end-to-end | ☐ Pass ☐ Fail |
    | 12 | EDIT-03 | Workflow Editor sequential-flexible | (1) Switch project mode to "sequential-flexible" via right-panel SegmentedControl (2) Save (3) Verify Phase Gate allows forward via flow edge AND return via feedback edge (4) Verify backward flow-edge transition is rejected | Activity feed records both forward and feedback transitions; backward flow rejected | ☐ Pass ☐ Fail |
    | 13 | EDIT-07 | Workflow Editor preset menu | (1) Open editor on an existing project (2) Click Şablon Yükle (3) Select "Artırımlı" (4) Verify ConfirmDialog "Mevcut değişiklikler kaybolacak..." appears (5) Click Devam Et (6) Verify canvas swaps to Incremental layout (7) Click Save (8) Reload | New workflow persists; nodes match Incremental preset shape | ☐ Pass ☐ Fail |
    | 14 | D-60 (methodology lock) | Settings > General | (1) Open `/projects/{id}?tab=settings&sub=general` (2) Verify methodology field is read-only display (NOT a select) (3) Hover info icon, verify Tooltip "Metodoloji proje oluşturulduğu an sabittir..." | Field is informational only; cannot be edited | ☐ Pass ☐ Fail |
    | 15 | Viewport (cross-EDIT) | Workflow Editor viewport gate | (1) Open editor at viewport >=1024px (2) Resize to 768px (3) Verify "Workflow editörü 1024px+ ekran gerektirir." fallback page renders (4) Click "Projeye Dön" | Fallback shown; navigate back to project | ☐ Pass ☐ Fail |
    
    **Sign-off:**
    - [ ] All 15 rows marked Pass
    - [ ] Date completed: ______________
    - [ ] Tester: ______________
    
    **Note:** Row 15 ("Viewport") is a bonus check from SPEC Constraints; the 14 LIFE/EDIT requirements are rows 1-14.
    ```
  </how-to-verify>
  <resume-signal>Type "approved" once all 14 (or 15) rows are signed off as Pass. Type "fix: {description}" if any row fails — that triggers a fix-iteration cycle within this plan.</resume-signal>
</task>

</tasks>

<verification_criteria>
The plan-checker confirms this plan by:
1. Running `cd Frontend2 && npm run test -- presets preset-menu editor-page` and observing exit 0
2. Verifying `.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UAT-CHECKLIST.md` exists with 14 (or 15) rows
3. Verifying every row in the UAT checklist has a Pass mark before declaring the phase complete
4. Confirming all 9 preset entries pass `validateWorkflow()` via the unit test
5. Confirming the editor's preset menu opens, shows the 9 entries, and ConfirmDialog flow works
</verification_criteria>

<success_criteria>
- EDIT-07 fully delivered (3 new presets + Şablon Yükle menu + ConfirmDialog flow)
- All 9 presets pass validateWorkflow with zero errors
- Cycle counter end-to-end verified (Plan 12-08 visibility + this plan's UAT row 11)
- 12-UAT-CHECKLIST.md created with 14 rows (LIFE-01..07 + EDIT-01..07) plus optional viewport row
- All 14 LIFE-* + EDIT-* requirements pass UAT
- Plan-checker can run the full Frontend2 + Backend test suites green: `cd Frontend2 && npm test && cd ../Backend && python -m pytest`
</success_criteria>

<output>
After completion, create `.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-10-SUMMARY.md` documenting:
- Confirmation that all 9 presets pass validateWorkflow
- Tester sign-off date for the UAT checklist
- Any UAT-discovered issues that triggered a fix iteration in this plan
- Final cycle-counter end-to-end verification result
- Confirmation that the methodology read-only field (Plan 12-03 D-60) and viewport <1024 fallback (Plan 12-07) work as designed
- Whether `concaveman` was upgraded from convex-hull-plus-padding baseline (RESEARCH Open Question 1) based on UAT visual feedback
</output>
