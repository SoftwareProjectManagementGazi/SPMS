---
phase: 12
plan: 05
plan_id: "12-05"
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - Frontend2/components/lifecycle/milestones-subtab.tsx
  - Frontend2/components/lifecycle/milestones-subtab.test.tsx
  - Frontend2/components/lifecycle/milestone-inline-add-row.tsx
  - Frontend2/components/lifecycle/lifecycle-tab.tsx
  - Frontend2/components/project-detail/timeline-tab.tsx
  - Frontend2/components/project-detail/timeline-tab.test.tsx
  - Frontend2/app/(shell)/projects/[id]/page.tsx
autonomous: true
requirements: [LIFE-05]
must_haves:
  truths:
    - "Milestones sub-tab renders milestones list as cards (name + target date + status chip + ProgressBar derived from linked phases) (LIFE-05)"
    - "'Ekle' button opens inline add row with name Input + date Input + multi-select chip picker for `linked_phase_ids` sourced from non-archived `workflow.nodes`"
    - "Milestone CRUD uses Plan 12-01 `useMilestones` hook with optimistic insert + rollback on 4xx"
    - "Delete is gated by ConfirmDialog (Phase 10 pattern)"
    - "Phase 11 Timeline tab Gantt accepts a new `milestones` prop and renders vertical flag lines at each milestone target date"
    - "Clicking a flag line opens a popover showing milestone name + status + days-remaining + linked phases"
    - "Empty `linked_phase_ids` is accepted (project-wide milestone per Phase 9 D-24)"
    - "Permission gate: Add/Edit/Delete buttons hidden when `useTransitionAuthority` returns false"
  artifacts:
    - path: "Frontend2/components/lifecycle/milestones-subtab.tsx"
      provides: "Milestone list + inline add + edit + delete confirm"
    - path: "Frontend2/components/lifecycle/milestone-inline-add-row.tsx"
      provides: "Inline-add Card with name + date + chip picker + Save/Cancel"
  key_links:
    - from: "Frontend2/components/lifecycle/milestones-subtab.tsx"
      to: "Frontend2/hooks/use-milestones.ts"
      via: "Optimistic CRUD via Plan 12-01 hooks"
      pattern: "useMilestones|useCreateMilestone|useUpdateMilestone|useDeleteMilestone"
    - from: "Frontend2/components/project-detail/timeline-tab.tsx"
      to: "Frontend2/hooks/use-milestones.ts"
      via: "Reads milestone list to render flag lines"
      pattern: "milestones"
---

<objective>
Ship the Milestones sub-tab + integrate vertical milestone flag lines into the Phase 11 Timeline tab Gantt. LIFE-05 in full.

Purpose: Plan 12-04 routed the lifecycle Tabs primitive but left the milestones sub-tab as a placeholder. This plan replaces that placeholder. The Timeline integration is a small additive change to Phase 11's custom SVG Gantt — no new chart library.

Output: 3 new files (milestones-subtab + milestone-inline-add-row + 1 test) + 4 modified (lifecycle-tab.tsx wires milestones-subtab, timeline-tab.tsx adds flag lines, timeline-tab.test.tsx extends, projects/[id]/page.tsx forwards milestones prop).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-CONTEXT.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-SPEC.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-PATTERNS.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-VALIDATION.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-01-SUMMARY.md

<interfaces>
From Plan 12-01:
- `useMilestones(projectId)` returns `UseQueryResult<Milestone[]>`
- `useCreateMilestone(projectId)`, `useUpdateMilestone`, `useDeleteMilestone` mutations
- `Milestone = { id: number; projectId: number; name: string; targetDate: string; status: string; linkedPhaseIds: string[] }`

Phase 9 Milestone POST body (D-24 — empty `linked_phase_ids` allowed):
```ts
interface MilestoneCreateDTO { name: string; target_date: string; linked_phase_ids: string[] }
```

Phase 11 Timeline tab Gantt today-line formula (line 106): `todayX = ((today - min) / MS_PER_DAY) * DAY_WIDTH[view]` — milestone flag uses the same formula with `target_date` instead of `today`.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to POST /projects/{id}/milestones | Phase 9 D-46 RPTA membership check on project_id-bodied POSTs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 (carry-forward) | E (Elevation of Privilege) | Milestone POST | mitigate | Backend already enforces inline RPTA membership check on project_id-bodied POSTs (Phase 9 D-46 + Phase 11 D-46). Frontend defense-in-depth: Add/Edit/Delete buttons hidden when `useTransitionAuthority(project)` returns false (CONTEXT D-40). |
| T-12-05-01 | T (Tampering) | linked_phase_ids array | mitigate | Backend Pydantic validates each id against `workflow.nodes` (Phase 9 D-50 + manage_milestones.py:20-41). Frontend chip picker is sourced from the same list, so tampering would have to bypass the dropdown — backend remains authoritative. |
</threat_model>

<validation_files>
- `Frontend2/components/lifecycle/milestones-subtab.test.tsx` — optimistic create + rollback on 422; ConfirmDialog on delete; chip picker multi-select; empty linked_phase_ids accepted
- `Frontend2/components/project-detail/timeline-tab.test.tsx` — extended with milestone flag-line rendering at correct x-position + popover content
</validation_files>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship milestones-subtab.tsx + milestone-inline-add-row.tsx with full CRUD + chip picker + ConfirmDialog</name>
  <files>
    Frontend2/components/lifecycle/milestones-subtab.tsx,
    Frontend2/components/lifecycle/milestones-subtab.test.tsx,
    Frontend2/components/lifecycle/milestone-inline-add-row.tsx,
    Frontend2/components/lifecycle/lifecycle-tab.tsx
  </files>
  <read_first>
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md lines 988-1014 — MilestoneInlineAddRow anatomy
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md lines 350-380 — Milestones sub-tab copy table
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-CONTEXT.md D-49, D-50, D-51 — milestone inline-add + chip picker + Timeline integration
    - New_Frontend/src/pages/lifecycle-tab.jsx lines 206-267 — prototype MilestonesSubTab + inline-add (visual ref)
    - Frontend2/components/project-detail/settings-columns-subtab.tsx lines 80-294 — canonical inline-add row pattern (per-row drafts + commit-on-blur)
    - Frontend2/hooks/use-milestones.ts (Plan 12-01) — optimistic mutation hooks
    - Frontend2/components/projects/confirm-dialog.tsx — Phase 10 D-25 ConfirmDialog
    - Frontend2/components/primitives/badge.tsx — chip rendering
    - Frontend2/components/primitives/progress-bar.tsx — milestone progress derivation
    - Frontend2/hooks/use-transition-authority.ts (Plan 12-01)
  </read_first>
  <behavior>
    - Test 1 (list render): with `useMilestones` returning 2 milestones, renders 2 cards with name + status chip + target-date + ProgressBar
    - Test 2 (Add inline): clicking Ekle, MilestoneInlineAddRow appears at top of list with empty Inputs
    - Test 3 (chip picker multi-select): opening chip picker, selecting 2 phase ids, both render as chips inside the Input row
    - Test 4 (empty linked_phase_ids accepted): create milestone with no phase chips selected, POST body has `linked_phase_ids: []`; insertion succeeds (project-wide milestone per Phase 9 D-24)
    - Test 5 (optimistic + rollback): mock POST returning 422, UI inserts optimistically then rolls back; AlertBanner shows error
    - Test 6 (delete confirm): click delete X on a card, ConfirmDialog opens; cancel issues no DELETE; confirm issues DELETE + optimistic removal
    - Test 7 (permission gate): with `useTransitionAuthority` returning false, Add button hidden + delete buttons hidden
    - Test 8 (empty list state): with milestones=[], renders "Henüz kilometre taşı tanımlanmamış."
  </behavior>
  <action>
    **Step 1 — Create `Frontend2/components/lifecycle/milestone-inline-add-row.tsx`** per UI-SPEC §6 lines 988-1014. `"use client"`. Component: `export function MilestoneInlineAddRow({ project, workflow, onSave, onCancel, initial }: { project: Project; workflow: WorkflowConfig; onSave: (dto: MilestoneCreateDTO) => Promise<void>; onCancel: () => void; initial?: Partial<MilestoneCreateDTO> })`.

    Layout per prototype `lifecycle-tab.jsx:224-229`: Card padding 14, marginBottom 12. 2-col grid `1fr 140px` for name + date. Full-width chip picker for `linked_phase_ids`. Bottom: 2 Buttons.

    Local state: `name`, `targetDate`, `linkedPhaseIds: string[]`, `chipOpen: boolean`. All initialized from `initial` prop or empty.

    Chip picker (multi-select dropdown): trigger button shows selected chips (one Badge size=xs tone=primary per id, with X delete affordance). Dropdown lists `workflow.nodes.filter(n => !n.isArchived)` with checkmark + name; click toggles membership in `linkedPhaseIds`. Click-outside dismisses (mousedown listener like Phase 8 SidebarUserMenu).

    Save button: disabled until `name.trim()` AND `targetDate` are non-empty. On click, calls `onSave({ name: name.trim(), target_date: targetDate, linked_phase_ids: linkedPhaseIds })`. **Empty `linked_phase_ids` IS allowed** — do NOT add a min-length check (Phase 9 D-24).

    Cancel button: calls `onCancel()`.

    **Step 2 — Create `Frontend2/components/lifecycle/milestones-subtab.tsx`.** `"use client"`. Component: `export function MilestonesSubTab({ project, workflow }: { project: Project; workflow: WorkflowConfig })`.

    State: `[adding, setAdding] = useState(false)`, `[deleteId, setDeleteId] = useState<number | null>(null)`, `[editingId, setEditingId] = useState<number | null>(null)`.

    Hooks: `const { data: milestones = [], isLoading } = useMilestones(project.id); const create = useCreateMilestone(project.id); const update = useUpdateMilestone(project.id); const remove = useDeleteMilestone(project.id); const canEdit = useTransitionAuthority(project)`.

    Layout: Section header "Kilometre Taşları" + (canEdit and not adding `<Button onClick={() => setAdding(true)}>{T('Ekle','Add')}</Button>`). Below: list of milestone Cards.

    Each milestone Card per UI-SPEC: padding 14, marginBottom 10, internal gap 10. Header row: name (heading 14px) + status Badge + target-date chip "{daysRemaining} gün". ProgressBar derived: when `linkedPhaseIds.length > 0`, average progress of linked phases (compute from tasks). Footer: edit button (sets editingId) + delete button (sets deleteId), both gated on canEdit.

    Inline add: when `adding`, render `<MilestoneInlineAddRow project={project} workflow={workflow} onSave={async (dto) => { await create.mutateAsync(dto); setAdding(false) }} onCancel={() => setAdding(false)}/>` at the TOP of the list.

    Edit mode: when `editingId === milestone.id`, replace that card with `<MilestoneInlineAddRow ... initial={milestone} onSave={async (dto) => { await update.mutateAsync({ id: milestone.id, ...dto }); setEditingId(null) }}/>`.

    Delete confirm: `<ConfirmDialog open={deleteId !== null} title={T('Kilometre Taşını Sil','Delete Milestone')} body={T('Bu işlem geri alınamaz.','This action cannot be undone.')} confirmLabel={T('Sil','Delete')} confirmTone="danger" onConfirm={async () => { await remove.mutateAsync(deleteId!); setDeleteId(null) }} onCancel={() => setDeleteId(null)}/>`.

    Empty state: when `milestones.length === 0 and not adding`, render `<div style={{ padding: 30, textAlign: 'center', color: 'var(--fg-muted)' }}>{T('Henüz kilometre taşı tanımlanmamış.','No milestones defined yet.')}</div>` per UI-SPEC line 1411.

    **Step 3 — Create `Frontend2/components/lifecycle/milestones-subtab.test.tsx`** with all 8 cases per `<behavior>`. Mock `apiClient.post`/`apiClient.delete` via `vi.mock('@/lib/api-client')` to verify request payloads. For Test 4 (empty linked_phase_ids), assert the POST body has `linked_phase_ids: []`.

    **Step 4 — MODIFY `Frontend2/components/lifecycle/lifecycle-tab.tsx`.** Replace the milestones placeholder from Plan 12-04 with `subTab === 'milestones' && <MilestonesSubTab project={project} workflow={workflow}/>`. Add the import at the top: `import { MilestonesSubTab } from "./milestones-subtab"`.
  </action>
  <verify>
    <automated>cd Frontend2 && npm run test -- milestones-subtab</automated>
  </verify>
  <acceptance_criteria>
    - `cd Frontend2 && npm run test -- milestones-subtab` exits 0
    - `grep "useMilestones\\|useCreateMilestone\\|useDeleteMilestone" Frontend2/components/lifecycle/milestones-subtab.tsx | wc -l` returns >= 3
    - `grep "ConfirmDialog" Frontend2/components/lifecycle/milestones-subtab.tsx` finds the delete confirm
    - `grep "useTransitionAuthority\\|canEdit" Frontend2/components/lifecycle/milestones-subtab.tsx` finds the permission gate
    - `grep "linked_phase_ids" Frontend2/components/lifecycle/milestone-inline-add-row.tsx` finds the chip-picker payload field
    - `grep "isArchived" Frontend2/components/lifecycle/milestone-inline-add-row.tsx` finds the archived-node filter for the chip picker dropdown
    - `grep "MilestonesSubTab" Frontend2/components/lifecycle/lifecycle-tab.tsx` finds the wire-up
  </acceptance_criteria>
  <done>MilestonesSubTab + MilestoneInlineAddRow ship with full CRUD + chip picker + ConfirmDialog + permission gate. lifecycle-tab.tsx routes the milestones sub-tab. All 8 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend timeline-tab.tsx with milestones prop + vertical flag lines + popover</name>
  <files>
    Frontend2/components/project-detail/timeline-tab.tsx,
    Frontend2/components/project-detail/timeline-tab.test.tsx,
    Frontend2/app/(shell)/projects/[id]/page.tsx
  </files>
  <read_first>
    - Frontend2/components/project-detail/timeline-tab.tsx — current Phase 11 custom SVG Gantt; specifically line 106 (`todayX = ((today - min) / MS_PER_DAY) * DAY_WIDTH[view]`) for the same coordinate-formula reuse; lines 151-268 for the SVG render block where the new `<g>` group is inserted
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-CONTEXT.md D-51 — Timeline integration spec
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md milestone popover copy ("Alpha Launch · 15 Nis" example)
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-PATTERNS.md `project-detail/timeline-tab.tsx` row — specifies "add a new `<g>` group rendering vertical flag lines" + "popover content"
    - Frontend2/hooks/use-milestones.ts (Plan 12-01)
  </read_first>
  <behavior>
    - Test 1 (flag-line render): with milestones prop containing one milestone at 2026-05-15, SVG includes a `<line>` element at the correct x-position (verified by computing the expected x via the same formula and asserting the rendered line's x1 attribute)
    - Test 2 (label chip): label chip "Alpha Launch · 15 May" appears at the top of the flag line (text content lookup)
    - Test 3 (popover content on click): clicking the flag opens a popover containing milestone name + status badge + days-remaining + linked phases names
    - Test 4 (multi-milestone): 3 milestones render 3 distinct flag lines without overlap
    - Test 5 (no milestones): no `<g aria-label="milestones-layer">` element renders when milestones is empty/undefined
  </behavior>
  <action>
    **Step 1 — Read `Frontend2/components/project-detail/timeline-tab.tsx`** to identify the function signature, the SVG render block, the `view` state variable (`'day' | 'week' | 'month'`), the `DAY_WIDTH` map, and the today-line formula at line 106. Note `min`, `MS_PER_DAY`, and `totalWidth` / `totalHeight` for coordinate math.

    **Step 2 — Add `milestones?: Milestone[]` prop** to the function signature with default `[]`. Existing callers don't break.

    **Step 3 — Inside the SVG render block** (after the today-line, before the closing `</svg>`), insert a milestones layer:
    ```tsx
    {(milestones ?? []).length > 0 && (
      <g aria-label="milestones-layer">
        {milestones.map((m) => {
          const target = new Date(m.targetDate).getTime()
          const x = ((target - min) / MS_PER_DAY) * DAY_WIDTH[view]
          if (x < 0 || x > totalWidth) return null
          return (
            <g key={m.id} onClick={() => setOpenPopoverId(m.id)} style={{ cursor: 'pointer' }}>
              <line x1={x} y1={0} x2={x} y2={totalHeight} stroke="var(--priority-high)" strokeWidth={1.5} strokeDasharray="6 3"/>
              <rect x={x - 60} y={0} width={120} height={20} fill="var(--surface)" stroke="var(--priority-high)" strokeWidth={1} rx={4}/>
              <text x={x} y={14} textAnchor="middle" fontSize={10.5} fill="var(--fg)">{m.name}{' · '}{formatDateShort(m.targetDate, language)}</text>
            </g>
          )
        })}
      </g>
    )}
    ```

    **Step 4 — Add the popover.** State `[openPopoverId, setOpenPopoverId] = useState<number | null>(null)`. When `openPopoverId !== null`, render an absolute-positioned Card near the clicked flag with: name + status Badge + `{daysRemaining} gün kaldı` + linked phase names (joined with comma). Click-outside dismisses via mousedown useEffect.

    Helper `formatDateShort(iso: string, lang: 'tr' | 'en'): string` returns `"15 May"` (lang='tr') or `"May 15"` (lang='en') using `toLocaleDateString` with `{ month: 'short', day: 'numeric' }` and `tr-TR` / `en-US` locale per UI-SPEC line 1571-1572.

    **Step 5 — Update `Frontend2/components/project-detail/timeline-tab.test.tsx`** to add the 5 cases per `<behavior>`. Either pass milestones as a direct prop in the test or `vi.mock('@/hooks/use-milestones')` if the timeline-tab itself fetches them.

    **Step 6 — MODIFY `Frontend2/app/(shell)/projects/[id]/page.tsx`** to ensure `milestones` is passed to TimelineTab (via `<ProjectDetailShell>` if it forwards, or by lifting `useMilestones(project.id)` to the page and passing as a prop). Choose whichever pattern matches existing Phase 11 prop-drilling for consistency.
  </action>
  <verify>
    <automated>cd Frontend2 && npm run test -- timeline-tab</automated>
  </verify>
  <acceptance_criteria>
    - `cd Frontend2 && npm run test -- timeline-tab` exits 0
    - `grep "milestones" Frontend2/components/project-detail/timeline-tab.tsx | wc -l` returns >= 3 (prop, render, popover)
    - `grep "DAY_WIDTH\\[view\\]" Frontend2/components/project-detail/timeline-tab.tsx` finds the same coordinate formula used for milestones (existing today-line reuses)
    - `grep "milestones-layer" Frontend2/components/project-detail/timeline-tab.tsx` finds the new `<g>` element
    - `grep "openPopoverId\\|setOpenPopoverId" Frontend2/components/project-detail/timeline-tab.tsx | wc -l` returns >= 2 (popover state)
    - `grep "formatDateShort\\|toLocaleDateString" Frontend2/components/project-detail/timeline-tab.tsx` finds the date formatter
    - `grep -c "it\\(" Frontend2/components/project-detail/timeline-tab.test.tsx` returns >= 5 (existing Phase 11 tests + new 5)
  </acceptance_criteria>
  <done>Timeline Gantt accepts milestones prop. Vertical flag lines render at target dates with label chips. Click opens popover. All Phase 11 tests still pass + 5 new milestone tests green.</done>
</task>

</tasks>

<verification_criteria>
The plan-checker confirms this plan by:
1. Running `cd Frontend2 && npm run test -- milestones-subtab timeline-tab` and observing exit 0
2. Smoke test on `/projects/{id}?tab=lifecycle&sub=milestones` — Add a milestone with empty linked phases, verify it appears project-wide
3. Smoke test on `/projects/{id}?tab=timeline` — verify the new milestone renders as a vertical flag line at the correct date
4. Confirm permission gate hides Add/Delete for non-PM users (use `useTransitionAuthority`)
5. Verify ConfirmDialog blocks accidental deletes
</verification_criteria>

<success_criteria>
- LIFE-05 fully delivered
- Empty `linked_phase_ids` accepted (project-wide milestone supported)
- Timeline Gantt extended with milestone flag lines without rearchitecture
- All RTL tests green (existing Phase 11 timeline-tab tests + new milestone-subtab + milestone-extension tests)
- Permission gate honored
</success_criteria>

<output>
After completion, create `.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-05-SUMMARY.md` documenting:
- Whether the chip-picker is built inline or reuses an existing Phase 11 chip-picker pattern (CONTEXT D-50 + D-51 reference)
- The data flow chosen for milestones-to-TimelineTab (prop drill from page or hook in timeline-tab itself)
- Notes on milestone progress derivation when `linkedPhaseIds.length === 0` (project-wide — show "—" or 0%?)
</output>
