---
phase: 12
plan: 02
plan_id: "12-02"
type: execute
wave: 1
depends_on: ["12-01"]
files_modified:
  - Frontend2/components/lifecycle/phase-gate-expand.tsx
  - Frontend2/components/lifecycle/phase-gate-expand.test.tsx
  - Frontend2/components/lifecycle/summary-strip.tsx
  - Frontend2/components/lifecycle/summary-strip.test.tsx
  - Frontend2/components/lifecycle/lifecycle-tab.tsx
  - Frontend2/components/project-detail/lifecycle-stub-tab.tsx
  - Frontend2/components/project-detail/project-detail-shell.tsx
autonomous: true
requirements: [LIFE-02, LIFE-03]
must_haves:
  truths:
    - "User can click 'Sonraki Faza Geç' on the Lifecycle summary strip and a Phase Gate panel expands inline between the strip and the canvas (LIFE-02)"
    - "Submit POST sends `Idempotency-Key` header generated once via `crypto.randomUUID()` per panel-open session"
    - "Same Idempotency-Key is reused on retry within the same panel-open session; closing then reopening generates a new UUID (CONTEXT D-42)"
    - "Override checkbox 'Kriterler karşılanmadan geçilsin' appears only when criteria fail in `sequential-locked` mode; primary button relabels to 'Zorla Geç' with danger tone (CONTEXT D-39)"
    - "409/422/429/400/network errors each render the canonical AlertBanner / inline-failure-list / countdown-toast / safety-net-banner / network-toast per CONTEXT D-41"
    - "'Sonraki Faza Geç' button is hidden when `use-transition-authority` returns false OR when `wf.mode === 'continuous'`"
    - "Transition note textarea is 500-char limited with live counter; submit disabled when length > 500 (CONTEXT D-46)"
    - "lifecycle-stub-tab.tsx is deleted; project-detail-shell.tsx routes the lifecycle tab to `<LifecycleTab/>`"
  artifacts:
    - path: "Frontend2/components/lifecycle/phase-gate-expand.tsx"
      provides: "Inline Phase Gate panel with full error matrix"
    - path: "Frontend2/components/lifecycle/summary-strip.tsx"
      provides: "Methodology-aware summary strip with active-phase Badge + ProgressBar + 'Sonraki Faza Geç' + 'Düzenle' buttons"
    - path: "Frontend2/components/lifecycle/lifecycle-tab.tsx"
      provides: "Outer LifecycleTab shell — initial version with summary-strip + Phase Gate (sub-tabs added in Plan 12-04)"
  key_links:
    - from: "Frontend2/components/lifecycle/phase-gate-expand.tsx"
      to: "Frontend2/hooks/use-phase-transitions.ts"
      via: "Mutation usage with idempotencyKey from useState"
      pattern: "usePhaseTransition"
    - from: "Frontend2/components/lifecycle/summary-strip.tsx"
      to: "Frontend2/hooks/use-transition-authority.ts"
      via: "Permission gate hides Sonraki Faza Geç when false"
      pattern: "useTransitionAuthority"
    - from: "Frontend2/components/project-detail/project-detail-shell.tsx"
      to: "Frontend2/components/lifecycle/lifecycle-tab.tsx"
      via: "Replaces LifecycleStubTab in the lifecycle tab branch"
      pattern: "LifecycleTab"
---

<objective>
Replace the Phase 11 D-10 `LifecycleStubTab` with a real `<LifecycleTab/>` shell that ships the Lifecycle summary strip and the Phase Gate inline-expand panel. This plan delivers LIFE-02 in full and owns the Phase-Gate side of LIFE-03 (the "Uygulanamaz" auto-criteria branch + the info AlertBanner above the gate panel when `phaseStats.total === 0`). The `MiniMetric` component for LIFE-03 (`---` mono-zero branch in the summary strip) is a Plan 12-04 deliverable — Plan 04 ships `mini-metric.tsx`; this plan only consumes the summary-strip layout primitives. Sub-tabs (Overview / Milestones / History / Artifacts) are added by Plans 12-04..06; this plan only ships summary-strip + Phase Gate + lifecycle-tab routing.

Purpose: LIFE-02 is the most user-visible Phase 12 deliverable per CONTEXT D-01 ("highest user-visible value"). Delivers the Idempotency-Key + 5-error-matrix flow that downstream UAT depends on.

Output: 5 new files (2 components + 1 outer LifecycleTab + 2 test files) + 2 modified files (lifecycle-stub-tab deleted via empty replacement, project-detail-shell routing change).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-CONTEXT.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-SPEC.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-RESEARCH.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-PATTERNS.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-VALIDATION.md
@.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-01-SUMMARY.md

<interfaces>
From Plan 12-01 (already shipped — DO NOT redefine):
- `useTransitionAuthority(project) → boolean` from `Frontend2/hooks/use-transition-authority.ts`
- `usePhaseTransition(projectId) → { idempotencyKey, open, reset, mutation }` from `Frontend2/hooks/use-phase-transitions.ts`
- `WorkflowConfig`, `WorkflowNode`, `WorkflowMode` types from `Frontend2/services/lifecycle-service.ts`

Phase Gate POST request body (Phase 9 D-04 + D-05):
```ts
interface PhaseTransitionDTO {
  source_phase_id: string
  target_phase_id: string
  allow_override: boolean
  exceptions: Array<{ task_id: number; action: 'next' | 'backlog' | 'stay' }>
  note: string
}
```

Phase Gate POST error responses (Phase 9 D-03):
- 409 → `{ error_code: 'PHASE_GATE_LOCKED', detail: '...' }`
- 422 → `{ error_code: 'CRITERIA_UNMET', unmet: [{ check: string; passed: boolean; detail?: string }] }`
- 429 → `{ error_code: 'RATE_LIMITED', retry_after_seconds: number }`
- 400 → `{ error_code: 'INVALID_MODE', detail: 'Continuous mode does not support phase transitions.' }`
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Phase Gate POST | Submit hits `POST /projects/{id}/phase-transitions` with Idempotency-Key header; backend caches response 10 min (Phase 9 D-50) |
| Browser → 'Kriterleri düzenle →' link | Deep link to `/projects/{id}?tab=settings&sub=lifecycle&phase={current.id}` — same-app navigation, no cross-origin risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | T (Tampering) | Phase Gate Idempotency-Key replay | mitigate | Idempotency-Key generated once per panel-open session via `crypto.randomUUID()` (CONTEXT D-42); `usePhaseTransition` from Plan 12-01 enforces this via React state. Backend Phase 9 D-50 caches response 10 min so retries are server-validated. |
| T-12-02-01 | E (Elevation of Privilege) | "Sonraki Faza Geç" button | mitigate | Button hidden when `useTransitionAuthority(project)` returns false (CONTEXT D-40). Backend re-checks authorization on every POST regardless. The hook is the single source of truth from Plan 12-01. |
| T-12-02-02 | T (Tampering) | Override checkbox | mitigate | Override checkbox is conditional on `wf.mode === 'sequential-locked' && !allCriteriaPassed`. When checked, `allow_override: true` is sent in the body and the audit log records `override_used: true` (Phase 9 D-05). Backend re-validates the override authority (Phase 9 D-04). |
</threat_model>

<validation_files>
- `Frontend2/components/lifecycle/phase-gate-expand.test.tsx` — 6 RTL cases: happy path, 422 unmet, 409 lock, 429 countdown, 400 wrong-mode, override flow + 1 case for Idempotency-Key reuse-on-retry + 1 case for Idempotency-Key new-on-reopen
- `Frontend2/components/lifecycle/summary-strip.test.tsx` — 3 cases: button visibility (continuous mode hides), permission false hides, normal render
</validation_files>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship phase-gate-expand.tsx with the full inline-panel UI + 5-error matrix + override flow + Idempotency-Key reuse</name>
  <files>
    Frontend2/components/lifecycle/phase-gate-expand.tsx,
    Frontend2/components/lifecycle/phase-gate-expand.test.tsx
  </files>
  <read_first>
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md lines 861-927 — PhaseGateExpand anatomy + auto-check icon mapping + click semantics + error states + Idempotency-Key lifecycle
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md lines 480-620 — Phase Gate copy table (Turkish + English) for every label
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-CONTEXT.md D-36, D-38, D-39, D-41, D-42, D-43, D-46 — full Phase Gate decisions
    - New_Frontend/src/pages/lifecycle-tab.jsx lines 408-498 — prototype PhaseGateExpand (visual ref)
    - Frontend2/components/task-detail/properties-sidebar.tsx — closest "between two siblings" component pattern; reuse the editor-style for the textarea
    - Frontend2/components/primitives/segmented-control.tsx — for "Açık Görevler" SegmentedControl
    - Frontend2/components/primitives/collapsible.tsx — for "Farklı davranış gerekli?" exception list
    - Frontend2/components/primitives/alert-banner.tsx — for mode warnings
    - Frontend2/hooks/use-phase-transitions.ts (Plan 12-01) — the mutation + idempotencyKey state hook
    - Frontend2/hooks/use-tasks.ts — used to fetch open tasks for the "Açık Görevler" Collapsible
    - Frontend2/components/lifecycle/__tests__/setup.tsx (Plan 12-01) — RTL fixtures
  </read_first>
  <behavior>
    - Test 1 (happy path): All auto-criteria pass, manual checkboxes optional, click "Faz Geçişini Onayla" → submit POST, success toast, panel collapses
    - Test 2 (422 CriteriaUnmet): backend returns `{ unmet: [{ check: 'all_tasks_done', passed: false, detail: '3 open tasks' }] }` → AlertBanner danger + per-criterion list rendered
    - Test 3 (409 Locked): backend returns 409 → inline AlertBanner warning "Başka bir kullanıcı aynı anda geçiş yapıyor..." + "Tekrar Dene" button visible; clicking it re-fires the mutation with the SAME Idempotency-Key
    - Test 4 (429 Rate-Limit): backend returns `{ retry_after_seconds: 10 }` → countdown toast "10 saniye bekleyin" + submit button disabled until countdown ends + auto-re-enables
    - Test 5 (400 Wrong-mode): backend returns 400 → AlertBanner "Sürekli akış (Kanban) modunda Phase Gate kullanılamaz." (safety net for the hide-button-upstream rule)
    - Test 6 (override flow): `wf.mode === 'sequential-locked'` AND criteria.unmet.length > 0 → override checkbox renders. Check it → submit button relabels "Zorla Geç" + danger tone. Submit → POST sent with `allow_override: true`
    - Test 7 (Idempotency-Key reuse on retry): submit fails with 409; click "Tekrar Dene" → second POST sends the same `Idempotency-Key` header value as the first
    - Test 8 (new key on reopen): submit succeeds, close panel, reopen → second submit uses a DIFFERENT `Idempotency-Key` value
    - Test 9 (note 500-char limit): typing 501 characters disables the submit button; counter renders "501/500" with red border
    - Test 10 (zero-task LIFE-03 branch): `phaseStats.total === 0` → auto-criteria render "Uygulanamaz" + grey Circle icon + info AlertBanner "Bu fazda henüz görev yok. Geçiş serbestçe yapılabilir."
    - Test 11 (open-tasks SegmentedControl + exceptions): SegmentedControl with 3 options; expanding "Farklı davranış gerekli?" lists every open task with mini dropdown
    - Test 12 ("Kriterleri düzenle →" deep link): clicking the link calls `router.push('/projects/' + projectId + '?tab=settings&sub=lifecycle&phase=' + current.id)`
  </behavior>
  <action>
    Create `Frontend2/components/lifecycle/phase-gate-expand.tsx`. Open with `"use client"`. Imports: `import * as React from "react"`, `import { useApp } from "@/context/app-context"`, `import { Button, Card, AlertBanner, SegmentedControl, Collapsible, Toggle, Badge } from "@/components/primitives"`, `import { ConfirmDialog } from "@/components/projects/confirm-dialog"` (verify path per RESEARCH A7), `import { useToast } from "@/components/toast"`, `import { useRouter } from "next/navigation"`, `import { usePhaseTransition } from "@/hooks/use-phase-transitions"`, `import { useTasks } from "@/hooks/use-tasks"`, `import { useTransitionAuthority } from "@/hooks/use-transition-authority"`, `import { CircleCheck, AlertCircle, Circle, X } from "lucide-react"`.

    Component signature: `export function PhaseGateExpand({ project, currentPhase, nextPhase, criteria, onClose }: { project: Project; currentPhase: WorkflowNode; nextPhase: WorkflowNode; criteria: { auto: { all_tasks_done: boolean; no_critical_tasks: boolean; no_blockers: boolean }; manual: string[] }; onClose: () => void })`.

    Local state:
    - `const { idempotencyKey, open, reset, mutation } = usePhaseTransition(project.id)` — Plan 12-01 hook owns the Idempotency-Key state.
    - `const [manualChecks, setManualChecks] = React.useState<Record&lt;number, boolean&gt;>({})` — index-keyed manual criteria checkboxes.
    - `const [openTaskAction, setOpenTaskAction] = React.useState&lt;'next' | 'backlog' | 'stay'&gt;('next')`.
    - `const [exceptions, setExceptions] = React.useState&lt;Record&lt;number, 'same' | 'next' | 'backlog' | 'stay'&gt;>({})` — task-id-keyed.
    - `const [note, setNote] = React.useState("")` (max 500 chars).
    - `const [allowOverride, setAllowOverride] = React.useState(false)`.
    - `const [countdown, setCountdown] = React.useState&lt;number | null&gt;(null)` — 429 countdown timer.

    React.useEffect on mount: call `open()` once to generate the UUID. On unmount, call `reset()`.

    Compute `phaseStats` from `useTasks(project.id, { phase_id: currentPhase.id })`: `total`, `done`, `open`. Track `phaseStats.total === 0` for LIFE-03 branch.

    Compute `autoCriteriaResults`: for each of `all_tasks_done` / `no_critical_tasks` / `no_blockers`, evaluate against tasks. When `phaseStats.total === 0`, all return `null` (rendered as "Uygulanamaz" + grey Circle).

    Layout per UI-SPEC §4 lines 861-892. Card padding 20, background `var(--bg-2)`, border-bottom 2px solid `var(--border)`. Inside:
    1. Header row: `&lt;div&gt;Faz Geçişi: {currentPhase.name} → {nextPhase.name}&lt;/div&gt;` + close X button (calls `onClose()`).
    2. Task summary: `Toplam: {phaseStats.total} · Tamamlanan: {phaseStats.done} · Açık: {phaseStats.open}` (mono font for numbers).
    3. 2-column grid (`1fr 1fr`, gap 16) with two Cards padding 14:
       - Left: "OTOMATİK KRİTERLER" — list 3 rows with icons per UI-SPEC line 896-899: success CircleCheck if `c.ok && total > 0`, danger AlertCircle if `!c.ok && total > 0`, grey Circle + "Uygulanamaz —" prefix if `total === 0`.
       - Right: "MANUEL KRİTERLER" — list `criteria.manual` rows with checkboxes; below: "Kriterleri düzenle →" link (`<a onClick={() => router.push('/projects/' + project.id + '?tab=settings&sub=lifecycle&phase=' + currentPhase.id)}>`).
    4. "AÇIK GÖREVLER ({phaseStats.open})" SegmentedControl with 3 options: `[{ id: 'next', label: T('Sonraki faza taşı','Move to next phase') }, { id: 'backlog', label: T('Backlog\'a taşı','Move to backlog') }, { id: 'stay', label: T('Bu fazda bırak','Leave in phase') }]`.
    5. Collapsible title "Farklı davranış gerekli? ({phaseStats.open} görev)". Inside: list every open task as a row `[StatusDot + key + title + mini select with options Aynı/Sonraki/Backlog/Kalsın]`. The "Aynı" option means the row inherits `openTaskAction`; non-"Aynı" entries populate `exceptions`.
    6. Note textarea: `&lt;textarea value={note} onChange={e =&gt; setNote(e.target.value)} maxLength={550} style={{ borderColor: note.length > 500 ? 'var(--priority-critical)' : 'var(--border)' }}/&gt;` + counter `&lt;small&gt;{note.length}/500&lt;/small&gt;`.
    7. Mode-aware AlertBanner: when `wf.mode === 'sequential-locked' && unmetCount > 0` → tone="danger" "Bazı kriterler karşılanmadı..."; when `'flexible' || 'sequential-flexible'` && unmetCount > 0 → tone="warning"; when `phaseStats.total === 0` → tone="info" "Bu fazda henüz görev yok. Geçiş serbestçe yapılabilir." (LIFE-03).
    8. Override checkbox: render only when `wf.mode === 'sequential-locked' && unmetCount > 0`. Label `T('Kriterler karşılanmadan geçilsin','Submit without meeting criteria')`.
    9. Submit button: when `allowOverride`, label `T('Zorla Geç','Force transition')` + `tone="danger"`; else `T('Faz Geçişini Onayla','Approve transition')` + `tone="primary"`. `disabled={note.length > 500 || mutation.isPending || countdown !== null}`.

    On submit: build `dto = { source_phase_id: currentPhase.id, target_phase_id: nextPhase.id, allow_override: allowOverride, exceptions: Object.entries(exceptions).filter(([_, action]) => action !== 'same').map(([taskId, action]) => ({ task_id: Number(taskId), action: action === 'stay' ? 'stay' : action })), note }`. Call `mutation.mutate(dto)`.

    On 429: `setCountdown(err.response.data.retry_after_seconds || 30)`; setInterval decrement; clear at 0.

    On success: toast `T('Geçiş tamam. Aktif faz: ','Transition complete. Active phase: ') + data.target_phase_name`; call `onClose()`; do NOT reset() — useEffect cleanup handles it.

    On 409: AlertBanner warning + render "Tekrar Dene" Button that re-calls `mutation.mutate(dto)` with the SAME `idempotencyKey` (already managed by `usePhaseTransition`). No new UUID.

    Idempotency-Key visibility: pass through to the service as `mutation.mutate(dto)` parameter; service signature `phaseGateService.execute(projectId, dto, idempotencyKey)` includes it as a header.

    Create `Frontend2/components/lifecycle/phase-gate-expand.test.tsx` covering all 12 cases. Mock `phaseGateService.execute` via `vi.mock('@/services/phase-gate-service')`. For Test 7 (key reuse), capture the third positional argument across two calls and `expect(call1[2]).toBe(call2[2])`. For Test 8 (key new on reopen), unmount + remount the component and assert the third arg differs.
  </action>
  <verify>
    <automated>cd Frontend2 && npm run test -- phase-gate-expand</automated>
  </verify>
  <acceptance_criteria>
    - `cd Frontend2 && npm run test -- phase-gate-expand` exits 0
    - `grep "usePhaseTransition\\|idempotencyKey" Frontend2/components/lifecycle/phase-gate-expand.tsx | wc -l` returns >= 2 (consumes the Plan 12-01 hook)
    - `grep "Uygulanamaz" Frontend2/components/lifecycle/phase-gate-expand.tsx` finds the LIFE-03 branch copy
    - `grep "phaseStats.total\\s*===\\s*0\\|total\\s*===\\s*0" Frontend2/components/lifecycle/phase-gate-expand.tsx` finds the zero-task gate
    - `grep "Zorla Geç" Frontend2/components/lifecycle/phase-gate-expand.tsx` finds the override-button relabel
    - `grep "Kriterleri düzenle\\|sub=lifecycle&phase=" Frontend2/components/lifecycle/phase-gate-expand.tsx` finds the deep-link
    - `grep "maxLength\\|note.length\\s*>\\s*500" Frontend2/components/lifecycle/phase-gate-expand.tsx` finds the 500-char gate
    - `grep "retry_after_seconds\\|countdown" Frontend2/components/lifecycle/phase-gate-expand.tsx` finds the 429 countdown
    - All 12 test cases present (`grep -c "it\\(" Frontend2/components/lifecycle/phase-gate-expand.test.tsx` returns >= 12)
  </acceptance_criteria>
  <done>Phase Gate inline panel ships with all 5 error states + LIFE-03 zero-task branch + override flow + Idempotency-Key reuse semantics + deep-link to Settings > Yaşam Döngüsü. Tests cover all 12 scenarios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship summary-strip.tsx + lifecycle-tab.tsx outer shell + replace lifecycle-stub-tab.tsx in project-detail-shell.tsx</name>
  <files>
    Frontend2/components/lifecycle/summary-strip.tsx,
    Frontend2/components/lifecycle/summary-strip.test.tsx,
    Frontend2/components/lifecycle/lifecycle-tab.tsx,
    Frontend2/components/project-detail/lifecycle-stub-tab.tsx,
    Frontend2/components/project-detail/project-detail-shell.tsx
  </files>
  <read_first>
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md lines 771-796 — SummaryStrip anatomy
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-UI-SPEC.md lines 351-440 — Lifecycle tab copy tables
    - .planning/phases/12-lifecycle-phase-gate-workflow-editor/12-CONTEXT.md D-36, D-40, D-59 — summary strip + Kanban-mode visibility
    - New_Frontend/src/pages/lifecycle-tab.jsx lines 51-75 — prototype SummaryStrip (visual ref)
    - Frontend2/components/project-detail/lifecycle-stub-tab.tsx — current stub (8 lines) being replaced
    - Frontend2/components/project-detail/project-detail-shell.tsx — current routing (look for the lifecycle tab branch importing LifecycleStubTab)
    - Frontend2/components/project-detail/board-tab.tsx — model file for `useTasks` + `useProjectDetail` consumption pattern (lines 50-80)
    - Frontend2/components/primitives/badge.tsx + progress-bar.tsx — primitives consumed
    - Frontend2/hooks/use-transition-authority.ts (Plan 12-01)
  </read_first>
  <behavior>
    - Test 1 (summary-strip render): with default workflow shows current phase Badge + ProgressBar + active phase index + 'Sonraki Faza Geç' + 'Düzenle' buttons
    - Test 2 (continuous mode hides "Sonraki Faza Geç"): when `wf.mode === 'continuous'`, "Sonraki Faza Geç" button does NOT render
    - Test 3 (permission false hides "Sonraki Faza Geç"): when `useTransitionAuthority` returns false, "Sonraki Faza Geç" button does NOT render
    - Test 4 (Düzenle navigates): clicking "Düzenle" calls `router.push('/workflow-editor?projectId=' + project.id)`
    - Test 5 (project-detail-shell wires lifecycle to LifecycleTab): the lifecycle branch of project-detail-shell.tsx imports `LifecycleTab` from `@/components/lifecycle/lifecycle-tab` instead of `LifecycleStubTab`
  </behavior>
  <action>
    **Step 1 — Create `Frontend2/components/lifecycle/summary-strip.tsx`** per UI-SPEC §1 lines 771-796. `"use client"`. Component: `export function SummaryStrip({ project, workflow, activePhase, phaseProgress, onOpenGate }: { project: Project; workflow: WorkflowConfig; activePhase: WorkflowNode | null; phaseProgress: number; onOpenGate: () => void })`.

    Layout: flex row, padding `10px 16px`, `border-bottom: 1px solid var(--border)`, gap 16, flex-wrap.
    - `&lt;Badge tone="primary"&gt;{indexOfActive}/{totalPhases} — {activePhase.name}&lt;/Badge&gt;`
    - `&lt;ProgressBar value={phaseProgress} max={100} height={4} style={{ width: 120 }}/&gt;`
    - `&lt;span style={{ fontFamily: 'var(--font-mono)' }}&gt;{phaseProgress}%&lt;/span&gt;`
    - `&lt;span&gt;{phaseStats.open} kalan&lt;/span&gt;`
    - Optional next-milestone chip (degrades gracefully): `&lt;span&gt;⊕ {nextMilestone.name} — {daysRemaining} gün&lt;/span&gt;`
    - flex-spacer
    - `&lt;Badge size="xs" tone="neutral"&gt;{modeLabel}&lt;/Badge&gt;` where `modeLabel` is the localized label for `workflow.mode` per UI-SPEC copy.
    - `{useTransitionAuthority(project) && workflow.mode !== 'continuous' && &lt;Button tone="primary" onClick={onOpenGate}&gt;{T('Sonraki Faza Geç','Move to next phase')}&lt;/Button&gt;}`
    - `&lt;Button tone="ghost" onClick={() =&gt; router.push('/workflow-editor?projectId=' + project.id)}&gt;{T('Düzenle','Edit')}&lt;/Button&gt;`

    **Step 2 — Create `Frontend2/components/lifecycle/summary-strip.test.tsx`** with 4 RTL cases per `<behavior>` Tests 1-4. Use `vi.mock` for `useTransitionAuthority` to flip the return value across test cases.

    **Step 3 — Create `Frontend2/components/lifecycle/lifecycle-tab.tsx`** as the outer shell. `"use client"`. Component: `export function LifecycleTab({ project }: { project: Project })`.
    - Read workflow + activity via `useQuery(['project', project.id])` (already cached) and `useCycleCounters(project.id)` (Plan 12-01).
    - Compute `nodeStates = computeNodeStates({ workflow, phaseTransitions: activity })` from Plan 12-01 pure lib.
    - Compute `activePhase` = first node with state==='active' in `nodeStates`.
    - Compute `nextPhase` from outgoing flow edges of `activePhase` (first matching target).
    - State: `gateOpen: boolean`. Toggle via `onOpenGate` from summary-strip.
    - Render layout: `&lt;div&gt;&lt;SummaryStrip ... onOpenGate={() =&gt; setGateOpen(true)}/&gt;{gateOpen && &lt;PhaseGateExpand currentPhase={activePhase} nextPhase={nextPhase} criteria={criteria} onClose={() =&gt; setGateOpen(false)}/&gt;}&lt;WorkflowCanvas workflow={workflow} readOnly activePhase={activePhase}/&gt;&lt;div&gt;{T('Alt sekmeler Plan 12-04..06\'da geliyor.','Sub-tabs land in Plan 12-04..06.')}&lt;/div&gt;&lt;/div&gt;`. The placeholder text for sub-tabs is replaced in Plan 12-04.

    **Step 4 — DELETE `Frontend2/components/project-detail/lifecycle-stub-tab.tsx`** by replacing the file content with a deprecation marker that re-exports `LifecycleTab` from `@/components/lifecycle/lifecycle-tab`. This avoids breaking any consumer that imports `LifecycleStubTab` while making the stub a no-op:
    ```tsx
    // Phase 12 Plan 12-02: this stub is replaced by LifecycleTab.
    // Kept as a re-export to minimize import churn; remove in Phase 13 if desired.
    export { LifecycleTab as LifecycleStubTab } from "@/components/lifecycle/lifecycle-tab"
    ```

    **Step 5 — MODIFY `Frontend2/components/project-detail/project-detail-shell.tsx`**: in the lifecycle branch of the tab switch, import `LifecycleTab` from `@/components/lifecycle/lifecycle-tab` and render `&lt;LifecycleTab project={project}/&gt;` directly. Remove the `LifecycleStubTab` import.
  </action>
  <verify>
    <automated>cd Frontend2 && npm run test -- summary-strip</automated>
  </verify>
  <acceptance_criteria>
    - `cd Frontend2 && npm run test -- summary-strip` exits 0
    - `grep "useTransitionAuthority" Frontend2/components/lifecycle/summary-strip.tsx` finds the permission gate
    - `grep "wf.mode === 'continuous'\\|workflow.mode === 'continuous'\\|workflow.mode !== 'continuous'" Frontend2/components/lifecycle/summary-strip.tsx` finds the Kanban-mode hide rule
    - `grep "/workflow-editor?projectId=" Frontend2/components/lifecycle/summary-strip.tsx` finds the Düzenle link
    - `grep "PhaseGateExpand\\|SummaryStrip\\|WorkflowCanvas" Frontend2/components/lifecycle/lifecycle-tab.tsx | wc -l` returns >= 3 (consumes all three child components)
    - `grep "computeNodeStates" Frontend2/components/lifecycle/lifecycle-tab.tsx` finds the BFS consumption from Plan 12-01
    - `cat Frontend2/components/project-detail/lifecycle-stub-tab.tsx | wc -l` returns < 5 (stub now a thin re-export)
    - `grep "LifecycleTab" Frontend2/components/project-detail/project-detail-shell.tsx` finds the new import + usage
    - `grep "LifecycleStubTab" Frontend2/components/project-detail/project-detail-shell.tsx | wc -l` returns 0 (no remaining LifecycleStubTab references — only re-export from the stub file)
  </acceptance_criteria>
  <done>SummaryStrip ships with permission + mode gates. LifecycleTab outer shell ships with Plan 12-01 BFS-driven activePhase computation and PhaseGate trigger. project-detail-shell.tsx routes lifecycle tab to the real LifecycleTab. lifecycle-stub-tab.tsx becomes a thin re-export.</done>
</task>

</tasks>

<verification_criteria>
The plan-checker confirms this plan by:
1. Running `cd Frontend2 && npm run test -- phase-gate-expand summary-strip lifecycle-tab` and observing exit 0
2. Visiting `/projects/{id}` for an existing project (manual smoke test) and verifying the AlertBanner stub no longer shows
3. Confirming `lifecycle-stub-tab.tsx` shrunk to a thin re-export
4. Confirming `project-detail-shell.tsx` no longer imports `LifecycleStubTab` directly (only re-export from the stub re-points)
5. Visually inspecting the Phase Gate panel renders the 5 error states (manual UAT row in Plan 12-10)
</verification_criteria>

<success_criteria>
- LIFE-02 fully delivered: Phase Gate panel + 5-error matrix + override flow + Idempotency-Key reuse + zero-task branch
- `lifecycle-stub-tab.tsx` deprecated to re-export
- All RTL test cases green
- No imports from old `Frontend/`
- All user-facing strings via `T()` per `useApp().language`
</success_criteria>

<output>
After completion, create `.planning/phases/12-lifecycle-phase-gate-workflow-editor/12-02-SUMMARY.md` documenting:
- Confirmation that Idempotency-Key reuse-on-retry vs new-on-reopen behavior is implemented and tested
- Whether the "Açık Görevler" SegmentedControl + exceptions Collapsible needed any backend DTO adaptation (none expected per Phase 9 D-04)
- Notes on the methodology-aware mode label localization
</output>
