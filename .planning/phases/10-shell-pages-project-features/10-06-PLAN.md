---
phase: 10-shell-pages-project-features
plan: "06"
type: execute
wave: 4
depends_on:
  - "10-01"
  - "10-03"
  - "10-05"
files_modified:
  - Frontend2/app/(shell)/settings/page.tsx
autonomous: true
requirements:
  - PAGE-05

must_haves:
  truths:
    - "Settings page renders all 5 prototype tabs: Profil, Tercihler, Görünüm, Bildirimler, Güvenlik"
    - "Profil tab shows avatar upload + name/email edit form wired to authService.updateProfile"
    - "Tercihler tab wires language and density to real AppContext state"
    - "Görünüm tab shows theme preset cards + brand color sliders"
    - "Güvenlik tab shows only password change form (current + new + confirm) — no 2FA or sessions list (D-32)"
  artifacts:
    - path: "Frontend2/app/(shell)/settings/page.tsx"
      provides: "5-tab settings page"
      contains: "Profil"
  key_links:
    - from: "Frontend2/app/(shell)/settings/page.tsx"
      to: "authService.updateProfile"
      via: "useMutation in Profil and Güvenlik tabs"
      pattern: "authService\\.updateProfile"
    - from: "Frontend2/app/(shell)/settings/page.tsx"
      to: "useApp().setDensity"
      via: "Tercihler density select"
      pattern: "setDensity"
---

<objective>
Implement the Settings page with all 5 prototype tabs fully functional.

Purpose: PAGE-05 requirement. Settings are needed for language/density/theme user preferences, profile management, and password change.

Output: settings/page.tsx replacing the stub, implementing all 5 tabs (Profil, Tercihler, Görünüm, Bildirimler, Güvenlik) as specified in prototype settings.jsx and UI-SPEC.
</objective>

<execution_context>
@/home/.claude/get-shit-done/workflows/execute-plan.md
@/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-shell-pages-project-features/10-CONTEXT.md
@.planning/phases/10-shell-pages-project-features/10-UI-SPEC.md
@.planning/phases/10-shell-pages-project-features/10-PATTERNS.md
</context>

<interfaces>
<!-- Settings page spec from UI-SPEC + settings.jsx -->

Settings layout (settings.jsx — 2-column: left sidebar + right content):
- Left sidebar: min-width:200, padding-right:24, border-right:1px solid var(--border)
  - Title: "Ayarlar" 15px 600, subtitle "Kişisel tercihleriniz" 12px muted
  - 5 tab buttons (vertical list, gap:2)
  - Active tab: background:var(--accent), color:var(--accent-fg), border-radius:6
  - Inactive: hover background:var(--surface-2)
  - Each tab: padding:8px 10px, font-size:12.5, font-weight:500, display:flex, gap:8, align-items:center
  - Icons: size=14 (Users/Settings/Palette/Bell/Shield)
- Right content: flex:1, padding-left:24

Tab icons (UI-SPEC):
- Profil → Icons.Users size=14
- Tercihler → Icons.Settings size=14
- Görünüm → Icons.Palette size=14
- Bildirimler → Icons.Bell size=14
- Güvenlik → Icons.Shield size=14

Tercihler tab (D-31): 6 rows
1. Dil — select (Türkçe/English) — wired to setLanguage()
2. Başlangıç sayfası — select (Dashboard/Projeler/Görevlerim) — visual only (no-op)
3. Görünüm yoğunluğu — select (Kompakt/Rahat/Geniş) — wired to setDensity()
4. Hafta başlangıcı — select (Pazartesi/Pazar) — visual only
5. Klavye kısayolları — Toggle — visual only
6. Komut paleti (⌘K) — Toggle — visual only

Güvenlik tab (D-32): ONE card only — "Parola" card:
- 3 fields: Mevcut parola / Yeni parola / Tekrar
- Button: "Parolayı güncelle" variant="primary"
- Calls authService.updateProfile({ current_password, new_password })
- NO 2FA card, NO active sessions card

AppContext density enum: "compact" | "cozy" | "comfortable" (verified from app-context.tsx)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Settings page — all 5 tabs (PAGE-05)</name>
  <read_first>
    - New_Frontend/src/pages/settings.jsx (full file — 336 lines. All 5 tabs exact structure, ProfileSection, PreferencesSection, AppearanceSection, NotificationsSection, SecuritySection)
    - Frontend2/app/(shell)/settings/page.tsx (current stub to replace)
    - Frontend2/context/app-context.tsx (density/setDensity, language/setLanguage — exact prop names)
    - Frontend2/context/auth-context.tsx (user, useAuth)
    - Frontend2/services/auth-service.ts (updateProfile, uploadAvatar)
    - Frontend2/lib/theme.ts (applyTokens/applyMode/applyRadius — for Görünüm tab theme picker)
  </read_first>
  <files>
    Frontend2/app/(shell)/settings/page.tsx
  </files>
  <action>
Replace stub entirely. Implement all 5 tabs in a single file. Match settings.jsx layout:
- Outer: `display:grid; grid-template-columns:200px 1fr; gap:0; min-height:calc(100vh - 52px)`
- Left sidebar + right panel pattern as described in interfaces section above.

**Profil tab:**
- Avatar: 64px circle, click to upload (input[type=file] hidden, click trigger). Call `authService.uploadAvatar(file)` via useMutation. On success, update AuthContext user (call authService.getCurrentUser() after upload).
- Form: full_name input + email input. "Değişiklikleri kaydet" Button variant="primary". On submit: `authService.updateProfile({ full_name, email })`.
- useMutation with onSuccess toast "Değişiklikler kaydedildi." and onError toast "Bir şeyler ters gitti."

**Tercihler tab (D-31):**
- Language row: label "Dil" + select with options "Türkçe" (value='tr') / "English" (value='en'). onChange: `setLanguage(value)`. Wired to real AppContext.
- UI Density row: label "Görünüm yoğunluğu" + select "Kompakt" (value='compact') / "Rahat" (value='cozy') / "Geniş" (value='comfortable'). onChange: `setDensity(value)`. Wired to real AppContext.
- Other 4 rows: render visually as per prototype but no state wiring (onClick no-op):
  - Başlangıç sayfası: select with options (visual only)
  - Hafta başlangıcı: select with options (visual only)
  - Klavye kısayolları: Toggle component (visual only, state not persisted)
  - Komut paleti (⌘K): Toggle component (visual only)

**Görünüm tab:**
Match settings.jsx appearance section. Theme preset grid (presets from lib/theme.ts), brand color input/sliders, dark mode toggle, radius slider. Call `applyTokens()` / `applyMode()` / `applyRadius()` from lib/theme.ts on change. Use the existing `useApp().mode` and `setMode()`.

**Bildirimler tab:**
Render notification preference toggles as in settings.jsx. All visual-only (no backend wiring in Phase 10). Each toggle uses Toggle primitive.

**Güvenlik tab (D-32 — password change only):**
ONE card "Parola" with:
```typescript
const [currentPass, setCurrentPass] = React.useState('')
const [newPass, setNewPass] = React.useState('')
const [confirmPass, setConfirmPass] = React.useState('')
const changePasswordMutation = useMutation({
  mutationFn: () => authService.updateProfile({ current_password: currentPass, new_password: newPass }),
  onSuccess: () => {
    showToast({ message: language === 'tr' ? 'Parolanız güncellendi.' : 'Password updated.', variant: 'success' })
    setCurrentPass(''); setNewPass(''); setConfirmPass('')
  },
  onError: () => showToast({ message: language === 'tr' ? 'Parola güncellenemedi.' : 'Password update failed.', variant: 'error' }),
})
const canChangePassword = currentPass.length > 0 && newPass.length >= 8 && newPass === confirmPass
```
- 3 Input fields (type="password") with labels "Mevcut parola" / "Yeni parola" / "Tekrar"
- Button variant="primary" disabled={!canChangePassword} text "Parolayı güncelle"
- DO NOT render 2FA card or Active Sessions card (D-32 scope limit)

Use Tab rendering pattern:
```typescript
const [activeTab, setActiveTab] = React.useState('Profil')
// Render tab content based on activeTab string
```

Tab label/icon pairs (UI-SPEC):
- "Profil" → Users icon SVG (14px)
- "Tercihler" → Settings gear SVG (14px)
- "Görünüm" → Palette SVG (14px)
- "Bildirimler" → Bell SVG (14px)
- "Güvenlik" → Shield SVG (14px)

Use inline SVG paths for icons (from New_Frontend/src/icons.jsx) since Frontend2/components/icons/ is being established in Phase 10. Port the 5 icons needed here as inline SVG elements.

Each settings section is wrapped in a Card with `padding={20}` per prototype. Multiple cards per tab are stacked with `gap:16px`.

The full file will be approximately 400-500 lines. Structure with local components for each tab section:
- function ProfileSection() {...}
- function PreferencesSection() {...}
- function AppearanceSection() {...}
- function NotificationsSection() {...}
- function SecuritySection() {...}

These are defined inside the page file (local to module, not exported) to avoid file explosion.
  </action>
  <verify>
    <automated>cd "c:\Users\yusti\Desktop\bitirme projesi\SPMS\Frontend2" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Frontend2/app/(shell)/settings/page.tsx contains "Profil", "Tercihler", "Görünüm", "Bildirimler", "Güvenlik" tab labels
    - Frontend2/app/(shell)/settings/page.tsx contains "setDensity" and "setLanguage" calls (D-31 wired)
    - Frontend2/app/(shell)/settings/page.tsx contains "authService.updateProfile" for profile save
    - Frontend2/app/(shell)/settings/page.tsx contains "authService.uploadAvatar" for avatar upload
    - Frontend2/app/(shell)/settings/page.tsx contains "current_password" and "new_password" fields (D-32 password change)
    - Frontend2/app/(shell)/settings/page.tsx does NOT contain "iki" or "iki faktörlü" or "sessions" strings (D-32 exclusion)
    - Frontend2/app/(shell)/settings/page.tsx contains "Parolayı güncelle" (Güvenlik tab CTA)
    - npx next build exits 0 (full build)
  </acceptance_criteria>
  <done>Settings page with all 5 tabs: Profil (avatar + profile edit), Tercihler (language+density wired), Görünüm (theme picker), Bildirimler (visual toggles), Güvenlik (password change only, D-32) — all prototype-faithful and build-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings form → PUT /auth/me | Profile update + password change both route through same endpoint |
| Avatar upload → POST /auth/me/avatar | File upload with multipart/form-data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Tampering | Güvenlik tab password change | mitigate | `current_password` required in request body — FastAPI validates it before allowing update. Client-side: disable button until all fields valid + new passwords match. |
| T-10-06-02 | Information Disclosure | Avatar upload form | mitigate | File input accepts only image types (accept="image/*"). Upload goes to authenticated endpoint only. |
| T-10-06-03 | Tampering | Tercihler density/language | accept | Client-side AppContext state only — no backend persistence in Phase 10. Preference survives session via localStorage (existing AppContext pattern). |
| T-10-06-04 | Denial of Service | Avatar upload file size | accept | FastAPI backend enforces file size limit per Phase 9 configuration. Frontend doesn't pre-validate size. |
</threat_model>

<verification>
After task:
- npx next build exits 0
- Settings renders 5 tabs
- Language change reflects in UI immediately
- Density change changes data-density attribute on html element
- Password change form: button disabled until newPass === confirmPass
</verification>

<success_criteria>
- All 5 settings tabs render prototype-faithful (PAGE-05)
- Language + UI density wired to real AppContext (D-31)
- Password change form calls PUT /auth/me (D-32)
- No 2FA or sessions UI (D-32 scope limit respected)
</success_criteria>

<output>
After completion, create `.planning/phases/10-shell-pages-project-features/10-06-SUMMARY.md`
</output>
