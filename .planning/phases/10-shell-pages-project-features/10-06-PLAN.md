---
phase: 10-shell-pages-project-features
plan: "06"
type: execute
wave: 4
depends_on:
  - "10-01"
  - "10-03"
  - "10-05"
files_modified:
  - Frontend2/app/(shell)/settings/page.tsx
autonomous: true
requirements:
  - PAGE-05

must_haves:
  truths:
    - "Settings page renders all 5 prototype tabs: Profil, Tercihler, Görünüm, Bildirimler, Güvenlik"
    - "Profil tab shows avatar upload + name/email edit form wired to authService.updateProfile"
    - "Tercihler tab wires language and density to real AppContext state"
    - "Görünüm tab shows theme preset cards + brand color sliders"
    - "Güvenlik tab shows only password change form (current + new + confirm) — no 2FA or sessions list (D-32)"
  artifacts:
    - path: "Frontend2/app/(shell)/settings/page.tsx"
      provides: "5-tab settings page"
      contains: "Profil"
  key_links:
    - from: "Frontend2/app/(shell)/settings/page.tsx"
      to: "authService.updateProfile"
      via: "useMutation in Profil and Güvenlik tabs"
      pattern: "authService\\.updateProfile"
    - from: "Frontend2/app/(shell)/settings/page.tsx"
      to: "useApp().setDensity"
      via: "Tercihler density select"
      pattern: "setDensity"
---

<objective>
Implement the Settings page with all 5 prototype tabs fully functional, split into two tasks for scope control (W5 fix).

Purpose: PAGE-05 requirement. Settings are needed for language/density/theme user preferences, profile management, and password change.

Output: settings/page.tsx replacing the stub. Task 1 covers API-wired tabs (Profil + Tercihler + Güvenlik). Task 2 covers UI-only tabs (Görünüm + Bildirimler) plus AppContext density wiring.
</objective>

<execution_context>
@/home/.claude/get-shit-done/workflows/execute-plan.md
@/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-shell-pages-project-features/10-CONTEXT.md
@.planning/phases/10-shell-pages-project-features/10-UI-SPEC.md
@.planning/phases/10-shell-pages-project-features/10-PATTERNS.md
</context>

<interfaces>
<!-- Settings page spec from UI-SPEC + settings.jsx -->

Settings layout (settings.jsx — 2-column: left sidebar + right content):
- Left sidebar: min-width:200, padding-right:24, border-right:1px solid var(--border)
  - Title: "Ayarlar" 15px 600, subtitle "Kişisel tercihleriniz" 12px muted
  - 5 tab buttons (vertical list, gap:2)
  - Active tab: background:var(--accent), color:var(--accent-fg), border-radius:6
  - Inactive: hover background:var(--surface-2)
  - Each tab: padding:8px 10px, font-size:12.5, font-weight:500, display:flex, gap:8, align-items:center
  - Icons: size=14 (Users/Settings/Palette/Bell/Shield)
- Right content: flex:1, padding-left:24

Tab icons (UI-SPEC):
- Profil → Icons.Users size=14
- Tercihler → Icons.Settings size=14
- Görünüm → Icons.Palette size=14
- Bildirimler → Icons.Bell size=14
- Güvenlik → Icons.Shield size=14

Tercihler tab (D-31): 6 rows
1. Dil — select (Türkçe/English) — wired to setLanguage()
2. Başlangıç sayfası — select (Dashboard/Projeler/Görevlerim) — visual only (no-op)
3. Görünüm yoğunluğu — select (Kompakt/Rahat/Geniş) — wired to setDensity()
4. Hafta başlangıcı — select (Pazartesi/Pazar) — visual only
5. Klavye kısayolları — Toggle — visual only
6. Komut paleti (⌘K) — Toggle — visual only

Güvenlik tab (D-32): ONE card only — "Parola" card:
- 3 fields: Mevcut parola / Yeni parola / Tekrar
- Button: "Parolayı güncelle" variant="primary"
- Calls authService.updateProfile({ current_password, new_password })
- NO 2FA card, NO active sessions card

AppContext density enum: "compact" | "cozy" | "comfortable" (verified from app-context.tsx)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Settings page skeleton + API-wired tabs (Profil, Tercihler, Güvenlik)</name>
  <read_first>
    - New_Frontend/src/pages/settings.jsx (full file — 336 lines. All 5 tabs exact structure)
    - Frontend2/app/(shell)/settings/page.tsx (current stub to replace)
    - Frontend2/context/app-context.tsx (density/setDensity, language/setLanguage — exact prop names)
    - Frontend2/context/auth-context.tsx (user, useAuth)
    - Frontend2/services/auth-service.ts (updateProfile, uploadAvatar)
  </read_first>
  <files>
    Frontend2/app/(shell)/settings/page.tsx
  </files>
  <action>
Create `Frontend2/app/(shell)/settings/page.tsx` with the full 2-column layout, all 5 tab navigation, and the three API-wired tabs (Profil, Tercihler, Güvenlik). Görünüm and Bildirimler tab content is added in Task 2.

**Outer layout:**
```typescript
"use client"
import * as React from "react"
import { useMutation } from "@tanstack/react-query"
import { authService } from "@/services/auth-service"
import { useAuth } from "@/context/auth-context"
import { useApp } from "@/context/app-context"
import { useToast } from "@/components/toast"
import { Card, Button } from "@/components/primitives"

const [activeTab, setActiveTab] = React.useState('Profil')
// Outer: display:grid; grid-template-columns:200px 1fr; gap:0; min-height:calc(100vh - 52px)
```

Match the sidebar + right panel 2-column grid exactly per settings.jsx. Use inline SVG paths for the 5 icons (Users, Settings, Palette, Bell, Shield) since Frontend2/components/icons/ may not be fully established yet. Each tab button is:
```typescript
style={{
  display:"flex", alignItems:"center", gap:8, padding:"8px 10px",
  fontSize:12.5, fontWeight:500, borderRadius:6, width:"100%",
  background: activeTab === label ? "var(--accent)" : "transparent",
  color: activeTab === label ? "var(--accent-fg)" : "var(--fg)",
  border:"none", cursor:"pointer", textAlign:"left",
}}
```

**Profil tab (ProfileSection):**
- Avatar: 64px circle (border-radius 50%), click to trigger hidden `input[type=file accept="image/*"]`. Call `authService.uploadAvatar(file)` via useMutation. On success: call `authService.getCurrentUser()` to update auth context.
- Form fields: full_name (labeled "Ad Soyad") + email. Pre-populated from `useAuth().user`.
- "Değişiklikleri kaydet" Button variant="primary". On submit: `authService.updateProfile({ full_name, email })`.
- useMutation with:
  - onSuccess: `showToast({ message: language === 'tr' ? 'Değişiklikler kaydedildi.' : 'Changes saved.', variant: 'success' })`
  - onError: `showToast({ message: language === 'tr' ? 'Bir şeyler ters gitti.' : 'Something went wrong.', variant: 'error' })`

**Tercihler tab (PreferencesSection) — D-31:**
Implement as described in interfaces section:
- Row 1 (Dil): `<select value={language} onChange={e => setLanguage(e.target.value)}>` with `<option value="tr">Türkçe</option><option value="en">English</option>`. onChange immediately calls setLanguage — no save button needed.
- Row 3 (Görünüm yoğunluğu): `<select value={density} onChange={e => setDensity(e.target.value as 'compact' | 'cozy' | 'comfortable')}>` with the 3 options.
- Rows 2, 4, 5, 6: render select/toggles visually (no-op handlers).

Each row styled as: `display:flex; justify-content:space-between; align-items:center; padding:12px 0; border-bottom:1px solid var(--border)`. Last row omits border-bottom.

**Güvenlik tab (SecuritySection) — D-32:**
ONE card "Parola":
```typescript
const [currentPass, setCurrentPass] = React.useState('')
const [newPass, setNewPass] = React.useState('')
const [confirmPass, setConfirmPass] = React.useState('')
const changePasswordMutation = useMutation({
  mutationFn: () => authService.updateProfile({ current_password: currentPass, new_password: newPass }),
  onSuccess: () => {
    showToast({ message: language === 'tr' ? 'Parolanız güncellendi.' : 'Password updated.', variant: 'success' })
    setCurrentPass(''); setNewPass(''); setConfirmPass('')
  },
  onError: () => showToast({ message: language === 'tr' ? 'Parola güncellenemedi.' : 'Password update failed.', variant: 'error' }),
})
const canChangePassword = currentPass.length > 0 && newPass.length >= 8 && newPass === confirmPass
```
- 3 Input fields (type="password") with labels "Mevcut parola" / "Yeni parola" / "Tekrar"
- Button variant="primary" `disabled={!canChangePassword || changePasswordMutation.isPending}` text "Parolayı güncelle"
- DO NOT render 2FA card or Active Sessions card (D-32 scope limit)

**Görünüm and Bildirimler tabs:** For now render a placeholder `<div style={{color:'var(--fg-muted)',fontSize:13,padding:'20px 0'}}>Yükleniyor...</div>` — Task 2 will replace with full implementation.

Structure with local component functions inside the file:
```
function ProfileSection() { ... }
function PreferencesSection() { ... }
function SecuritySection() { ... }
function AppearanceSection() { ... }   // placeholder for Task 2
function NotificationsSection() { ... } // placeholder for Task 2
```
  </action>
  <verify>
    <automated>cd "c:\Users\yusti\Desktop\bitirme projesi\SPMS\Frontend2" && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - Frontend2/app/(shell)/settings/page.tsx contains "Profil", "Tercihler", "Görünüm", "Bildirimler", "Güvenlik" tab labels
    - Frontend2/app/(shell)/settings/page.tsx contains "setDensity" and "setLanguage" calls (D-31 wired)
    - Frontend2/app/(shell)/settings/page.tsx contains "authService.updateProfile" for profile save
    - Frontend2/app/(shell)/settings/page.tsx contains "authService.uploadAvatar" for avatar upload
    - Frontend2/app/(shell)/settings/page.tsx contains "current_password" and "new_password" fields (D-32 password change)
    - Frontend2/app/(shell)/settings/page.tsx does NOT contain "iki" or "iki faktörlü" or "sessions" strings (D-32 exclusion)
    - Frontend2/app/(shell)/settings/page.tsx contains "Parolayı güncelle" (Güvenlik tab CTA)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Settings page skeleton + 3 API-wired tabs (Profil, Tercihler, Güvenlik) implemented. Görünüm and Bildirimler have stubs — filled in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Settings page — UI-only tabs (Görünüm + Bildirimler) + AppContext density wiring</name>
  <read_first>
    - Frontend2/app/(shell)/settings/page.tsx (just created in Task 1 — read to find AppearanceSection and NotificationsSection placeholder locations)
    - New_Frontend/src/pages/settings.jsx (lines 170-336 — AppearanceSection + NotificationsSection exact structure and all toggle/slider values)
    - Frontend2/lib/theme.ts (applyTokens/applyMode/applyRadius — for Görünüm tab theme picker)
    - Frontend2/context/app-context.tsx (mode/setMode for dark mode toggle in Görünüm tab)
  </read_first>
  <files>
    Frontend2/app/(shell)/settings/page.tsx
  </files>
  <action>
Read `Frontend2/app/(shell)/settings/page.tsx` to find the `AppearanceSection` and `NotificationsSection` placeholder functions (both render a `Yükleniyor...` div from Task 1). Replace the placeholder bodies with full implementations:

**AppearanceSection (Görünüm tab):**
Match settings.jsx appearance section:
- Theme preset grid: fetch presets from `lib/theme.ts` (the `PRESETS` or `themes` export — read the file first). Render each preset as a clickable card (40×40 color swatch + name). On click: `applyTokens(preset)`.
- Dark/light mode toggle: uses `useApp().mode` and `setMode()`. Toggle between 'dark' and 'light'.
- Brand color hue slider: range input (0-360) for `--primary-hue` CSS variable. On change: `applyTokens({ hue: value })` or direct `document.documentElement.style.setProperty('--primary-hue', value)` — check lib/theme.ts first to see which API to use.
- Border radius slider: range input. On change: `applyRadius(value)` from lib/theme.ts.
- Each control row styled like Tercihler rows (flex, justify-between, 12px 0 padding, border-bottom).

**NotificationsSection (Bildirimler tab):**
Match settings.jsx notifications section:
- Render notification preference toggles as in settings.jsx. All visual-only — no backend wiring in Phase 10.
- Typical toggles: "E-posta bildirimleri", "Görev atamaları", "Proje güncellemeleri", "Haftalık özet" — check settings.jsx for exact labels.
- Each toggle row: label (13px) + Toggle component (from @/components/primitives if available, else render a styled checkbox).

After replacing both section functions, the file should have all 5 tabs fully implemented.
  </action>
  <verify>
    <automated>cd "c:\Users\yusti\Desktop\bitirme projesi\SPMS\Frontend2" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Frontend2/app/(shell)/settings/page.tsx no longer contains "Yükleniyor..." inside AppearanceSection or NotificationsSection
    - Frontend2/app/(shell)/settings/page.tsx contains "applyTokens" or "applyMode" or "applyRadius" call (Görünüm tab theme wiring)
    - Frontend2/app/(shell)/settings/page.tsx contains "setMode" call (dark/light mode toggle)
    - Frontend2/app/(shell)/settings/page.tsx contains notification toggle entries from settings.jsx
    - npx next build exits 0 (full build)
  </acceptance_criteria>
  <done>Settings page complete with all 5 tabs: Profil (avatar + profile edit), Tercihler (language+density wired), Görünüm (theme picker + mode toggle), Bildirimler (visual toggles), Güvenlik (password change only, D-32) — all prototype-faithful and build-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings form → PUT /auth/me | Profile update + password change both route through same endpoint |
| Avatar upload → POST /auth/me/avatar | File upload with multipart/form-data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Tampering | Güvenlik tab password change | mitigate | `current_password` required in request body — FastAPI validates it before allowing update. Client-side: disable button until all fields valid + new passwords match. |
| T-10-06-02 | Information Disclosure | Avatar upload form | mitigate | File input accepts only image types (accept="image/*"). Upload goes to authenticated endpoint only. |
| T-10-06-03 | Tampering | Tercihler density/language | accept | Client-side AppContext state only — no backend persistence in Phase 10. Preference survives session via localStorage (existing AppContext pattern). |
| T-10-06-04 | Denial of Service | Avatar upload file size | accept | FastAPI backend enforces file size limit per Phase 9 configuration. Frontend doesn't pre-validate size. |
</threat_model>

<verification>
After both tasks:
- npx next build exits 0
- Settings renders 5 tabs
- Language change reflects in UI immediately
- Density change changes data-density attribute on html element
- Password change form: button disabled until newPass === confirmPass
- Görünüm tab: theme presets clickable, mode toggle works
</verification>

<success_criteria>
- All 5 settings tabs render prototype-faithful (PAGE-05)
- Language + UI density wired to real AppContext (D-31)
- Password change form calls PUT /auth/me (D-32)
- No 2FA or sessions UI (D-32 scope limit respected)
- Task 1 scope: ~200-250 lines (3 API-wired tabs); Task 2 scope: ~150-200 lines (2 UI-only tabs)
</success_criteria>

<output>
After completion, create `.planning/phases/10-shell-pages-project-features/10-06-SUMMARY.md`
</output>
