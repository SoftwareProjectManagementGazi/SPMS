---
phase: 10-shell-pages-project-features
plan: "09"
type: execute
wave: 4
depends_on:
  - "10-01"
  - "10-03"
  - "10-08"
files_modified:
  - Frontend2/app/(shell)/layout.tsx
  - Frontend2/components/app-shell.tsx
autonomous: false
requirements:
  - PAGE-01
  - PAGE-02

user_setup: []

must_haves:
  truths:
    - "Header 'Yeni proje' Create button navigates to /projects/new (deferred from Phase 8 per STATE.md 08-04 note)"
    - "Backend seeder run instructions documented for developer testing"
  artifacts:
    - path: "Frontend2/components/app-shell.tsx"
      provides: "AppShell with wired header Create button"
      contains: "projects/new"
  key_links:
    - from: "Frontend2/components/app-shell.tsx"
      to: "router.push('/projects/new')"
      via: "Header Create button onClick"
      pattern: "projects/new"
---

<objective>
Wire the deferred Header "Yeni proje" button to /projects/new, then verify the full phase integration works end-to-end with a human-verify checkpoint.

Purpose: STATE.md 08-04 note explicitly defers the Header Create button to Phase 10. The wizard page now exists, so this button can be wired. The checkpoint then confirms visual fidelity across all Phase 10 pages.

Output: Header Create button wired + human visual verification of all 5 phase deliverables.
</objective>

<execution_context>
@/home/.claude/get-shit-done/workflows/execute-plan.md
@/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-shell-pages-project-features/10-CONTEXT.md
@.planning/phases/10-shell-pages-project-features/10-UI-SPEC.md
</context>

<interfaces>
<!-- Header Create button — from STATE.md 08-04 note -->
STATE.md 08-04 note: "Create button in Header deferred to Phase 10 — avoids rendering inert buttons that look interactive"
The Header button is currently rendered as a non-functional placeholder (or commented out).
Need to find the button in app-shell.tsx or header component and add onClick navigation.

Shell header location: Frontend2/components/app-shell.tsx or Frontend2/components/header.tsx
The Create button should use: import { useRouter } from 'next/navigation'; router.push('/projects/new')
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire Header Create button to /projects/new</name>
  <read_first>
    - Frontend2/components/app-shell.tsx (find the Header section — look for Create/Yeni proje button that was deferred in 08-04)
    - New_Frontend/src/shell.jsx (lines 164-200 — header implementation in prototype, Create button location and style)
    - Frontend2/app/(shell)/layout.tsx (current content — may need "use client" addition if not already added from plan 03)
  </read_first>
  <files>
    Frontend2/components/app-shell.tsx
  </files>
  <action>
Read Frontend2/components/app-shell.tsx to locate the Create/Yeni proje header button. In Phase 8, this button was either:
(a) omitted entirely from the render
(b) rendered as a non-functional placeholder
(c) commented out

Per STATE.md 08-04 note: "Create → Phase 10 project-create wizard".

Find the relevant section (the header controls area) and wire it:
1. Add `"use client"` to app-shell.tsx if not already present (needed for useRouter)
2. Add import: `import { useRouter } from "next/navigation"`
3. Find the Create button location in the Header component
4. Add: `const router = useRouter()` in the Header component
5. Add `onClick={() => router.push('/projects/new')}` to the Create button

If the button was omitted in Phase 8 (deferred entirely), add it back following the prototype header pattern (shell.jsx). The Create button in prototype (shell.jsx ~line 184):
- Button variant="primary" size="sm"
- Icon: + or Plus SVG (14px)
- Text: "Yeni proje" (TR) / "New project" (EN)
- Positioned in the header right controls area after search input, before theme toggle

The button should NOT appear on the Login/session-expired pages (those use (auth) layout, not AppShell — already handled by route groups).

After wiring, run full TypeScript check.
  </action>
  <verify>
    <automated>cd "c:\Users\yusti\Desktop\bitirme projesi\SPMS\Frontend2" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Frontend2/components/app-shell.tsx contains "projects/new" in onClick or href
    - Frontend2/components/app-shell.tsx contains `router.push('/projects/new')` or `href="/projects/new"`
    - npx next build exits 0 (full build clean)
  </acceptance_criteria>
  <done>Header Create button wired to /projects/new — full Next.js build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All Phase 10 frontend + backend deliverables:

1. **Auth flow** (Plan 01/03): api-client.ts (axios + Bearer interceptor), AuthContext, middleware cookie guard, login page (2-column layout), session-expired page
2. **Dashboard** (Plan 04): StatCards + PortfolioTable + ActivityFeed + MethodologyCard, Manager/Member toggle
3. **Projects list** (Plan 05): 3-column grid, status filter SegmentedControl, ProjectCard with status strip/badge/overflow menu, confirmation dialog, Toast notifications
4. **Create wizard** (Plan 07): 4-step wizard at /projects/new with URL step params, sessionStorage draft, template fetch, lifecycle preview, project creation
5. **Settings** (Plan 06): All 5 tabs (Profil, Tercihler, Görünüm, Bildirimler, Güvenlik), language/density wired, password change
6. **Archive banner** (Plan 08): ArchiveBanner on archived project detail, Aktif Et reactivation
7. **Backend**: GET /api/v1/activity global endpoint, seeder with D-36 fields
8. **Header Create button** wired to /projects/new (Task 1 above)
  </what-built>
  <how-to-verify>
**Setup (backend must be running):**
1. Start backend: `cd Backend && uvicorn app.api.main:app --reload`
2. Run seeder (if fresh DB): `cd Backend && python -m app.infrastructure.database.seeder`
3. Verify seeder created varied statuses: `curl http://localhost:8000/api/v1/projects | python -m json.tool | grep status`
4. Start frontend: `cd Frontend2 && npm run dev`

**Auth flow verification:**
5. Open browser, visit `http://localhost:3000/dashboard`
6. Confirm redirect to `/login` (middleware cookie guard working)
7. Login with test credentials (seeded PM user: ayse.oz@testexample.com or check seeder)
8. Confirm redirect to `/dashboard` after login
9. Open DevTools → Application → LocalStorage: confirm `auth_token` is set
10. Open DevTools → Application → Cookies: confirm `auth_session=1` cookie exists

**Dashboard verification:**
11. Dashboard shows "Merhaba, {firstName}" greeting
12. 4 StatCards visible with numbers from API
13. PortfolioTable shows active projects (if any ACTIVE in seeded data)
14. ActivityFeed shows activity items from GET /api/v1/activity
15. Manager/Member toggle switches view
16. Open Network tab → confirm GET /activity called (not /projects/{id}/activity)

**Projects page verification:**
17. Navigate to `/projects` via sidebar
18. Confirm 3-column responsive grid (resize window to test 2-col and 1-col breakpoints)
19. Confirm project cards have colored top strip (ACTIVE=primary, COMPLETED=green, ON_HOLD=amber)
20. Archived cards: confirm opacity 0.6
21. SegmentedControl: click "Aktif" → confirm only ACTIVE projects shown; Network tab shows `?status=ACTIVE`
22. 3-dot menu on a card → confirm Tamamla/Askıya Al/Arşivle options appear
23. Click "Arşivle" → confirm dialog opens → click "Onayla" → confirm success toast

**Wizard verification:**
24. Click "Yeni proje" button (header OR projects page) → confirm navigation to /projects/new?step=1
25. Step 1: Fill in project name + key → "Devam" button enables
26. Step 2: Templates load from API as cards
27. Browser back button → returns to step 1 with form data preserved (sessionStorage)
28. Refresh page → confirm form fields restored (sessionStorage draft)
29. Complete all 4 steps → click "Projeyi Oluştur" → project created → redirected to /projects/{id}
30. Confirm sessionStorage cleared after creation

**Settings verification:**
31. Navigate to `/settings`
32. Confirm 5 tabs in left sidebar: Profil, Tercihler, Görünüm, Bildirimler, Güvenlik
33. Tercihler tab: change language to English → confirm UI switches to English
34. Tercihler tab: change density to "Kompakt" → confirm `data-density="compact"` on html element (DevTools)
35. Güvenlik tab: confirm ONLY password change form — no 2FA card, no sessions list

**Archive banner verification:**
36. Navigate to `/projects/{id_of_archived_project}`
37. Confirm orange AlertBanner at top: "Bu proje arşivlenmiştir..."
38. Confirm "Aktif Et" button visible in banner
39. Click "Aktif Et" → confirm dialog opens → confirm → banner disappears, project status changes

**Visual fidelity:**
40. Open New_Frontend/index.html (if available) or review prototype screenshots
41. Compare each page side-by-side: spacing, colors, fonts should match prototype

**Report any issues found.**
  </how-to-verify>
  <resume-signal>
Type "approved" if all checks pass, OR describe specific issues to fix (e.g., "Dashboard StatCards not loading", "Archive banner not showing", "3-dot menu missing").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Header Create button | Navigates authenticated users to wizard — no additional auth check needed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09-01 | Spoofing | Human verify checkpoint | accept | Checkpoint is a manual quality gate, not an attack surface |
| T-10-09-02 | Denial of Service | Full integration test | accept | Developer verifies locally — no production risk during verification |
</threat_model>

<verification>
After Task 1:
- npx next build exits 0
- Header Create button navigates to /projects/new

After checkpoint approval:
- All 5 Phase 10 success criteria confirmed visually + functionally
</verification>

<success_criteria>
- Header Create button wired to /projects/new (resolving STATE.md 08-04 deferral)
- Human verifies: all pages render visually correct, all API integrations work, auth flow complete
</success_criteria>

<output>
After completion, create `.planning/phases/10-shell-pages-project-features/10-09-SUMMARY.md`
</output>
