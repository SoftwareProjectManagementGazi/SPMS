---
phase: 01-foundation-security-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - Backend/app/infrastructure/database/models/base.py
  - Backend/app/infrastructure/database/models/task.py
  - Backend/app/infrastructure/database/models/project.py
  - Backend/app/infrastructure/database/models/user.py
  - Backend/app/infrastructure/database/models/comment.py
  - Backend/app/infrastructure/database/models/file.py
  - Backend/app/infrastructure/database/models/label.py
  - Backend/app/infrastructure/database/models/sprint.py
  - Backend/app/infrastructure/database/models/audit_log.py
  - Backend/alembic.ini
  - Backend/alembic/env.py
  - Backend/alembic/versions/001_phase1_schema.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05

must_haves:
  truths:
    - "All main model classes inherit from TimestampedMixin providing version, updated_at, is_deleted, deleted_at columns"
    - "AuditLogModel exists in audit_log.py with the exact schema from CONTEXT.md"
    - "TaskModel has recurrence_interval, recurrence_end_date, recurrence_count columns (schema only)"
    - "Alembic is configured and a Phase 1 migration script exists covering all new columns, the audit_log table, and 4 indexes"
    - "Alembic migration runs without error on a fresh database"
  artifacts:
    - path: "Backend/app/infrastructure/database/models/base.py"
      provides: "TimestampedMixin with version, updated_at, is_deleted, deleted_at"
      contains: "class TimestampedMixin"
    - path: "Backend/app/infrastructure/database/models/audit_log.py"
      provides: "AuditLogModel with entity_type, entity_id, field_name, old_value, new_value, user_id, action, timestamp"
      contains: "class AuditLogModel"
    - path: "Backend/alembic/versions/001_phase1_schema.py"
      provides: "Alembic migration for all Phase 1 schema changes"
      exports: ["upgrade", "downgrade"]
  key_links:
    - from: "Backend/app/infrastructure/database/models/base.py"
      to: "all model files"
      via: "TimestampedMixin multiple inheritance"
      pattern: "class.*TimestampedMixin.*Base"
    - from: "Backend/alembic/versions/001_phase1_schema.py"
      to: "database"
      via: "alembic upgrade head"
      pattern: "op.add_column|op.create_table|op.create_index"
---

<objective>
Establish the database schema foundation that all subsequent Phase 1 plans depend on: shared TimestampedMixin, AuditLogModel, soft-delete columns on all main tables, recurring task schema columns, and DB indexes — all delivered via a proper Alembic migration.

Purpose: DATA-01 through DATA-05. Every other plan in this phase reads or writes these columns. This plan must complete before any repository or frontend work begins.
Output: Updated SQLAlchemy models, new AuditLogModel, Alembic configured, migration script 001_phase1_schema.py that applies cleanly.
</objective>

<execution_context>
@C:/Users/yusti/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/yusti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-security-hardening/01-CONTEXT.md
@.planning/phases/01-foundation-security-hardening/01-RESEARCH.md

<interfaces>
<!-- Key existing patterns to follow. Extracted from codebase via research. -->

From Backend/app/infrastructure/database/models/base.py (current):
```python
from sqlalchemy.ext.declarative import declarative_base
Base = declarative_base()
```

TimestampedMixin to create (per CONTEXT.md locked decision):
```python
class TimestampedMixin:
    version = Column(Integer, default=1, nullable=False)
    updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())
    is_deleted = Column(Boolean, default=False, nullable=False)
    deleted_at = Column(DateTime(timezone=True), nullable=True)
```

AuditLogModel schema (per CONTEXT.md locked decision):
```python
# entity_type: "task" or "project"
# action: "created", "updated", "deleted"
# entity_id: references task or project id (no FK constraint — cross-entity)
# user_id: FK to users.id ON DELETE SET NULL
```

Note: TaskModel already has updated_at — the mixin replaces the existing column definition. Apply mixin before Base in class declaration: class TaskModel(TimestampedMixin, Base).

Alembic pattern (new addition — project has no existing Alembic):
- Install: pip install alembic
- Init: cd Backend && alembic init alembic
- Configure env.py to use async engine and import all models
- Generate or hand-write migration 001_phase1_schema.py
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TimestampedMixin and update all model files</name>
  <files>
    Backend/app/infrastructure/database/models/base.py
    Backend/app/infrastructure/database/models/task.py
    Backend/app/infrastructure/database/models/project.py
    Backend/app/infrastructure/database/models/user.py
    Backend/app/infrastructure/database/models/comment.py
    Backend/app/infrastructure/database/models/file.py
    Backend/app/infrastructure/database/models/label.py
    Backend/app/infrastructure/database/models/sprint.py
    Backend/app/infrastructure/database/models/audit_log.py
  </files>
  <action>
    Step 1 — base.py: Add TimestampedMixin class (version: int default 1, updated_at: DateTime timezone with server_default and onupdate, is_deleted: bool default False, deleted_at: DateTime nullable). Keep the existing Base = declarative_base() line.

    Step 2 — audit_log.py (NEW file): Create AuditLogModel(Base) with columns: id (Integer PK index), entity_type (String 50 not null), entity_id (Integer not null, index=True), field_name (String 100 not null), old_value (Text nullable), new_value (Text nullable), user_id (Integer FK to users.id ON DELETE SET NULL, nullable=True), action (String 50 not null), timestamp (DateTime timezone server_default func.now()). Add relationship to UserModel via backref="audit_logs". Do NOT use TimestampedMixin on AuditLogModel — it is an immutable log table.

    Step 3 — Apply TimestampedMixin to: TaskModel, ProjectModel, UserModel, CommentModel (or equivalent), FileModel (or equivalent), LabelModel (or equivalent), SprintModel (or equivalent). For each: change class declaration to class XModel(TimestampedMixin, Base) — mixin BEFORE Base. Remove any existing updated_at column definition from the class body (the mixin provides it). If a model already has version or updated_at defined, remove the duplicate.

    Step 4 — task.py additional columns (DATA-03, schema only — no business logic): Add to TaskModel: recurrence_interval (String 20, nullable=True, comment "daily, weekly, monthly"), recurrence_end_date (Date, nullable=True), recurrence_count (Integer, nullable=True). Also add index=True to project_id, assignee_id, and parent_task_id ForeignKey columns.

    Step 5 — project.py: Add index=True to manager_id ForeignKey column (DATA-05).

    IMPORTANT: Notifications model (if it exists) should NOT get TimestampedMixin — notifications use hard delete (per CONTEXT.md locked decision).
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Backend" && python -c "from app.infrastructure.database.models.base import TimestampedMixin, Base; from app.infrastructure.database.models.task import TaskModel; from app.infrastructure.database.models.project import ProjectModel; from app.infrastructure.database.models.audit_log import AuditLogModel; assert hasattr(TaskModel, 'is_deleted'); assert hasattr(TaskModel, 'version'); assert hasattr(AuditLogModel, 'entity_type'); assert hasattr(TaskModel, 'recurrence_interval'); print('Model imports OK')"</automated>
  </verify>
  <done>
    All 7 main models have is_deleted, deleted_at, version, updated_at via TimestampedMixin. AuditLogModel exists with all 8 required columns. TaskModel has 3 recurrence columns. task.project_id, task.assignee_id, task.parent_task_id, project.manager_id all have index=True. No import errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Alembic and write Phase 1 migration</name>
  <files>
    Backend/alembic.ini
    Backend/alembic/env.py
    Backend/alembic/versions/001_phase1_schema.py
  </files>
  <action>
    Step 1 — Install Alembic if not already present: pip install alembic (run from Backend/ directory). Then run: cd Backend && alembic init alembic. This creates alembic.ini and alembic/env.py.

    Step 2 — Configure alembic.ini: Set sqlalchemy.url to use the DATABASE_URL from settings. Prefer referencing an env variable: sqlalchemy.url = driver://... will be overridden in env.py, so set it to a placeholder.

    Step 3 — Configure alembic/env.py for async SQLAlchemy:
    - Import settings from app.infrastructure.config
    - Import Base from app.infrastructure.database.models.base
    - Import ALL model modules so Alembic sees them: import app.infrastructure.database.models.task, .project, .user, .comment, .file, .label, .sprint, .audit_log (and any others)
    - Set target_metadata = Base.metadata
    - Use AsyncEngine with run_sync for the "online" migrations path (standard Alembic + asyncpg pattern)
    - Set config.set_main_option("sqlalchemy.url", settings.DATABASE_URL)

    Step 4 — Write migration 001_phase1_schema.py manually (do NOT use alembic autogenerate — the existing DB may have a different state; write explicit ops):

    upgrade() must contain:
    - op.add_column for each new column on each affected table:
      - tasks: version (Integer not null default 1), is_deleted (Boolean not null default false), deleted_at (TIMESTAMP WITH TIME ZONE nullable), recurrence_interval (VARCHAR 20 nullable), recurrence_end_date (DATE nullable), recurrence_count (Integer nullable). Note: updated_at may already exist on tasks — use op.alter_column or skip if already present; add if absent.
      - projects: version (Integer not null default 1), updated_at (TIMESTAMP WITH TIME ZONE), is_deleted (Boolean not null default false), deleted_at (TIMESTAMP WITH TIME ZONE nullable)
      - users: version (Integer not null default 1), updated_at (TIMESTAMP WITH TIME ZONE), is_deleted (Boolean not null default false), deleted_at (TIMESTAMP WITH TIME ZONE nullable)
      - comments: same 4 mixin columns
      - files: same 4 mixin columns
      - labels: same 4 mixin columns
      - sprints: same 4 mixin columns
    - op.create_table("audit_log", ...) with all 8 columns from AuditLogModel
    - op.create_index("ix_tasks_project_id", "tasks", ["project_id"])
    - op.create_index("ix_tasks_assignee_id", "tasks", ["assignee_id"])
    - op.create_index("ix_tasks_parent_task_id", "tasks", ["parent_task_id"])
    - op.create_index("ix_projects_manager_id", "projects", ["manager_id"])

    downgrade() must reverse all upgrade() ops in reverse order (drop_index, drop_table audit_log, drop_column for each added column).

    Set revision = "001_phase1" and down_revision = None (first migration).

    NOTE: Use try/except or IF NOT EXISTS equivalents for add_column ops where columns might already exist (e.g., tasks.updated_at). Alternatively, check the existing schema and only add what's missing. The safest approach: wrap each add_column in a try/except that passes on DuplicateColumn errors, or query information_schema first.
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Backend" && python -c "import alembic; print('alembic importable')" && python -c "from alembic.config import Config; c = Config('alembic.ini'); print('alembic.ini valid')"</automated>
  </verify>
  <done>
    alembic.ini exists, alembic/env.py imports all models and sets target_metadata. alembic/versions/001_phase1_schema.py has upgrade() and downgrade() functions covering all schema changes. alembic check (dry-run) or alembic upgrade head completes without Python errors.
  </done>
</task>

</tasks>

<verification>
Schema model verification:
cd "C:/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Backend" && python -c "
from app.infrastructure.database.models.task import TaskModel
from app.infrastructure.database.models.project import ProjectModel
from app.infrastructure.database.models.audit_log import AuditLogModel
for col in ['version','updated_at','is_deleted','deleted_at']:
    assert hasattr(TaskModel, col), f'TaskModel missing {col}'
    assert hasattr(ProjectModel, col), f'ProjectModel missing {col}'
for col in ['entity_type','entity_id','field_name','old_value','new_value','user_id','action','timestamp']:
    assert hasattr(AuditLogModel, col), f'AuditLogModel missing {col}'
for col in ['recurrence_interval','recurrence_end_date','recurrence_count']:
    assert hasattr(TaskModel, col), f'TaskModel missing {col}'
print('ALL SCHEMA CHECKS PASSED')
"
</verification>

<success_criteria>
- TimestampedMixin defined in base.py; all 7 main models (Tasks, Projects, Users, Comments, Files, Labels, Sprints) inherit it
- AuditLogModel with all 8 columns (entity_type, entity_id, field_name, old_value, new_value, user_id, action, timestamp)
- TaskModel has recurrence_interval, recurrence_end_date, recurrence_count (nullable, no business logic)
- index=True on tasks.project_id, tasks.assignee_id, tasks.parent_task_id, projects.manager_id
- Alembic migration 001_phase1_schema.py with upgrade() and downgrade()
- All model files import without errors
- Unit test stubs from 01-01 now pass (xfail behavior: collected and marked as expected failures)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-security-hardening/01-02-SUMMARY.md`
</output>
