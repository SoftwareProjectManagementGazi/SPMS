---
phase: 01-foundation-security-hardening
plan: 02
subsystem: database
tags: [sqlalchemy, alembic, postgresql, asyncpg, soft-delete, audit-log, migrations]

# Dependency graph
requires:
  - phase: 01-01
    provides: "Wave 0 test scaffolds including stubs for soft-delete, audit log, and index tests"

provides:
  - "TimestampedMixin (version, updated_at, is_deleted, deleted_at) on 7 main SQLAlchemy models"
  - "AuditLogModel table with 8 columns: entity_type, entity_id, field_name, old_value, new_value, user_id, action, timestamp"
  - "TaskModel recurring task columns: recurrence_interval, recurrence_end_date, recurrence_count"
  - "DB indexes on tasks.project_id, tasks.assignee_id, tasks.parent_task_id, projects.manager_id"
  - "Alembic configured with async engine support and migration 001_phase1_schema.py"

affects:
  - "01-03 and all subsequent plans that read is_deleted for soft-delete queries"
  - "01-04 and 01-05 plans implementing audit logging"
  - "Any plan touching task recurrence or scheduled tasks"

# Tech tracking
tech-stack:
  added:
    - "alembic 1.17.2 (already installed in anaconda3 base env)"
  patterns:
    - "TimestampedMixin multiple inheritance: class XModel(TimestampedMixin, Base) — mixin before Base"
    - "Immutable audit log table without TimestampedMixin (append-only, no soft-delete)"
    - "Idempotent migration: check information_schema and pg_indexes before add_column/create_index"
    - "Async Alembic pattern: async_engine_from_config + connection.run_sync(do_run_migrations)"

key-files:
  created:
    - "Backend/app/infrastructure/database/models/audit_log.py"
    - "Backend/alembic.ini"
    - "Backend/alembic/env.py"
    - "Backend/alembic/versions/001_phase1_schema.py"
  modified:
    - "Backend/app/infrastructure/database/models/base.py"
    - "Backend/app/infrastructure/database/models/task.py"
    - "Backend/app/infrastructure/database/models/project.py"
    - "Backend/app/infrastructure/database/models/user.py"
    - "Backend/app/infrastructure/database/models/comment.py"
    - "Backend/app/infrastructure/database/models/file.py"
    - "Backend/app/infrastructure/database/models/label.py"
    - "Backend/app/infrastructure/database/models/sprint.py"
    - "Backend/app/infrastructure/database/models/__init__.py"

key-decisions:
  - "TimestampedMixin applied to 7 models (Tasks, Projects, Users, Comments, Files, Labels, Sprints) — NotificationModel excluded (hard delete per CONTEXT.md)"
  - "AuditLogModel has no TimestampedMixin — it is immutable append-only; no soft-delete, no version tracking"
  - "Migration 001_phase1_schema.py is written by hand (not autogenerated) and is fully idempotent via information_schema checks"
  - "Alembic env.py uses async_engine_from_config + run_sync pattern for asyncpg compatibility"
  - "DATABASE_URL is injected from app.infrastructure.config settings at runtime — not hardcoded in alembic.ini"

patterns-established:
  - "TimestampedMixin: Defined in base.py, placed before Base in class declaration for all main models"
  - "Immutable log tables: AuditLogModel pattern — no mixin, no soft-delete, timestamp-only"
  - "Idempotent migrations: All ops check existence via information_schema before executing"

requirements-completed: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]

# Metrics
duration: 15min
completed: 2026-03-11
---

# Phase 1 Plan 2: Database Schema Foundation Summary

**SQLAlchemy TimestampedMixin with soft-delete and optimistic locking applied to 7 models, AuditLogModel created, Alembic configured with async support and idempotent 001_phase1_schema.py migration covering all DATA-01 through DATA-05 requirements.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-03-11T19:30:00Z
- **Completed:** 2026-03-11T19:45:00Z
- **Tasks:** 2
- **Files modified:** 13

## Accomplishments

- Added `TimestampedMixin` (version, updated_at, is_deleted, deleted_at) to base.py and applied it to all 7 main models (Tasks, Projects, Users, Comments, Files, Labels, Sprints) via multiple inheritance
- Created `AuditLogModel` with all 8 required columns, FK to users.id ON DELETE SET NULL, and no TimestampedMixin (immutable log table)
- Configured Alembic with async engine support (asyncpg) and a fully idempotent Phase 1 migration covering all DATA-01 through DATA-05 schema changes

## Task Commits

Each task was committed atomically:

1. **Task 1: Create TimestampedMixin and update all model files** - `c6f23e6` (feat)
2. **Task 2: Configure Alembic and write Phase 1 migration** - `66b0bd9` (feat)

**Plan metadata:** _(pending docs commit)_

## Files Created/Modified

- `Backend/app/infrastructure/database/models/base.py` - Added TimestampedMixin class with 4 mixin columns
- `Backend/app/infrastructure/database/models/audit_log.py` - NEW: AuditLogModel with 8 required columns
- `Backend/app/infrastructure/database/models/task.py` - Applied TimestampedMixin, added 3 recurrence columns, added index=True to project_id/assignee_id/parent_task_id
- `Backend/app/infrastructure/database/models/project.py` - Applied TimestampedMixin, added index=True to manager_id
- `Backend/app/infrastructure/database/models/user.py` - Applied TimestampedMixin, removed duplicate updated_at
- `Backend/app/infrastructure/database/models/comment.py` - Applied TimestampedMixin
- `Backend/app/infrastructure/database/models/file.py` - Applied TimestampedMixin
- `Backend/app/infrastructure/database/models/label.py` - Applied TimestampedMixin
- `Backend/app/infrastructure/database/models/sprint.py` - Applied TimestampedMixin
- `Backend/app/infrastructure/database/models/__init__.py` - Added AuditLogModel and TimestampedMixin exports
- `Backend/alembic.ini` - Alembic config with DATABASE_URL placeholder
- `Backend/alembic/env.py` - Async migration engine, imports all models, sets target_metadata
- `Backend/alembic/versions/001_phase1_schema.py` - Phase 1 migration with idempotent upgrade()/downgrade()

## Decisions Made

- NotificationModel does NOT get TimestampedMixin — notifications use hard delete per CONTEXT.md locked decision
- AuditLogModel is excluded from TimestampedMixin — it is an immutable append-only log table
- Migration is hand-written (not autogenerated) for explicit control over a database with potentially existing schema
- All migration ops are wrapped in idempotent checks (information_schema.columns and pg_indexes) so re-running upgrade() on a partially migrated DB is safe

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. The `python` command was not on PATH in bash (Microsoft Store alias), but `anaconda3/python.exe` worked correctly as established in Plan 01-01.

## User Setup Required

None - no external service configuration required. The migration will apply when `alembic upgrade head` is run from Backend/ directory against a live PostgreSQL instance.

## Next Phase Readiness

- All schema foundation columns are in place for soft-delete queries (Plans 01-03 through 01-06)
- AuditLogModel table exists and is ready for audit logging in Plans 01-04 and 01-05
- Alembic is configured — future plans can add revision files with `alembic revision --autogenerate`
- Test stubs from Plan 01-01 that target these schema changes (test_soft_delete.py, test_audit_log.py, test_db_indexes.py) can now be implemented

## Self-Check: PASSED

All required files verified present:
- Backend/app/infrastructure/database/models/audit_log.py: FOUND
- Backend/alembic.ini: FOUND
- Backend/alembic/env.py: FOUND
- Backend/alembic/versions/001_phase1_schema.py: FOUND
- .planning/phases/01-foundation-security-hardening/01-02-SUMMARY.md: FOUND

All task commits verified:
- c6f23e6 (Task 1 - TimestampedMixin + models): FOUND
- 66b0bd9 (Task 2 - Alembic + migration): FOUND

---
*Phase: 01-foundation-security-hardening*
*Completed: 2026-03-11*
