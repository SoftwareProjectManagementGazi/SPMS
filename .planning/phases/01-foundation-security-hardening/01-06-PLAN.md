---
phase: 01-foundation-security-hardening
plan: 06
type: execute
wave: 4
depends_on:
  - "01-03"
  - "01-04"
files_modified:
  - Frontend/app/settings/page.tsx
  - Frontend/components/dashboard/manager-view.tsx
  - Frontend/components/dashboard/member-view.tsx
  - Frontend/lib/api-client.ts
  - Backend/app/api/v1/tasks.py
  - Backend/app/api/main.py
autonomous: false
requirements:
  - ARCH-09
  - ARCH-03
  - SAFE-02
  - SAFE-03

must_haves:
  truths:
    - "Settings page shows the authenticated user's real name and email — no mock data"
    - "Manager dashboard shows real project list and task counts from the API"
    - "Member dashboard shows real assigned tasks or activity feed — no mock import"
    - "Frontend redirects to /login with a 'Session expired' message when any API call returns 401"
    - "A structured log middleware/utility exists for future monitoring integration"
    - "No file in Frontend/ imports from @/lib/mock-data for profile, projects, or activity data"
  artifacts:
    - path: "Frontend/app/settings/page.tsx"
      provides: "Settings page wired to GET /auth/me — no mock-data import"
    - path: "Frontend/components/dashboard/manager-view.tsx"
      provides: "Manager view using projectService.getAll() with React Query"
    - path: "Frontend/components/dashboard/member-view.tsx"
      provides: "Member view using taskService.getMyTasks() and audit log activity"
    - path: "Frontend/lib/api-client.ts"
      provides: "401 intercept that stores session_expired flag and redirects to /login"
    - path: "Backend/app/api/main.py"
      provides: "Structured log middleware emitting JSON-format request logs"
  key_links:
    - from: "Frontend/app/settings/page.tsx"
      to: "GET /auth/me"
      via: "authService.getCurrentUser() in useEffect"
      pattern: "authService\\.getCurrentUser|getCurrentUser"
    - from: "Frontend/lib/api-client.ts"
      to: "window.location.href = '/login'"
      via: "response.status === 401 intercept"
      pattern: "status.*401|401.*status"
    - from: "Backend/app/api/main.py"
      to: "logging output"
      via: "request logging middleware emitting structured JSON"
      pattern: "logging\\.info|logger\\.info"
---

<objective>
Wire the frontend to live API data (replacing all mock data), implement the session expiry redirect (SAFE-02), and add the structured logging infrastructure hook (SAFE-03).

Purpose: ARCH-09, ARCH-03, SAFE-02, SAFE-03. Completes Phase 1 — the application is now usable with real data and the remaining security behaviors are in place.
Output: Mock data eliminated from Dashboard and Settings, session expiry UX working, log middleware in place.
</objective>

<execution_context>
@C:/Users/yusti/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/yusti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-security-hardening/01-CONTEXT.md
@.planning/phases/01-foundation-security-hardening/01-RESEARCH.md
@.planning/phases/01-foundation-security-hardening/01-03-SUMMARY.md
@.planning/phases/01-foundation-security-hardening/01-04-SUMMARY.md

<interfaces>
<!-- Key existing frontend patterns. Derived from RESEARCH.md codebase inspection. -->

Existing pattern in Frontend/app/page.tsx (already uses authService):
```typescript
// Settings page currently does:
import { currentUser } from "@/lib/mock-data"
// Replace with:
const [user, setUser] = React.useState(null)
React.useEffect(() => {
  authService.getCurrentUser().then(setUser)
}, [])
```

Existing pattern in Frontend/app/projects/page.tsx (uses React Query):
```typescript
// manager-view.tsx should follow this same pattern:
const { data: projects } = useQuery({ queryKey: ['projects'], queryFn: projectService.getAll })
```

Member view activity feed: wire to GET /api/v1/tasks/{task_id}/activity endpoint
(This endpoint needs to be added to Backend/app/api/v1/tasks.py — a simple GET endpoint that calls audit_repo.get_by_entity("task", task_id) for the user's tasks. Scope: return last 20 audit events for the current user's assigned tasks.)

401 intercept pattern (RESEARCH.md):
```typescript
// In api-client.ts fetch wrapper or axios interceptor:
if (response.status === 401) {
  localStorage.setItem("session_expired", "true")
  window.location.href = "/login"
  return  // Stop processing
}
// On /login page mount, check:
const expired = localStorage.getItem("session_expired")
if (expired) {
  localStorage.removeItem("session_expired")
  // show toast/banner: "Your session has expired. Please log in again."
}
```

Structured logging (SAFE-03 — Claude's discretion on implementation):
- Use Python stdlib logging (no new dependencies per RESEARCH.md)
- Add a FastAPI middleware class that logs each request as JSON: method, path, status_code, duration_ms, user_id (if available)
- Output to stdout via logging.info()
- Design the log format to be parseable by monitoring tools (Sentry, Datadog) in future — use structured dict format
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire frontend settings and dashboard to live API; add session expiry redirect</name>
  <files>
    Frontend/app/settings/page.tsx
    Frontend/components/dashboard/manager-view.tsx
    Frontend/components/dashboard/member-view.tsx
    Frontend/lib/api-client.ts
  </files>
  <action>
    settings/page.tsx:
    1. Read the current file to understand its component structure and state.
    2. Remove the import of currentUser (or any profile data) from @/lib/mock-data.
    3. Add a React.useEffect that calls authService.getCurrentUser() and sets user state.
    4. Replace all references to the mock currentUser object with the live user state.
    5. Handle loading state: show a skeleton or spinner while the API call is in progress.
    6. Handle null/error state: if the user cannot be fetched, show an error message (not a crash).

    manager-view.tsx:
    1. Read the current file. It imports projects and parentTasks from mock-data.
    2. Remove mock-data imports for projects.
    3. Replace with useQuery (React Query) calling projectService.getAll() — follow the exact same pattern as projects/page.tsx. If React Query is not already set up in this component, add the import.
    4. For Manager view: display project list with task count per project. The task count must come from the API response (ProjectResponseDTO should include task count or total_tasks — check the existing DTO; if not present, display the project list without count and note that full stats are Phase 6 scope per CONTEXT.md deferred list).
    5. Do NOT implement team performance metrics or sprint velocity — those are deferred to Phase 6.

    member-view.tsx:
    1. Remove mock-data import for activities (activity feed).
    2. Task list: already uses taskService.getMyTasks() (live) per RESEARCH.md — confirm and keep.
    3. Activity feed: add a fetch for recent audit events. Call GET /api/v1/tasks/activity/me (or the endpoint added in Task 2) to get recent activity items. Display the last 10-20 events. If the endpoint is not yet available, show an empty state "No recent activity" rather than mock data.

    api-client.ts (SAFE-02 — session expiry):
    1. Read the current API client implementation to understand if it uses fetch directly, axios, or a wrapper.
    2. Add a 401 response interceptor:
       - On any 401 response: localStorage.setItem("session_expired", "true"), then window.location.href = "/login"
       - Do NOT trigger this for the /auth/login endpoint itself (that 401 means wrong credentials, not session expiry — add a path exclusion)
    3. In the login page component (Frontend/app/login/page.tsx or similar): on component mount, check localStorage.getItem("session_expired"). If truthy: remove the key and display a notification/toast: "Your session has expired. Please log in again." Use the existing toast/notification system (check what the project uses — Sonner, react-hot-toast, or custom).
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Frontend" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    TypeScript compiles with no new errors. No @/lib/mock-data import exists in settings/page.tsx, manager-view.tsx, or member-view.tsx for profile/projects/activities data. api-client.ts has a 401 intercept with path exclusion for /auth/login. Login page checks and displays session expiry message.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add task activity endpoint and structured logging middleware</name>
  <files>
    Backend/app/api/v1/tasks.py
    Backend/app/api/main.py
  </files>
  <action>
    Backend/app/api/v1/tasks.py — Add activity endpoint:
    1. Add: GET /api/v1/tasks/activity/me — returns the last 20 audit log entries for tasks assigned to the current user. Implementation: fetch current_user's assigned task IDs (use task_repo.get_my_tasks(user_id) or equivalent), then call audit_repo.get_by_entity("task", task_id) for each, merge and sort by timestamp desc, return top 20. Return as list of dicts (no formal DTO required for Phase 1 — return the raw audit log fields).
    2. This endpoint uses Depends(get_current_user) only — no project membership check needed (it's the user's own activity).

    Backend/app/api/main.py — Structured logging middleware (SAFE-03):
    1. Add Python logging configuration at module top: configure root logger to emit JSON-formatted messages to stdout at INFO level. Use stdlib logging only (no structlog).
    2. Create a RequestLoggingMiddleware (Starlette BaseHTTPMiddleware subclass) that:
       - Records request start time
       - Calls the next handler
       - On response: logs a structured dict via logging.info() containing: method, path, status_code, duration_ms (float), and optionally user_id (extracted from JWT if Authorization header present — catch any errors silently and omit user_id rather than failing)
       - Log format: logging.info(json.dumps({"event": "http_request", "method": ..., "path": ..., "status": ..., "duration_ms": ..., "user_id": ...}))
    3. Register middleware: app.add_middleware(RequestLoggingMiddleware)
    4. This is the monitoring integration hook — external tools (Sentry, Datadog) can consume these stdout JSON logs in future phases.

    Note: Do not add Sentry SDK or any external monitoring library — this is infrastructure-only per SAFE-03 (log monitoring service integration infrastructure, not full integration).
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Backend" && python -m pytest tests/ -x -q --ignore=tests/integration/api/test_auth_rbac.py 2>&1 | tail -15</automated>
  </verify>
  <done>
    GET /api/v1/tasks/activity/me endpoint exists and returns list of audit events for the current user's tasks. RequestLoggingMiddleware registered in main.py and emits JSON log lines on each request. Full backend test suite passes with no regressions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify live data wiring and session expiry in browser</name>
  <action>Human verification checkpoint — follow the steps in how-to-verify to confirm live data wiring and session expiry behavior are correct before proceeding.</action>
  <what-built>
    Complete frontend wiring: Settings page, Manager dashboard, Member dashboard all show live API data. Session expiry redirect from api-client.ts 401 intercept. Structured logging middleware in backend.
  </what-built>
  <how-to-verify>
    1. Start the backend: cd Backend && uvicorn app.api.main:app --reload
    2. Start the frontend: cd Frontend && npm run dev
    3. Log in with a real user account
    4. Navigate to Settings — verify your real name and email appear (not "John Doe" or any placeholder)
    5. Navigate to Dashboard as a Manager user — verify real project names appear (not hardcoded mock project names)
    6. Navigate to Dashboard as a Member user — verify assigned tasks appear (not mock data)
    7. Open browser DevTools, delete the JWT token from localStorage/cookies, then navigate to any protected page — verify redirect to /login with "Your session has expired" message visible
    8. Check backend terminal output — verify JSON-formatted log lines appear for each request (should look like: {"event": "http_request", "method": "GET", "path": "/api/v1/...", ...})
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 checks pass, or describe which checks failed</resume-signal>
</task>

</tasks>

<verification>
Frontend type check:
cd "C:/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Frontend" && npx tsc --noEmit

Backend full suite:
cd "C:/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Backend" && python -m pytest tests/ -x -q

Mock data audit (should return no results for core data files):
grep -r "from.*mock-data" "C:/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Frontend/app/settings" "C:/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Frontend/components/dashboard"
</verification>

<success_criteria>
- Settings page displays real user profile data from GET /auth/me
- Manager dashboard displays real projects from projectService.getAll()
- Member dashboard displays real assigned tasks (no mock activities import)
- api-client.ts intercepts 401, stores session_expired flag, redirects to /login
- Login page reads and displays "Your session has expired" message when flag is set
- GET /api/v1/tasks/activity/me endpoint returns audit log entries for current user's tasks
- RequestLoggingMiddleware logs structured JSON per request to stdout
- TypeScript compiles cleanly; no @/lib/mock-data imports in settings or dashboard files
- Human verification checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-security-hardening/01-06-SUMMARY.md`
</output>
