---
phase: 01-foundation-security-hardening
plan: 05
type: execute
wave: 4
depends_on:
  - "01-03"
  - "01-04"
files_modified:
  - Backend/app/application/dtos/auth_dtos.py
  - Backend/app/api/v1/auth.py
  - Backend/app/application/dtos/project_dtos.py
  - Backend/app/infrastructure/database/repositories/project_repo.py
  - Backend/app/application/use_cases/manage_tasks.py
  - Backend/app/infrastructure/database/repositories/task_repo.py
  - Backend/app/api/v1/tasks.py
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - ARCH-05
  - ARCH-06

must_haves:
  truths:
    - "UserListDTO is defined only in auth_dtos.py; auth.py router imports it from there"
    - "task_repo.create() returns the full entity with relationships loaded in one round-trip"
    - "task_repo.update() returns the full entity; use case does NOT call get_by_id after update"
    - "ProjectResponseDTO includes manager_id, manager_name, manager_avatar fields"
    - "project_repo.get_all() joins the manager user to populate those fields"
    - "Frontend project-service.ts reads manager_name and manager_avatar from the API response"
    - "taskService has a single getByProjectId method; createTask duplicate is merged"
  artifacts:
    - path: "Backend/app/application/dtos/auth_dtos.py"
      provides: "Canonical UserListDTO (no duplicate in auth.py router)"
      contains: "class UserListDTO"
    - path: "Backend/app/application/dtos/project_dtos.py"
      provides: "ProjectResponseDTO with manager_id, manager_name, manager_avatar"
      contains: "manager_name"
    - path: "Backend/app/application/use_cases/manage_tasks.py"
      provides: "CreateTaskUseCase and UpdateTaskUseCase do NOT call get_by_id after repo returns entity"
  key_links:
    - from: "Backend/app/api/v1/auth.py"
      to: "Backend/app/application/dtos/auth_dtos.py"
      via: "from app.application.dtos.auth_dtos import UserListDTO"
      pattern: "from.*auth_dtos.*import.*UserListDTO"
    - from: "Backend/app/infrastructure/database/repositories/project_repo.py"
      to: "Backend/app/application/dtos/project_dtos.py"
      via: "joinedload(ProjectModel.manager) populates manager fields in response"
      pattern: "joinedload.*manager"
---

<objective>
Clean up all backend architecture violations: eliminate duplicate DTO, merge duplicate service methods, fix double round-trips in create/update use cases, and add nested manager data to ProjectResponseDTO.

Purpose: ARCH-01 through ARCH-06. These are quality-of-life fixes that prevent confusion and technical debt from compounding as new features are added.
Output: Clean architecture — no duplicate classes, no redundant DB round-trips, ProjectResponseDTO embeds manager data.
</objective>

<execution_context>
@C:/Users/yusti/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/yusti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-security-hardening/01-CONTEXT.md
@.planning/phases/01-foundation-security-hardening/01-RESEARCH.md
@.planning/phases/01-foundation-security-hardening/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DTO migration, double round-trips, and manager DTO</name>
  <files>
    Backend/app/application/dtos/auth_dtos.py
    Backend/app/api/v1/auth.py
    Backend/app/application/dtos/project_dtos.py
    Backend/app/infrastructure/database/repositories/project_repo.py
    Backend/app/application/use_cases/manage_tasks.py
    Backend/app/infrastructure/database/repositories/task_repo.py
  </files>
  <action>
    ARCH-02 — UserListDTO migration:
    1. Read auth.py router and auth_dtos.py. Confirm that UserListDTO is defined inline in auth.py AND in auth_dtos.py.
    2. Ensure auth_dtos.py has the canonical UserListDTO (match the fields from auth.py's inline definition — do not lose any fields).
    3. Remove the inline UserListDTO class from auth.py.
    4. Add import: from app.application.dtos.auth_dtos import UserListDTO at the top of auth.py.
    5. Verify no other router files define their own inline DTOs that should live in the dtos/ layer.

    ARCH-03 — Nested manager DTO:
    1. Read project_dtos.py and the existing TaskResponseDTO for reference (TaskResponseDTO already embeds assignee as a nested object — follow the exact same pattern).
    2. Add to ProjectResponseDTO: manager_id: Optional[int], manager_name: Optional[str], manager_avatar: Optional[str] (all Optional to handle projects that may not have a manager set).
    3. Read project_repo.py. In get_all() and get_by_id() queries, add joinedload(ProjectModel.manager) to eagerly load the manager user. When mapping ProjectModel to ProjectResponseDTO, populate manager_id, manager_name, manager_avatar from the joined manager user object (manager.id, manager.name, manager.avatar or profile_image — check the UserModel field name).

    ARCH-04 — Eliminate task_repo.create double round-trip:
    1. Read task_repo.py. The create() method currently saves the model, then calls get_by_id(model.id) internally. Read the CreateTaskUseCase.execute() — it also calls get_by_id after create.
    2. Fix task_repo.create(): after session.flush() (to get the ID), eagerly load relationships using selectinload/joinedload in the same query. Return the fully populated entity. Remove the internal get_by_id call from create().
    3. Fix CreateTaskUseCase.execute(): remove the get_by_id call after task_repo.create() returns. Trust the returned entity.

    ARCH-05 — Eliminate task_repo.update double round-trip:
    1. task_repo.update() was modified in Plan 01-04 to add audit diff. It currently calls get_by_id at the end. This call is now the ONLY round-trip for the update — the use case should NOT call get_by_id again.
    2. Read UpdateTaskUseCase.execute(): if it calls get_by_id after task_repo.update(), remove that call. Trust the entity returned by task_repo.update().
    3. Confirm task_repo.update() already returns the full entity with relationships (add eager loading if not).

    NOTE: ARCH-06 (dynamic project_repo.update()) was handled in Plan 01-04 as part of the audit diff work. Confirm the fix is in place; do not duplicate changes.
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Backend" && python -c "
from app.application.dtos.auth_dtos import UserListDTO
from app.api.v1.auth import router as auth_router
import inspect
auth_src = inspect.getsource(auth_router.__module__ if hasattr(auth_router, '__module__') else type(auth_router))
# Verify no inline UserListDTO in auth.py
import app.api.v1.auth as auth_mod
import ast, pathlib
src = pathlib.Path('app/api/v1/auth.py').read_text()
tree = ast.parse(src)
inline_dtos = [n.name for n in ast.walk(tree) if isinstance(n, ast.ClassDef) and 'DTO' in n.name]
assert 'UserListDTO' not in inline_dtos, f'Found inline UserListDTO in auth.py'
from app.application.dtos.project_dtos import ProjectResponseDTO
assert hasattr(ProjectResponseDTO, 'model_fields') and 'manager_name' in ProjectResponseDTO.model_fields, 'manager_name missing from ProjectResponseDTO'
print('ARCH-02 and ARCH-03 checks passed')
"</automated>
  </verify>
  <done>
    UserListDTO defined only in auth_dtos.py; auth.py imports it. ProjectResponseDTO has manager_id, manager_name, manager_avatar. project_repo queries joinedload the manager. task_repo.create() and task_repo.update() each return full entities without triggering a redundant get_by_id in their use cases.
  </done>
</task>

<task type="auto">
  <name>Task 2: Merge duplicate task service methods in frontend</name>
  <files>
    Backend/app/application/use_cases/manage_tasks.py
    Backend/app/api/v1/tasks.py
  </files>
  <action>
    ARCH-01 — Duplicate task service method merge. This is a frontend TypeScript concern (taskService.getByProjectId / getTasksByProject and create / createTask) AND potentially a backend use case concern.

    Backend side:
    1. Read manage_tasks.py. Check if there are two separate use cases or methods that do the same thing — one named ListProjectTasksUseCase and one named GetTasksByProjectUseCase (or similar). If duplicates exist: keep the one that is actually called from tasks.py router; delete or merge the other. Update any import or dependency reference.
    2. Check if CreateTaskUseCase and a separate createTask function/use case exist as duplicates. Merge if so.

    Frontend side (TypeScript — in Frontend/services/task-service.ts or similar):
    1. Read the existing file. Locate getByProjectId and getTasksByProject — they should do the same thing (fetch tasks for a project). Keep getByProjectId; make getTasksByProject an alias or remove it (if nothing imports getTasksByProject, remove it; if something imports it, replace the import site and then remove the duplicate).
    2. Locate create and createTask. Keep create; make createTask an alias or remove the duplicate. Update import sites.
    3. Do not change API call URLs or business logic — only consolidate duplicate method names.

    After merging, verify no compilation errors by running the TypeScript type-check:
    cd Frontend && npx tsc --noEmit 2>&1 | head -20
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Backend" && python -m pytest tests/ -x -q --ignore=tests/integration/api/test_auth_rbac.py 2>&1 | tail -10</automated>
  </verify>
  <done>
    No duplicate getByProjectId/getTasksByProject or create/createTask methods in taskService. Backend test suite passes without regressions. TypeScript type-check produces no new errors related to the merged methods.
  </done>
</task>

</tasks>

<verification>
Architecture cleanup check:
cd "C:/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Backend" && python -m pytest tests/ -x -q
</verification>

<success_criteria>
- UserListDTO in one location only (auth_dtos.py); auth.py router imports it
- ProjectResponseDTO has manager_id, manager_name, manager_avatar fields (all Optional)
- project_repo joinedloads manager user in get_all/get_by_id
- task_repo.create returns full entity; CreateTaskUseCase does not call get_by_id post-create
- task_repo.update returns full entity; UpdateTaskUseCase does not call get_by_id post-update
- project_repo.update uses dynamic DTO field mapping (confirmed from 01-04 or fixed here)
- Frontend taskService: single getByProjectId and single create (no duplicates)
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-security-hardening/01-05-SUMMARY.md`
</output>
