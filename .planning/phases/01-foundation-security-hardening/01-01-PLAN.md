---
phase: 01-foundation-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Backend/tests/integration/api/test_auth_rbac.py
  - Backend/tests/integration/api/test_rbac_tasks.py
  - Backend/tests/unit/test_startup_validation.py
  - Backend/tests/unit/test_config.py
  - Backend/tests/unit/infrastructure/test_task_repo_soft_delete.py
  - Backend/tests/integration/infrastructure/test_audit_log.py
  - Backend/tests/integration/infrastructure/test_indexes.py
  - Backend/tests/integration/api/test_cors.py
autonomous: true
requirements:
  - ARCH-07
  - ARCH-08
  - ARCH-10
  - DATA-02
  - DATA-04
  - DATA-05
  - SEC-03

must_haves:
  truths:
    - "All Wave 0 test files exist and are importable"
    - "test_auth_rbac.py no longer references the non-existent status_id field"
    - "Each test file contains at least one test function (even if it marks xfail or skips pending implementation)"
    - "pytest collects all test files without import errors"
  artifacts:
    - path: "Backend/tests/integration/api/test_rbac_tasks.py"
      provides: "RBAC non-member 403 and admin bypass test stubs"
    - path: "Backend/tests/unit/test_startup_validation.py"
      provides: "Startup crash test stubs for insecure JWT/DB defaults"
    - path: "Backend/tests/unit/test_config.py"
      provides: "DEBUG flag / SQLAlchemy echo test stubs"
    - path: "Backend/tests/unit/infrastructure/test_task_repo_soft_delete.py"
      provides: "Soft-delete filter behavior test stubs"
    - path: "Backend/tests/integration/infrastructure/test_audit_log.py"
      provides: "Audit log field-level diff test stubs"
    - path: "Backend/tests/integration/infrastructure/test_indexes.py"
      provides: "DB index existence test stubs"
    - path: "Backend/tests/integration/api/test_cors.py"
      provides: "CORS rejection from non-allowlisted origins test stubs"
  key_links:
    - from: "Backend/tests/integration/api/test_auth_rbac.py"
      to: "TaskModel schema"
      via: "removes status_id=1 (field does not exist)"
      pattern: "status_id"
---

<objective>
Create all Wave 0 test scaffolds required by the Nyquist rule and fix the existing broken test. Every subsequent task in this phase has an automated verify command — these test files must exist first.

Purpose: Nyquist compliance — no implementation task can have a missing automated verify command. The broken test_auth_rbac.py also blocks the test suite from running cleanly.
Output: 8 test files (7 new stubs + 1 fixed existing file), all importable by pytest.
</objective>

<execution_context>
@C:/Users/yusti/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/yusti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-security-hardening/01-CONTEXT.md
@.planning/phases/01-foundation-security-hardening/01-RESEARCH.md
@.planning/phases/01-foundation-security-hardening/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix broken test and create RBAC test stubs</name>
  <files>
    Backend/tests/integration/api/test_auth_rbac.py
    Backend/tests/integration/api/test_rbac_tasks.py
  </files>
  <behavior>
    - test_auth_rbac.py: any Task creation in fixtures must NOT use status_id=1 (field does not exist in TaskModel — status derives from column_id/board columns). Remove status_id from all Task() constructor calls; use column_id or omit the field entirely.
    - test_rbac_tasks.py stub tests (marked pytest.mark.xfail or skip with reason="not implemented yet"):
      - test_non_member_gets_403_on_list_tasks: authenticated user who is not a project member gets HTTP 403 on GET /api/v1/tasks/project/{project_id}
      - test_non_member_gets_403_on_get_task: non-member gets 403 on GET /api/v1/tasks/{task_id}
      - test_non_member_gets_403_on_create_task: non-member gets 403 on POST /api/v1/tasks/project/{project_id}
      - test_non_member_gets_403_on_update_task: non-member gets 403 on PUT /api/v1/tasks/{task_id}
      - test_non_member_gets_403_on_delete_task: non-member gets 403 on DELETE /api/v1/tasks/{task_id}
      - test_admin_can_access_any_project_tasks: admin user gets 200 on GET /api/v1/tasks/project/{project_id} even when not a project member
  </behavior>
  <action>
    Fix test_auth_rbac.py: read the existing file, find all Task() constructor calls, remove the status_id=1 argument (TaskModel has no status_id column — status is derived from the board column the task belongs to). The fix may require replacing status_id with a valid column_id or removing it if optional.

    Create test_rbac_tasks.py as a new file with the stub tests listed above. Use pytest.mark.xfail(reason="RBAC not implemented yet — pending Plan 01-03") so the tests are collected but fail expectedly. Import the app's test client using the existing conftest pattern (look at how other integration tests set up the AsyncClient — follow the exact same fixture pattern). Do NOT try to write passing tests — these are RED stubs for the Nyquist requirement.
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Backend" && python -m pytest tests/integration/api/test_auth_rbac.py tests/integration/api/test_rbac_tasks.py --collect-only -q</automated>
  </verify>
  <done>
    pytest collects both files with no ImportError or collection errors. test_auth_rbac.py no longer contains status_id references.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create unit test stubs for config, startup validation, and soft-delete</name>
  <files>
    Backend/tests/unit/test_startup_validation.py
    Backend/tests/unit/test_config.py
    Backend/tests/unit/infrastructure/test_task_repo_soft_delete.py
  </files>
  <behavior>
    - test_startup_validation.py stubs (xfail):
      - test_startup_raises_on_default_jwt_secret: when JWT_SECRET == "supersecretkey" app raises RuntimeError at startup
      - test_startup_raises_on_default_db_password: when DB_PASSWORD == "secretpassword" app raises RuntimeError at startup
      - test_startup_succeeds_with_secure_secrets: no RuntimeError when both secrets are non-default values
    - test_config.py stubs (xfail):
      - test_debug_false_sets_echo_false: Settings(DEBUG=False) results in SQLAlchemy engine echo=False
      - test_cors_origins_parsed_from_env: Settings(CORS_ORIGINS="http://a.com,http://b.com").cors_origins_list == ["http://a.com", "http://b.com"]
      - test_cors_origins_strips_whitespace: "http://a.com, http://b.com" parses correctly (strips spaces)
    - test_task_repo_soft_delete.py stubs (xfail):
      - test_soft_deleted_task_not_in_list: after soft-deleting a task, it does not appear in get_all_by_project()
      - test_soft_delete_sets_is_deleted_true: soft-delete sets is_deleted=True and deleted_at is not None
      - test_hard_delete_blocked_for_non_admin: (placeholder for future admin-only enforcement)
  </behavior>
  <action>
    Create the three unit test files following the existing pytest conventions in the Backend/tests/unit/ directory. Read an existing unit test file first to understand the import style and fixture patterns (look at conftest.py if present). Mark all test functions with pytest.mark.xfail(reason="pending implementation in 01-02/01-03"). Create the Backend/tests/unit/infrastructure/ directory if it does not exist (add __init__.py).
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Backend" && python -m pytest tests/unit/test_startup_validation.py tests/unit/test_config.py tests/unit/infrastructure/test_task_repo_soft_delete.py --collect-only -q</automated>
  </verify>
  <done>
    pytest collects all three files with no ImportError. Each file has at least 2 test functions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create integration test stubs for audit log, indexes, and CORS</name>
  <files>
    Backend/tests/integration/infrastructure/test_audit_log.py
    Backend/tests/integration/infrastructure/test_indexes.py
    Backend/tests/integration/api/test_cors.py
  </files>
  <behavior>
    - test_audit_log.py stubs (xfail):
      - test_update_task_writes_audit_row: updating a task's title creates one audit_log row with entity_type="task", field_name="title", old_value=previous title, new_value=new title
      - test_update_project_writes_audit_row: updating a project's name creates one audit_log row with entity_type="project"
      - test_audit_row_has_user_id: audit row records the user_id of the user who made the change
    - test_indexes.py stubs (xfail):
      - test_tasks_project_id_index_exists: DB reflection confirms index on tasks.project_id
      - test_tasks_assignee_id_index_exists: DB reflection confirms index on tasks.assignee_id
      - test_tasks_parent_task_id_index_exists: DB reflection confirms index on tasks.parent_task_id
      - test_projects_manager_id_index_exists: DB reflection confirms index on projects.manager_id
    - test_cors.py stubs (xfail):
      - test_cors_rejects_non_allowlisted_origin: OPTIONS request from "http://evil.com" receives CORS headers that do NOT include that origin
      - test_cors_allows_allowlisted_origin: OPTIONS request from an origin in CORS_ORIGINS list is accepted
  </behavior>
  <action>
    Create the three integration test files. Check if Backend/tests/integration/infrastructure/ directory exists; create it with __init__.py if not. Follow the existing integration test fixture patterns (AsyncClient, db session setup — read existing conftest.py files to match). Mark all test functions with pytest.mark.xfail(reason="pending implementation in 01-02/01-04/01-03").
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Backend" && python -m pytest tests/integration/infrastructure/test_audit_log.py tests/integration/infrastructure/test_indexes.py tests/integration/api/test_cors.py --collect-only -q</automated>
  </verify>
  <done>
    pytest collects all three files. No ImportError. All test stubs are collected.
  </done>
</task>

</tasks>

<verification>
Full Wave 0 check — all 8 test files collected cleanly:
cd "C:/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Backend" && python -m pytest tests/integration/api/test_auth_rbac.py tests/integration/api/test_rbac_tasks.py tests/unit/test_startup_validation.py tests/unit/test_config.py tests/unit/infrastructure/test_task_repo_soft_delete.py tests/integration/infrastructure/test_audit_log.py tests/integration/infrastructure/test_indexes.py tests/integration/api/test_cors.py --collect-only -q
</verification>

<success_criteria>
- 8 test files exist and are collected by pytest without ImportError
- test_auth_rbac.py contains no status_id references
- All stub tests are marked xfail (not skipped entirely) so they appear in collection
- cd Backend && python -m pytest tests/unit -x -q passes (unit stubs collected, xfail as expected)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-security-hardening/01-01-SUMMARY.md`
</output>
