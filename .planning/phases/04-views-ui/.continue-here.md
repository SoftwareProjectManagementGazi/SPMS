---
phase: 04-views-ui
task: post-verification-bugfixes
total_tasks: N/A
status: phase_complete_bugfixes_applied
last_updated: 2026-03-15T11:16:11.292Z
---

<current_state>
Phase 4 (Views & UI) is COMPLETE — all 5 plans executed, verified 29/29 must-haves.
This session was post-verification human UAT bug-fixing (not part of a formal plan).
All bug fixes committed. Ready to move to Phase 5 (Notifications).
</current_state>

<completed_work>

Phase 4 plans — all done prior to this session:
- 04-01: Board column CRUD API + sprint close/delete + patchTask frontend method
- 04-02: CalendarTab (VIEW-02) + GanttTab (VIEW-03) components
- 04-03: Kanban board drag-and-drop + view persistence + Calendar/Gantt wiring
- 04-04: List view + Phase management page (/sprints) + Board Columns settings
- 04-05: Sprint bands wired into CalendarTab (gap closure)

Bug fixes applied this session (all committed):
- `fix(04): frappe-gantt CSS from public/` — package exports field blocked direct subpath import
- `fix(views): 6 UI/API bugs`:
  - Added PATCH /tasks/{id} backend endpoint (drag-and-drop was returning 405)
  - Avatar URL resolution via resolveAvatarUrl() in task-service.ts
  - Priority badge colored (red/orange/yellow/grey) instead of flat grey
  - Calendar Hide/Undated button moved above FullCalendar header (no longer overlaps Month/Week)
  - Tab views wrapped in card with shadow (rounded-xl border bg-card shadow-sm p-4)
  - Settings editable by project manager (isProjectManager prop added)
- `fix(views): gantt + calendar + hover + delete cache`:
  - Gantt: frappe-gantt breaks on start>=end; added +1 day when end<=start
  - Calendar: timeZone="UTC" added to fix drag off-by-one day in UTC+3 locale
  - Calendar: eventDragStart clears hover popover
  - Calendar: hover popover rendered via createPortal to body (prevents layout reflow)
  - Delete project: queryClient.invalidateQueries(['projects']) added before router.push
</completed_work>

<remaining_work>

No formal remaining work in Phase 4.

Known open UX points (not blocking, noted by user):
- Sprint close moves tasks to new sprint_id but their due_dates don't change → calendar still shows tasks on old dates. This is by design (due_date and sprint are independent). Could optionally update due_dates on sprint close if desired as Phase 5+ polish.
- Avatar/PFP not showing in list/calendar views for assignees — fixed in task-service but may need verification that backend returns avatar_url in assignee DTO
- Settings still shows read-only view for non-project-managers (correct behavior; project lead sees editable version)

Next phase to execute: **Phase 5 — Notifications**
Goal: Real-time in-app notifications, event-triggered alerts, email notifications, messaging permissions, notification preferences
</remaining_work>

<decisions_made>

- PATCH /tasks/{id} added as dedicated endpoint rather than changing frontend to use PUT — cleaner REST semantics
- frappe-gantt CSS copied to public/ (not CDN) — avoids external dependency at runtime
- isProjectManager prop pattern used for settings — avoids passing full project object to settings component
- timeZone="UTC" on FullCalendar — consistent with backend naive datetime storage (effectively UTC)
- Portal for hover popover — prevents React re-render of FullCalendar on hover state changes
</decisions_made>

<blockers>

None — phase complete, all known bugs fixed.

Potential concerns for Phase 5:
- WebSockets/SSE for real-time notifications will require backend infrastructure (FastAPI WebSocket or SSE endpoint)
- Email delivery requires SMTP config (already in Backend/app/infrastructure/config.py from Phase 2 password reset)
</blockers>

<context>
Phase 4 went through full GSD cycle: execute → verify → gap-close → re-verify → human UAT → bug fixes.
The codebase now has a working Kanban board (DnD), Calendar view (FullCalendar with sprint bands), Gantt/Timeline (frappe-gantt), and List view — all with view persistence, sprint filtering, and settings management.

All 4 views are in Frontend/app/projects/[id]/page.tsx using Tabs with localStorage persistence. Heavy tabs (Calendar, Gantt) use next/dynamic with ssr:false.

The backend has a full board column CRUD API at /api/v1/projects/{id}/columns and sprint management at /api/v1/sprints.

Project is 4/7 phases complete (plus Phases 1-3 backend foundation). Next is Phase 5 Notifications.
</context>

<next_action>
Start with: `/gsd:progress` to confirm state, then `/gsd:plan-phase 5` to plan Phase 5 (Notifications).
Or do a `/clear` first for a fresh context window before planning.
</next_action>
