---
phase: 08-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - Frontend2/components/primitives/avatar.tsx
  - Frontend2/components/primitives/avatar-stack.tsx
  - Frontend2/components/primitives/badge.tsx
  - Frontend2/components/primitives/button.tsx
  - Frontend2/components/primitives/card.tsx
  - Frontend2/components/primitives/kbd.tsx
  - Frontend2/components/primitives/tabs.tsx
  - Frontend2/components/primitives/section.tsx
autonomous: true
requirements: [FOUND-02]
must_haves:
  truths:
    - "Avatar renders user initials on a colored background using --av-* tokens"
    - "AvatarStack overlaps avatars with negative -6px margin and shows +N overflow circle"
    - "Badge renders 7 tones (neutral/primary/success/warning/danger/info/mono) with color-mix backgrounds as inline styles"
    - "Button renders 5 variants (primary/secondary/ghost/subtle/danger) with inset shadow tokens and mouseDown translateY effect"
    - "Card renders with shadow and inset-card depth, interactive prop enables hover shadow-md + translateY(-1px)"
    - "Kbd renders keyboard shortcut hint with mono class and inset border shadow"
    - "Tabs renders tab bar with active tab 2px solid var(--primary) bottom border"
    - "Section renders heading with optional subtitle and action slot"
  artifacts:
    - path: "Frontend2/components/primitives/avatar.tsx"
      provides: "Avatar component"
      exports: ["Avatar"]
    - path: "Frontend2/components/primitives/avatar-stack.tsx"
      provides: "AvatarStack component"
      exports: ["AvatarStack"]
    - path: "Frontend2/components/primitives/badge.tsx"
      provides: "Badge component with 7 tones"
      exports: ["Badge"]
    - path: "Frontend2/components/primitives/button.tsx"
      provides: "Button component with 5 variants and 5 sizes"
      exports: ["Button"]
    - path: "Frontend2/components/primitives/card.tsx"
      provides: "Card component with interactive hover"
      exports: ["Card"]
    - path: "Frontend2/components/primitives/kbd.tsx"
      provides: "Kbd component"
      exports: ["Kbd"]
    - path: "Frontend2/components/primitives/tabs.tsx"
      provides: "Tabs component"
      exports: ["Tabs"]
    - path: "Frontend2/components/primitives/section.tsx"
      provides: "Section component"
      exports: ["Section"]
  key_links:
    - from: "Frontend2/components/primitives/avatar-stack.tsx"
      to: "Frontend2/components/primitives/avatar.tsx"
      via: "imports Avatar for rendering each user"
      pattern: "import.*Avatar.*from.*./avatar"
    - from: "Frontend2/components/primitives/tabs.tsx"
      to: "Frontend2/components/primitives/badge.tsx"
      via: "renders Badge inside tab items for badge count"
      pattern: "import.*Badge.*from.*./badge"
---

<objective>
Create the first batch of 8 primitive components (Avatar, AvatarStack, Badge, Button, Card, Kbd, Tabs, Section) ported from the prototype with TypeScript interfaces, and full visual fidelity to the prototype.

Purpose: These primitives are building blocks used by the App Shell (Plan 04) and all subsequent phases. Badge and Kbd are used in Sidebar nav items. Button is used in Header. Card is used in dashboard and project pages. Tabs is used in project detail.

Output: 8 production-ready TypeScript React components in Frontend2/components/primitives/.
</objective>

<execution_context>
@.planning/phases/08-foundation-design-system/08-PATTERNS.md
@.planning/phases/08-foundation-design-system/08-UI-SPEC.md
</execution_context>

<context>
@.planning/ROADMAP.md

This plan depends on Plan 01 having created:
- Frontend2/ project scaffold with globals.css (CSS tokens)
- Frontend2/lib/utils.ts (cn() function)

CONVERSION RULES (apply to EVERY component in this plan):
1. Add "use client" directive at top of every file
2. Use `import * as React from "react"` (namespace access convention)
3. Create explicit Props interface with all props typed
4. Add `className?: string` prop (missing from prototype) for external styling
5. Use `export function ComponentName` (named export, not default)
6. Convert static styles to Tailwind classes where clean mapping exists
7. Keep `style={}` for dynamic values that depend on props (height, color, width%, computed values)
8. color-mix() expressions MUST stay as inline style={} -- do NOT force into Tailwind arbitrary values
9. Use cn() for className merging
10. Keep var() CSS variable references for token values

CRITICAL RULES:
- Per D-01, these components come 100% from the prototype. Do NOT use shadcn/ui components or patterns.
- Per D-02, all var() references use prototype token names (--bg, --surface, --fg, etc.)
- Frontend/ is IGNORED. Nothing copied or referenced from there.

<interfaces>
From Frontend2/lib/utils.ts (created by Plan 01):
```typescript
export function cn(...inputs: ClassValue[]): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Avatar, AvatarStack, Badge, and Button components</name>
  <files>
    Frontend2/components/primitives/avatar.tsx
    Frontend2/components/primitives/avatar-stack.tsx
    Frontend2/components/primitives/badge.tsx
    Frontend2/components/primitives/button.tsx
  </files>
  <read_first>
    New_Frontend/src/primitives.jsx (lines 6-118 for Avatar lines 6-20, AvatarStack lines 22-43, Badge lines 46-70, Button lines 73-118)
    Frontend2/lib/utils.ts (for cn() import path verification)
  </read_first>
  <action>
Create 4 component files in Frontend2/components/primitives/:

**avatar.tsx** -- port from primitives.jsx lines 6-20:

Props interface:
```typescript
interface AvatarUser { initials: string; avColor?: number }
interface AvatarProps {
  user: AvatarUser | null
  size?: number       // default 28
  ring?: boolean      // default false
  className?: string
  style?: React.CSSProperties
}
```
- Return null if !user
- Render a div with:
  - width/height = size, borderRadius "50%"
  - background: `var(--av-${user.avColor || 1})`
  - color: "#fff"
  - display inline-flex, alignItems/justifyContent center
  - fontSize: size * 0.4, fontWeight 600, letterSpacing -0.3
  - flexShrink 0
  - boxShadow: if ring then "0 0 0 2px var(--surface), 0 0 0 4px var(--primary)" else "inset 0 0 0 1px oklch(0 0 0 / 0.08)"
- Content: user.initials

**avatar-stack.tsx** -- port from primitives.jsx lines 22-43:

Props interface:
```typescript
interface AvatarStackProps {
  users: AvatarUser[]
  max?: number    // default 4
  size?: number   // default 22
}
```
- Import Avatar from "./avatar"
- Show first `max` users with Avatar component
- Each avatar wrapper: marginLeft i===0 ? 0 : -6, position relative, zIndex 10-i
- Extra count shown as circle: marginLeft -6, same size, borderRadius 50%, bg var(--surface-2), color var(--fg-muted), fontSize size*0.38, fontWeight 600, boxShadow "0 0 0 2px var(--surface), inset 0 0 0 1px var(--border)"

**badge.tsx** -- port from primitives.jsx lines 46-70:

Props interface:
```typescript
type BadgeTone = "neutral" | "primary" | "success" | "warning" | "danger" | "info" | "mono"
type BadgeSize = "xs" | "sm"
interface BadgeProps {
  children: React.ReactNode
  tone?: BadgeTone     // default "neutral"
  size?: BadgeSize     // default "sm"
  dot?: boolean
  className?: string
  style?: React.CSSProperties
}
```
- 7 tones object with bg/fg/bd values -- ALL tone colors use color-mix or var() as INLINE STYLES:
  - neutral: { bg: "var(--surface-2)", fg: "var(--fg-muted)", bd: "var(--border)" }
  - primary: { bg: "color-mix(in oklch, var(--primary) 12%, transparent)", fg: "var(--primary)", bd: "color-mix(in oklch, var(--primary) 25%, transparent)" }
  - success: { bg: "color-mix(in oklch, var(--status-done) 15%, transparent)", fg: "var(--status-done)", bd: "color-mix(in oklch, var(--status-done) 30%, transparent)" }
  - warning: { bg: "color-mix(in oklch, var(--status-review) 18%, transparent)", fg: "color-mix(in oklch, var(--status-review) 85%, var(--fg))", bd: "color-mix(in oklch, var(--status-review) 35%, transparent)" }
  - danger: { bg: "color-mix(in oklch, var(--priority-critical) 14%, transparent)", fg: "var(--priority-critical)", bd: "color-mix(in oklch, var(--priority-critical) 30%, transparent)" }
  - info: { bg: "color-mix(in oklch, var(--status-progress) 13%, transparent)", fg: "var(--status-progress)", bd: "color-mix(in oklch, var(--status-progress) 25%, transparent)" }
  - mono: { bg: "var(--accent)", fg: "var(--accent-fg)", bd: "transparent" }
- Size maps: xs = { padding: "1px 6px", fontSize: 10.5, height: 18 }, sm = { padding: "2px 8px", fontSize: 11.5, height: 20 }
- Render span with: display inline-flex, alignItems center, gap 4, borderRadius 999, fontWeight 500, whiteSpace nowrap
- boxShadow: `inset 0 0 0 1px ${s.bd}` (where s = tones[tone])
- Optional dot: 6x6px circle span with s.fg background before children

**button.tsx** -- port from primitives.jsx lines 73-118:

Props interface:
```typescript
type ButtonVariant = "primary" | "secondary" | "ghost" | "subtle" | "danger"
type ButtonSize = "xs" | "sm" | "md" | "lg" | "icon"
interface ButtonProps {
  variant?: ButtonVariant    // default "secondary"
  size?: ButtonSize          // default "md"
  icon?: React.ReactNode
  iconRight?: React.ReactNode
  children?: React.ReactNode
  onClick?: (e: React.MouseEvent) => void
  disabled?: boolean
  type?: "button" | "submit" | "reset"  // default "button"
  title?: string
  active?: boolean
  className?: string
  style?: React.CSSProperties
}
```
- 5 variant styles (keep as inline style -- inset shadows are dynamic token values):
  - primary: { background: "var(--primary)", color: "var(--primary-fg)", boxShadow: "0 2px 4px oklch(0.60 0.17 40 / 0.24), 0 1px 2px oklch(0 0 0 / 0.08), var(--inset-primary-top), var(--inset-primary-bottom)" }
  - secondary: { background: "var(--surface)", color: "var(--fg)", boxShadow: "0 1px 2px oklch(0 0 0 / 0.05), var(--inset-top), var(--inset-bottom), inset 0 0 0 1px var(--border-strong)" }
  - ghost: { background: "transparent", color: "var(--fg)", boxShadow: "none" }
  - subtle: { background: "var(--surface-2)", color: "var(--fg)", boxShadow: "none" }
  - danger: { background: "var(--priority-critical)", color: "#fff", boxShadow: "0 2px 4px oklch(0.58 0.22 25 / 0.26), var(--inset-top), var(--inset-primary-bottom)" }
- 5 size styles: xs { height:24, padding:"0 8px", fontSize:12, gap:4 }, sm { height:28, padding:"0 10px", fontSize:12.5, gap:6 }, md { height:32, padding:"0 12px", fontSize:13, gap:6 }, lg { height:40, padding:"0 16px", fontSize:14, gap:8 }, icon { height:28, width:28, padding:0, fontSize:13, gap:0 }
- Base style: display inline-flex, alignItems/justifyContent center, borderRadius var(--radius-sm), fontWeight 500, transition "transform 0.08s ease, background 0.1s ease, box-shadow 0.1s ease"
- disabled: cursor not-allowed, opacity 0.5
- active prop override: background var(--accent), color var(--accent-fg)
- mouseDown handler: if !disabled, set transform translateY(0.5px)
- mouseUp + mouseLeave: reset transform to translateY(0)
- Render: {icon}{children}{iconRight}
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/Desktop/bitirme projesi/SPMS/Frontend2" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Frontend2/components/primitives/avatar.tsx` starts with `"use client"` and contains `export function Avatar`
    - `Frontend2/components/primitives/avatar.tsx` contains the string `var(--av-${user.avColor` (avatar color token reference)
    - `Frontend2/components/primitives/avatar-stack.tsx` contains `export function AvatarStack` and `marginLeft: i === 0 ? 0 : -6`
    - `Frontend2/components/primitives/avatar-stack.tsx` contains `import` from `"./avatar"` (not from "@/components/ui/")
    - `Frontend2/components/primitives/badge.tsx` contains `export function Badge` and strings for all 7 tones: "neutral", "primary", "success", "warning", "danger", "info", "mono"
    - `Frontend2/components/primitives/badge.tsx` contains `color-mix(in oklch` as inline style values (NOT as Tailwind classes)
    - `Frontend2/components/primitives/button.tsx` contains `export function Button` and strings for all 5 variants: "primary", "secondary", "ghost", "subtle", "danger"
    - `Frontend2/components/primitives/button.tsx` contains `var(--inset-primary-top)` and `var(--inset-primary-bottom)` in the primary variant
    - `Frontend2/components/primitives/button.tsx` contains `translateY(0.5px)` for mouseDown effect
    - No file imports from `@/components/ui/` (no shadcn components)
    - `npx next build` succeeds without errors
  </acceptance_criteria>
  <done>Avatar, AvatarStack, Badge, Button components created with full TypeScript props, prototype-faithful styling, color-mix preserved as inline styles, and inset shadow tokens used correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Create Card, Kbd, Tabs, and Section components</name>
  <files>
    Frontend2/components/primitives/card.tsx
    Frontend2/components/primitives/kbd.tsx
    Frontend2/components/primitives/tabs.tsx
    Frontend2/components/primitives/section.tsx
  </files>
  <read_first>
    New_Frontend/src/primitives.jsx (lines 121-195 for Card lines 121-142, Kbd lines 145-154, Tabs lines 157-181, Section lines 184-195)
    Frontend2/components/primitives/badge.tsx (Tabs imports Badge for tab.badge rendering)
  </read_first>
  <action>
Create 4 component files:

**card.tsx** -- port from primitives.jsx lines 121-142:

Props interface:
```typescript
interface CardProps {
  children: React.ReactNode
  interactive?: boolean
  padding?: number    // default 16
  className?: string
  style?: React.CSSProperties
}
```
- Background: var(--surface)
- borderRadius: var(--radius)
- boxShadow: "var(--shadow), var(--inset-card)"
- padding: props.padding
- transition: "box-shadow 0.12s ease, transform 0.12s ease"
- Interactive mode (only when interactive prop is true):
  - cursor: pointer
  - onMouseEnter: set boxShadow to "var(--shadow-md), var(--inset-card)" and transform to "translateY(-1px)"
  - onMouseLeave: reset boxShadow to "var(--shadow), var(--inset-card)" and transform to "translateY(0)"
- Accept ...rest spread for additional HTML div attributes

**kbd.tsx** -- port from primitives.jsx lines 145-154:

Props interface:
```typescript
interface KbdProps {
  children: React.ReactNode
  className?: string
  style?: React.CSSProperties
}
```
- Add className="mono" to the span for monospace font
- Styles: display inline-flex, alignItems center, height 18, padding "0 5px", minWidth 18, justifyContent center, borderRadius 4, fontSize 10.5
- background: var(--surface-2), color: var(--fg-muted)
- boxShadow: "inset 0 0 0 1px var(--border), 0 1px 0 var(--border)"

**tabs.tsx** -- port from primitives.jsx lines 157-181:

Props interface:
```typescript
interface TabItem {
  id: string
  label: string
  icon?: React.ReactNode
  badge?: number | string
}
interface TabsProps {
  tabs: TabItem[]
  active: string
  onChange: (id: string) => void
  size?: "sm" | "md" | "lg"    // default "md"
  style?: React.CSSProperties
}
```
- Import Badge from "./badge"
- Container div: display flex, gap 2, borderBottom "1px solid var(--border)"
- Padding map: sm="6px 10px", md="8px 14px", lg="10px 16px"
- Font size map: sm=12, md=13, lg=14
- Each tab button:
  - Active: fontWeight 600, color var(--fg), borderBottom "2px solid var(--primary)", marginBottom -1
  - Inactive: fontWeight 500, color var(--fg-muted), borderBottom "2px solid transparent", marginBottom -1
  - display inline-flex, alignItems center, gap 6
  - Render: {tab.icon}{tab.label}
  - If tab.badge != null: render `<Badge size="xs" tone={isActive ? "primary" : "neutral"}>{tab.badge}</Badge>`

**section.tsx** -- port from primitives.jsx lines 184-195:

Props interface:
```typescript
interface SectionProps {
  title: string
  subtitle?: string
  action?: React.ReactNode
  children: React.ReactNode
  className?: string
  style?: React.CSSProperties
}
```
- Outer div with className + style
- Header row div: display flex, alignItems flex-end, justifyContent space-between, marginBottom 12
- Title: fontSize 13, fontWeight 600, color var(--fg)
- Subtitle (if provided): fontSize 12, color var(--fg-muted), marginTop 2
- Action slot: rendered to the right of the title div
- Children rendered below header
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/Desktop/bitirme projesi/SPMS/Frontend2" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Frontend2/components/primitives/card.tsx` contains `export function Card` and `var(--shadow), var(--inset-card)`
    - `Frontend2/components/primitives/card.tsx` contains `var(--shadow-md)` in the mouseEnter handler logic
    - `Frontend2/components/primitives/kbd.tsx` contains `export function Kbd` and `"mono"` className
    - `Frontend2/components/primitives/kbd.tsx` contains `inset 0 0 0 1px var(--border), 0 1px 0 var(--border)`
    - `Frontend2/components/primitives/tabs.tsx` contains `export function Tabs` and `import` from `"./badge"`
    - `Frontend2/components/primitives/tabs.tsx` contains `2px solid var(--primary)` for active tab border
    - `Frontend2/components/primitives/section.tsx` contains `export function Section`
    - All 4 files start with `"use client"` directive
    - No file imports from `@/components/ui/` (no shadcn)
    - `npx next build` succeeds without errors
  </acceptance_criteria>
  <done>Card, Kbd, Tabs, Section components created with full TypeScript props and prototype-faithful styling. All 8 batch-1 primitives are ready for use by App Shell and subsequent phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure UI rendering components with no data processing or external communication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Spoofing | Avatar user.initials prop | accept | Avatar renders user-provided initials as text content. No XSS risk -- React escapes text by default. Initials are display-only. |
</threat_model>

<verification>
1. `npx next build` in Frontend2/ succeeds
2. All 8 component files exist in Frontend2/components/primitives/
3. Every file starts with "use client" and uses named exports
4. Badge uses color-mix as inline styles (not Tailwind classes)
5. Button uses inset shadow tokens from globals.css (var(--inset-*))
6. Tabs imports Badge from ./badge (not from @/components/ui/)
</verification>

<success_criteria>
- 8 primitive components (Avatar, AvatarStack, Badge, Button, Card, Kbd, Tabs, Section) created with TypeScript props interfaces
- All components use "use client" directive and named exports
- color-mix() expressions preserved as inline styles (Badge tones)
- Button inset shadows use var(--inset-*) tokens
- Card interactive mode uses shadow-md on hover
- Build passes without type errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundation-design-system/08-02-SUMMARY.md`
</output>
