---
phase: 08-foundation-design-system
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Frontend2/components/primitives/priority-chip.tsx
  - Frontend2/components/primitives/status-dot.tsx
  - Frontend2/components/primitives/input.tsx
  - Frontend2/components/primitives/progress-bar.tsx
  - Frontend2/components/primitives/segmented-control.tsx
  - Frontend2/components/primitives/collapsible.tsx
  - Frontend2/components/primitives/alert-banner.tsx
  - Frontend2/components/primitives/toggle.tsx
  - Frontend2/components/primitives/index.ts
autonomous: true
requirements: [FOUND-02]

must_haves:
  truths:
    - "ProgressBar renders a percent-width inner bar with configurable height and colors"
    - "SegmentedControl renders radio-button-like option group with active highlight"
    - "Collapsible renders with animated chevron rotation and expandable content"
    - "AlertBanner renders 4 tones (warning/danger/success/info) with color-mix backgrounds"
    - "StatusDot renders 5 status colors (todo/progress/review/done/blocked)"
    - "PriorityChip renders 4 priority levels with rotated diamond indicator"
    - "Toggle renders sliding knob switch with CSS transition"
    - "All 16 primitives are importable from @/components/primitives barrel export"
  artifacts:
    - path: "Frontend2/components/primitives/progress-bar.tsx"
      provides: "ProgressBar component with value/max/height/color props"
      exports: ["ProgressBar"]
    - path: "Frontend2/components/primitives/segmented-control.tsx"
      provides: "SegmentedControl with options/value/onChange props"
      exports: ["SegmentedControl"]
    - path: "Frontend2/components/primitives/collapsible.tsx"
      provides: "Collapsible with animated expand/collapse"
      exports: ["Collapsible"]
    - path: "Frontend2/components/primitives/alert-banner.tsx"
      provides: "AlertBanner with 4 tones using color-mix"
      exports: ["AlertBanner"]
    - path: "Frontend2/components/primitives/toggle.tsx"
      provides: "Toggle switch component"
      exports: ["Toggle"]
    - path: "Frontend2/components/primitives/index.ts"
      provides: "Barrel export for all 16 primitives"
      exports: ["Avatar", "AvatarStack", "Badge", "Button", "Card", "Kbd", "Tabs", "Section", "PriorityChip", "StatusDot", "Input", "ProgressBar", "SegmentedControl", "Collapsible", "AlertBanner", "Toggle"]
  key_links:
    - from: "Frontend2/components/primitives/index.ts"
      to: "all 16 component files"
      via: "re-export statements"
      pattern: "export.*from"
    - from: "Frontend2/components/primitives/alert-banner.tsx"
      to: "globals.css tokens"
      via: "color-mix(in oklch, var(--status-*)) in style prop"
      pattern: "color-mix"
    - from: "Frontend2/components/primitives/status-dot.tsx"
      to: "globals.css tokens"
      via: "var(--status-todo), var(--status-blocked), etc."
      pattern: "status-"
---

<objective>
Create the second batch of 8 primitive components (PriorityChip, StatusDot, Input, ProgressBar, SegmentedControl, Collapsible, AlertBanner, Toggle) and the barrel export file for all 16 primitives.

Purpose: Completes the full primitive component library (FOUND-02). ProgressBar, SegmentedControl, Collapsible, and AlertBanner are specifically called out in the requirement.
Output: 8 TypeScript React components + barrel export index.ts, completing the full 16-component library.
</objective>

<execution_context>
@.planning/phases/08-foundation-design-system/08-PATTERNS.md
@.planning/phases/08-foundation-design-system/08-RESEARCH.md
@.planning/phases/08-foundation-design-system/08-UI-SPEC.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Same conversion rules as Plan 02 — refer to RESEARCH.md Pattern 2 -->
<!-- Executor MUST read New_Frontend/src/primitives.jsx for full source code -->

Import pattern for completed components (from Plan 02):
```typescript
// Barrel export enables clean imports in later phases:
import { Badge, Button, Card, Avatar } from "@/components/primitives"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PriorityChip, StatusDot, Input, ProgressBar components</name>
  <files>
    Frontend2/components/primitives/priority-chip.tsx,
    Frontend2/components/primitives/status-dot.tsx,
    Frontend2/components/primitives/input.tsx,
    Frontend2/components/primitives/progress-bar.tsx
  </files>
  <read_first>
    New_Frontend/src/primitives.jsx,
    Frontend2/lib/utils.ts
  </read_first>
  <action>
    Create 4 primitive components. Read `New_Frontend/src/primitives.jsx` for exact source code.

    **Frontend2/components/primitives/priority-chip.tsx** (source: primitives.jsx lines 197-222):
    - `PriorityChipProps`: `level: "critical" | "high" | "medium" | "low"`, `lang?: "tr" | "en"` (default "tr"), `withLabel?: boolean` (default false), `className?: string`
    - Priority colors from tokens: `{ critical: "var(--priority-critical)", high: "var(--priority-high)", medium: "var(--priority-med)", low: "var(--priority-low)" }`
    - Priority labels: `{ critical: { tr: "Kritik", en: "Critical" }, high: { tr: "Yuksek", en: "High" }, medium: { tr: "Orta", en: "Medium" }, low: { tr: "Dusuk", en: "Low" } }`
    - Diamond indicator: 8x8 div rotated 45deg with the priority color as background
    - Tailwind for container: `inline-flex items-center gap-1.5`
    - Diamond style: `{ width: 8, height: 8, borderRadius: 2, transform: "rotate(45deg)", background: color }`
    - Label (if withLabel): fontSize 12, color `var(--fg-muted)` -> Tailwind `text-[13px] text-muted-foreground` (mapped to 13px per UI-SPEC typography)

    **Frontend2/components/primitives/status-dot.tsx** (source: primitives.jsx lines 224-234):
    - `StatusDotProps`: `status: "todo" | "progress" | "review" | "done" | "blocked"`, `size?: number` (default 8), `className?: string`
    - Status colors from tokens: `{ todo: "var(--status-todo)", progress: "var(--status-progress)", review: "var(--status-review)", done: "var(--status-done)", blocked: "var(--status-blocked)" }`
    - Render: circle div with background = status color, width/height = size
    - Tailwind classes: `rounded-full shrink-0`
    - Style: `{ width: size, height: size, background: statusColors[status] }`

    **Frontend2/components/primitives/input.tsx** (source: primitives.jsx lines 245-272):
    - `InputProps`: extends `React.InputHTMLAttributes<HTMLInputElement>` PLUS `icon?: React.ReactNode`, `kbdHint?: string`, `size?: "sm" | "md"` (default "md"), `className?: string`
    - Wrapper div with icon slot (left), input element (center), optional Kbd hint (right)
    - Wrapper Tailwind: `flex items-center gap-2 rounded-[var(--radius-sm)] transition-colors`
    - Wrapper style: `{ background: "var(--surface)", border: "1px solid var(--border)", height: size === "sm" ? 28 : 32, padding: "0 8px" }`
    - Focus state: on wrapper div, change border to `var(--border-strong)` — use React.useState for focus tracking via onFocus/onBlur on the input
    - Icon: rendered at 14px, color `var(--fg-subtle)` -> Tailwind `text-fg-subtle` or style
    - Input element: `bg-transparent border-0 outline-0 flex-1 text-[13px]` with color `var(--fg)`, placeholder color from CSS
    - Kbd hint: render `<Kbd>` component (import from `./kbd`) if kbdHint provided

    **Frontend2/components/primitives/progress-bar.tsx** (source: primitives.jsx lines 236-240, RESEARCH.md Pattern 2 has full conversion):
    - `ProgressBarProps`: `value?: number` (default 0), `max?: number` (default 100), `height?: number` (default 4), `color?: string` (default "var(--primary)"), `bg?: string` (default "var(--surface-2)"), `className?: string`, `style?: React.CSSProperties`
    - Compute percent: `Math.min((value / max) * 100, 100)`
    - Outer div: overflow hidden, background = bg, borderRadius = height
    - Inner div: width = `${percent}%`, height 100%, background = color, borderRadius = height, transition width 0.2s
    - Tailwind on outer: `overflow-hidden`
    - Tailwind on inner: `h-full transition-[width] duration-200`
    - Keep `style={}` for height, background, borderRadius, width (all prop-dependent)
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/Desktop/bitirme projesi/SPMS/Frontend2" && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File Frontend2/components/primitives/priority-chip.tsx contains `export function PriorityChip` and `rotate(45deg)` and `var(--priority-critical)`
    - File Frontend2/components/primitives/status-dot.tsx contains `export function StatusDot` and `var(--status-todo)` and `var(--status-blocked)`
    - File Frontend2/components/primitives/input.tsx contains `export function Input` and `var(--border-strong)` and `icon?:`
    - File Frontend2/components/primitives/progress-bar.tsx contains `export function ProgressBar` and `transition-[width]` and `Math.min`
    - All 4 files contain `"use client"` and `import * as React from "react"` and `import { cn } from "@/lib/utils"`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>PriorityChip (rotated diamond), StatusDot (5 status colors including blocked), Input (with icon slot and Kbd hint), ProgressBar (percent-width bar) are created with full TypeScript props.</done>
</task>

<task type="auto">
  <name>Task 2: Create SegmentedControl, Collapsible, AlertBanner, Toggle + barrel export</name>
  <files>
    Frontend2/components/primitives/segmented-control.tsx,
    Frontend2/components/primitives/collapsible.tsx,
    Frontend2/components/primitives/alert-banner.tsx,
    Frontend2/components/primitives/toggle.tsx,
    Frontend2/components/primitives/index.ts
  </files>
  <read_first>
    New_Frontend/src/primitives.jsx,
    Frontend2/lib/utils.ts,
    Frontend2/components/primitives/kbd.tsx
  </read_first>
  <action>
    Create 4 final primitive components and the barrel export. Read `New_Frontend/src/primitives.jsx` for exact source.

    **Frontend2/components/primitives/segmented-control.tsx** (source: primitives.jsx lines 274-288):
    - `SegmentedOption`: `{ value: string; label: string }`
    - `SegmentedControlProps`: `options: SegmentedOption[]`, `value: string`, `onChange: (value: string) => void`, `size?: "xs" | "default"` (default "default"), `className?: string`
    - Container with flex row, background `var(--surface-2)`, borderRadius `var(--radius-sm)`, padding 2px, gap 2px
    - Each option: button element. When active: background `var(--surface)`, color `var(--fg)`, fontWeight 600, boxShadow `var(--shadow-sm)`. When inactive: color `var(--fg-muted)`, fontWeight 400, no background.
    - Sizes: xs = `{ height: 22, fontSize: 11, padding: "0 8px" }`, default = `{ height: 26, fontSize: 12, padding: "0 10px" }`
    - Tailwind for each option: `rounded-[calc(var(--radius-sm)-2px)] cursor-pointer transition-all duration-100 border-0`

    **Frontend2/components/primitives/collapsible.tsx** (source: primitives.jsx lines 153-175 approximately — check exact lines):
    - `CollapsibleProps`: `title: string`, `badge?: React.ReactNode`, `defaultOpen?: boolean` (default false), `children: React.ReactNode`, `className?: string`
    - Uses `React.useState` for open state, initialized from `defaultOpen`
    - Title row: flex between, cursor pointer, onClick toggles open state
    - Left side: ChevronRight icon (from lucide-react) with rotation transform: `rotate(${open ? 90 : 0}deg)` and transition `transform 0.15s ease`. Then title text. Then optional badge.
    - Content: conditionally rendered when open. Wrap in a div with padding-left for indentation.
    - Tailwind for title row: `flex items-center gap-2 cursor-pointer select-none py-1`
    - ChevronRight icon: 14px size, color `var(--fg-subtle)`, style `{ transform: \`rotate(${open ? 90 : 0}deg)\`, transition: "transform 0.15s ease" }`
    - Title text: `text-[13px] font-semibold text-foreground`
    - Import `ChevronRight` from `lucide-react`

    **Frontend2/components/primitives/alert-banner.tsx** (source: primitives.jsx lines 200-230 approximately — check exact lines):
    - `AlertBannerProps`: `tone?: "warning" | "danger" | "success" | "info"` (default "warning"), `icon?: React.ReactNode`, `children: React.ReactNode`, `action?: React.ReactNode`, `className?: string`
    - 4 tones with bg/fg/border using color-mix — KEEP as inline `style={}`:
      - warning: `{ bg: "color-mix(in oklch, var(--status-review) 12%, var(--surface))", fg: "var(--status-review)", bd: "color-mix(in oklch, var(--status-review) 25%, transparent)" }`
      - danger: `{ bg: "color-mix(in oklch, var(--priority-critical) 10%, var(--surface))", fg: "var(--priority-critical)", bd: "color-mix(in oklch, var(--priority-critical) 20%, transparent)" }`
      - success: `{ bg: "color-mix(in oklch, var(--status-done) 12%, var(--surface))", fg: "var(--status-done)", bd: "color-mix(in oklch, var(--status-done) 25%, transparent)" }`
      - info: `{ bg: "color-mix(in oklch, var(--status-progress) 12%, var(--surface))", fg: "var(--status-progress)", bd: "color-mix(in oklch, var(--status-progress) 25%, transparent)" }`
    - Layout: flex row with icon (left), children text (center flex-1), action button (right)
    - Tailwind: `flex items-start gap-3 rounded-lg p-4`
    - Style: background, border (1px solid), color from tone object
    - Icon: rendered at 16px in tone fg color
    - If no icon provided, use a default icon per tone: AlertTriangle for warning/danger, CheckCircle for success, Info for info (import from lucide-react)

    **Frontend2/components/primitives/toggle.tsx** (source: primitives.jsx lines 290-304):
    - `ToggleProps`: `on: boolean`, `onChange?: (value: boolean) => void`, `size?: "sm" | "md"` (default "md"), `className?: string`
    - Sizes: sm = `{ w: 30, h: 16, d: 12 }`, md = `{ w: 36, h: 20, d: 16 }`
    - Track (outer div): width=w, height=h, borderRadius=h, position relative
      - On state: background `var(--primary)`, boxShadow `var(--inset-primary-top), var(--inset-primary-bottom)`
      - Off state: background `var(--surface-2)`, boxShadow `inset 0 0 0 1px var(--border-strong)`
    - Knob (inner div): width=d, height=d, borderRadius 50%, background #fff, position absolute
      - top: `(h - d) / 2`, left: on ? `w - d - (h - d) / 2` : `(h - d) / 2`
      - transition `left 0.12s`, boxShadow `0 1px 3px oklch(0 0 0 / 0.15)`
    - onClick: call `onChange?.(!on)`
    - Tailwind for track: `relative cursor-pointer transition-colors duration-100`
    - Tailwind for knob: `absolute rounded-full`
    - Keep ALL styles as inline `style={}` — dimensions are computed from size prop

    **Frontend2/components/primitives/index.ts** (barrel export):
    ```typescript
    export { Avatar } from "./avatar"
    export type { AvatarProps } from "./avatar"
    export { AvatarStack } from "./avatar-stack"
    export { Badge } from "./badge"
    export { Button } from "./button"
    export { Card } from "./card"
    export { Kbd } from "./kbd"
    export { Tabs } from "./tabs"
    export { Section } from "./section"
    export { PriorityChip } from "./priority-chip"
    export { StatusDot } from "./status-dot"
    export { Input } from "./input"
    export { ProgressBar } from "./progress-bar"
    export { SegmentedControl } from "./segmented-control"
    export { Collapsible } from "./collapsible"
    export { AlertBanner } from "./alert-banner"
    export { Toggle } from "./toggle"
    ```
    NOTE: Only export the types that other files need. At minimum export each component function. Export prop types only if they need to be used externally. Adjust based on what each file actually exports.

    After all files created, run build to verify.
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/Desktop/bitirme projesi/SPMS/Frontend2" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File Frontend2/components/primitives/segmented-control.tsx contains `export function SegmentedControl` and `SegmentedOption` and `var(--shadow-sm)`
    - File Frontend2/components/primitives/collapsible.tsx contains `export function Collapsible` and `ChevronRight` and `rotate(` and `defaultOpen`
    - File Frontend2/components/primitives/alert-banner.tsx contains `export function AlertBanner` and `color-mix(in oklch` (at least 2 occurrences)
    - File Frontend2/components/primitives/alert-banner.tsx contains `tone?: "warning" | "danger" | "success" | "info"`
    - File Frontend2/components/primitives/toggle.tsx contains `export function Toggle` and `var(--inset-primary-top)` and `onChange?.(!on)`
    - File Frontend2/components/primitives/index.ts contains all 16 component export statements
    - `grep -c "export.*from" Frontend2/components/primitives/index.ts` returns at least 16
    - `cd Frontend2 && npx next build` passes without errors
  </acceptance_criteria>
  <done>All 16 primitive components are created (FOUND-02 complete). ProgressBar, SegmentedControl, Collapsible, AlertBanner specifically satisfy the requirement. Barrel export enables `import { X } from "@/components/primitives"` for all components. Full project builds cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure presentational components with no user input processing beyond standard React events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | T (Tampering) | Input component | accept | Input component is a styled wrapper. Actual input validation happens at form/API layer, not in primitives. No XSS risk — React auto-escapes all rendered values. |
</threat_model>

<verification>
1. `cd Frontend2 && npx next build` passes
2. 16 component files exist in Frontend2/components/primitives/
3. `grep -c "export.*from" Frontend2/components/primitives/index.ts` returns >= 16
4. `grep "color-mix" Frontend2/components/primitives/alert-banner.tsx` shows color-mix usage
5. `grep "var(--status-blocked)" Frontend2/components/primitives/status-dot.tsx` confirms blocked status token usage
</verification>

<success_criteria>
- All 16 primitive components exist and compile (FOUND-02)
- ProgressBar, SegmentedControl, Collapsible, AlertBanner render correctly with TypeScript props (FOUND-02 specific)
- StatusDot supports 5 statuses including blocked (uses FOUND-04 tokens)
- AlertBanner uses color-mix(in oklch) for 4 tone backgrounds
- Toggle uses inset shadow tokens for raised look
- Barrel export index.ts re-exports all 16 components
- Full project builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundation-design-system/08-03-SUMMARY.md`
</output>
