---
phase: 08-foundation-design-system
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - Frontend2/components/primitives/priority-chip.tsx
  - Frontend2/components/primitives/status-dot.tsx
  - Frontend2/components/primitives/input.tsx
  - Frontend2/components/primitives/progress-bar.tsx
  - Frontend2/components/primitives/segmented-control.tsx
  - Frontend2/components/primitives/collapsible.tsx
  - Frontend2/components/primitives/alert-banner.tsx
  - Frontend2/components/primitives/toggle.tsx
  - Frontend2/components/primitives/index.ts
autonomous: true
requirements: [FOUND-02, FOUND-04]
must_haves:
  truths:
    - "PriorityChip renders rotated 45deg diamond with priority color token and optional label via t()"
    - "StatusDot renders colored circle using --status-* tokens including --status-blocked (FOUND-04)"
    - "Input renders with icon slot, keyboard hint (Kbd), and inset border shadow"
    - "ProgressBar renders percentage-width inner bar with configurable color and height"
    - "SegmentedControl renders radio-button-like group with active shadow and inset-top effect"
    - "Collapsible renders with animated chevron rotation (0deg to 90deg) and toggleable content"
    - "AlertBanner renders 4 tones (warning/danger/success/info) with color-mix backgrounds as inline styles"
    - "Toggle renders sliding knob with CSS transition and primary background when on"
    - "Barrel export index.ts re-exports all 16 primitive components"
  artifacts:
    - path: "Frontend2/components/primitives/progress-bar.tsx"
      provides: "ProgressBar component"
      exports: ["ProgressBar"]
    - path: "Frontend2/components/primitives/segmented-control.tsx"
      provides: "SegmentedControl component"
      exports: ["SegmentedControl"]
    - path: "Frontend2/components/primitives/collapsible.tsx"
      provides: "Collapsible component"
      exports: ["Collapsible"]
    - path: "Frontend2/components/primitives/alert-banner.tsx"
      provides: "AlertBanner component"
      exports: ["AlertBanner"]
    - path: "Frontend2/components/primitives/index.ts"
      provides: "Barrel export for all 16 primitives"
      exports: ["Avatar", "AvatarStack", "Badge", "Button", "Card", "Kbd", "Tabs", "Section", "PriorityChip", "StatusDot", "Input", "ProgressBar", "SegmentedControl", "Collapsible", "AlertBanner", "Toggle"]
  key_links:
    - from: "Frontend2/components/primitives/collapsible.tsx"
      to: "lucide-react"
      via: "imports ChevronRight for expand/collapse indicator"
      pattern: "import.*ChevronRight.*from.*lucide-react"
    - from: "Frontend2/components/primitives/input.tsx"
      to: "Frontend2/components/primitives/kbd.tsx"
      via: "imports Kbd for keyboard hint display"
      pattern: "import.*Kbd.*from.*./kbd"
    - from: "Frontend2/components/primitives/priority-chip.tsx"
      to: "Frontend2/lib/i18n.ts"
      via: "uses t() for priority label text"
      pattern: "import.*t.*from.*@/lib/i18n"
---

<objective>
Create the second batch of 8 primitive components (PriorityChip, StatusDot, Input, ProgressBar, SegmentedControl, Collapsible, AlertBanner, Toggle) and the barrel export index.ts that re-exports all 16 primitives.

Purpose: These components complete the primitive library. ProgressBar, SegmentedControl, Collapsible, and AlertBanner are specifically called out in FOUND-02. StatusDot uses --status-blocked token (FOUND-04). Input is used in Header search. The barrel export enables clean imports: `import { Badge, Button } from "@/components/primitives"`.

Output: 8 additional components + barrel export completing the full 16-component primitive library.
</objective>

<execution_context>
@.planning/phases/08-foundation-design-system/08-PATTERNS.md
@.planning/phases/08-foundation-design-system/08-UI-SPEC.md
</execution_context>

<context>
@.planning/ROADMAP.md

This plan depends on Plan 01 having created:
- Frontend2/ project scaffold with globals.css (CSS tokens including --status-* and --priority-*)
- Frontend2/lib/utils.ts (cn() function)
- Frontend2/lib/i18n.ts (t() function for PriorityChip label)

This plan runs in PARALLEL with Plan 02 (Wave 2). It does NOT depend on Plan 02 components directly.
However, Input imports Kbd (Plan 02) and Collapsible imports Badge (Plan 02). Since both plans run in the same wave, the executor should create files assuming those imports will resolve once the wave completes. Build verification runs after the entire wave finishes.
The barrel export index.ts references ALL 16 component files from both Plan 02 and Plan 03.

CONVERSION RULES (same as Plan 02 -- apply to every component):
1. "use client" directive at top of every file
2. `import * as React from "react"` (namespace access convention)
3. Explicit Props interface with all props typed
4. Add `className?: string` prop for external styling
5. Named export: `export function ComponentName`
6. Static styles -> Tailwind where clean mapping exists
7. Dynamic values + color-mix() -> keep as style={}
8. cn() for className merging
9. var() token references preserved

CRITICAL: No shadcn/ui. No Frontend/ references. All from New_Frontend/ prototype.

<interfaces>
From Frontend2/lib/utils.ts (Plan 01):
```typescript
export function cn(...inputs: ClassValue[]): string
```

From Frontend2/lib/i18n.ts (Plan 01):
```typescript
export type LangCode = "tr" | "en"
export function t(path: string, lang?: LangCode): string
```

From Frontend2/components/primitives/kbd.tsx (Plan 02, same wave):
```typescript
export function Kbd({ children, className, style }: KbdProps): JSX.Element
```

From Frontend2/components/primitives/badge.tsx (Plan 02, same wave):
```typescript
export function Badge({ children, tone, size, dot, className, style }: BadgeProps): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PriorityChip, StatusDot, Input, and ProgressBar components</name>
  <files>
    Frontend2/components/primitives/priority-chip.tsx
    Frontend2/components/primitives/status-dot.tsx
    Frontend2/components/primitives/input.tsx
    Frontend2/components/primitives/progress-bar.tsx
  </files>
  <read_first>
    New_Frontend/src/primitives.jsx (lines 198-240 for PriorityChip lines 198-207, StatusDot lines 210-213, Input lines 216-233, ProgressBar lines 236-240)
    Frontend2/lib/i18n.ts (for t() function import and LangCode type)
  </read_first>
  <action>
Create 4 component files in Frontend2/components/primitives/:

**priority-chip.tsx** -- port from primitives.jsx lines 198-207:

Props interface:
```typescript
interface PriorityChipProps {
  level: "critical" | "high" | "medium" | "low"
  lang: LangCode
  withLabel?: boolean    // default true
  className?: string
}
```
- Import `{ t, type LangCode }` from `"@/lib/i18n"`
- Label: `t(\`priority.${level}\`, lang)`
- Color token mapping -- IMPORTANT: prototype CSS uses `--priority-med` (not `--priority-medium`):
  `const tokenLevel = level === "medium" ? "med" : level`
  `const color = \`var(--priority-${tokenLevel})\``
- Outer span: display inline-flex, alignItems center, gap 4, fontSize 12, color var(--fg-muted), fontWeight 500
- Diamond indicator inner span: width 8, height 8, borderRadius 2, background color, transform "rotate(45deg)"
- If withLabel is true, render label text after diamond

**status-dot.tsx** -- port from primitives.jsx lines 210-213:

Props interface:
```typescript
interface StatusDotProps {
  status: "todo" | "progress" | "review" | "done" | "blocked"
  size?: number    // default 8
  className?: string
}
```
- Color: `var(--status-${status})` -- all 5 values (including "blocked" for FOUND-04) map directly to CSS token names
- Render span: width size, height size, borderRadius "50%", background color, flexShrink 0, display inline-block

**input.tsx** -- port from primitives.jsx lines 216-233:

Props interface:
```typescript
interface InputProps {
  icon?: React.ReactNode
  placeholder?: string
  value?: string
  onChange?: (e: React.ChangeEvent<HTMLInputElement>) => void
  kbdHint?: string
  size?: "sm" | "md" | "lg"    // default "md"
  type?: string                 // default "text"
  className?: string
  style?: React.CSSProperties
}
```
- Import Kbd from "./kbd" (created by Plan 02 in same wave)
- Height map: sm=28, md=32, lg=38
- Container div: display inline-flex, alignItems center, background var(--surface), borderRadius var(--radius-sm), boxShadow "inset 0 0 0 1px var(--border)", height from map, padding "0 8px", gap 6, transition "box-shadow 0.12s"
- If icon: render span wrapper with color var(--fg-subtle), display inline-flex
- Native input element: flex 1, minWidth 0, height 100%, background transparent, border 0, outline 0, fontSize 13, color var(--fg)
- If kbdHint: render `<Kbd>{kbdHint}</Kbd>` at the end

**progress-bar.tsx** -- port from primitives.jsx lines 236-240:

Props interface:
```typescript
interface ProgressBarProps {
  value?: number      // default 0
  max?: number        // default 100
  height?: number     // default 4
  color?: string      // default "var(--primary)"
  bg?: string         // default "var(--surface-2)"
  className?: string
  style?: React.CSSProperties
}
```
- Percent calculation: `Math.min((value / max) * 100, 100)`
- Outer div: height, background bg, borderRadius height, overflow hidden. Use cn(className) on outer div.
- Inner div: width `${percent}%`, height "100%", background color, borderRadius height, transition "width 0.2s"
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/Desktop/bitirme projesi/SPMS/Frontend2" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Frontend2/components/primitives/priority-chip.tsx` contains `export function PriorityChip` and `rotate(45deg)` transform
    - `Frontend2/components/primitives/priority-chip.tsx` imports `t` from `"@/lib/i18n"` (NOT from window.SPMSi18n)
    - `Frontend2/components/primitives/priority-chip.tsx` handles the "medium" -> "med" token mapping (contains string `"med"`)
    - `Frontend2/components/primitives/status-dot.tsx` contains `export function StatusDot` and `var(--status-${status})`
    - `Frontend2/components/primitives/status-dot.tsx` type includes `"blocked"` (FOUND-04)
    - `Frontend2/components/primitives/input.tsx` contains `export function Input` and imports from `"./kbd"`
    - `Frontend2/components/primitives/input.tsx` contains `inset 0 0 0 1px var(--border)` for container shadow
    - `Frontend2/components/primitives/input.tsx` has height map values 28, 32, 38
    - `Frontend2/components/primitives/progress-bar.tsx` contains `export function ProgressBar` and `Math.min`
    - `Frontend2/components/primitives/progress-bar.tsx` default color is `var(--primary)` and default bg is `var(--surface-2)`
    - All 4 files start with `"use client"` directive
  </acceptance_criteria>
  <done>PriorityChip, StatusDot, Input, ProgressBar components created with TypeScript props and prototype-faithful styling. StatusDot supports "blocked" status (FOUND-04). PriorityChip uses i18n t() for labels and handles med/medium token mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Create SegmentedControl, Collapsible, AlertBanner, Toggle, and barrel export</name>
  <files>
    Frontend2/components/primitives/segmented-control.tsx
    Frontend2/components/primitives/collapsible.tsx
    Frontend2/components/primitives/alert-banner.tsx
    Frontend2/components/primitives/toggle.tsx
    Frontend2/components/primitives/index.ts
  </files>
  <read_first>
    New_Frontend/src/primitives.jsx (lines 243-304 for SegmentedControl lines 243-256, Collapsible lines 259-272, AlertBanner lines 275-287, Toggle lines 290-304)
  </read_first>
  <action>
Create 5 files:

**segmented-control.tsx** -- port from primitives.jsx lines 243-256:

Props interface:
```typescript
interface SegmentedOption {
  id: string
  label: string
  icon?: React.ReactNode
}
interface SegmentedControlProps {
  options: SegmentedOption[]
  value: string
  onChange: (id: string) => void
  size?: "xs" | "sm"    // default "sm"
  className?: string
}
```
- Container div: display inline-flex, background var(--surface-2), padding 2, borderRadius 6, boxShadow "inset 0 0 0 1px var(--border)"
- Each option button:
  - Padding: xs="3px 8px", sm="4px 10px"
  - fontSize: xs=11, sm=11.5
  - fontWeight 600, borderRadius 4
  - Active (value === opt.id): background var(--surface), color var(--fg), boxShadow "var(--shadow-sm), var(--inset-top)"
  - Inactive: background transparent, color var(--fg-muted), boxShadow none
  - display inline-flex, alignItems center, gap 5
  - Render: {opt.icon}{opt.label}

**collapsible.tsx** -- port from primitives.jsx lines 259-272:

Props interface:
```typescript
interface CollapsibleProps {
  title: string
  badge?: string | number
  defaultOpen?: boolean    // default false
  children: React.ReactNode
  className?: string
  style?: React.CSSProperties
}
```
- Import `{ ChevronRight }` from "lucide-react"
- Import `{ Badge }` from "./badge" (created by Plan 02 in same wave)
- State: `const [open, setOpen] = React.useState(defaultOpen)`
- Container div: border "1px solid var(--border)", borderRadius var(--radius-sm)
- Toggle button: width 100%, padding "10px 14px", display flex, alignItems center, gap 8, fontSize 13, fontWeight 600, background transparent
  - ChevronRight: size={13}, style={{ transform: open ? "rotate(90deg)" : "none", transition: "transform 0.15s", color: "var(--fg-subtle)" }}
  - Title span: flex 1, textAlign left
  - If badge: render `<Badge size="xs" tone="neutral">{badge}</Badge>`
- Content div (only when open): padding "0 14px 14px", borderTop "1px solid var(--border)"

**alert-banner.tsx** -- port from primitives.jsx lines 275-287:

Props interface:
```typescript
type AlertTone = "warning" | "danger" | "success" | "info"
interface AlertBannerProps {
  tone?: AlertTone    // default "warning"
  icon?: React.ReactNode
  children: React.ReactNode
  action?: React.ReactNode
  className?: string
  style?: React.CSSProperties
}
```
- Tone -> CSS variable mapping:
  - danger -> "--priority-critical"
  - success -> "--status-done"
  - info -> "--status-progress"
  - warning (default) -> "--status-review"
- ALL color-dependent styles as INLINE STYLES (color-mix must not be in Tailwind classes):
  - background: `color-mix(in oklch, var(${colorVar}) 10%, var(--surface))`
  - boxShadow: `inset 0 0 0 1px color-mix(in oklch, var(${colorVar}) 25%, transparent)`
  - color: `var(${colorVar})`
- Layout: padding "10px 16px", display flex, alignItems center, gap 8, fontSize 12.5, borderRadius var(--radius-sm)
- Render: {icon} <span style={{flex:1}}>{children}</span> {action}

**toggle.tsx** -- port from primitives.jsx lines 290-304:

Props interface:
```typescript
interface ToggleProps {
  on: boolean
  onChange?: (value: boolean) => void
  size?: "sm" | "md"    // default "md"
  className?: string
}
```
- Size dimensions: sm -> w=30, h=16, d=12; md -> w=36, h=20, d=16
- Track (outer div, onClick calls onChange(!on)):
  - width w, height h, borderRadius h, cursor pointer, position relative
  - On state: background var(--primary), boxShadow "var(--inset-primary-top), var(--inset-primary-bottom)"
  - Off state: background var(--surface-2), boxShadow "inset 0 0 0 1px var(--border-strong)"
  - transition "background 0.12s"
- Knob (inner div):
  - width d, height d, borderRadius "50%", background "#fff"
  - position absolute, top (h-d)/2
  - left: on ? w - d - (h-d)/2 : (h-d)/2
  - transition "left 0.12s"
  - boxShadow "0 1px 3px oklch(0 0 0 / 0.15)"

**index.ts** -- barrel export for ALL 16 primitives:
```typescript
export { Avatar } from "./avatar"
export type { AvatarUser } from "./avatar"
export { AvatarStack } from "./avatar-stack"
export { Badge } from "./badge"
export { Button } from "./button"
export { Card } from "./card"
export { Kbd } from "./kbd"
export { Tabs } from "./tabs"
export type { TabItem } from "./tabs"
export { Section } from "./section"
export { PriorityChip } from "./priority-chip"
export { StatusDot } from "./status-dot"
export { Input } from "./input"
export { ProgressBar } from "./progress-bar"
export { SegmentedControl } from "./segmented-control"
export { Collapsible } from "./collapsible"
export { AlertBanner } from "./alert-banner"
export { Toggle } from "./toggle"
```
Note: Export useful types (AvatarUser, TabItem) from the barrel. Add other type exports as needed. Ensure the types are exported with `export type` syntax from source files for re-export to work.
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/Desktop/bitirme projesi/SPMS/Frontend2" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Frontend2/components/primitives/segmented-control.tsx` contains `export function SegmentedControl` and `var(--shadow-sm), var(--inset-top)` for active state
    - `Frontend2/components/primitives/collapsible.tsx` contains `export function Collapsible` and imports `ChevronRight` from `"lucide-react"`
    - `Frontend2/components/primitives/collapsible.tsx` contains `rotate(90deg)` for open state chevron
    - `Frontend2/components/primitives/collapsible.tsx` imports `Badge` from `"./badge"`
    - `Frontend2/components/primitives/alert-banner.tsx` contains `export function AlertBanner` and `color-mix(in oklch` as inline style values
    - `Frontend2/components/primitives/alert-banner.tsx` maps: danger to `--priority-critical`, success to `--status-done`, info to `--status-progress`, warning to `--status-review`
    - `Frontend2/components/primitives/toggle.tsx` contains `export function Toggle` and `var(--inset-primary-top)` for on-state shadow
    - `Frontend2/components/primitives/toggle.tsx` contains knob transition (string `left 0.12s`)
    - `Frontend2/components/primitives/index.ts` contains export statements for all 16 components: Avatar, AvatarStack, Badge, Button, Card, Kbd, Tabs, Section, PriorityChip, StatusDot, Input, ProgressBar, SegmentedControl, Collapsible, AlertBanner, Toggle
    - No file imports from `@/components/ui/` (no shadcn)
    - `npx next build` succeeds (requires Plan 02 files to also exist from same wave)
  </acceptance_criteria>
  <done>SegmentedControl, Collapsible, AlertBanner, Toggle components created. Barrel index.ts re-exports all 16 primitives. The complete primitive library is available via `import { ... } from "@/components/primitives"`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure UI rendering components with no data processing or external communication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | Input component value | accept | Input wraps a native HTML input element with controlled value prop. No custom input processing that could bypass browser security. React's built-in escaping prevents XSS. |
</threat_model>

<verification>
1. `npx next build` in Frontend2/ succeeds
2. All 8 new component files + index.ts exist in Frontend2/components/primitives/
3. Every component file starts with "use client" and uses named exports
4. AlertBanner uses color-mix as inline styles (not Tailwind classes)
5. Toggle uses inset-primary-top/bottom shadow tokens for on state
6. Collapsible imports ChevronRight from lucide-react (not a custom icon)
7. index.ts barrel exports all 16 component names
8. StatusDot type includes "blocked" (FOUND-04)
</verification>

<success_criteria>
- 8 primitive components (PriorityChip, StatusDot, Input, ProgressBar, SegmentedControl, Collapsible, AlertBanner, Toggle) created with TypeScript props
- ProgressBar, SegmentedControl, Collapsible, AlertBanner specifically satisfy FOUND-02
- StatusDot "blocked" status uses --status-blocked token (FOUND-04)
- Barrel index.ts re-exports all 16 primitives for clean imports
- color-mix() preserved as inline styles (AlertBanner)
- Build passes without type errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundation-design-system/08-03-SUMMARY.md`
</output>
