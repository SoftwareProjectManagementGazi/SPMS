---
phase: 08-foundation-design-system
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - Frontend2/components/ui/
  - Frontend2/components/app-shell.tsx
  - Frontend2/components/sidebar.tsx
  - Frontend2/components/header.tsx
  - Frontend2/components/breadcrumb.tsx
  - Frontend2/context/auth-context.tsx
  - Frontend2/lib/api-client.ts
  - Frontend2/components/providers/query-provider.tsx
  - Frontend2/app/layout.tsx
  - Frontend2/app/(authenticated)/layout.tsx
autonomous: false
requirements: [FOUND-05]

must_haves:
  truths:
    - "Sidebar renders with collapsible navigation, admin section, user area, keyboard shortcut hints"
    - "Header renders with breadcrumb, search, notifications bell, theme toggle, create button, user menu"
    - "App Shell layout matches prototype: horizontal flex with sticky sidebar (232px/56px) and sticky header (52px)"
    - "Navigation uses Next.js Link and usePathname for active state detection"
    - "All nav labels come from t() i18n function"
    - "Keyboard shortcuts G D, G P, G T navigate to respective pages"
    - "Shadcn/ui overlay components are available in Frontend2/components/ui/ (copied from Frontend/)"
    - "Auth context and API client are available in Frontend2/ (copied from Frontend/)"
  artifacts:
    - path: "Frontend2/components/sidebar.tsx"
      provides: "Collapsible sidebar with nav items, admin section, user area"
      exports: ["Sidebar"]
    - path: "Frontend2/components/header.tsx"
      provides: "Header with breadcrumb, search, theme toggle, create button, user menu"
      exports: ["Header"]
    - path: "Frontend2/components/app-shell.tsx"
      provides: "App Shell layout wrapping Sidebar + Header + main content"
      exports: ["AppShell"]
    - path: "Frontend2/components/breadcrumb.tsx"
      provides: "Breadcrumb component using usePathname"
      exports: ["Breadcrumb"]
    - path: "Frontend2/components/ui/"
      provides: "59 shadcn/ui overlay components copied from Frontend/"
    - path: "Frontend2/context/auth-context.tsx"
      provides: "Auth state management (copied and adapted)"
      exports: ["AuthProvider", "useAuth"]
  key_links:
    - from: "Frontend2/components/sidebar.tsx"
      to: "Frontend2/context/app-context.tsx"
      via: "useApp() for sidebarCollapsed, language"
      pattern: "useApp"
    - from: "Frontend2/components/sidebar.tsx"
      to: "Frontend2/lib/i18n.ts"
      via: "t() for nav labels"
      pattern: "import.*t.*from.*i18n"
    - from: "Frontend2/components/header.tsx"
      to: "Frontend2/context/app-context.tsx"
      via: "useApp() for mode toggle, language"
      pattern: "useApp"
    - from: "Frontend2/components/app-shell.tsx"
      to: "Frontend2/components/sidebar.tsx"
      via: "Sidebar component import"
      pattern: "import.*Sidebar.*from"
    - from: "Frontend2/app/layout.tsx"
      to: "Frontend2/context/auth-context.tsx"
      via: "AuthProvider wraps children"
      pattern: "AuthProvider"
---

<objective>
Convert the App Shell from prototype to Next.js (Sidebar, Header, Layout), copy shadcn/ui overlay components and auth infrastructure from Frontend/, and verify visual parity with the HTML prototype.

Purpose: This is FOUND-05 — the App Shell is the chrome that wraps all page content in subsequent phases. Without it, no page can be built.
Output: Working App Shell in Frontend2/ with sidebar navigation, header toolbar, copied shadcn/ui components, and auth infrastructure.
</objective>

<execution_context>
@.planning/phases/08-foundation-design-system/08-PATTERNS.md
@.planning/phases/08-foundation-design-system/08-RESEARCH.md
@.planning/phases/08-foundation-design-system/08-UI-SPEC.md
@.planning/phases/08-foundation-design-system/08-01-SUMMARY.md
@.planning/phases/08-foundation-design-system/08-02-SUMMARY.md
@.planning/phases/08-foundation-design-system/08-03-SUMMARY.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- From Plan 01 outputs -->
From Frontend2/context/app-context.tsx:
```typescript
export function useApp(): {
  language: LangCode
  setLanguage: (lang: LangCode) => void
  mode: ThemeMode
  setMode: (mode: ThemeMode) => void
  preset: ThemePreset | string
  applyPreset: (id: string) => void
  sidebarCollapsed: boolean
  setSidebarCollapsed: (collapsed: boolean) => void
  density: Density
  setDensity: (density: Density) => void
  brandLight: number
  brandChroma: number
  brandHue: number
  customColors: boolean
  applyCustomBrand: (params: { L: number; C: number; H: number }) => void
}
```

From Frontend2/lib/i18n.ts:
```typescript
export function t(path: string, lang: LangCode = "tr"): string
```

From Frontend2/lib/theme.ts:
```typescript
export function applyMode(mode: "light" | "dark"): void
```

<!-- From Plan 02/03 outputs -->
From Frontend2/components/primitives/index.ts:
```typescript
export { Avatar, AvatarStack, Badge, Button, Card, Kbd, Tabs, Section,
         PriorityChip, StatusDot, Input, ProgressBar, SegmentedControl,
         Collapsible, AlertBanner, Toggle }
```

<!-- Source files to port from -->
From Frontend/context/auth-context.tsx (to be copied):
```typescript
export function AuthProvider({ children }: { children: React.ReactNode })
export function useAuth(): { user: User | null, isLoading: boolean, login: ..., logout: ... }
```

From Frontend/lib/api-client.ts (to be copied):
```typescript
// Axios instance with auth interceptors
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy shadcn/ui components, auth context, and API infrastructure from Frontend/ to Frontend2/</name>
  <files>
    Frontend2/components/ui/ (59 files),
    Frontend2/context/auth-context.tsx,
    Frontend2/lib/api-client.ts,
    Frontend2/components/providers/query-provider.tsx,
    Frontend2/app/layout.tsx
  </files>
  <read_first>
    Frontend/context/auth-context.tsx,
    Frontend/lib/api-client.ts,
    Frontend/components/providers/query-provider.tsx,
    Frontend/app/layout.tsx
  </read_first>
  <action>
    **Step 1: Copy all 59 shadcn/ui components from Frontend/components/ui/ to Frontend2/components/ui/**

    Use a bulk copy command:
    ```bash
    cp -r "Frontend/components/ui/" "Frontend2/components/ui/"
    ```

    These are shadcn/ui overlay components (Dialog, Popover, Sheet, Select, DropdownMenu, Command, etc.) that will be used alongside the new primitives. They remain unchanged — their `@/lib/utils` imports will resolve correctly because Frontend2/ has the same path alias.

    **Step 2: Copy and adapt auth-context.tsx**

    Copy `Frontend/context/auth-context.tsx` to `Frontend2/context/auth-context.tsx`. Read the source file first to understand imports. The file should work as-is because it uses `@/` path aliases which resolve the same way in Frontend2/. Verify it imports from `@/lib/api-client` and `@/services/auth` — if `services/auth` exists in Frontend, copy that too.

    Check if there are other files referenced by auth-context (types, services, etc.) and copy those as needed:
    - `Frontend/services/auth.ts` -> `Frontend2/services/auth.ts` (if it exists)
    - `Frontend/types/` -> `Frontend2/types/` (if auth-context references types from there)

    **Step 3: Copy and adapt api-client.ts**

    Copy `Frontend/lib/api-client.ts` to `Frontend2/lib/api-client.ts`. This is the Axios instance with auth interceptors. Should work as-is with path aliases.

    **Step 4: Copy query-provider.tsx**

    Copy `Frontend/components/providers/query-provider.tsx` to `Frontend2/components/providers/query-provider.tsx`. This wraps the app with TanStack React Query's QueryClientProvider.

    **Step 5: Copy auth-guard.tsx**

    Check if `Frontend/components/auth-guard.tsx` exists and copy to `Frontend2/components/auth-guard.tsx`. The AppShell wraps content in AuthGuard.

    **Step 6: Update Frontend2/app/layout.tsx**

    Update the root layout to include ALL providers now that auth infrastructure is copied:
    ```tsx
    import QueryProvider from "@/components/providers/query-provider"
    import { AuthProvider } from "@/context/auth-context"
    import { AppProvider } from "@/context/app-context"
    ```

    Provider nesting (outermost to innermost):
    `QueryProvider > AuthProvider > AppProvider > {children}`

    NOTE: Remove SystemConfigProvider for now — its brand color functionality is now handled by AppContext (per RESEARCH.md Pitfall 7 resolution). If the app needs it later, it can be added back.

    **Step 7: Resolve any import chains**

    After copying, run `cd Frontend2 && npx tsc --noEmit` to find any broken imports. If auth-context imports from services, hooks, or types directories that don't exist in Frontend2/ yet, copy those files too. The goal is a green build with all providers wired.

    Be thorough — follow EVERY import chain from auth-context.tsx and api-client.ts until all dependencies are resolved.
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/Desktop/bitirme projesi/SPMS/Frontend2" && npx tsc --noEmit --pretty 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Directory Frontend2/components/ui/ contains at least 50 .tsx files (shadcn components)
    - File Frontend2/context/auth-context.tsx exists and contains `export function AuthProvider` and `export function useAuth`
    - File Frontend2/lib/api-client.ts exists and contains `axios`
    - File Frontend2/components/providers/query-provider.tsx exists and contains `QueryClient`
    - File Frontend2/app/layout.tsx imports `QueryProvider`, `AuthProvider`, and `AppProvider`
    - File Frontend2/app/layout.tsx nests providers in order: QueryProvider > AuthProvider > AppProvider
    - `npx tsc --noEmit` passes without errors (all import chains resolved)
  </acceptance_criteria>
  <done>All shadcn/ui overlay components, auth context, API client, and query provider are copied to Frontend2/ with all import chains resolved. Root layout wraps with all providers. Build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Create Sidebar, Header, Breadcrumb, and AppShell components</name>
  <files>
    Frontend2/components/sidebar.tsx,
    Frontend2/components/header.tsx,
    Frontend2/components/breadcrumb.tsx,
    Frontend2/components/app-shell.tsx,
    Frontend2/app/(authenticated)/layout.tsx
  </files>
  <read_first>
    New_Frontend/src/shell.jsx,
    New_Frontend/src/i18n.jsx,
    Frontend/components/sidebar.tsx,
    Frontend/components/header.tsx,
    Frontend/components/app-shell.tsx,
    Frontend2/context/app-context.tsx,
    Frontend2/lib/i18n.ts,
    Frontend2/components/primitives/index.ts
  </read_first>
  <action>
    Create the App Shell components by porting from `New_Frontend/src/shell.jsx` to TypeScript + Tailwind, using Next.js App Router patterns (PATTERNS.md Patterns C and D).

    **Frontend2/components/sidebar.tsx** (source: shell.jsx Sidebar component):

    Structure:
    - `SidebarProps`: `className?: string`
    - Get `sidebarCollapsed`, `setSidebarCollapsed`, `language` from `useApp()`
    - Get `user` from `useAuth()` (if available, or handle null)
    - Get `pathname` from `usePathname()` (next/navigation)

    Nav items array (use t() for all labels per D-05):
    ```typescript
    const navItems = [
      { href: "/", label: t("nav.dashboard", language), icon: LayoutDashboard, shortcut: "G D" },
      { href: "/projects", label: t("nav.projects", language), icon: FolderKanban, shortcut: "G P" },
      { href: "/my-tasks", label: t("nav.myTasks", language), icon: CheckSquare, shortcut: "G T" },
      { href: "/teams", label: t("nav.teams", language), icon: Users },
      { href: "/reports", label: t("nav.reports", language), icon: BarChart3 },
      { href: "/settings", label: t("nav.settings", language), icon: Settings },
    ]
    ```

    Admin items (shown when user?.role includes admin):
    ```typescript
    const adminItems = [
      { href: "/admin", label: t("nav.adminPanel", language), icon: Shield },
    ]
    ```

    Layout per UI-SPEC App Shell Layout Contract:
    - `<aside>` element, sticky, full height
    - Width: 232px expanded, 56px collapsed (NOT 64/256 — use prototype-exact values)
    - Background: `var(--bg-2)`, right border: `1px solid var(--border)`
    - Transition: `width 0.18s ease`
    - Sections:
      1. Logo area (top): 52px height. Brand mark (26x26 div with bg primary, rounded, centered icon). App name "SPMS" in semibold 13px when expanded.
      2. Section label "CALISMA" / "WORKSPACE" (from t()) in 11px, color fg-subtle, uppercase, tracking-wider
      3. Nav items: each item is `<Link>` with icon + label + optional Kbd shortcut hint. Active: bg accent, icon color primary, font semibold. Inactive: color fg-muted, hover bg surface-2.
      4. Section label "YONETIM" / "ADMIN" (if admin items exist)
      5. Admin nav items (same style)
      6. Bottom area: User avatar + name + role, collapse toggle button

    Keyboard shortcuts (G D, G P, G T):
    - Add `React.useEffect` with keydown listener on document
    - Track a "g" key press buffer — if "g" was pressed, next key triggers navigation
    - Use `router.push()` from `useRouter()` (next/navigation) for navigation
    - Only active when no input/textarea is focused

    Collapsed state:
    - Icons only, no labels, no Kbd hints
    - Tooltip on hover showing label (can use `title` attribute or a simple tooltip div)

    Tailwind classes for nav item:
    ```
    flex items-center gap-3 rounded-lg px-2.5 h-[30px] text-[13px] transition-colors duration-100
    ```
    Active: `bg-accent text-foreground font-semibold` + icon gets `text-primary`
    Inactive: `text-muted-foreground hover:bg-[var(--surface-2)]`

    **Frontend2/components/header.tsx** (source: shell.jsx Header component):

    Structure:
    - `HeaderProps`: `className?: string`
    - Get `mode`, `setMode`, `language` from `useApp()`
    - Get `user` from `useAuth()`

    Layout per UI-SPEC:
    - `<header>` element, sticky top-0, height 52px
    - Background: `var(--surface)` / `bg-card`, bottom border
    - Flex between: left side (Breadcrumb), right side (actions)
    - Right side actions (left to right):
      1. Search input (collapsed to icon button, or use Input primitive with icon)
      2. Theme mode toggle: Moon icon (to dark) / Sun icon (to light) — `aria-label` from i18n
      3. Notifications bell with unread count badge (Badge component, hardcode count=0 for now)
      4. Create button: Button primitive variant="primary" size="sm" with Plus icon + "Olustur"/t("common.create") label
      5. User avatar (Avatar primitive) — clicking shows user menu dropdown (use shadcn DropdownMenu)

    User menu dropdown items (per UI-SPEC Copywriting):
    - Profilim / My Profile -> /profile
    - Ayarlar / Settings -> /settings
    - Yardim / Help -> (no-op for now)
    - Separator
    - Cikis Yap / Log Out -> calls `logout()` from useAuth

    Accessibility: Icon-only buttons get `aria-label` per UI-SPEC table:
    - Theme toggle dark: `aria-label={t("common.darkMode", language)}` (add these keys to i18n if missing)
    - Theme toggle light: `aria-label={t("common.lightMode", language)}`
    - Notifications: `aria-label={t("common.notifications", language)}`

    **Frontend2/components/breadcrumb.tsx:**
    - Uses `usePathname()` to build breadcrumb segments
    - Maps path segments to display names using a lookup or t() function
    - Separator: ChevronRight icon at 14px
    - Simple implementation: split pathname by /, capitalize each segment, link each

    **Frontend2/components/app-shell.tsx** (source: shell.jsx + Frontend/components/app-shell.tsx pattern):
    - `AppShellProps`: `children: React.ReactNode`
    - Wraps in AuthGuard (import from `@/components/auth-guard` — if it exists)
    - Layout: `<div className="flex h-screen">` with Sidebar and content area
    - Content area: `<div className="flex flex-1 flex-col overflow-hidden">` with Header and `<main className="flex-1 overflow-auto">{children}</main>`

    **Frontend2/app/(authenticated)/layout.tsx:**
    Create a route group layout that applies the AppShell to all authenticated pages:
    ```tsx
    import { AppShell } from "@/components/app-shell"

    export default function AuthenticatedLayout({ children }: { children: React.ReactNode }) {
      return <AppShell>{children}</AppShell>
    }
    ```
    Move the placeholder page.tsx to `Frontend2/app/(authenticated)/page.tsx` so it renders within the shell.

    After all files created, run build verification.
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/Desktop/bitirme projesi/SPMS/Frontend2" && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File Frontend2/components/sidebar.tsx contains `export function Sidebar` and `useApp()` and `usePathname()` and `t("nav.` and `width.*232`
    - File Frontend2/components/sidebar.tsx contains `G D` or `G P` (keyboard shortcut handling)
    - File Frontend2/components/sidebar.tsx contains Kbd import (from primitives or inline)
    - File Frontend2/components/header.tsx contains `export function Header` and `useApp()` and `Moon` and `Sun` and `Bell` and `Plus`
    - File Frontend2/components/header.tsx contains `aria-label` (accessibility labels)
    - File Frontend2/components/header.tsx contains `DropdownMenu` import (for user menu)
    - File Frontend2/components/breadcrumb.tsx contains `export function Breadcrumb` and `usePathname`
    - File Frontend2/components/app-shell.tsx contains `export function AppShell` and imports `Sidebar` and `Header`
    - File Frontend2/app/(authenticated)/layout.tsx contains `AppShell`
    - `cd Frontend2 && npx next build` passes without errors
  </acceptance_criteria>
  <done>App Shell (Sidebar, Header, Layout) is converted from prototype to Next.js with i18n labels, keyboard shortcuts, theme toggle, auth integration, and prototype-exact dimensions (232px sidebar, 52px header). FOUND-05 complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Frontend2/ application with:
    1. Theme token system (6 presets, dark mode, brand derivation)
    2. 16 primitive components (Avatar through Toggle)
    3. App Shell (Sidebar + Header + Layout) with i18n
    4. Copied shadcn/ui overlay components and auth infrastructure
  </what-built>
  <how-to-verify>
    1. Start the development server:
       ```bash
       cd Frontend2 && npm run dev
       ```
    2. Open http://localhost:3001 (or whatever port the dev server uses) in browser

    3. **Visual Parity Check:** Open `New_Frontend/SPMS Prototype.html` in another browser tab. Compare side-by-side:
       - Sidebar: Same width (232px), same nav items, same colors, same collapse behavior
       - Header: Same height (52px), same layout (breadcrumb left, actions right)
       - Overall layout: Sidebar + Header + Main content area

    4. **Theme Toggle Check:** Click the Moon/Sun icon in the header:
       - Dark mode should activate (background darkens, text lightens)
       - Sidebar and header should both respond to dark mode
       - Toggle back to light mode

    5. **Sidebar Collapse Check:** Click the collapse toggle in sidebar bottom:
       - Sidebar should animate from 232px to 56px
       - Only icons should be visible in collapsed state
       - Expand back and verify labels return

    6. **I18n Check:** If language switch is accessible (through settings or header), toggle between TR and EN:
       - Nav labels should change (Panel/Dashboard, Projeler/Projects, etc.)
       - If not accessible yet, verify default Turkish labels are showing

    7. **Keyboard Shortcuts:** Press G then D — should navigate to dashboard (if routing works)

    Note: Some features may not be fully functional if auth is not configured (login may be required). The visual layout and component rendering are the primary verification targets.
  </how-to-verify>
  <resume-signal>Type "approved" if visual parity is acceptable, or describe specific issues (e.g., "sidebar is too wide", "dark mode doesn't work on header")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend2/ -> Backend API | Auth token sent from localStorage via api-client.ts — copied from existing Frontend/ |
| User keyboard input -> Navigation | Keyboard shortcuts (G D, G P, G T) trigger router navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | S (Spoofing) | auth-context.tsx | accept | Copied from existing working Frontend/ — same security posture. JWT in localStorage. No changes to auth flow in this phase. |
| T-08-06 | T (Tampering) | api-client.ts | accept | Copied verbatim from Frontend/ — same interceptor logic, same auth header attachment. |
| T-08-07 | I (Info Disclosure) | Sidebar admin section | mitigate | Admin nav items only rendered when user role includes admin — same pattern as existing Frontend/components/sidebar.tsx. Verify admin check is preserved in copy. |
| T-08-08 | D (Denial of Service) | Keyboard shortcut listener | accept | Global keydown listener is lightweight. Disabled when input/textarea focused. No DoS risk. |
</threat_model>

<verification>
1. `cd Frontend2 && npx next build` passes (full build green)
2. `cd Frontend2 && npm run dev` starts without errors
3. Visual comparison with `New_Frontend/SPMS Prototype.html` shows matching layout
4. Sidebar collapses from 232px to 56px with animation
5. Dark mode toggle works via Moon/Sun button
6. Nav items show Turkish labels by default
7. Keyboard shortcuts (G D) trigger navigation
</verification>

<success_criteria>
- App Shell (Sidebar, Header, Layout) renders in Next.js matching the HTML prototype layout (FOUND-05)
- No visual difference in sidebar width, header height, nav item styling between Next.js and prototype
- Theme toggle switches between light/dark mode using data-mode attribute
- Nav labels use t() function with Turkish default (FOUND-03 integration)
- Sidebar shows keyboard shortcut hints (G D, G P, G T) via Kbd component
- Shadcn/ui overlay components available for future phases
- Auth context and API client available for future phases
- Human verification confirms visual parity
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundation-design-system/08-04-SUMMARY.md`
</output>
