---
phase: 08-foundation-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Frontend2/package.json
  - Frontend2/tsconfig.json
  - Frontend2/next.config.mjs
  - Frontend2/postcss.config.mjs
  - Frontend2/components.json
  - Frontend2/app/globals.css
  - Frontend2/app/layout.tsx
  - Frontend2/app/page.tsx
  - Frontend2/lib/utils.ts
  - Frontend2/lib/theme.ts
  - Frontend2/lib/i18n.ts
  - Frontend2/context/app-context.tsx
autonomous: true
requirements: [FOUND-01, FOUND-03, FOUND-04]

must_haves:
  truths:
    - "Frontend2/ is a working Next.js project that builds without errors"
    - "globals.css contains all prototype oklch tokens mapped to shadcn naming conventions"
    - "status-todo and status-blocked tokens are defined in globals.css (FOUND-04)"
    - "dark mode uses data-mode='dark' attribute, not .dark CSS class"
    - "t() function resolves dot-notation keys and returns Turkish by default, English when requested"
    - "AppContext provides language, mode, preset, sidebarCollapsed, density state"
    - "Theme presets can be switched and tokens applied to document.documentElement"
    - "deriveFromBrand() generates a full token set from arbitrary oklch values"
  artifacts:
    - path: "Frontend2/app/globals.css"
      provides: "All CSS token definitions — mapped shadcn tokens, prototype extras, status, priority, shadow, inset, avatar colors"
      contains: "--status-blocked"
    - path: "Frontend2/lib/theme.ts"
      provides: "PRESETS object with 6 themes, deriveFromBrand(), applyTokens(), applyMode()"
      exports: ["PRESETS", "deriveFromBrand", "applyTokens", "applyMode"]
    - path: "Frontend2/lib/i18n.ts"
      provides: "STRINGS object with TR/EN translations, t() function"
      exports: ["STRINGS", "t"]
    - path: "Frontend2/context/app-context.tsx"
      provides: "AppProvider component and useApp() hook"
      exports: ["AppProvider", "useApp"]
  key_links:
    - from: "Frontend2/context/app-context.tsx"
      to: "Frontend2/lib/theme.ts"
      via: "import { PRESETS, applyTokens, applyMode, deriveFromBrand }"
      pattern: "import.*PRESETS.*from.*theme"
    - from: "Frontend2/context/app-context.tsx"
      to: "Frontend2/lib/i18n.ts"
      via: "t() used via useApp().language"
      pattern: "import.*from.*i18n"
    - from: "Frontend2/app/layout.tsx"
      to: "Frontend2/context/app-context.tsx"
      via: "AppProvider wraps children"
      pattern: "AppProvider"
---

<objective>
Scaffold the Frontend2/ Next.js project from scratch, establish the complete theme token system (6 presets + brand derivation), i18n foundation, and AppContext provider.

Purpose: This is the base everything else depends on — subsequent plans need a buildable project, working tokens, and the context provider.
Output: A new Frontend2/ Next.js project with globals.css tokens, theme.ts, i18n.ts, and app-context.tsx.
</objective>

<execution_context>
@.planning/phases/08-foundation-design-system/08-PATTERNS.md
@.planning/phases/08-foundation-design-system/08-RESEARCH.md
@.planning/phases/08-foundation-design-system/08-UI-SPEC.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
<!-- Source files the executor must port from -->

From Frontend/lib/utils.ts:
```typescript
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

From Frontend/tsconfig.json:
```json
{
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "target": "ES6",
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [{"name": "next"}],
    "paths": {"@/*": ["./*"]}
  }
}
```

From Frontend/components.json:
```json
{
  "style": "new-york",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "app/globals.css",
    "baseColor": "neutral",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Frontend2/ Next.js project and configure build tooling</name>
  <files>
    Frontend2/package.json,
    Frontend2/tsconfig.json,
    Frontend2/next.config.mjs,
    Frontend2/postcss.config.mjs,
    Frontend2/components.json,
    Frontend2/app/page.tsx,
    Frontend2/app/layout.tsx,
    Frontend2/lib/utils.ts
  </files>
  <read_first>
    Frontend/package.json,
    Frontend/tsconfig.json,
    Frontend/next.config.mjs,
    Frontend/postcss.config.mjs,
    Frontend/components.json,
    Frontend/lib/utils.ts,
    Frontend/app/layout.tsx
  </read_first>
  <action>
    Create the Frontend2/ directory and initialize a Next.js project. Do NOT use `npx create-next-app` interactively — instead, create the files manually to match the existing Frontend/ configuration exactly.

    **Frontend2/package.json:** Create with the same dependencies as Frontend/package.json. Copy ALL dependencies and devDependencies verbatim. Set name to "spms-frontend-v2", version "0.1.0", private true. Same scripts: build, dev, lint, start (but pointing to next commands).

    **Frontend2/tsconfig.json:** Copy verbatim from Frontend/tsconfig.json. Must include `"paths": {"@/*": ["./*"]}` for the path alias.

    **Frontend2/next.config.mjs:** Copy from Frontend/next.config.mjs.

    **Frontend2/postcss.config.mjs:** Copy from Frontend/postcss.config.mjs.

    **Frontend2/components.json:** Copy from Frontend/components.json — same shadcn new-york style config.

    **Frontend2/lib/utils.ts:** Copy verbatim from Frontend/lib/utils.ts — the `cn()` function using clsx + tailwind-merge.

    **Frontend2/app/page.tsx:** Create a minimal placeholder page:
    ```tsx
    export default function Home() {
      return <div>SPMS v2.0</div>
    }
    ```

    **Frontend2/app/layout.tsx:** Create the root layout following Frontend/app/layout.tsx structure, but with these changes:
    - Set `<html lang="tr">` (per D-06: default language is Turkish)
    - Add `suppressHydrationWarning` on `<html>` element (for data-mode attribute changes)
    - Import and wrap children with `QueryProvider`, `AuthProvider`, `SystemConfigProvider` (these will be copied in a later step but reference the paths now — commented out until files exist)
    - For now, just import globals.css and set up the font variables (Inter + Geist Mono)
    - DO NOT import AppProvider yet — it will be created in Task 2

    After creating all files, run `cd Frontend2 && npm install` to install dependencies.

    Then run `cd Frontend2 && npx next build` to verify the project builds cleanly.
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/Desktop/bitirme projesi/SPMS/Frontend2" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File Frontend2/package.json exists and contains `"next": "^16.1.1"` and `"react": "19.2.0"` and `"tailwindcss"` in devDependencies
    - File Frontend2/tsconfig.json contains `"@/*": ["./*"]`
    - File Frontend2/lib/utils.ts contains `export function cn(`
    - File Frontend2/app/layout.tsx contains `lang="tr"` and `suppressHydrationWarning`
    - `cd Frontend2 && npx next build` completes without errors
    - Frontend2/node_modules/ exists (npm install succeeded)
  </acceptance_criteria>
  <done>Frontend2/ is a buildable Next.js project with identical dependency set to Frontend/, Turkish default lang, and all config files in place.</done>
</task>

<task type="auto">
  <name>Task 2: Create theme token system, i18n module, and AppContext provider</name>
  <files>
    Frontend2/app/globals.css,
    Frontend2/lib/theme.ts,
    Frontend2/lib/i18n.ts,
    Frontend2/context/app-context.tsx,
    Frontend2/app/layout.tsx
  </files>
  <read_first>
    Frontend/app/globals.css,
    New_Frontend/src/theme.jsx,
    New_Frontend/src/i18n.jsx,
    New_Frontend/src/shell.jsx,
    New_Frontend/SPMS Prototype.html,
    Frontend/context/auth-context.tsx
  </read_first>
  <action>
    **Frontend2/app/globals.css:** Replace the scaffold globals.css with the full token system. Per D-02, map prototype token names to shadcn naming conventions. The complete structure is defined in RESEARCH.md Code Examples section "globals.css Token Structure (after migration)". Key requirements:

    1. First lines: `@import "tailwindcss";` and `@import "tw-animate-css";`
    2. Dark mode variant: `@custom-variant dark (&:is([data-mode="dark"], [data-mode="dark"] *));` — NOT `.dark` class
    3. `:root` block with ALL tokens:
       - Mapped tokens: `--background: oklch(0.985 0.006 75)`, `--foreground: oklch(0.20 0.025 50)`, `--card: oklch(1 0 0)`, `--primary: oklch(0.60 0.17 40)`, etc.
       - Prototype extras: `--bg-2`, `--surface-2`, `--fg-subtle`, `--border-strong`, `--primary-hover`
       - Status tokens (FOUND-04): `--status-todo: oklch(0.62 0.012 60)`, `--status-progress: oklch(0.55 0.15 230)`, `--status-review: oklch(0.65 0.15 65)`, `--status-done: oklch(0.58 0.14 150)`, `--status-blocked: oklch(0.58 0.20 25)`
       - Priority tokens: `--priority-critical`, `--priority-high`, `--priority-med`, `--priority-low`
       - Shadow tokens: `--shadow-sm` through `--shadow-xl` (5 levels)
       - Inset depth tokens (light mode): `--inset-top`, `--inset-bottom`, `--inset-card`, `--inset-primary-top`, `--inset-primary-bottom`
       - Avatar colors: `--av-1` through `--av-8`
       - Radius tokens: `--radius: 8px`, `--radius-sm: 6px`, `--radius-lg: 12px`
       - Keep shadcn chart tokens and sidebar tokens
    4. `[data-mode="dark"]` block (NOT `.dark`):
       - Dark mode inset tokens (different values from light)
       - Dark mode shadow tokens (stronger opacity values)
    5. `[data-density]` attribute selectors: compact (28px), cozy (34px), comfortable (40px)
    6. `@theme inline` block: ALL existing Tailwind mappings from Frontend/app/globals.css PLUS new entries for prototype tokens:
       - `--color-bg-2: var(--bg-2)`, `--color-surface-2: var(--surface-2)`, `--color-fg-subtle: var(--fg-subtle)`, etc.
       - `--color-border-strong: var(--border-strong)`, `--color-primary-hover: var(--primary-hover)`
       - Status color mappings: `--color-status-todo: var(--status-todo)`, etc.
       - Priority color mappings: `--color-priority-critical: var(--priority-critical)`, etc.
    7. `@layer base` block identical to existing

    Use the exact oklch values from RESEARCH.md Code Examples section. Every value is specified there.

    **Frontend2/lib/theme.ts:** Port from `New_Frontend/src/theme.jsx`. Create TypeScript module with:
    - `ThemeTokens` interface (all token keys)
    - `ThemePreset` interface: `{ id: string; name: { tr: string; en: string }; mode: "light" | "dark"; tokens: ThemeTokens }`
    - `PRESETS` object: 6 presets (default/ocean/forest/monochrome/midnight/graphite) — port all token values from theme.jsx
    - `TOKEN_MAP` object: maps prototype token names to shadcn CSS variable names (bg->background, surface->card, fg->foreground, fg-muted->muted-foreground, primary-fg->primary-foreground, accent-fg->accent-foreground)
    - `applyTokens(tokens: ThemeTokens): void` — sets CSS variables on `document.documentElement` using both mapped name and original name. Also derives shadcn-specific tokens: `--popover` = tokens.surface, `--popover-foreground` = tokens.fg, `--card-foreground` = tokens.fg, `--input` = tokens.border
    - `applyMode(mode: "light" | "dark"): void` — sets `document.documentElement.dataset.mode = mode`
    - `deriveFromBrand(params: { L: number; C: number; H: number; mode?: "light" | "dark" }): ThemeTokens` — port from theme.jsx line 140-156
    - `estimateContrast(fg: string, bg: string): number` — port from theme.jsx
    - `oklchLightness(color: string): number` — port from theme.jsx
    - All functions and constants as named exports

    **Frontend2/lib/i18n.ts:** Port from `New_Frontend/src/i18n.jsx`. Create TypeScript module with:
    - `type LangCode = "tr" | "en"`
    - `StringEntry` interface: `{ tr: string; en?: string }`
    - `StringNamespace` interface for nested structure
    - `STRINGS` object: port ALL ~85 keys from prototype's i18n.jsx verbatim, organized by section (nav, common, priority, dashboard, project, workflow, etc.)
    - `t(path: string, lang: LangCode = "tr"): string` — resolves dot-notation keys, falls back to Turkish (per D-06), returns key path if not found

    **Frontend2/context/app-context.tsx:** Create following the auth-context.tsx provider pattern (PATTERNS.md Pattern B). Must include:
    - `"use client"` directive
    - `import * as React from "react"` (namespace access convention)
    - `AppContextType` interface with: language (LangCode), setLanguage, mode (ThemeMode), setMode, preset (ThemePreset | string), applyPreset, sidebarCollapsed, setSidebarCollapsed, density (Density), setDensity, brandLight, brandChroma, brandHue, customColors, applyCustomBrand
    - `AppProvider` component that:
      - Initializes state from localStorage on mount (with fallbacks: lang=tr, mode=light, preset=default, sidebar=false, density=cozy)
      - Calls `applyTokens()` and `applyMode()` on preset/mode changes
      - Persists state to localStorage on changes
      - Wraps children in `AppContext.Provider`
    - `useApp()` hook with error boundary check
    - Use `React.useState`, `React.useEffect`, `React.useContext`, `React.createContext` (namespace access)

    **Frontend2/app/layout.tsx:** UPDATE the layout created in Task 1 to:
    - Import and wrap with `AppProvider` from `@/context/app-context`
    - Provider nesting order: `<QueryProvider> <AuthProvider> <SystemConfigProvider> <AppProvider> {children} </AppProvider> </SystemConfigProvider> </AuthProvider> </QueryProvider>`
    - NOTE: QueryProvider, AuthProvider, SystemConfigProvider are NOT yet copied to Frontend2/. For now, comment out those providers and just use AppProvider. They will be wired when later phases copy them.
    - Updated layout wraps children with `<AppProvider>{children}</AppProvider>` only

    After all files created, run `cd Frontend2 && npx next build` to verify everything compiles.
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/Desktop/bitirme projesi/SPMS/Frontend2" && npx next build 2>&1 | tail -5 && grep -c "status-blocked" Frontend2/app/globals.css && grep -c "status-todo" Frontend2/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - Frontend2/app/globals.css contains `@custom-variant dark (&:is([data-mode="dark"], [data-mode="dark"] *))`
    - Frontend2/app/globals.css contains `--status-todo: oklch(0.62 0.012 60)` and `--status-blocked: oklch(0.58 0.20 25)` (FOUND-04)
    - Frontend2/app/globals.css contains `--bg-2:` and `--surface-2:` and `--fg-subtle:` (prototype extras)
    - Frontend2/app/globals.css contains `--inset-top:` and `--shadow-sm:` (depth tokens)
    - Frontend2/app/globals.css contains `--av-1:` through `--av-8:` (avatar colors)
    - Frontend2/app/globals.css contains `[data-mode="dark"]` (NOT `.dark`)
    - Frontend2/app/globals.css contains `[data-density="compact"]` and `[data-density="cozy"]` and `[data-density="comfortable"]`
    - Frontend2/lib/theme.ts exports `PRESETS`, `deriveFromBrand`, `applyTokens`, `applyMode`
    - Frontend2/lib/theme.ts contains `TOKEN_MAP` object
    - Frontend2/lib/i18n.ts exports `STRINGS` and `t`
    - Frontend2/lib/i18n.ts `t` function signature includes `lang: LangCode = "tr"`
    - Frontend2/context/app-context.tsx exports `AppProvider` and `useApp`
    - Frontend2/context/app-context.tsx contains `"use client"` and `import * as React from "react"`
    - Frontend2/context/app-context.tsx contains `localStorage` read/write logic
    - Frontend2/app/layout.tsx imports `AppProvider`
    - `cd Frontend2 && npx next build` completes without errors
  </acceptance_criteria>
  <done>Theme token system with 6 presets (D-08) and brand derivation is fully operational, i18n t() function works with TR/EN (D-05, D-06), AppContext provider manages all UI state, and globals.css has complete token definitions including status-todo and status-blocked (FOUND-04). The project builds cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage -> AppContext | Theme/language preferences read from localStorage — untrusted user-controlled data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T (Tampering) | localStorage theme data | accept | Theme preferences are non-sensitive UI customization. Corrupted data causes visual glitch only. AppContext initializes with safe defaults if localStorage parse fails. |
| T-08-02 | I (Info Disclosure) | CSS custom properties | accept | Token values are visual styling only — no secrets. Visible in browser DevTools by design. |
</threat_model>

<verification>
1. `cd Frontend2 && npx next build` passes (type-check + build)
2. `grep "status-blocked" Frontend2/app/globals.css` returns match (FOUND-04)
3. `grep "status-todo" Frontend2/app/globals.css` returns match (FOUND-04)
4. `grep "data-mode" Frontend2/app/globals.css` returns match for dark mode
5. `grep "export function t" Frontend2/lib/i18n.ts` returns match (FOUND-03)
6. `grep "PRESETS" Frontend2/lib/theme.ts` returns match (FOUND-01)
7. `grep "deriveFromBrand" Frontend2/lib/theme.ts` returns match (D-08)
</verification>

<success_criteria>
- Frontend2/ is a buildable Next.js project with all dependencies installed
- globals.css contains complete prototype oklch token system mapped to shadcn naming (FOUND-01)
- status-todo and status-blocked tokens are present (FOUND-04)
- Theme system supports 6 presets and custom brand derivation (D-08)
- i18n t() function resolves TR/EN keys with Turkish default (FOUND-03, D-05, D-06)
- AppContext manages language, theme, sidebar, density state with localStorage persistence
- Dark mode uses data-mode="dark" attribute
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundation-design-system/08-01-SUMMARY.md`
</output>
