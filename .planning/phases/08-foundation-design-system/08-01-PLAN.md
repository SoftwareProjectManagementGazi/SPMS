---
phase: 08-foundation-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Frontend2/package.json
  - Frontend2/tsconfig.json
  - Frontend2/app/globals.css
  - Frontend2/app/layout.tsx
  - Frontend2/app/page.tsx
  - Frontend2/lib/utils.ts
  - Frontend2/lib/theme.ts
  - Frontend2/lib/i18n.ts
  - Frontend2/context/app-context.tsx
autonomous: true
requirements: [FOUND-01, FOUND-03, FOUND-04]
must_haves:
  truths:
    - "Theme tokens from prototype (oklch variables) are active in Frontend2/app/globals.css using prototype names directly (--bg, --surface, --fg, NOT --background, --card, --foreground)"
    - "6 theme presets (default, ocean, forest, monochrome, midnight, graphite) switch correctly via applyPreset()"
    - "deriveFromBrand() computes a full token set from any oklch hue"
    - "Dark mode toggles via data-mode attribute on html element"
    - "t() function returns Turkish or English strings based on language parameter, defaults to Turkish"
    - "status-todo and status-blocked color tokens are defined in globals.css (FOUND-04)"
    - "AppContext provider exposes language, mode, preset, sidebarCollapsed, density state with localStorage persistence"
  artifacts:
    - path: "Frontend2/app/globals.css"
      provides: "All CSS token definitions from prototype"
      contains: "--status-blocked"
    - path: "Frontend2/lib/theme.ts"
      provides: "PRESETS, deriveFromBrand, applyTokens, applyMode, applyRadius"
      exports: ["PRESETS", "deriveFromBrand", "applyTokens", "applyMode", "applyRadius", "resolvePreset"]
    - path: "Frontend2/lib/i18n.ts"
      provides: "STRINGS object and t() function"
      exports: ["STRINGS", "t"]
    - path: "Frontend2/context/app-context.tsx"
      provides: "AppContext provider and useApp hook"
      exports: ["AppProvider", "useApp"]
  key_links:
    - from: "Frontend2/context/app-context.tsx"
      to: "Frontend2/lib/theme.ts"
      via: "imports PRESETS, deriveFromBrand, applyTokens, applyMode"
      pattern: "import.*from.*@/lib/theme"
    - from: "Frontend2/context/app-context.tsx"
      to: "Frontend2/lib/i18n.ts"
      via: "exposes language state consumed by t() callers"
      pattern: "language.*setLanguage"
    - from: "Frontend2/app/layout.tsx"
      to: "Frontend2/context/app-context.tsx"
      via: "wraps children with AppProvider"
      pattern: "AppProvider"
---

<objective>
Scaffold the Frontend2/ Next.js project from scratch and establish the complete theme token system, i18n foundation, and AppContext provider -- the infrastructure that all subsequent plans depend on.

Purpose: This plan creates the base project and core infrastructure (tokens, themes, i18n, context) so that primitive components (Plans 02/03) and App Shell (Plan 04) can be built on top of it.

Output: A buildable Next.js project in Frontend2/ with working theme system (6 presets + custom brand derivation), i18n (TR/EN), and React context provider.
</objective>

<execution_context>
@.planning/phases/08-foundation-design-system/08-CONTEXT.md
@.planning/phases/08-foundation-design-system/08-RESEARCH.md
@.planning/phases/08-foundation-design-system/08-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

CRITICAL CONTEXT DECISIONS (from CONTEXT.md -- these override RESEARCH.md where they conflict):
- D-01: ALL components come from prototype. shadcn/ui is NOT used at all -- not even for overlays.
- D-02: Token names from prototype used DIRECTLY as-is (--bg, --surface, --fg, etc.). NO mapping to shadcn naming conventions. The prototype's token system IS the design system.
- D-04: Inline styles converted to Tailwind CSS utility classes. CSS custom properties via var() where needed.
- D-05: Simple i18n with useApp().language + t() function. Single strings.ts file. No external i18n library.
- D-06: Default language Turkish. t() falls back to Turkish.
- D-08: Full theme system -- 6 presets, light/dark, deriveFromBrand().
- D-09: Next.js App Router. RouterContext removed. File-based routing.

NOTE: RESEARCH.md references shadcn token mapping (--bg -> --background, etc.) but CONTEXT.md D-02 explicitly overrides this: "Token names from the prototype will be used directly as-is... No mapping to shadcn naming conventions." Follow CONTEXT.md D-02.

ABSOLUTE RULES:
- ALL output goes to Frontend2/ (new folder, does NOT exist yet)
- Frontend/ is COMPLETELY IGNORED -- nothing read, copied, or referenced
- shadcn/ui is NOT used -- no shadcn init, no shadcn add, no components.json
- No @shadcn packages in dependencies
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Frontend2/ project and establish globals.css with prototype tokens</name>
  <files>
    Frontend2/package.json
    Frontend2/tsconfig.json
    Frontend2/app/globals.css
    Frontend2/app/page.tsx
    Frontend2/lib/utils.ts
  </files>
  <read_first>
    New_Frontend/SPMS Prototype.html (lines 10-112 for CSS token definitions, base styles, density selectors)
    New_Frontend/src/theme.jsx (lines 4-137 for all 6 PRESETS token values -- use default preset values for :root)
  </read_first>
  <action>
1. Scaffold the Next.js project in Frontend2/:
   ```
   cd "C:/Users/yusti/Desktop/bitirme projesi/SPMS"
   npx create-next-app@latest Frontend2 --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*" --use-npm
   ```
   If prompted, answer yes/default for all prompts.

2. After scaffold completes, install additional runtime dependencies:
   ```
   cd "C:/Users/yusti/Desktop/bitirme projesi/SPMS/Frontend2"
   npm install clsx tailwind-merge @tanstack/react-query axios lucide-react
   ```
   Do NOT install any @shadcn packages. Do NOT run `npx shadcn@latest init`. Do NOT create `components.json`.

3. Create `Frontend2/lib/utils.ts`:
   ```typescript
   import { type ClassValue, clsx } from "clsx"
   import { twMerge } from "tailwind-merge"

   export function cn(...inputs: ClassValue[]) {
     return twMerge(clsx(inputs))
   }
   ```

4. Verify `Frontend2/tsconfig.json` has paths configured:
   ```json
   "paths": { "@/*": ["./*"] }
   ```
   If missing, add under `compilerOptions`.

5. Replace `Frontend2/app/globals.css` entirely. The complete file must contain (in this exact order):

   a. Tailwind import and dark mode variant:
   ```css
   @import "tailwindcss";

   @custom-variant dark (&:is([data-mode="dark"], [data-mode="dark"] *));
   ```

   b. `:root` block with ALL prototype tokens using prototype names DIRECTLY (per D-02):
   - Font vars: --font-sans: "Geist", "Geist Fallback", ui-sans-serif, system-ui, sans-serif; --font-mono: "Geist Mono", ui-monospace, SFMono-Regular, Menlo, monospace
   - Radius: --radius: 8px; --radius-sm: 6px; --radius-lg: 12px
   - Core surface tokens: --bg: oklch(0.985 0.006 75); --bg-2: oklch(0.97 0.008 70); --surface: oklch(1 0 0); --surface-2: oklch(0.975 0.006 70)
   - Foreground tokens: --fg: oklch(0.20 0.025 50); --fg-muted: oklch(0.48 0.018 55); --fg-subtle: oklch(0.62 0.012 60)
   - Border tokens: --border: oklch(0.90 0.01 65); --border-strong: oklch(0.82 0.015 60)
   - Primary tokens: --primary: oklch(0.60 0.17 40); --primary-fg: oklch(0.985 0.005 75); --primary-hover: oklch(0.55 0.18 40)
   - Accent tokens: --accent: oklch(0.93 0.03 50); --accent-fg: oklch(0.25 0.04 50); --ring: oklch(0.60 0.17 40 / 0.4)
   - Status tokens (FOUND-04): --status-todo: oklch(0.62 0.012 60); --status-progress: oklch(0.55 0.15 230); --status-review: oklch(0.65 0.15 65); --status-done: oklch(0.58 0.14 150); --status-blocked: oklch(0.58 0.20 25)
   - Priority tokens: --priority-critical: oklch(0.58 0.22 25); --priority-high: oklch(0.65 0.18 45); --priority-med: oklch(0.67 0.13 80); --priority-low: oklch(0.60 0.05 240)
   - Shadow tokens (5 levels): --shadow-sm: 0 1px 2px oklch(0 0 0 / 0.06); --shadow: 0 2px 8px oklch(0 0 0 / 0.08), 0 1px 3px oklch(0 0 0 / 0.05); --shadow-md: 0 4px 12px oklch(0 0 0 / 0.1), 0 2px 4px oklch(0 0 0 / 0.06); --shadow-lg: 0 12px 28px oklch(0 0 0 / 0.12), 0 4px 8px oklch(0 0 0 / 0.08); --shadow-xl: 0 20px 40px oklch(0 0 0 / 0.14), 0 8px 16px oklch(0 0 0 / 0.1)
   - Inset depth tokens (light mode): --inset-top: inset 0 1px 0 oklch(1 0 0 / 0.65); --inset-bottom: inset 0 -1px 0 oklch(0.2 0 0 / 0.10); --inset-card: inset 0 1px 0 oklch(1 0 0 / 0.80), inset 0 -1px 0 oklch(0.2 0 0 / 0.06); --inset-primary-top: inset 0 1px 0 oklch(1 0 0 / 0.20); --inset-primary-bottom: inset 0 -1px 0 oklch(0.1 0.05 40 / 0.30)
   - Avatar colors: --av-1: oklch(0.65 0.18 25); --av-2: oklch(0.60 0.15 250); --av-3: oklch(0.62 0.16 150); --av-4: oklch(0.65 0.14 320); --av-5: oklch(0.60 0.17 45); --av-6: oklch(0.63 0.12 200); --av-7: oklch(0.60 0.15 290); --av-8: oklch(0.65 0.10 100)

   c. `[data-mode="dark"]` block with dark overrides for inset and shadow tokens:
   - --inset-top: inset 0 1px 0 oklch(0.30 0 0 / 0.50)
   - --inset-bottom: inset 0 -1px 0 oklch(0.10 0 0 / 0.70)
   - --inset-card: inset 0 1px 0 oklch(0.28 0 0 / 0.55), inset 0 -1px 0 oklch(0.10 0 0 / 0.60)
   - --inset-primary-top: inset 0 1px 0 oklch(0.28 0.06 40 / 0.50)
   - --inset-primary-bottom: inset 0 -1px 0 oklch(0.10 0.02 40 / 0.80)
   - --shadow-sm through --shadow-xl dark mode values (from prototype HTML lines 113-117)

   d. Density attribute selectors:
   ```css
   [data-density="compact"] { --density-row: 28px; }
   [data-density="cozy"] { --density-row: 34px; }
   [data-density="comfortable"] { --density-row: 40px; }
   ```

   e. Base reset styles from prototype:
   ```css
   * { box-sizing: border-box; }
   body {
     font-family: var(--font-sans);
     font-size: 14px;
     line-height: 1.45;
     color: var(--fg);
     background: var(--bg);
     -webkit-font-smoothing: antialiased;
     -moz-osx-font-smoothing: grayscale;
     font-feature-settings: "cv11", "ss01", "ss03";
   }
   button { font-family: inherit; font-size: inherit; color: inherit; background: transparent; border: 0; cursor: pointer; padding: 0; }
   input, textarea, select { font-family: inherit; color: inherit; outline: none; }
   input:focus-visible, textarea:focus-visible, button:focus-visible { outline: 2px solid var(--ring); outline-offset: 1px; }
   .mono { font-family: var(--font-mono); font-feature-settings: "ss01"; font-variant-numeric: tabular-nums; }
   ```

6. Replace `Frontend2/app/page.tsx` with minimal placeholder:
   ```tsx
   export default function Home() {
     return <main className="p-6"><h1>SPMS Frontend2</h1></main>
   }
   ```
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/Desktop/bitirme projesi/SPMS/Frontend2" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Frontend2/package.json` exists and contains "next", "react", "clsx", "tailwind-merge", "@tanstack/react-query", "axios", "lucide-react" in dependencies
    - `Frontend2/package.json` does NOT contain "@shadcn" or "shadcn" anywhere
    - No `Frontend2/components.json` file exists (shadcn NOT initialized)
    - `Frontend2/lib/utils.ts` exists and contains `export function cn`
    - `Frontend2/app/globals.css` contains `--bg:` (prototype name, NOT --background)
    - `Frontend2/app/globals.css` contains `--surface:` (prototype name, NOT --card)
    - `Frontend2/app/globals.css` contains `--fg:` (prototype name, NOT --foreground)
    - `Frontend2/app/globals.css` contains `--status-todo:` and `--status-blocked:` (FOUND-04)
    - `Frontend2/app/globals.css` contains `--priority-critical:` and `--priority-med:`
    - `Frontend2/app/globals.css` contains `@custom-variant dark (&:is([data-mode="dark"]`
    - `Frontend2/app/globals.css` contains `--inset-top:` in both `:root` and `[data-mode="dark"]` blocks
    - `Frontend2/app/globals.css` contains `--av-1:` through `--av-8:`
    - `Frontend2/app/globals.css` contains `[data-density="compact"]` and `[data-density="cozy"]` and `[data-density="comfortable"]`
    - `Frontend2/app/globals.css` contains `--shadow-sm:` through `--shadow-xl:`
    - `Frontend2/tsconfig.json` contains `"@/*"` path alias
    - `npx next build` in Frontend2/ succeeds without errors
  </acceptance_criteria>
  <done>Frontend2/ is a buildable Next.js project with all prototype CSS tokens in globals.css, cn() utility, and path alias configured. No shadcn/ui installed. All token names follow prototype convention (--bg, --surface, --fg), not shadcn convention.</done>
</task>

<task type="auto">
  <name>Task 2: Create theme system (lib/theme.ts), i18n module (lib/i18n.ts), and AppContext provider</name>
  <files>
    Frontend2/lib/theme.ts
    Frontend2/lib/i18n.ts
    Frontend2/context/app-context.tsx
    Frontend2/app/layout.tsx
  </files>
  <read_first>
    New_Frontend/src/theme.jsx (full file -- all 6 PRESETS with exact oklch values, deriveFromBrand lines 140-156, applyTokens lines 158-162, applyMode line 164, applyRadius lines 169-173, resolvePreset line 175, contrast utilities lines 178-189)
    New_Frontend/src/i18n.jsx (full file -- STRINGS object with all sections: nav 8 keys, common 25 keys, priority 4 keys, dashboard 10 keys, project 9 keys, workflow 12 keys, t() function)
    New_Frontend/src/app.jsx (lines 39-131 for useAppState with localStorage persistence, theme application effect, mode auto-switch; lines 134-176 for App provider wrapping)
  </read_first>
  <action>
1. Create `Frontend2/lib/theme.ts` -- port from `New_Frontend/src/theme.jsx`:

   Define and export the following TypeScript types and functions:

   a. `ThemeTokens` interface -- 15 string fields:
      bg, "bg-2", surface, "surface-2", fg, "fg-muted", "fg-subtle", border, "border-strong", primary, "primary-fg", "primary-hover", accent, "accent-fg", ring

   b. `ThemePreset` interface:
      { id: string; name: { tr: string; en: string }; mode: "light" | "dark"; tokens: ThemeTokens }

   c. `PRESETS` -- Record<string, ThemePreset> containing all 6 presets with EXACT values from theme.jsx:
      - default: { id: "default", name: { tr: "Varsayilan (Terracotta)", en: "Default (Terracotta)" }, mode: "light", tokens: { bg: "oklch(0.985 0.006 75)", ... all 15 tokens } }
      - ocean: { id: "ocean", name: { tr: "Okyanus", en: "Ocean" }, mode: "light", tokens: { ... } }
      - forest: { id: "forest", name: { tr: "Orman", en: "Forest" }, mode: "light", tokens: { ... } }
      - monochrome: { id: "monochrome", name: { tr: "Monokrom", en: "Monochrome" }, mode: "light", tokens: { ... } }
      - midnight: { id: "midnight", name: { tr: "Geceyarisi", en: "Midnight" }, mode: "dark", tokens: { ... } }
      - graphite: { id: "graphite", name: { tr: "Grafit", en: "Graphite" }, mode: "dark", tokens: { ... } }
      All token values MUST be copied exactly from theme.jsx -- see 08-PATTERNS.md "Frontend2/lib/theme.ts" section for every value.

   d. `deriveFromBrand({ L, C, H, mode })` -- exact logic from theme.jsx lines 140-156:
      - Start from default (light) or graphite (dark) base tokens
      - Set primary = oklch(L C H)
      - primary-fg lightness: L < 0.55 ? 0.985 : 0.15
      - primary-hover: L-0.05 lightness, C+0.01 chroma
      - ring: oklch(L C H / 0.4)
      - Light mode: accent = L+0.32 lightness, C*0.15 chroma; accent-fg = L-0.35 lightness, C*0.3 chroma
      - Dark mode: accent = L-0.4, C*0.3; accent-fg = L+0.22, C*0.1

   e. `applyTokens(tokens: ThemeTokens)` -- sets CSS vars on document.documentElement:
      CRITICAL per D-02: Use `root.style.setProperty('--bg', tokens.bg)`, `root.style.setProperty('--surface', tokens.surface)`, etc.
      Do NOT map to --background, --card, --foreground. Prototype names only.

   f. `applyMode(mode: "light" | "dark")` -- sets document.documentElement.dataset.mode = mode

   g. `applyRadius(r: number)` -- sets --radius (rpx), --radius-sm (max(2,r-4)px), --radius-lg (r+4)px

   h. `resolvePreset(presetId: string, customPresets?: ThemePreset[])` -- finds in PRESETS or customPresets, falls back to PRESETS.default

   i. `oklchLightness(str: string)` and `estimateContrast(fg: string, bg: string)` utility functions (from theme.jsx lines 178-189)

2. Create `Frontend2/lib/i18n.ts` -- port ALL keys from `New_Frontend/src/i18n.jsx`:

   a. Type definitions:
      ```typescript
      export type LangCode = "tr" | "en"
      interface StringEntry { tr: string; en?: string }
      ```

   b. `STRINGS` object -- port ALL sections with exact TR/EN values from i18n.jsx:
      - nav: 8 keys (dashboard, projects, myTasks, teams, reports, settings, admin, notifications)
      - common: 25 keys (create, createTask, createProject, save, cancel, edit, delete, back, search, filter, all, today, overdue, dueDate, assignee, priority, status, description, addComment, viewAll, openProject, loading, empty, draft, saved, unsaved)
      - priority: 4 keys (critical, high, medium, low)
      - dashboard: 10 keys (welcome, management, myWork, totalProjects, activeTasks, completedTasks, overdue, portfolio, currentActivity, upcoming, groupedTasks)
      - project: 9 keys (board, list, timeline, calendar, lifecycle, members, projectSettings, methodology, lead)
      - workflow: 12 keys (title, lifecycle, status, addNode, template, sequential, noBackward, bidirectional, initial, final, rules, locked)
      NOTE: Use exact Turkish characters from i18n.jsx (Gorevlerim -> Gorevlerim with proper chars if present in source, etc.)

   c. `t(path: string, lang: LangCode = "tr"): string` function:
      - Split path by "."
      - Walk STRINGS tree
      - Return cur[lang] || cur.tr || path (falls back to Turkish per D-06)
      - Return path string if key not found

3. Create `Frontend2/context/app-context.tsx` -- port from app.jsx useAppState:

   Must start with `"use client"` directive.

   a. `AppContextType` interface:
      language: LangCode, setLanguage: (lang: LangCode) => void,
      mode: "light" | "dark", setMode: (mode: "light" | "dark") => void,
      preset: string, applyPreset: (id: string) => void,
      sidebarCollapsed: boolean, setSidebarCollapsed: (v: boolean) => void,
      density: "compact" | "cozy" | "comfortable", setDensity: (d: ...) => void,
      brandLight: number, brandChroma: number, brandHue: number,
      customColors: boolean, applyCustomBrand: (params: { L: number; C: number; H: number }) => void,
      radius: number, setRadius: (r: number) => void

   b. `useApp()` hook -- throws "useApp must be used within AppProvider" if context is undefined

   c. `AppProvider` component:
      - localStorage persistence using "spms." prefix keys (spms.preset, spms.mode, spms.language, spms.density, spms.sidebarCollapsed, spms.radius, spms.brandLight, spms.brandChroma, spms.brandHue, spms.customColors)
      - load() helper with try/catch JSON.parse, default fallback
      - save() helper with try/catch JSON.stringify
      - useEffect for theme application: resolve preset, get tokens (customColors ? deriveFromBrand : preset.tokens), applyTokens(tokens), applyMode(mode or preset.mode)
      - useEffect for radius: applyRadius(radius)
      - useEffect for density: document.documentElement.dataset.density = density
      - Mode auto-switch logic: when mode set to dark + non-custom + light preset active (default/ocean/forest/monochrome) -> auto-switch to midnight; when light + dark preset (midnight/graphite) -> auto-switch to default
      - Wraps children in AppContext.Provider

4. Update `Frontend2/app/layout.tsx`:
   - Import AppProvider from "@/context/app-context"
   - Import "./globals.css"
   - Configure Inter font from next/font/google (assign to --font-sans CSS variable via className or style)
   - Configure Geist Mono from next/font/google for mono
   - Set `suppressHydrationWarning` on html element (needed for data-mode attribute set client-side)
   - Metadata: title "SPMS", description "Software Project Management System"
   - Body wraps children with AppProvider:
     ```tsx
     <html lang="tr" suppressHydrationWarning>
       <body className={`${inter.variable} ${geistMono.variable}`}>
         <AppProvider>{children}</AppProvider>
       </body>
     </html>
     ```
  </action>
  <verify>
    <automated>cd "C:/Users/yusti/Desktop/bitirme projesi/SPMS/Frontend2" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Frontend2/lib/theme.ts` contains `export const PRESETS` with keys "default", "ocean", "forest", "monochrome", "midnight", "graphite"
    - `Frontend2/lib/theme.ts` contains `export function deriveFromBrand`
    - `Frontend2/lib/theme.ts` contains `export function applyTokens` -- and the function body uses `--${key}` format (NOT --background, NOT --card, NOT --foreground)
    - `Frontend2/lib/theme.ts` contains `export function applyMode` with `dataset.mode`
    - `Frontend2/lib/theme.ts` contains `export function applyRadius`
    - `Frontend2/lib/theme.ts` contains `export function resolvePreset`
    - `Frontend2/lib/i18n.ts` contains `export const STRINGS` with sections: nav, common, priority, dashboard, project, workflow
    - `Frontend2/lib/i18n.ts` contains `export function t` with default parameter `lang: LangCode = "tr"`
    - `Frontend2/lib/i18n.ts` common section contains at least 25 keys including "draft", "saved", "unsaved"
    - `Frontend2/context/app-context.tsx` starts with `"use client"`
    - `Frontend2/context/app-context.tsx` contains `export function useApp`
    - `Frontend2/context/app-context.tsx` contains `export function AppProvider`
    - `Frontend2/context/app-context.tsx` contains `localStorage.getItem("spms.`
    - `Frontend2/context/app-context.tsx` contains mode auto-switch logic (midnight/default preset switching)
    - `Frontend2/app/layout.tsx` contains `AppProvider`
    - `Frontend2/app/layout.tsx` contains `suppressHydrationWarning`
    - `npx next build` in Frontend2/ succeeds without errors
  </acceptance_criteria>
  <done>Theme system with 6 presets and brand derivation is functional. i18n t() function resolves all TR/EN strings from prototype. AppContext provider manages theme, language, sidebar, density, and radius state with localStorage persistence and mode auto-switch. Root layout wraps app with provider.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage read/write | User preference data (theme, language, density) persisted client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | localStorage theme/lang data | accept | Low risk: only affects personal UI preferences, no PII or auth data. Corrupted values fall back to defaults via try/catch in load() helper. |
| T-08-02 | Information Disclosure | localStorage keys (spms.*) | accept | Low risk: theme preset and language preference are not sensitive. No secrets stored. |
</threat_model>

<verification>
1. `npx next build` in Frontend2/ succeeds (TypeScript + CSS validation)
2. `grep "--status-blocked" Frontend2/app/globals.css` returns a match (FOUND-04)
3. `grep "--status-todo" Frontend2/app/globals.css` returns a match (FOUND-04)
4. `grep "--bg:" Frontend2/app/globals.css` returns a match (D-02 prototype naming)
5. `grep "PRESETS" Frontend2/lib/theme.ts` shows export
6. `grep "export function t" Frontend2/lib/i18n.ts` shows function
7. `grep "AppProvider" Frontend2/app/layout.tsx` shows provider wrapping
8. `grep "components.json" Frontend2/` returns NO match (no shadcn)
</verification>

<success_criteria>
- Frontend2/ builds successfully with `npx next build`
- All 6 theme presets defined with exact token values from prototype theme.jsx
- Token names use prototype naming (--bg, --surface, --fg) per D-02 -- NOT shadcn naming
- status-todo and status-blocked tokens defined in globals.css (FOUND-04)
- i18n t() function resolves dot-notation keys to TR or EN strings with Turkish default (D-06)
- AppContext persists all state to localStorage and applies theme on mount
- No shadcn/ui installed anywhere in Frontend2/
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundation-design-system/08-01-SUMMARY.md`
</output>
