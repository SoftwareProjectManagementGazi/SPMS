---
phase: 05-notifications
plan: "06"
type: execute
wave: 6
depends_on:
  - "05-05"
files_modified:
  - Frontend/app/notifications/page.tsx
  - Frontend/components/notifications/notification-preferences-form.tsx
  - Frontend/app/settings/page.tsx
  - Frontend/components/task-detail/task-sidebar.tsx
autonomous: false
requirements:
  - NOTIF-01
  - NOTIF-03
  - NOTIF-04
  - NOTIF-05
  - NOTIF-06

must_haves:
  truths:
    - "User can navigate to /notifications and see full notification history, newest first"
    - "Load More button loads the next page of notifications"
    - "Filter tabs Tümü | Okunmamış filter the list client-side"
    - "Settings page has a Bildirimler section with per-type in-app/email toggles"
    - "Global email toggle disables all email notifications regardless of per-type settings"
    - "Deadline days selector (1, 2, 3, 7) is visible and saves correctly"
    - "Task sidebar has Görevi İzle / İzlemeyi Bırak toggle button"
  artifacts:
    - path: "Frontend/app/notifications/page.tsx"
      provides: "Full notification history page with pagination and filter tabs"
    - path: "Frontend/components/notifications/notification-preferences-form.tsx"
      provides: "Notification preferences form component for settings page"
    - path: "Frontend/app/settings/page.tsx"
      provides: "Settings page updated with Bildirimler section"
    - path: "Frontend/components/task-detail/task-sidebar.tsx"
      provides: "Task sidebar updated with watch toggle"
  key_links:
    - from: "Frontend/app/notifications/page.tsx"
      to: "Frontend/services/notification-service.ts"
      via: "notificationService.list() with offset for pagination"
      pattern: "notificationService.list"
    - from: "Frontend/components/notifications/notification-preferences-form.tsx"
      to: "Frontend/services/notification-service.ts"
      via: "notificationService.getPreferences() + updatePreferences()"
      pattern: "notificationService.getPreferences"
---

<objective>
Complete the notification UI: full /notifications history page, notification preferences form in Settings, and watch-task toggle in the task sidebar.

Purpose: Closes the remaining user-facing features: browsing history, controlling notification behavior, and opting into task watching.
Output: /notifications page, preferences form, settings page update, task sidebar watch toggle.
</objective>

<execution_context>
@C:/Users/yusti/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/yusti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-notifications/05-CONTEXT.md
@.planning/phases/05-notifications/05-RESEARCH.md
@.planning/phases/05-notifications/05-05-SUMMARY.md

<interfaces>
<!-- Notification service from Plan 05 -->
From Frontend/services/notification-service.ts:
```typescript
notificationService.list({ unread_only?: boolean, limit?: number, offset?: number })
  → Promise<{ notifications: NotificationItem[], unread_count: number, total: number }>
notificationService.getPreferences() → Promise<NotificationPreferences>
notificationService.updatePreferences(data) → Promise<NotificationPreferences>
notificationService.markRead(id) / markAllRead() / deleteNotification(id) / clearRead()
```

<!-- Watch task backend endpoints (from Plan 04 — task sidebar needs these) -->
Watch task endpoints (added in tasks.py in Plan 04):
  POST /api/v1/tasks/{id}/watch    → 200 { message: "Görev izlemeye alındı" }
  DELETE /api/v1/tasks/{id}/watch  → 200 { message: "Görev izlemesi kaldırıldı" }
  GET /api/v1/tasks/{id}/watch     → 200 { is_watching: boolean }
  (These need to be added to task-service.ts)

<!-- NotificationItem type (from Plan 05) -->
type NotificationItem = {
  id: number; message: string; type: string; is_read: boolean
  related_entity_id: number | null; related_entity_type: string | null
  created_at: string
}

<!-- Existing settings page structure -->
Frontend/app/settings/page.tsx — existing sections: profile edit, avatar upload, danger zone.
Add new section "Bildirimler" as a Card below existing cards.

<!-- Task sidebar structure -->
Frontend/components/task-detail/task-sidebar.tsx — existing sidebar with sprint dropdown, labels, dependencies, recurrence cards. Add watch toggle button as a new card at the top (before other cards).

Available shadcn/ui: Card, CardHeader, CardContent, Switch, Label, Select, SelectTrigger, SelectContent, SelectItem, Tabs, TabsList, TabsTrigger, TabsContent, Button, Separator.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full /notifications page + notification preferences form</name>
  <files>
    Frontend/app/notifications/page.tsx,
    Frontend/components/notifications/notification-preferences-form.tsx
  </files>
  <action>
1. Create `Frontend/app/notifications/page.tsx`:
   - `"use client"` directive.
   - Use TanStack Query with `notificationService.list()`. Maintain local `offset` state for pagination. On "Daha Fazla Yükle" click, increment offset by 20 and accumulate results (same pattern as board pagination from Phase 3/4 — page 1 replaces, subsequent pages append).
   - Filter tabs using shadcn Tabs: "Tümü" | "Okunmamış". Filtering is client-side: when "Okunmamış" tab is active, filter accumulated list to `is_read === false`.
   - List renders `<NotificationItem>` components from `@/components/notifications/notification-item`.
   - Page header: "Bildirimler" (h1 font-bold text-xl) + "Tümünü Okundu İşaretle" + "Temizle" buttons on the right.
   - Below header: Tabs component with "Tümü" and "Okunmamış" tabs.
   - Below tabs: notification list in a Card (or direct list).
   - "Daha Fazla Yükle" button at bottom: only shown when `total > accumulated.length`. On click: fetch next page and append to list.
   - Loading state: show skeleton rows (3 placeholder divs with animate-pulse) while initial load.
   - Empty state: "Tüm bildirimlerinizi okudunuz!" message (same as bell dropdown) when list is empty.
   - All text in Turkish.
   - Mutations (mark read, delete) use `notificationService` directly with `useQueryClient().invalidateQueries` on success.

2. Create `Frontend/components/notifications/notification-preferences-form.tsx`:
   - Fetches preferences via `useQuery({ queryKey: ["notification-preferences"], queryFn: () => notificationService.getPreferences() })`.
   - Saves via `useMutation` calling `notificationService.updatePreferences()` + `toast("Tercihler kaydedildi")` on success.
   - UI structure (all in Turkish):
     - "E-posta Bildirimleri" section: single Switch labeled "Tüm e-posta bildirimlerini etkinleştir" — maps to `email_enabled`. When off, all per-type email toggles are visually disabled (opacity-50 pointer-events-none) but NOT removed from DOM.
     - "Son Tarih Uyarısı" section: "Kaç gün önce uyarı alayım?" label + Select with options: 1, 2, 3, 7 gün.
     - "Bildirim Türleri" section: table/list of 5 notification types with 2 toggles each (Uygulama içi / E-posta):
       | Tür | Uygulama içi | E-posta |
       | Görev Atama (`TASK_ASSIGNED`) | Switch | Switch |
       | Yorum Eklendi (`COMMENT_ADDED`) | Switch | Switch |
       | Son Tarih Yaklaşıyor (`DEADLINE_APPROACHING`) | Switch | Switch |
       | Durum Değişikliği (`STATUS_CHANGE`) | Switch | Switch |
       | Görev Silindi (`TASK_DELETED`) | Switch | Switch |
     - Per-type toggles map to `preferences.TASK_ASSIGNED.in_app`, `preferences.TASK_ASSIGNED.email`, etc. Default both to `true` if key not in preferences object.
     - "Kaydet" Button at bottom. Disabled while mutation is pending.
   - Export: `export function NotificationPreferencesForm()`.
  </action>
  <verify>
    <automated>cd "C:\Users\yusti\OneDrive\Masaüstü\bitirme projesi\SPMS\Frontend" && npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "notifications/page|notification-preferences-form" | head -10 || echo "TypeScript check done"</automated>
  </verify>
  <done>/notifications page TypeScript-valid with pagination state, filter tabs, NotificationItem list, and Load More button. notification-preferences-form.tsx compiles, shows per-type toggles for 5 event types with in-app/email switches, global email toggle, and deadline days selector.</done>
</task>

<task type="auto">
  <name>Task 2: Wire preferences into Settings + watch toggle in task sidebar</name>
  <files>
    Frontend/app/settings/page.tsx,
    Frontend/components/task-detail/task-sidebar.tsx
  </files>
  <action>
1. In `Frontend/app/settings/page.tsx`:
   - Import `NotificationPreferencesForm` from `@/components/notifications/notification-preferences-form`.
   - Add a new Card section after the existing cards (before or after the Danger Zone card — place before Danger Zone):
     ```tsx
     <Card>
       <CardHeader>
         <CardTitle>Bildirimler</CardTitle>
         <CardDescription>Bildirim tercihlerinizi özelleştirin</CardDescription>
       </CardHeader>
       <CardContent>
         <NotificationPreferencesForm />
       </CardContent>
     </Card>
     ```
   - Do NOT modify any existing settings sections (profile, avatar, danger zone).

2. In `Frontend/components/task-detail/task-sidebar.tsx`:
   a. Add watch/unwatch endpoints to `task-service.ts`:
      - File: `Frontend/services/task-service.ts` — add 3 methods:
        `getWatchStatus(taskId: number): Promise<{ is_watching: boolean }>`
        `watchTask(taskId: number): Promise<void>`
        `unwatchTask(taskId: number): Promise<void>`
      - These call `GET/POST/DELETE /api/v1/tasks/{id}/watch` respectively.
      - NOTE: These backend endpoints (watch/unwatch) need to be added to `Backend/app/api/v1/tasks.py` and `Backend/app/infrastructure/database/repositories/` if not done in Plan 04. Include them in this task to keep the feature complete.
      - Backend: add `POST /api/v1/tasks/{id}/watch` and `DELETE /api/v1/tasks/{id}/watch` endpoints to tasks.py. These insert/delete from `task_watchers` table via a direct repository call. Add `get_watcher_status(task_id, user_id)`, `add_watcher(task_id, user_id)`, `remove_watcher(task_id, user_id)` to `INotificationRepository` (or create a minimal `ITaskWatcherRepository` interface and impl). Use direct SQLAlchemy in tasks.py via a `get_watcher_repo` dependency for simplicity.

   b. In `Frontend/components/task-detail/task-sidebar.tsx`:
      - Add watch status query: `useQuery({ queryKey: ["watch-status", taskId], queryFn: () => taskService.getWatchStatus(taskId) })`.
      - Add watch/unwatch mutations with `queryClient.invalidateQueries({ queryKey: ["watch-status", taskId] })` on success.
      - Add a new Card/section at the top of the sidebar (before Sprint dropdown):
        ```tsx
        <div className="mb-4">
          <Button
            variant={isWatching ? "secondary" : "outline"}
            size="sm"
            className="w-full"
            onClick={() => isWatching ? unwatchTask() : watchTask()}
          >
            {isWatching ? "İzlemeyi Bırak" : "Görevi İzle"}
          </Button>
        </div>
        ```
      - `isWatching` = `watchStatusData?.is_watching ?? false`.
      - Only render the watch toggle if the current user is NOT the task assignee (watchers are non-assignees; assignees get notifications automatically). Check via `currentUser?.id !== task.assignee_id`.
  </action>
  <verify>
    <automated>cd "C:\Users\yusti\OneDrive\Masaüstü\bitirme projesi\SPMS\Frontend" && npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "settings/page|task-sidebar" | head -10 || echo "TypeScript check done"</automated>
  </verify>
  <done>settings/page.tsx has "Bildirimler" card section with NotificationPreferencesForm. task-sidebar.tsx has watch/unwatch toggle button visible for non-assignee users. task-service.ts has getWatchStatus, watchTask, unwatchTask methods. Backend has POST/DELETE /api/v1/tasks/{id}/watch endpoints.</done>
</task>

</tasks>

<verification>
Manual verification steps:
1. Navigate to /notifications — page loads with full history, filter tabs visible, Load More works
2. Go to Settings → scroll to "Bildirimler" section — toggles and deadline selector visible
3. Toggle "Tüm e-posta bildirimlerini etkinleştir" off → per-type email switches gray out
4. Change deadline days to 3, click "Kaydet" → toast "Tercihler kaydedildi"
5. Open task detail where current user is NOT assignee → "Görevi İzle" button visible in sidebar
6. Click "Görevi İzle" → button changes to "İzlemeyi Bırak"
7. As watcher, when someone adds a comment to that task → notification appears for watcher
</verification>

<success_criteria>
- /notifications page: paginated list (Load More), filter tabs Tümü/Okunmamış, mark/delete actions work
- Preferences form: 5 event types × 2 toggles, global email toggle, deadline days selector, saved to API
- Global email toggle disables all per-type email toggles visually (opacity-50)
- Watch toggle visible for non-assignees; hidden for assignees
- Clicking "Görevi İzle" creates watcher record; "İzlemeyi Bırak" removes it
- Watchers receive COMMENT_ADDED, STATUS_CHANGE, TASK_DELETED notifications
</success_criteria>

<output>
After completion, create `.planning/phases/05-notifications/05-06-SUMMARY.md`
</output>
