---
phase: 14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f
plan: 12
type: execute
wave: 4
depends_on: [14-01, 14-02, 14-03, 14-04, 14-05, 14-06, 14-07, 14-08, 14-09, 14-10, 14-11]
files_modified:
  - Frontend2/e2e/admin-route-guard.spec.ts
  - Frontend2/e2e/admin-overview.spec.ts
  - Frontend2/e2e/admin-users-crud.spec.ts
  - Frontend2/e2e/admin-audit-filter.spec.ts
  - Frontend2/e2e/admin-stats-render.spec.ts
  - .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-UAT-CHECKLIST.md
  - .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-VALIDATION.md
autonomous: true
requirements:
  - D-00
  - D-01
  - D-A1
  - D-A2
  - D-A3
  - D-A4
  - D-A5
  - D-A6
  - D-A7
  - D-A8
  - D-B1
  - D-B2
  - D-B3
  - D-B4
  - D-B5
  - D-B6
  - D-B7
  - D-B8
  - D-C1
  - D-C2
  - D-C3
  - D-C4
  - D-C5
  - D-C6
  - D-D1
  - D-D2
  - D-D3
  - D-D4
  - D-D5
  - D-D6
  - D-W1
  - D-W2
  - D-W3
  - D-X1
  - D-X2
  - D-X3
  - D-X4
  - D-Y1
  - D-Z1
  - D-Z2
user_setup: []

must_haves:
  truths:
    - "5 Playwright e2e specs cover admin route guard + Overview + Users CRUD + Audit filter + Stats render — each skip-guarded per Phase 11 D-50 / Phase 13 13-10 pattern"
    - "admin-route-guard.spec.ts covers Pitfall 10 (no /admin flash for unauthenticated) + Pitfall 3 (admin layout doesn't bounce admin user during isLoading)"
    - "Each spec has skip-guard checking for env var GSD_E2E_DB_SEEDED — if false, spec skips gracefully (Phase 11 D-50)"
    - "14-UAT-CHECKLIST.md has 25-30 rows covering all 8 admin tabs + admin route guard + invite/reset email delivery + bulk CSV happy/error paths"
    - "VALIDATION.md per-task verification map populated with rows for every PLAN task that has automated verify (set nyquist_compliant: true)"
    - "All 12 plans' acceptance criteria pass (per-plan SUMMARYs verified)"
    - "D-00 (Quality Bar): Phase-gate UAT checklist explicitly verifies prototype-first execution — every surface ports prototype elements verbatim; deliberate-improvement-only deviations were called out in PLAN.md, justified, and reviewed (deviations log accumulated across 14-01..14-11 SUMMARYs)"
    - "D-01 (Plan Decomposition): All 12 strawman plans (14-01 Wave 0 fat infra → 14-12 phase gate) shipped per the locked plan structure — VALIDATION.md per-task verification map confirms 22-task coverage spanning all 12 plans"
  artifacts:
    - path: "Frontend2/e2e/admin-route-guard.spec.ts"
      provides: "Route guard E2E (Pitfall 3 + 10)"
      contains: "test.describe"
    - path: ".planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-UAT-CHECKLIST.md"
      provides: "Manual UAT checklist for /gsd-verify-work pass"
      contains: "## Surface A — Admin Layout"
    - path: ".planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-VALIDATION.md"
      provides: "Per-task verification map populated, nyquist_compliant: true"
      contains: "nyquist_compliant: true"
  key_links: []

tags:
  - admin-panel
  - e2e
  - uat
  - phase-gate
  - frontend2
---

<objective>
Phase 14 wrap-up: write 5 Playwright e2e specs (admin route guard + Overview + Users CRUD + Audit filter + Stats render) using skip-guarded pattern per Phase 11 D-50, write 14-UAT-CHECKLIST.md (25-30 rows for manual /gsd-verify-work pass), and finalize 14-VALIDATION.md per-task verification map (mark nyquist_compliant: true).

Purpose: Phase gate. Without this plan, the admin panel ships untested end-to-end and the Nyquist validation contract is incomplete. /gsd-verify-work pass picks up the UAT checklist post-merge.

Output: 5 NEW e2e spec files + 1 NEW UAT checklist + 1 EXTENDED VALIDATION.md (per-task verification map populated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-CONTEXT.md
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-RESEARCH.md
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-PATTERNS.md
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-VALIDATION.md

<interfaces>
From Frontend2/e2e/profile-page.spec.ts (Phase 13 — skip-guard pattern lines 17-36):
```typescript
test.beforeAll(async () => {
  if (!process.env.GSD_E2E_DB_SEEDED) {
    test.skip("DB seeder not run; skipping E2E");
  }
});
```

From Frontend2/playwright.config.ts (existing):
```typescript
baseURL: "http://localhost:3000"
testDir: "./e2e"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write 5 Playwright e2e specs (admin route guard + Overview + Users CRUD + Audit filter + Stats render) with skip-guard pattern + 14-UAT-CHECKLIST.md</name>
  <files>
    Frontend2/e2e/admin-route-guard.spec.ts,
    Frontend2/e2e/admin-overview.spec.ts,
    Frontend2/e2e/admin-users-crud.spec.ts,
    Frontend2/e2e/admin-audit-filter.spec.ts,
    Frontend2/e2e/admin-stats-render.spec.ts,
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-UAT-CHECKLIST.md
  </files>
  <read_first>
    Frontend2/e2e/profile-page.spec.ts (Phase 13 — skip-guard pattern verbatim donor),
    Frontend2/e2e/task-create.spec.ts (form submission pattern),
    Frontend2/e2e/avatar-dropdown.spec.ts (Phase 13 — Admin Paneli link interaction),
    Frontend2/e2e/reports-charts.spec.ts (Phase 13 — recharts render verification analog for admin-stats-render),
    Frontend2/playwright.config.ts (baseURL + testDir),
    .planning/phases/13-reporting-activity-user-profile/*-UAT-CHECKLIST.md (Phase 13 13-10 pattern — analog UAT structure),
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-PATTERNS.md "Plan 14-12" lines 248-256,
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-CONTEXT.md decisions D-00 through D-Z2 (the UAT checklist must verify each)
  </read_first>
  <behavior>
    - 5 e2e specs, each opens with the Phase 13 skip-guard:
      ```typescript
      import { test, expect } from "@playwright/test";

      test.beforeAll(async () => {
        if (!process.env.GSD_E2E_DB_SEEDED) {
          test.skip("DB seeder not run; skipping E2E");
        }
      });
      ```

    - admin-route-guard.spec.ts: 3 tests.
      1. Anonymous user → /admin/users redirects to /auth/login?next=/admin/users (assert URL after redirect; assert NO /admin DOM rendered — Pitfall 10).
      2. Member user → /admin/users redirects to /dashboard + Toast.danger message containing "yetki" (Pitfall 3 + access_denied_toast).
      3. Admin user → /admin renders Yönetim Konsolu heading + 8 NavTabs items.

    - admin-overview.spec.ts: 2 tests.
      1. Admin loads /admin → 5 StatCards + Pending Requests Card render.
      2. Admin can click "Tümünü gör" → modal opens (assert modal title "Tüm bekleyen istekler" — UI-SPEC §Surface B).

    - admin-users-crud.spec.ts: 2 tests.
      1. Admin clicks "Kullanıcı ekle" → AddUserModal opens; submit form with valid email → assert Toast "Davet gönderildi" + modal closes.
      2. Admin clicks "Toplu davet" → BulkInviteModal opens (assert modal title); attach file (synthetic CSV with 3 valid rows); preview shows 3 rows; submit → summary modal shows "{successful}/{total}" line.

    - admin-audit-filter.spec.ts: 2 tests.
      1. Admin loads /admin/audit → table renders with at least 1 row (seeded audit_log) + 6 columns (NO Risk column).
      2. Click "Filtre" → AuditFilterModal opens; set date_from to 2026-04-01 → click Uygula → assert URL contains "from=2026-04-01" (D-C5 URL-driven).

    - admin-stats-render.spec.ts: 1 test.
      1. Admin loads /admin/stats → 3 charts mount without crash. Assert ActiveUsersTrendChart svg.recharts-surface OR equivalent rendered. Assert MethodologyBars rendered. Assert VelocityCardsGrid rendered.

    - 14-UAT-CHECKLIST.md: 25-30 manual checklist rows organized by surface:
      - **Surface A — Admin Layout (5 rows)**: route guard for anonymous/member/admin; NavTabs strip click each of 8 tabs; Rapor al downloads PDF; Denetim günlüğü navigates to /admin/audit; AvatarDropdown Admin Paneli link reaches /admin
      - **Surface B — Genel/Overview (4 rows)**: 5 StatCards display correct counts; Pending Requests top-5 + Approve/Reject + Tümünü gör modal; Role distribution bars correct ratios; Recent admin events list renders Jira-style lines
      - **Surface C — Kullanıcılar (5 rows)**: Add User invite delivers email; Bulk Invite CSV happy path (5 valid rows commit); Bulk Invite CSV error mix (3 valid 2 invalid → summary correct); CSV export downloads with UTF-8 BOM; Bulk Deactivate + Bulk Role Change toolbar
      - **Surface D — Roller (2 rows)**: 4 cards + Yeni rol placeholder dashed; per-role user counts match Users tab
      - **Surface E — Permissions (2 rows)**: 14×4 toggles all disabled; AlertBanner explains v3.0 defer
      - **Surface F — Projeler (3 rows)**: Archive flow with confirm; Sil two-step key-typing confirm; NO transfer-ownership menu item
      - **Surface G — Şablonlar (2 rows)**: Düzenle redirects to /workflow-editor; Sil with active_project_count>0 requires "Yine de sil" checkbox
      - **Surface H — Audit (4 rows)**: Filter by date range URL-driven; JSON export downloads; 50k cap warning (mock truncated=true scenario); Detay column renders Jira-style for 5+ event types
      - **Surface I — İstatistik (2 rows)**: 3 charts render; Methodology bar % correct
      - **Cross-cutting (2 rows)**: TR/EN locale parity on every tab; mobile/desktop layouts (admin desktop-first per CONTEXT)

    - Each UAT row format:
      ```markdown
      - [ ] **U-14-XX**: {action description} → expected: {observable behavior}; verifies: D-XX
      ```
  </behavior>
  <action>
    Step 1 — Write 5 e2e specs. Each follows the skip-guard + describe/test structure. Use `page.goto`, `page.click`, `page.fill`, `expect(page.locator(...)).toBeVisible()`, `expect(page.url()).toContain(...)` per Playwright API.

    Example admin-route-guard.spec.ts:
    ```typescript
    import { test, expect } from "@playwright/test";

    test.beforeAll(async () => {
      if (!process.env.GSD_E2E_DB_SEEDED) {
        test.skip("DB seeder not run; skipping E2E");
      }
    });

    test.describe("Admin Route Guard", () => {
      test("anonymous user redirects to /auth/login when hitting /admin/users", async ({ page, context }) => {
        await context.clearCookies();
        await page.goto("/admin/users");
        await page.waitForURL(/\/auth\/login/);
        expect(page.url()).toContain("next=%2Fadmin%2Fusers");
        // Assert NO admin DOM rendered (Pitfall 10)
        await expect(page.locator("text=Yönetim Konsolu")).not.toBeVisible();
      });

      test("member user redirects to /dashboard + Toast", async ({ page }) => {
        // Assume seeded member user logged in via fixture
        await page.goto("/admin/users");
        await page.waitForURL(/\/dashboard/);
        await expect(page.locator("text=/yetki/i")).toBeVisible();
      });

      test("admin user renders Yönetim Konsolu + 8 NavTabs", async ({ page }) => {
        // Assume seeded admin user logged in
        await page.goto("/admin");
        await expect(page.locator("h1:has-text('Yönetim Konsolu')")).toBeVisible();
        await expect(page.locator("a[href='/admin/users']")).toBeVisible();
        await expect(page.locator("a[href='/admin/audit']")).toBeVisible();
        await expect(page.locator("a[href='/admin/stats']")).toBeVisible();
        // ... etc for all 8 tab hrefs
      });
    });
    ```

    Step 2 — Write the other 4 specs per behavior contract above.

    Step 3 — Write 14-UAT-CHECKLIST.md. Use markdown format:
    ```markdown
    # Phase 14 UAT Checklist

    > Manual verification for /gsd-verify-work pass post-merge.
    > Each row references the locked decision (D-XX) it verifies.

    ## Surface A — Admin Layout (Plan 14-02 + 14-11)

    - [ ] **U-14-01**: Anonymous user navigates to /admin/users → redirected to /auth/login?next=/admin/users (verifies: D-C3, Pitfall 10)
    - [ ] **U-14-02**: Member user navigates to /admin/users → redirected to /dashboard + Toast "Bu sayfaya erişim yetkiniz yok" (verifies: D-C3, Pitfall 3)
    - [ ] **U-14-03**: Admin user navigates to /admin → 8 NavTabs render with Turkish labels (Genel, Kullanıcılar, Roller, İzin Matrisi, Projeler, Şablonlar, Audit, İstatistik) (verifies: D-C2, D-C4)
    - [ ] **U-14-04**: Click "Rapor al" → admin-summary.pdf downloads with Content-Disposition (verifies: D-B6)
    - [ ] **U-14-05**: Click "Denetim günlüğü" → URL changes to /admin/audit (verifies: D-B6)

    ## Surface B — Genel/Overview (Plan 14-02)
    [...continue with 4 more rows...]

    ## Surface C — Kullanıcılar (Plan 14-03)
    [...continue with 5 rows...]

    [... continue for all 9 surfaces, ending at U-14-30 approximately ...]

    ## Cross-cutting

    - [ ] **U-14-29**: Toggle locale (TR ↔ EN) on every admin surface → no fallback strings, no missing translations (verifies: i18n parity)
    - [ ] **U-14-30**: Admin panel renders correctly on desktop (≥1024px) (verifies: CONTEXT desktop-first scope)

    ---

    Sign-off: ___________________   Date: ___________________
    ```
  </action>
  <verify>
    <automated>cd Frontend2 && GSD_E2E_DB_SEEDED= npm run test:e2e 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - 5 e2e spec files exist in `Frontend2/e2e/admin-*.spec.ts`
    - Each spec opens with `test.beforeAll` skip-guard checking GSD_E2E_DB_SEEDED env var
    - admin-route-guard.spec.ts has 3 tests covering anonymous + member + admin paths
    - admin-overview.spec.ts has 2 tests covering StatCards + Tümünü gör modal
    - admin-users-crud.spec.ts has 2 tests covering Add User + Bulk Invite
    - admin-audit-filter.spec.ts has 2 tests covering table render + URL-driven filter
    - admin-stats-render.spec.ts has 1 test covering 3-chart render
    - `.planning/phases/14-.../14-UAT-CHECKLIST.md` exists with at least 25 checklist rows (grep `- [ ]` count ≥ 25)
    - UAT checklist references at least 30 distinct D-XX decisions across the 9 surface sections (grep `D-` count ≥ 30)
    - UAT checklist organized by 9 surfaces (grep `## Surface` returns ≥9 headers)
    - `cd Frontend2 && GSD_E2E_DB_SEEDED= npm run test:e2e` exits 0 (skip-guarded — should report all tests skipped)
  </acceptance_criteria>
  <done>
    5 Playwright e2e specs cover the critical admin paths with skip-guard pattern (Phase 11 D-50 + 13-10 reuse). UAT checklist has 25-30 manual verification rows organized by 9 surfaces, each row referencing the D-XX decision it verifies. /gsd-verify-work picks this up post-merge for human sign-off.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finalize 14-VALIDATION.md per-task verification map (1 row per PLAN task with automated verify) + flip nyquist_compliant: true + run full Frontend + Backend test suites + commit phase</name>
  <files>
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-VALIDATION.md
  </files>
  <read_first>
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-VALIDATION.md (current — populate the per-task table),
    All 12 PLAN.md files for the phase (extract task IDs + automated verify commands)
  </read_first>
  <behavior>
    - Read every PLAN file 14-01..14-12 and extract each task with `<automated>...</automated>` block.
    - Populate the per-task verification map in 14-VALIDATION.md with one row per task. Columns: Task ID | Plan | Wave | Requirement (D-XX coverage) | Threat Ref (T-14-XX from threat_model) | Secure Behavior (one-line summary) | Test Type (RTL / pytest / playwright) | Automated Command (the actual command from the verify block) | File Exists (the test file path) | Status (⬜ pending until executor runs).
    - Update Wave 0 Requirements section: check off all items (papaparse install, vitest config, NavTabs, Modal, ConfirmDialog tone, admin service module, alembic migration 006, ProjectJoinRequest entity scaffold, IProjectJoinRequestRepository ABC scaffold).
    - Update Validation Sign-Off section: check off "All tasks have <automated> verify" + "Sampling continuity: no 3 consecutive tasks without automated verify" + "Wave 0 covers all MISSING references" + "No watch-mode flags" + "Feedback latency < 60s/300s" + "nyquist_compliant: true set in frontmatter".
    - Flip frontmatter: `nyquist_compliant: false` → `nyquist_compliant: true`. Flip `wave_0_complete: false` → `wave_0_complete: true`. Flip `status: draft` → `status: approved`.
    - Final smoke: run full Frontend + Backend test suites and assert all green. NO new failures introduced by the phase.
  </behavior>
  <action>
    Step 1 — Read all 12 PLAN.md files. For each, count tasks with `<automated>` blocks. Extract:
    - Task ID (e.g., 14-01 Task 1, 14-01 Task 2, 14-01 Task 3, 14-01 Task 4, 14-02 Task 1, ...)
    - Plan ID (14-01..14-12)
    - Wave (from frontmatter)
    - Requirements covered (from frontmatter requirements array — top 1-2 most relevant D-XX)
    - Threat ref (from <threat_model> table — most relevant T-14-XX)
    - Test command (from <verify><automated>...</automated></verify>)
    - File exists path (from <files> declaration — pick the test file)

    Step 2 — Build the table. Approximate rows: Plan 14-01 has 4 tasks → 4 rows. Plan 14-02 has 2 tasks → 2 rows. ... Total expected ≈ 25-30 rows across all 12 plans.

    Example row format:
    ```markdown
    | 14-01-T1 | 14-01 | 0 | D-A1, D-B4, D-C4 | T-14-01-05 | papaparse install + ConfirmDialog tone + NavTabs + Modal primitives | RTL | `cd Frontend2 && npm run test -- --run nav-tabs.test.tsx confirm-dialog.test.tsx` | Frontend2/components/primitives/nav-tabs.test.tsx | ⬜ |
    ```

    Step 3 — Update Wave 0 Requirements checklist (lines 60-72 of current VALIDATION.md):
    ```markdown
    - [x] `cd Frontend2 && npm install papaparse@^5.5.3 ...`
    - [x] `Frontend2/vitest.config.ts` — verified exists
    - [x] `Frontend2/components/primitives/nav-tabs.tsx` — built
    - [x] `Frontend2/components/primitives/modal.tsx` — built
    - [x] `Frontend2/components/primitives/confirm-dialog.tsx` — extended with tone
    - [x] `Frontend2/lib/api/admin.ts` (or admin-* services) — built
    - [x] `Backend/alembic/versions/006_phase14_admin_panel.py` — created + applied
    - [x] `Backend/app/domain/entities/project_join_request.py` — built
    - [x] `Backend/app/domain/repositories/project_join_request_repository.py` — built
    ```

    Step 4 — Update Validation Sign-Off checklist (lines 89-94):
    ```markdown
    - [x] All tasks have `<automated>` verify or Wave 0 dependencies
    - [x] Sampling continuity: no 3 consecutive tasks without automated verify
    - [x] Wave 0 covers all MISSING references
    - [x] No watch-mode flags (always `--run` for vitest)
    - [x] Feedback latency < 60s (unit) / 300s (full)
    - [x] `nyquist_compliant: true` set in frontmatter once planner populates the per-task table
    ```

    Step 5 — Update frontmatter:
    ```yaml
    status: approved
    nyquist_compliant: true
    wave_0_complete: true
    ```

    Step 6 — Run full smoke:
    ```bash
    cd Frontend2 && npm run test -- --run 2>&1 | tail -20
    cd Backend && python -m pytest -q 2>&1 | tail -20
    ```
    Both must exit 0.

    Step 7 — Update "Approval" line: `Approval: signed-off Plan 14-12 Task 2`.
  </action>
  <verify>
    <automated>cd Frontend2 && npm run test -- --run 2>&1 | tail -10 && cd ../Backend && python -m pytest -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/14-.../14-VALIDATION.md` per-task table populated with at least 25 rows (one per PLAN task with automated verify)
    - Wave 0 Requirements checklist all 9 items checked off (grep `- [x]` returns ≥9 inside Wave 0 section)
    - Validation Sign-Off checklist all 6 items checked off (grep `- [x]` ≥6 inside Sign-Off section)
    - Frontmatter `nyquist_compliant: true` AND `wave_0_complete: true` AND `status: approved`
    - `cd Frontend2 && npm run test -- --run` exits 0
    - `cd Backend && python -m pytest -q` exits 0
    - "Approval" line shows signed-off date or marker
  </acceptance_criteria>
  <done>
    14-VALIDATION.md is the audit trail for the entire phase — every task with automated verify has a row mapping it to the requirement (D-XX) and threat (T-14-XX) it satisfies. Wave 0 marked complete. nyquist_compliant flipped to true. Full Frontend + Backend test suites green. Phase 14 ready for /gsd-verify-work pass.
  </done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-12-01 | T (Tampering) | E2E specs run without DB seed | mitigate | Skip-guard pattern (Phase 11 D-50): if GSD_E2E_DB_SEEDED env var unset, tests skip gracefully — no false negatives. |
| T-14-12-02 | E (Elevation of Privilege) | Admin route guard tested | mitigate | admin-route-guard.spec.ts asserts anonymous → /auth/login + member → /dashboard + admin → /admin. Verifies Pitfall 3 + 10. |
| T-14-12-03 | I (Information Disclosure) | UAT skipped surfaces leave decisions unverified | mitigate | UAT checklist has 25-30 rows covering every D-XX decision. /gsd-verify-work pass blocks merge until all checked. |
</threat_model>

<verification>
- 5 e2e specs exist + skip-guard pattern.
- 14-UAT-CHECKLIST.md exists with ≥25 rows.
- 14-VALIDATION.md per-task table populated.
- Frontmatter nyquist_compliant: true.
- Full Frontend + Backend test suites green.
</verification>

<success_criteria>
- Phase 14 reaches phase gate: all 12 plans land, all tests green, all D-XX decisions tracked in VALIDATION.md, UAT artifact handed to /gsd-verify-work.
- AdminPanel implementation matches prototype "birebir aynısını tamamen işlevsel" per user-stated phase description.
- D-A2..A5 RBAC defer respected with multiple defenses tested.
- D-D1..D6 cross-cutting Jira-style audit detail uplift functional on 4 surfaces.
- All ~15 prototype actions functional (D-B1) except RBAC-deferred (D-A3..A5) and any v2.1-deferred items (Sil-on-Users + maybe CSV export on Projects — soft-disabled with tooltips).
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-12-SUMMARY.md` documenting:
- 5 e2e specs created (test count per spec)
- UAT checklist row count + decision coverage map
- VALIDATION.md per-task table size + nyquist_compliant flip
- Full smoke test results (Frontend npm test + Backend pytest)
- Phase 14 ready for /gsd-verify-work handoff
- Any items soft-disabled in v2.0 vs full implementation deltas (Sil-on-Users / CSV-on-Projects / etc.)
</output>
