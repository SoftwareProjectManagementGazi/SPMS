---
phase: 14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Backend/app/domain/entities/project_join_request.py
  - Backend/app/domain/repositories/project_join_request_repository.py
  - Backend/app/domain/repositories/audit_repository.py
  - Backend/app/infrastructure/database/models/project_join_request.py
  - Backend/app/infrastructure/database/repositories/project_join_request_repo.py
  - Backend/app/infrastructure/database/repositories/audit_repo.py
  - Backend/app/api/deps/project_join_request.py
  - Backend/app/api/dependencies.py
  - Backend/app/application/dtos/project_join_request_dtos.py
  - Backend/app/application/dtos/admin_user_dtos.py
  - Backend/app/application/dtos/admin_audit_dtos.py
  - Backend/app/application/dtos/admin_stats_dtos.py
  - Backend/app/application/use_cases/create_join_request.py
  - Backend/app/application/use_cases/approve_join_request.py
  - Backend/app/application/use_cases/reject_join_request.py
  - Backend/app/application/use_cases/list_pending_join_requests.py
  - Backend/app/application/use_cases/invite_user.py
  - Backend/app/application/use_cases/bulk_invite_user.py
  - Backend/app/application/use_cases/deactivate_user.py
  - Backend/app/application/use_cases/reset_user_password.py
  - Backend/app/application/use_cases/change_user_role.py
  - Backend/app/application/use_cases/bulk_action_user.py
  - Backend/app/application/use_cases/get_global_audit.py
  - Backend/app/application/use_cases/get_admin_stats.py
  - Backend/app/application/use_cases/generate_admin_summary_pdf.py
  - Backend/app/api/v1/admin_join_requests.py
  - Backend/app/api/v1/admin_users.py
  - Backend/app/api/v1/admin_audit.py
  - Backend/app/api/v1/admin_stats.py
  - Backend/app/api/v1/admin_summary.py
  - Backend/app/api/main.py
  - Backend/alembic/versions/006_phase14_admin_panel.py
  - Backend/app/core/config.py
  - Backend/tests/integration/test_create_join_request.py
  - Backend/tests/integration/test_approve_join_request.py
  - Backend/tests/integration/test_admin_users_crud.py
  - Backend/tests/integration/test_admin_audit_get_global.py
  - Backend/tests/integration/test_admin_stats.py
  - Backend/tests/integration/test_generate_admin_summary_pdf.py
  - Frontend2/package.json
  - Frontend2/services/admin-join-request-service.ts
  - Frontend2/services/admin-user-service.ts
  - Frontend2/services/admin-audit-service.ts
  - Frontend2/services/admin-stats-service.ts
  - Frontend2/hooks/use-pending-join-requests.ts
  - Frontend2/hooks/use-approve-join-request.ts
  - Frontend2/hooks/use-reject-join-request.ts
  - Frontend2/hooks/use-admin-users.ts
  - Frontend2/hooks/use-invite-user.ts
  - Frontend2/hooks/use-bulk-invite.ts
  - Frontend2/hooks/use-deactivate-user.ts
  - Frontend2/hooks/use-reset-password.ts
  - Frontend2/hooks/use-change-role.ts
  - Frontend2/hooks/use-bulk-action.ts
  - Frontend2/hooks/use-admin-audit.ts
  - Frontend2/hooks/use-admin-stats.ts
  - Frontend2/lib/admin/permissions-static.ts
  - Frontend2/lib/admin/audit-field-labels.ts
  - Frontend2/lib/admin/csv-parse.ts
  - Frontend2/lib/admin/csv-export.ts
  - Frontend2/components/primitives/nav-tabs.tsx
  - Frontend2/components/primitives/modal.tsx
  - Frontend2/components/admin/shared/more-menu.tsx
  - Frontend2/components/admin/shared/more-menu.test.tsx
  - Frontend2/components/primitives/index.ts
  - Frontend2/components/projects/confirm-dialog.tsx
  - Frontend2/components/primitives/nav-tabs.test.tsx
  - Frontend2/components/projects/confirm-dialog.test.tsx
autonomous: true
requirements:
  - D-00
  - D-01
  - D-A1
  - D-A6
  - D-A7
  - D-A8
  - D-B2
  - D-B3
  - D-B4
  - D-B6
  - D-B7
  - D-B8
  - D-W1
  - D-W2
  - D-W3
  - D-X1
  - D-X2
  - D-X3
  - D-X4
  - D-Z2
user_setup:
  - service: smtp
    why: "Reuses Phase 5 SMTP for invite + password reset emails — no new config; verify env vars set before exercising invite flows in dev."
    env_vars:
      - name: SMTP_HOST
        source: "Existing Phase 5 env (Backend/.env)"
      - name: SMTP_PORT
        source: "Existing"
      - name: SMTP_USER
        source: "Existing"
      - name: SMTP_PASSWORD
        source: "Existing"
      - name: INVITE_TOKEN_TTL_DAYS
        source: "NEW — defaults to 7 in Backend/app/core/config.py if unset (per CONTEXT Discretion + RESEARCH §Runtime State Inventory line 850)"

must_haves:
  truths:
    - "papaparse@^5.5.3 + @types/papaparse@^5.3.16 are installed in Frontend2/node_modules"
    - "ConfirmDialog accepts optional tone prop ('primary' | 'danger' | 'warning'), default 'primary' is backward-compat with all Phase 10/11/12 callers"
    - "NavTabs primitive renders Link-based tabs with usePathname-driven active detection; /admin (Overview) is NOT active when pathname is /admin/users (Pitfall 4)"
    - "Modal primitive renders overlay + panel + ESC + click-outside dismiss; ModalHeader/Body/Footer slots exported"
    - "Backend ProjectJoinRequest entity is pure Pydantic with Literal status enum (no Python Enum); IProjectJoinRequestRepository ABC has 4 abstract methods"
    - "Alembic migration 006 creates project_join_requests table only (NO RBAC tables — D-A2); idempotent via _table_exists/_index_exists helpers copied verbatim from 005"
    - "Backend admin endpoints exist for join-requests (4), users (7), audit (2), stats (1), summary PDF (1); every handler uses Depends(require_admin)"
    - "Audit JSON export is StreamingResponse (D-W3) with 50k row hard cap (D-Z2)"
    - "Bulk invite endpoint enforces 500-row hard cap server-side via Pydantic Field(max_items=500) + per-row commit-or-skip strategy (D-B4)"
    - "Frontend admin-* services map snake_case to camelCase via mapper functions matching profile-service.ts pattern; audit_log.metadata stays snake_case all the way (Pitfall 2)"
    - "Frontend admin-* hooks use TanStack Query with queryKey ['admin', ...] and staleTime: 30s (admin data) or 60s (stats)"
    - "Approve/reject join-request hooks implement optimistic update per D-W2"
    - "All new Backend use cases are pure (NO 'import sqlalchemy' or 'app.infrastructure' imports)"
    - "GetGlobalAuditUseCase response includes truncated: bool when actual count > 50k (Pitfall 6)"
    - "GetAdminStatsUseCase composes 3 parallel reads via asyncio.gather (active_users_trend / methodology_distribution / project_velocities top-30)"
    - "GenerateAdminSummaryPDFUseCase reuses Phase 12 fpdf2 service; router applies @limiter.limit('1/30seconds') (D-B6)"
    - "Shared MoreMenu primitive (Frontend2/components/admin/shared/more-menu.tsx) renders icon-button trigger + dropdown of action items with click-outside dismiss; consumed by Plans 14-03 (Users), 14-05 (Projects), 14-06 (Workflows) row-action menus + Plans 14-02 (Pending requests) and 14-07 (Audit) per RESEARCH §Don't Hand-Roll line 830"
  artifacts:
    - path: "Backend/app/domain/entities/project_join_request.py"
      provides: "ProjectJoinRequest Pydantic entity + JoinRequestStatus Literal"
      contains: "class ProjectJoinRequest"
    - path: "Backend/app/domain/repositories/project_join_request_repository.py"
      provides: "IProjectJoinRequestRepository ABC"
      contains: "class IProjectJoinRequestRepository(ABC)"
    - path: "Backend/alembic/versions/006_phase14_admin_panel.py"
      provides: "project_join_requests table migration (idempotent)"
      contains: "project_join_requests"
    - path: "Backend/app/api/v1/admin_join_requests.py"
      provides: "4 admin join-request endpoints"
      exports: ["router"]
    - path: "Backend/app/api/v1/admin_users.py"
      provides: "7 admin user CRUD + bulk endpoints"
      exports: ["router"]
    - path: "Backend/app/api/v1/admin_audit.py"
      provides: "/admin/audit + /admin/audit.json endpoints"
      exports: ["router"]
    - path: "Backend/app/api/v1/admin_stats.py"
      provides: "/admin/stats composite endpoint"
      exports: ["router"]
    - path: "Backend/app/api/v1/admin_summary.py"
      provides: "/admin/summary.pdf endpoint (rate-limited)"
      exports: ["router"]
    - path: "Frontend2/components/primitives/nav-tabs.tsx"
      provides: "Link-based tab strip primitive"
      exports: ["NavTabs", "NavTabItem", "NavTabsProps"]
    - path: "Frontend2/components/primitives/modal.tsx"
      provides: "Modal + ModalHeader/Body/Footer slot primitives"
      exports: ["Modal", "ModalHeader", "ModalBody", "ModalFooter"]
    - path: "Frontend2/components/admin/shared/more-menu.tsx"
      provides: "Shared 3-dot dropdown action menu (5 reuse sites planned: Users / Projects / Workflows / Pending requests / Audit)"
      exports: ["MoreMenu", "MoreMenuItem"]
    - path: "Frontend2/lib/admin/csv-parse.ts"
      provides: "parseBulkInviteCsv papaparse wrapper + per-row validators"
      exports: ["parseBulkInviteCsv", "BulkInviteRow"]
    - path: "Frontend2/services/admin-join-request-service.ts"
      provides: "listPending / approve / reject service methods"
      exports: ["adminJoinRequestService"]
    - path: "Frontend2/services/admin-user-service.ts"
      provides: "invite/bulkInvite/deactivate/resetPassword/changeRole/bulkAction/list/exportCsv"
      exports: ["adminUserService"]
  key_links:
    - from: "Backend/app/application/use_cases/approve_join_request.py"
      to: "Backend/app/infrastructure/database/repositories/team_repo.py add_member"
      via: "team_repo dependency injected through use case constructor"
      pattern: "team_repo.add_member"
    - from: "Backend/app/api/v1/admin_audit.py"
      to: "Backend/app/infrastructure/database/repositories/audit_repo.py get_global_audit"
      via: "GetGlobalAuditUseCase via Depends(get_audit_repo)"
      pattern: "get_global_audit"
    - from: "Backend/app/api/main.py"
      to: "Backend/app/api/v1/admin_*.py"
      via: "5 router includes after admin_settings_router_module"
      pattern: "include_router.*admin_(join_requests|users|audit|stats|summary)"
    - from: "Frontend2/components/primitives/index.ts"
      to: "Frontend2/components/primitives/nav-tabs.tsx + modal.tsx"
      via: "barrel export"
      pattern: "export \\{ NavTabs|Modal \\} from"
    - from: "Frontend2/hooks/use-approve-join-request.ts"
      to: "Frontend2/services/admin-join-request-service.ts approve"
      via: "useMutation onMutate optimistic update + onSettled invalidate"
      pattern: "queryKey: \\[.admin., .join-requests.\\]"

tags:
  - admin-panel
  - clean-architecture
  - audit-log
  - rbac-deferred
  - frontend2
  - papaparse
---

<objective>
Wave 0 fat infra: stand up the entire Backend ProjectJoinRequest vertical slice (entity / repo / use cases / DTOs / 5 admin routers / Alembic migration 006 / integration tests using in-memory fakes), the Frontend admin service+hook layer (4 services + 14 hooks + 3 lib utilities), 2 NEW primitives (NavTabs Link-based + Modal overlay), and one primitive extension (ConfirmDialog tone). Install papaparse@^5.5.3 + @types/papaparse@^5.3.16. After Plan 14-01 lands, every Wave 2 surface plan (14-02..14-08) consumes pre-built scaffolding without re-reading CONTEXT/RESEARCH for these decisions.

Purpose: Phase 12 D-02 / Phase 13 D-01 fat-infra pattern — centralize all primitives + services + hooks + libs + backend slice in one heavy plan so the 6 parallel surface plans in Wave 2 stay small (2-3 tasks each, < 50% context). Resists the "while we're here" temptation that produces drift across surfaces.

Output: ~33 NEW files + 4 EXTENDED files. After this plan: papaparse installed; ConfirmDialog has tone prop with backward-compat default; NavTabs + Modal exported from primitives barrel; Backend admin endpoints reachable + tested; Frontend service+hook layer ready for Wave 2 consumers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-CONTEXT.md
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-RESEARCH.md
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-PATTERNS.md
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-UI-SPEC.md
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-VALIDATION.md
@CLAUDE.md
@Backend/CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase + RESEARCH.md verbatim excerpts. -->

From Backend/app/domain/entities/password_reset_token.py (analog for ProjectJoinRequest entity shape):
```python
class PasswordResetToken(BaseModel):
    id: Optional[int] = None
    token_hash: str
    user_id: int
    expires_at: datetime
    used_at: Optional[datetime] = None
    created_at: Optional[datetime] = None
    model_config = ConfigDict(from_attributes=True)
```

From Backend/app/api/deps/audit.py (verbatim DI factory shape — copy for project_join_request DI):
```python
from fastapi import Depends
from sqlalchemy.ext.asyncio import AsyncSession
from app.infrastructure.database.database import get_db_session
from app.domain.repositories.audit_repository import IAuditRepository
from app.infrastructure.database.repositories.audit_repo import SqlAlchemyAuditRepository

def get_audit_repo(session: AsyncSession = Depends(get_db_session)) -> IAuditRepository:
    return SqlAlchemyAuditRepository(session)

__all__ = ["get_audit_repo"]
```

From Backend/app/api/v1/admin_settings.py (analog for admin router shape with require_admin):
```python
from fastapi import APIRouter, Depends
from app.api.dependencies import get_system_config_repo, require_admin
from app.application.dtos.system_config_dtos import SystemConfigResponseDTO
from app.application.use_cases.manage_system_config import GetSystemConfigUseCase
from app.domain.entities.user import User

router = APIRouter()

@router.get("/", response_model=SystemConfigResponseDTO)
async def get_settings(
    admin: User = Depends(require_admin),
    repo=Depends(get_system_config_repo),
):
    uc = GetSystemConfigUseCase(repo)
    return await uc.execute()
```

From Backend/app/infrastructure/database/repositories/audit_repo.py line 76+ (create_with_metadata signature — used by every admin use case):
```python
async def create_with_metadata(
    self,
    entity_type: str,
    entity_id: int,
    action: str,
    user_id: int,
    metadata: dict,
    field_name: Optional[str] = None,
    old_value: Optional[Any] = None,
    new_value: Optional[Any] = None,
) -> None:
    """D-08: insert audit_log row with full JSON envelope in extra_metadata column.
    Note: DB column is literally `metadata`; Python attr is `extra_metadata` (Pitfall 7/8)."""
    # ... persists with AuditLogModel(extra_metadata=metadata, ...)
```

From Frontend2/components/projects/confirm-dialog.tsx (CURRENT — to be EXTENDED with tone prop):
```typescript
interface ConfirmDialogProps {
  open: boolean
  title: string
  body: string
  confirmLabel?: string  // defaults to "Onayla"
  cancelLabel?: string   // defaults to "İptal"
  onConfirm: () => void
  onCancel: () => void
}
// Confirm Button is currently hardcoded variant="primary" on line 33.
// Plan 14-01 Task 1 ADDS optional tone?: "primary" | "danger" | "warning" (default "primary").
```

From Frontend2/components/primitives/tabs.tsx (analog for NavTabs visual API — copy PAD_MAP + FONT_MAP + active border):
```typescript
const PAD_MAP: Record<TabsSize, string> = { sm: "6px 10px", md: "8px 14px", lg: "10px 16px" }
const FONT_MAP: Record<TabsSize, number> = { sm: 12, md: 13, lg: 14 }
// Active state: borderBottom: "2px solid var(--primary)", color: "var(--fg)", fontWeight: 600
// Inactive: borderBottom: "2px solid transparent", color: "var(--fg-muted)", fontWeight: 500
```

From Frontend2/services/profile-service.ts (snake → camel mapper pattern — copy shape for admin-* services):
```typescript
interface UserSummaryResponseDTO {  // backend snake_case
  active_tasks: number
  completed_last_30d: number
}
export interface UserSummaryStats {  // domain camelCase
  activeTasks: number
  completedLast30d: number
}
function mapSummary(d: UserSummaryResponseDTO): UserSummaryStats {
  return { activeTasks: d.active_tasks, completedLast30d: d.completed_last_30d }
}
```

From Frontend2/hooks/use-projects.ts useUpdateProjectStatus (mutation+invalidate analog — copy shape for admin mutations):
```typescript
export function useUpdateProjectStatus() {
  const qc = useQueryClient()
  return useMutation({
    mutationFn: projectService.updateStatus,
    onSettled: () => qc.invalidateQueries({ queryKey: ["projects"] }),
  })
}
```

From Backend/alembic/versions/005_phase9_schema.py lines 39-97 (idempotent helpers — COPY VERBATIM into 006):
```python
def _table_exists(table_name: str) -> bool:
    conn = op.get_bind()
    result = conn.execute(
        sa.text("SELECT COUNT(*) FROM information_schema.tables "
                "WHERE table_schema='public' AND table_name=:t"),
        {"t": table_name},
    )
    return result.scalar() > 0

def _index_exists(index_name: str) -> bool:
    conn = op.get_bind()
    result = conn.execute(
        sa.text("SELECT COUNT(*) FROM pg_indexes "
                "WHERE schemaname='public' AND indexname=:i"),
        {"i": index_name},
    )
    return result.scalar() > 0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frontend Wave-0 — install papaparse + extend ConfirmDialog tone + build NavTabs + Modal primitives + barrel export + admin lib utilities (csv-parse / csv-export / permissions-static / audit-field-labels)</name>
  <files>
    Frontend2/package.json,
    Frontend2/components/projects/confirm-dialog.tsx,
    Frontend2/components/projects/confirm-dialog.test.tsx,
    Frontend2/components/primitives/nav-tabs.tsx,
    Frontend2/components/primitives/nav-tabs.test.tsx,
    Frontend2/components/primitives/modal.tsx,
    Frontend2/components/admin/shared/more-menu.tsx,
    Frontend2/components/admin/shared/more-menu.test.tsx,
    Frontend2/components/primitives/index.ts,
    Frontend2/lib/admin/permissions-static.ts,
    Frontend2/lib/admin/audit-field-labels.ts,
    Frontend2/lib/admin/csv-parse.ts,
    Frontend2/lib/admin/csv-export.ts
  </files>
  <read_first>
    Frontend2/package.json (verify exact versions of next/react/recharts before adding papaparse),
    Frontend2/components/projects/confirm-dialog.tsx (current shape — DO NOT touch existing call sites),
    Frontend2/components/primitives/tabs.tsx (verbatim style donor for NavTabs PAD_MAP/FONT_MAP/active treatment),
    Frontend2/components/primitives/index.ts (barrel — append new exports without reordering existing blocks),
    Frontend2/components/projects/project-card.tsx (existing analog showing icon-button dropdown pattern + click-outside dismiss — DONOR for shared more-menu.tsx),
    New_Frontend/src/pages/admin.jsx (prototype reference for visual menu shape — per-row 3-dot menus on admin tables),
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-PATTERNS.md "Pattern Excerpts" section (the relevant excerpt for shared MoreMenu),
    Frontend2/lib/methodology-matrix.ts (analog for permissions-static + audit-field-labels — constant table + locale helper shape),
    Frontend2/lib/api-client.ts (only needed if csv-export wraps fetch — NOT NEEDED here per S6: csv-export creates DOM anchor, no apiClient call),
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-RESEARCH.md §Pattern 1 lines 487-555 (verbatim NavTabs implementation),
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-RESEARCH.md §Pattern 2 lines 564-625 (verbatim Modal implementation),
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-RESEARCH.md §Pattern 3 lines 631-678 (verbatim ConfirmDialog tone diff),
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-UI-SPEC.md §Color destructive table lines 200-271 (tone semantics),
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-PATTERNS.md "Plan 14-01 — Frontend Libs + Primitives" lines 106-117 (per-file pattern notes)
  </read_first>
  <behavior>
    - papaparse install: package.json contains "papaparse": "^5.5.3" in dependencies AND "@types/papaparse": "^5.3.16" in devDependencies; node_modules/papaparse/package.json exists with version 5.5.3.x.
    - ConfirmDialog: new optional `tone?: "primary" | "danger" | "warning"` prop with default "primary". When tone="danger" the title gets an inline AlertTriangle icon (size=14, color=var(--priority-critical)) prefix and the confirm Button switches to variant="danger". When tone="warning" the title gets an AlertCircle icon (size=14, color=var(--status-review)) prefix and the confirm Button stays variant="primary" (warning amber is reserved for AlertBanner only). Default tone="primary" leaves rendering identical to current behavior — every Phase 10/11/12 caller (no `tone` prop) MUST render byte-equivalent to before.
    - NavTabs: Link-based, usePathname active detection. /admin tab is active ONLY when pathname === "/admin" OR (pathname.startsWith("/admin/") AND no other tab.href matches as a pathname prefix). All 8 admin paths route to the correct active tab. Renders Badge (Phase 8 primitive) when tab.badge != null with tone="primary" if active else tone="neutral". Keyboard nav: native Link tab-stop semantics (no custom keyboard handler needed because each Link is a focusable element).
    - Modal: overlay (position:fixed inset:0, z-index 1000, background oklch(0 0 0 / 0.5), backdropFilter blur(2px)). Panel (background var(--surface), borderRadius var(--radius), boxShadow var(--shadow-xl), maxWidth/maxHeight calc(100% - 32px)). ESC key dismiss + click-outside dismiss; click inside panel does NOT dismiss (e.stopPropagation). ModalHeader/Body/Footer slots with verbatim padding from RESEARCH §Pattern 2.
    - permissions-static: 14 permissions × 4 roles tri-state map ("granted" | "denied" | "n/a") matching UI-SPEC §Surface E permission rows. Helper getPermission(perm, role): TriState.
    - audit-field-labels: TR/EN dict for ~12 task field names (due_date, priority, story_points, description, title, assignee_id, column_id, sprint_id, parent_id, label_ids, watcher_ids, estimate). Helper getFieldLabel(name, lang): string with fallback to the raw name.
    - csv-parse: papaparse wrapper exporting parseBulkInviteCsv(file: File): Promise<{rows: BulkInviteRow[], errors: ParseError[]}>. Per-row validation (email regex matching backend Pydantic EmailStr lax, role enum case-insensitive normalization, name 0-100 chars defaulting to email local part). 500-row hard cap (parser stops + adds error AFTER the 500th row).
    - csv-export: downloadCsv(url: string, filename?: string) — creates hidden anchor, triggers click, removes from DOM. NO in-browser CSV assembly per D-W3.
    - Barrel export updated: NavTabs + NavTabItem + NavTabsProps + Modal + ModalHeader + ModalBody + ModalFooter + ModalProps appended to Frontend2/components/primitives/index.ts.
  </behavior>
  <action>
    Step 1 — Install papaparse:
    ```bash
    cd Frontend2 && npm install papaparse@^5.5.3 --save && npm install --save-dev @types/papaparse@^5.3.16
    ```
    Verify: `node -e "require('papaparse')"` exits 0; `Frontend2/node_modules/papaparse/package.json` exists.

    Step 2 — Extend ConfirmDialog (Frontend2/components/projects/confirm-dialog.tsx). Apply this diff verbatim from RESEARCH §Pattern 3 lines 637-678. Add `tone?: "primary" | "danger" | "warning"` to ConfirmDialogProps (default "primary"). Inside the component, derive `TitleIcon = tone === "danger" ? AlertTriangle : tone === "warning" ? AlertCircle : null` and `titleColor = tone === "danger" ? "var(--priority-critical)" : tone === "warning" ? "var(--status-review)" : "var(--fg)"`. Render `{TitleIcon && <TitleIcon size={14} color={titleColor} />}` BEFORE the title text inside the title div. Switch the confirm Button: `variant={tone === "danger" ? "danger" : "primary"}`. Import AlertTriangle + AlertCircle from "lucide-react". Existing call sites (Phase 10/11/12) MUST NOT be touched.

    Step 3 — Build NavTabs (Frontend2/components/primitives/nav-tabs.tsx). Copy verbatim from RESEARCH §Pattern 1 lines 494-555 — `"use client"` directive, import Link from "next/link", import usePathname from "next/navigation", import Badge from "./badge". Export `NavTabItem` (id, href, label, icon?, badge?), `NavTabsProps` (tabs, size?), `NavTabs` function. Active detection MUST handle the special case: pathname === tab.href OR (tab.href === "/admin" AND pathname.startsWith("/admin/") AND !tabs.some(t => t.href !== "/admin" && pathname.startsWith(t.href))). Inline styles: copy PAD_MAP and FONT_MAP from tabs.tsx. Active style: borderBottom "2px solid var(--primary)", marginBottom -1, color "var(--fg)", fontWeight 600. Inactive: borderBottom "2px solid transparent", color "var(--fg-muted)", fontWeight 500. Wrap `<Link href={tab.href} style={{...}}>` per tab. Each Link includes optional icon + label + optional Badge.

    Step 4 — Build Modal (Frontend2/components/primitives/modal.tsx). Copy verbatim from RESEARCH §Pattern 2 lines 569-625. `"use client"` directive. Export ModalProps (open, onClose, width=480, children), Modal, ModalHeader, ModalBody, ModalFooter. ESC handler attached/cleaned in useEffect with deps [open, onClose]. Click-outside (overlay onClick) calls onClose; click inside panel calls e.stopPropagation. Slot styles: ModalHeader padding 20, borderBottom 1px solid var(--border), fontSize 15, fontWeight 600. ModalBody padding 16, flex 1, overflowY auto. ModalFooter padding 12, borderTop 1px solid var(--border), display flex, gap 8, justifyContent flex-end.

    Step 5 — Update barrel (Frontend2/components/primitives/index.ts). APPEND (do not reorder existing blocks):
    ```typescript
    // Phase 14 Plan 14-01 — NavTabs Link-based tab strip primitive (D-C4).
    export { NavTabs } from "./nav-tabs"
    export type { NavTabItem, NavTabsProps } from "./nav-tabs"

    // Phase 14 Plan 14-01 — Modal primitive (overlay + panel + slots).
    export { Modal, ModalHeader, ModalBody, ModalFooter } from "./modal"
    export type { ModalProps } from "./modal"
    ```

    Step 6 — Build admin lib utilities:
    - Frontend2/lib/admin/permissions-static.ts: 14×4 tri-state matrix per UI-SPEC §Surface E (Create project / Edit project / Delete project / Archive / Create task / Change assignee / Change status / Delete task / Invite user / Assign role / Remove member / Edit workflow / Edit lifecycle / Publish template). Roles: Admin / "Project Manager" / Member / Guest. Admin → granted on all. PM → granted on Create/Edit/Archive project + all task perms + Invite/Assign/Remove member + workflow/lifecycle/template all granted (except Delete project). Member → granted only on Create task / Change assignee on own tasks ("granted") / Change status / no workflow access. Guest → "denied" on every cell. Export TriState type and getPermission(perm, role): TriState helper.
    - Frontend2/lib/admin/audit-field-labels.ts: TR/EN map for keys due_date, priority, story_points, description, title, assignee_id, column_id, sprint_id, parent_id, label_ids, watcher_ids, estimate, status, methodology, type, target_role, source_role. Export getFieldLabel(name: string, lang: "tr" | "en"): string falling back to the raw name when missing.
    - Frontend2/lib/admin/csv-parse.ts: import Papa from "papaparse". Export `interface BulkInviteRow { email: string; name: string; role: "Admin" | "Project Manager" | "Member" }`. Export parseBulkInviteCsv(file: File): Promise<{ rows: BulkInviteRow[]; errors: { row_number: number; message: string }[] }>. Use Papa.parse with header autodetect (sniff first row). Per-row validation: email matches /^[^\s@]+@[^\s@]+\.[^\s@]+$/i (RFC 5322 lax mirroring Pydantic EmailStr lax), role normalized case-insensitively to one of the 3 enum values, name 0-100 chars defaulting to email split("@")[0] when empty. Hard cap 500: stop reading rows after the 500th valid row + push error message "Maximum 500 rows; remaining rows skipped".
    - Frontend2/lib/admin/csv-export.ts: export downloadCsv(url: string, filename?: string): void. Create document.createElement("a"), set href = url, set download = filename if provided, set style.display = "none", document.body.appendChild(a), a.click(), document.body.removeChild(a). NO fetch(), NO blob assembly.

    Step 6.5 — Build SHARED MoreMenu primitive (Frontend2/components/admin/shared/more-menu.tsx). Lift the dropdown pattern from Frontend2/components/projects/project-card.tsx lines 167-197 (existing icon-button dropdown). Export `MoreMenu({ items: MoreMenuItem[]; trigger?: React.ReactNode })` and `MoreMenuItem` interface `{ id: string; label: string; icon?: React.ReactNode; onClick: () => void; disabled?: boolean; destructive?: boolean }`. Trigger defaults to `<Button variant="ghost" size="icon">` with the `MoreHorizontal` lucide icon (matches prototype's per-row action menu in admin tables — `New_Frontend/src/pages/admin.jsx`). Click-outside dismiss via `useEffect` mousedown listener on `document`. Position: `position: absolute`, anchored bottom-right of trigger via parent `position: relative`. Z-index 50. Each menu item renders as a `<button>` with destructive items styled `color: var(--priority-critical)` and disabled items styled `opacity: 0.5; cursor: not-allowed`. ESC closes the menu. Add a Vitest RTL test (`Frontend2/components/admin/shared/more-menu.test.tsx`) covering: (a) renders trigger + closed menu; (b) click trigger opens menu with N items; (c) click an item fires its onClick AND closes menu; (d) destructive item has critical-color style; (e) click-outside closes menu; (f) ESC closes menu. This primitive is consumed by Plans 14-02, 14-03, 14-05, 14-06, 14-07 — building it in Wave 0 keeps Wave 2 surface plans purely consumer-side.

    Step 7 — Tests:
    - Frontend2/components/primitives/nav-tabs.test.tsx: render NavTabs with 8 tabs (Overview /admin, Users /admin/users, Roles /admin/roles, Permissions /admin/permissions, Projects /admin/projects, Workflows /admin/workflows, Audit /admin/audit, Stats /admin/stats). Mock usePathname returning each path in turn. Assert exactly one tab has fontWeight 600 + the primary border for each pathname. SPECIFICALLY assert that when pathname === "/admin/users" the Overview tab is NOT active (Pitfall 4). Use vi.mock("next/navigation", () => ({ usePathname: vi.fn() })) and vi.mock("next/link", () => ({ default: ({ href, children, ...rest }: any) => <a href={href} {...rest}>{children}</a> })).
    - Frontend2/components/projects/confirm-dialog.test.tsx: 4 cases. (1) tone undefined (default) renders confirm Button with no role icon AND text "Onayla" using primary variant — match prior snapshot semantics. (2) tone="danger" renders AlertTriangle icon + danger Button. (3) tone="warning" renders AlertCircle icon + primary Button (warning amber NOT applied to button). (4) Existing call sites not touched: import the unchanged Phase 10 ProjectArchiveBanner consumer (or any consumer) AND assert it renders without crash and without `tone` warnings.
  </action>
  <verify>
    <automated>cd Frontend2 && node -e "require('papaparse')" && npm run test -- --run nav-tabs.test.tsx confirm-dialog.test.tsx more-menu.test.tsx 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `Frontend2/package.json` contains `"papaparse": "^5.5.3"` (grep: `grep -c '"papaparse": "\^5\\.5\\.3"' Frontend2/package.json` returns 1)
    - `Frontend2/package.json` contains `"@types/papaparse": "^5.3.16"` (grep returns 1)
    - `Frontend2/node_modules/papaparse/package.json` exists (file read returns content)
    - `Frontend2/components/projects/confirm-dialog.tsx` contains the literal string `tone?: "primary" | "danger" | "warning"` (grep returns 1)
    - `Frontend2/components/projects/confirm-dialog.tsx` imports `AlertTriangle` AND `AlertCircle` from `"lucide-react"` (grep returns 2)
    - `Frontend2/components/primitives/nav-tabs.tsx` contains `"use client"` AND `usePathname` AND `import Link from "next/link"`
    - `Frontend2/components/primitives/nav-tabs.tsx` contains the active-detection special case `tab.href === "/admin"` AND `!tabs.some` (grep returns ≥1 each)
    - `Frontend2/components/primitives/modal.tsx` exports `Modal` AND `ModalHeader` AND `ModalBody` AND `ModalFooter` (grep -c 'export function Modal' returns ≥4)
    - `Frontend2/components/primitives/index.ts` contains `export { NavTabs }` AND `export { Modal, ModalHeader, ModalBody, ModalFooter }`
    - `Frontend2/lib/admin/permissions-static.ts` exports `getPermission` AND defines 14 permission rows × 4 role columns (grep `getPermission|"granted"|"denied"` shows ≥40 matches across 14 rows)
    - `Frontend2/lib/admin/audit-field-labels.ts` exports `getFieldLabel` AND contains keys `due_date` AND `priority` AND `assignee_id`
    - `Frontend2/lib/admin/csv-parse.ts` imports `Papa from "papaparse"` AND exports `parseBulkInviteCsv` AND `BulkInviteRow`
    - `Frontend2/lib/admin/csv-parse.ts` enforces 500-row cap (grep `500` returns ≥1)
    - `Frontend2/lib/admin/csv-export.ts` exports `downloadCsv` AND creates `document.createElement("a")` (grep returns 1)
    - `cd Frontend2 && npm run test -- --run nav-tabs.test.tsx` exits 0
    - `cd Frontend2 && npm run test -- --run confirm-dialog.test.tsx` exits 0
    - Test asserting Overview tab NOT active when pathname is /admin/users PASSES (grep nav-tabs.test.tsx for `/admin/users` AND `not.toHaveStyle` OR `not.toBe`)
    - `Frontend2/components/admin/shared/more-menu.tsx` contains `function MoreMenu(` (grep returns 1) AND exports the `MoreMenuItem` type (grep `export interface MoreMenuItem` OR `export type MoreMenuItem` returns 1)
    - `Frontend2/components/admin/shared/more-menu.tsx` registers a `document.addEventListener("mousedown"` listener for click-outside dismiss (grep returns 1)
    - `Frontend2/components/admin/shared/more-menu.tsx` styles destructive items with `var(--priority-critical)` (grep returns ≥1)
    - `cd Frontend2 && npm run test -- --run more-menu.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    papaparse@^5.5.3 + @types/papaparse@^5.3.16 installed in Frontend2/node_modules. ConfirmDialog accepts optional `tone` prop (backward-compat default "primary"). NavTabs primitive renders Link-based tabs with usePathname-driven active detection (special-case /admin Overview NOT active when on /admin/*). Modal primitive renders overlay + panel + ESC + click-outside dismiss with slot sub-components exported. Shared MoreMenu primitive (`Frontend2/components/admin/shared/more-menu.tsx`) ships in Wave 0 with click-outside dismiss + destructive-item styling and is ready for consumption by Plans 14-02 / 14-03 / 14-05 / 14-06 / 14-07. Admin lib utilities (permissions-static / audit-field-labels / csv-parse / csv-export) ship with the right shape. Barrel export updated. Unit tests for NavTabs (8-path active detection + Pitfall 4 case), ConfirmDialog (3 tone cases + backward-compat), and MoreMenu (open/close/click-outside/destructive) all pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend ProjectJoinRequest vertical slice — entity / repo ABC + impl + DI / migration 006 / 4 use cases / DTOs / admin_join_requests router + integration tests with in-memory fakes</name>
  <files>
    Backend/app/domain/entities/project_join_request.py,
    Backend/app/domain/repositories/project_join_request_repository.py,
    Backend/app/infrastructure/database/models/project_join_request.py,
    Backend/app/infrastructure/database/repositories/project_join_request_repo.py,
    Backend/app/api/deps/project_join_request.py,
    Backend/app/api/dependencies.py,
    Backend/app/application/dtos/project_join_request_dtos.py,
    Backend/app/application/use_cases/create_join_request.py,
    Backend/app/application/use_cases/approve_join_request.py,
    Backend/app/application/use_cases/reject_join_request.py,
    Backend/app/application/use_cases/list_pending_join_requests.py,
    Backend/app/api/v1/admin_join_requests.py,
    Backend/alembic/versions/006_phase14_admin_panel.py,
    Backend/tests/integration/test_create_join_request.py,
    Backend/tests/integration/test_approve_join_request.py
  </files>
  <read_first>
    Backend/app/domain/entities/password_reset_token.py (verbatim entity shape donor),
    Backend/app/domain/repositories/password_reset_repository.py (verbatim ABC shape — 3 abstract methods),
    Backend/app/domain/repositories/audit_repository.py lines 1-110 (signature pattern + Tuple[List, int] for paginated reads + create_with_metadata signature),
    Backend/app/infrastructure/database/models/password_reset_token.py (Mapped/mapped_column + ForeignKey ON DELETE shape),
    Backend/app/infrastructure/database/models/team.py (composite ON DELETE precedent),
    Backend/app/infrastructure/database/repositories/team_repo.py lines 12-115 (SqlAlchemyTeamRepository — _to_entity helper + add_member with IntegrityError handling + soft_delete shape),
    Backend/app/infrastructure/database/repositories/audit_repo.py lines 76-103 (create_with_metadata signature) AND lines 199-255 (get_global_activity for projection block reuse),
    Backend/app/api/deps/audit.py (DI factory verbatim — 18 lines),
    Backend/app/api/dependencies.py lines 22-39 (legacy shim re-export pattern),
    Backend/app/application/use_cases/get_global_activity.py (use case minimal wrapper shape),
    Backend/app/application/use_cases/manage_teams.py (member-add + audit emission pattern),
    Backend/app/application/use_cases/execute_phase_transition.py (multi-repo orchestration + audit emit + transactional intent),
    Backend/app/application/dtos/activity_dtos.py (ActivityItemDTO shape with metadata field nullable),
    Backend/app/api/v1/admin_settings.py (verbatim 33-line router — every handler Depends(require_admin)),
    Backend/app/api/v1/activity.py (admin endpoint pattern + type[] Query alias usage if needed),
    Backend/alembic/versions/005_phase9_schema.py lines 39-110 (idempotent helpers VERBATIM copy: _table_exists, _column_exists, _index_exists),
    Backend/tests/integration/test_user_activity.py lines 31-78 (FakePrivacyFilteredAuditRepo — in-memory fake pattern),
    Backend/tests/integration/test_activity.py lines 13-36 (authenticated_client(role="admin") fixture usage),
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-PATTERNS.md "Plan 14-01 — Backend Domain/Infrastructure/Application/API/Migration/Tests" lines 19-83,
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-RESEARCH.md §Common Op 2 lines 1037-1085 (verbatim entity + repo interface code),
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-CONTEXT.md decisions D-A1, D-A2, D-A8, D-W2, D-D2,
    CLAUDE.md §2 (Domain/Application/Infrastructure layer rules — DIP enforcement)
  </read_first>
  <behavior>
    - ProjectJoinRequest entity: pure Pydantic BaseModel + ConfigDict(from_attributes=True). Fields: id (Optional int), project_id (int), requested_by_user_id (int), target_user_id (int), status (Literal["pending","approved","rejected","cancelled"] default "pending"), note (Optional str), reviewed_by_admin_id (Optional int), reviewed_at (Optional datetime), created_at (Optional datetime), updated_at (Optional datetime). NO sqlalchemy imports. JoinRequestStatus exported as a re-usable Literal alias.
    - IProjectJoinRequestRepository: abc.ABC with 4 abstract methods — create(request) → ProjectJoinRequest, get_by_id(request_id) → Optional[ProjectJoinRequest], list_by_status(status, limit=50, offset=0) → Tuple[List[ProjectJoinRequest], int], update_status(request_id, status, reviewed_by_admin_id=None) → Optional[ProjectJoinRequest]. NO sqlalchemy imports.
    - SQLAlchemy model project_join_request.py: __tablename__="project_join_requests". Mapped columns: id (PK auto), project_id (FK projects.id ON DELETE CASCADE), requested_by_user_id (FK users.id ON DELETE SET NULL nullable=True), target_user_id (FK users.id ON DELETE SET NULL nullable=True), reviewed_by_admin_id (FK users.id ON DELETE SET NULL nullable=True), status (String(20) default 'pending'), note (Text nullable), reviewed_at (DateTime nullable), created_at (DateTime server_default=func.now()), updated_at (DateTime server_default=func.now() onupdate=func.now()). Indexes: ix_project_join_requests_status_created on (status, created_at DESC).
    - SqlAlchemyProjectJoinRequestRepository: implements 4 ABC methods. _to_entity(model) → ProjectJoinRequest.model_validate(model). create commits + refreshes + returns mapped entity. list_by_status SELECT + COUNT in single async session, returns (items, total). update_status reads model, mutates status + reviewed_by_admin_id + reviewed_at=now() if approving/rejecting, commits.
    - DI factory deps/project_join_request.py: identical shape to deps/audit.py (18 lines). Re-exported from app/api/dependencies.py legacy shim.
    - Alembic 006: copy _table_exists + _index_exists from 005 verbatim. upgrade(): if not _table_exists("project_join_requests"): op.create_table with all columns + FKs + indexes. downgrade(): drop_index + drop_table.
    - DTOs project_join_request_dtos.py: CreateJoinRequestDTO (project_id int, target_user_id int, note Optional str), JoinRequestProjectDTO (id, key, name), JoinRequestUserDTO (id, email, full_name Optional, avatar_url Optional), JoinRequestResponseDTO (id, project nested, requested_by nested, target_user nested, status Literal, note Optional, created_at), JoinRequestListDTO (items List + total int).
    - Use cases (4): CreateJoinRequestUseCase(repo, audit_repo, project_repo) — execute(project_id, requested_by_user_id, target_user_id, note) creates entity + emits audit project_join_request.created with metadata {project_id, project_key, project_name, requested_by_user_id, target_user_id}. ApproveJoinRequestUseCase(repo, audit_repo, team_repo) — execute(request_id, approving_admin_id) fetches request, calls update_status("approved", reviewed_by=admin), team_repo.add_member(team_id, target_user_id) (idempotent — IntegrityError swallowed). If add_member fails for non-IntegrityError, rollback status update. Emits audit project_join_request.approved. RejectJoinRequestUseCase symmetric — no team_repo. ListPendingJoinRequestsUseCase wraps repo.list_by_status("pending", limit, offset) → JoinRequestListDTO.
    - Admin router admin_join_requests.py: 4 endpoints. GET /admin/join-requests?status=pending&limit=50&offset=0 (Depends(require_admin)) → JoinRequestListDTO. POST /admin/join-requests/{id}/approve (Depends(require_admin)) → JoinRequestResponseDTO. POST /admin/join-requests/{id}/reject (Depends(require_admin)) → JoinRequestResponseDTO. POST /projects/{project_id}/join-requests (uses Phase 9 require_project_transition_authority — PM-side create) → JoinRequestResponseDTO. All 4 handlers inject deps via DI factories.
    - Integration tests use in-memory fakes (Phase 12 D-09). Tests assert: create + audit emission with correct metadata fields; approve calls team_repo.add_member; approve atomic rollback on team_repo failure; list_by_status filter respected; pagination tuple shape (items, total).
  </behavior>
  <action>
    Step 1 — Domain entity (Backend/app/domain/entities/project_join_request.py). Use the verbatim code from RESEARCH §Common Op 2 lines 1037-1058:
    ```python
    from __future__ import annotations
    from pydantic import BaseModel, ConfigDict
    from typing import Optional, Literal
    from datetime import datetime

    JoinRequestStatus = Literal["pending", "approved", "rejected", "cancelled"]


    class ProjectJoinRequest(BaseModel):
        id: Optional[int] = None
        project_id: int
        requested_by_user_id: int
        target_user_id: int
        status: JoinRequestStatus = "pending"
        note: Optional[str] = None
        reviewed_by_admin_id: Optional[int] = None
        reviewed_at: Optional[datetime] = None
        created_at: Optional[datetime] = None
        updated_at: Optional[datetime] = None

        model_config = ConfigDict(from_attributes=True)
    ```
    NO sqlalchemy imports — verify with `grep -c "sqlalchemy" Backend/app/domain/entities/project_join_request.py` returns 0.

    Step 2 — Repository ABC (Backend/app/domain/repositories/project_join_request_repository.py). Verbatim from RESEARCH §Common Op 2 lines 1063-1085. NO sqlalchemy imports.

    Step 3 — SQLAlchemy model (Backend/app/infrastructure/database/models/project_join_request.py). Mirror models/password_reset_token.py shape but with FKs to projects (CASCADE) and users (SET NULL). Include __tablename__ = "project_join_requests". Add Index("ix_project_join_requests_status_created", "status", text("created_at DESC")) inside __table_args__.

    Step 4 — Repository impl (Backend/app/infrastructure/database/repositories/project_join_request_repo.py). Mirror team_repo.py pattern. _to_entity returns ProjectJoinRequest.model_validate(model). create(): session.add + flush + refresh + return _to_entity. get_by_id(): select(ProjectJoinRequestModel).where(id=request_id). list_by_status(): tuple of items_query + count_query in same session, run in parallel via asyncio.gather. update_status(): read model, mutate, commit, return _to_entity.

    Step 5 — DI factory (Backend/app/api/deps/project_join_request.py). Copy verbatim from PATTERNS.md EXCERPT-B lines 561-582. Append `from app.api.deps.project_join_request import get_project_join_request_repo  # noqa: F401` to Backend/app/api/dependencies.py at the bottom of the existing block (after admin_settings shim).

    Step 6 — Alembic migration (Backend/alembic/versions/006_phase14_admin_panel.py). Copy revision header pattern from 005_phase9_schema.py lines 1-15 (revision, down_revision='005_phase9_schema', branch_labels, depends_on). Copy _table_exists + _index_exists helpers VERBATIM from 005 lines 39-97 (DO NOT regenerate — these are battle-tested across phases 1-9). upgrade():
    ```python
    if not _table_exists("project_join_requests"):
        op.create_table(
            "project_join_requests",
            sa.Column("id", sa.Integer, primary_key=True, autoincrement=True),
            sa.Column("project_id", sa.Integer, sa.ForeignKey("projects.id", ondelete="CASCADE"), nullable=False),
            sa.Column("requested_by_user_id", sa.Integer, sa.ForeignKey("users.id", ondelete="SET NULL"), nullable=True),
            sa.Column("target_user_id", sa.Integer, sa.ForeignKey("users.id", ondelete="SET NULL"), nullable=True),
            sa.Column("status", sa.String(20), nullable=False, server_default="pending"),
            sa.Column("note", sa.Text, nullable=True),
            sa.Column("reviewed_by_admin_id", sa.Integer, sa.ForeignKey("users.id", ondelete="SET NULL"), nullable=True),
            sa.Column("reviewed_at", sa.DateTime(timezone=True), nullable=True),
            sa.Column("created_at", sa.DateTime(timezone=True), server_default=sa.func.now(), nullable=False),
            sa.Column("updated_at", sa.DateTime(timezone=True), server_default=sa.func.now(), onupdate=sa.func.now(), nullable=False),
        )
    if not _index_exists("ix_project_join_requests_status_created"):
        op.create_index("ix_project_join_requests_status_created", "project_join_requests", ["status", sa.text("created_at DESC")])
    ```
    downgrade(): drop_index + drop_table (with try/except IF EXISTS for idempotency).

    Step 7 — DTOs (Backend/app/application/dtos/project_join_request_dtos.py). Inline submodels (recommended per PATTERNS.md). Pydantic BaseModel + ConfigDict(from_attributes=True). Snake_case fields throughout.

    Step 8 — Use cases (4 separate files). Each is a class with __init__(repo, ...) and async execute(...). NO sqlalchemy imports — verify via `grep -c "sqlalchemy\\|app\\.infrastructure" Backend/app/application/use_cases/create_join_request.py approve_join_request.py reject_join_request.py list_pending_join_requests.py` returns 0 for all 4.

    For ApproveJoinRequestUseCase, use this orchestration:
    ```python
    async def execute(self, request_id: int, approving_admin_id: int) -> ProjectJoinRequest:
        req = await self.repo.get_by_id(request_id)
        if req is None:
            raise NotFoundError(f"join request {request_id} not found")
        if req.status != "pending":
            raise InvalidStateError(f"join request {request_id} is {req.status}")
        # 1) flip status
        updated = await self.repo.update_status(request_id, "approved", reviewed_by_admin_id=approving_admin_id)
        # 2) add to team — idempotent in team_repo.add_member (IntegrityError swallowed)
        try:
            team = await self.team_repo.get_team_for_project(req.project_id)
            if team is not None:
                await self.team_repo.add_member(team.id, req.target_user_id)
        except Exception as e:
            # rollback status to pending
            await self.repo.update_status(request_id, "pending", reviewed_by_admin_id=None)
            raise
        # 3) audit
        project = await self.project_repo.get_by_id(req.project_id)
        await self.audit_repo.create_with_metadata(
            entity_type="project_join_request",
            entity_id=request_id,
            action="approved",
            user_id=approving_admin_id,
            metadata={
                "project_id": req.project_id,
                "project_key": project.key if project else None,
                "project_name": project.name if project else None,
                "target_user_id": req.target_user_id,
                "requested_by_admin_id": approving_admin_id,
            },
        )
        return updated
    ```

    Step 9 — Router (Backend/app/api/v1/admin_join_requests.py). Mirror admin_settings.py shape exactly. APIRouter() module-level. 4 handlers, each with `admin: User = Depends(require_admin)` (or for the PM-side create endpoint use `require_project_transition_authority`). Inject DI via `Depends(get_project_join_request_repo)` etc.

    Step 10 — Integration tests:
    - Backend/tests/integration/test_create_join_request.py: FakeProjectJoinRequestRepository, FakeAuditRepository, FakeProjectRepository (in-memory). Test happy path — create writes to repo + emits audit with project metadata fields. Assert audit_repo.last_call.metadata["project_key"] == "TEST".
    - Backend/tests/integration/test_approve_join_request.py: Add FakeTeamRepository. Test (a) approve flips status + calls team_repo.add_member + emits audit; (b) approve when team_repo.add_member raises non-IntegrityError → rollback to status "pending" (atomic intent assertion).
  </action>
  <verify>
    <automated>cd Backend && python -m pytest -q tests/integration/test_create_join_request.py tests/integration/test_approve_join_request.py 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `Backend/app/domain/entities/project_join_request.py` contains `class ProjectJoinRequest(BaseModel)` AND `JoinRequestStatus = Literal["pending"`
    - `grep -c "sqlalchemy\|app.infrastructure" Backend/app/domain/entities/project_join_request.py` returns 0 (DIP)
    - `grep -c "sqlalchemy\|app.infrastructure" Backend/app/domain/repositories/project_join_request_repository.py` returns 0 (DIP)
    - `Backend/app/domain/repositories/project_join_request_repository.py` contains `class IProjectJoinRequestRepository(ABC)` AND 4 `@abstractmethod` decorators (grep -c "@abstractmethod" returns 4)
    - `grep -c "import sqlalchemy\|from app.infrastructure" Backend/app/application/use_cases/create_join_request.py Backend/app/application/use_cases/approve_join_request.py Backend/app/application/use_cases/reject_join_request.py Backend/app/application/use_cases/list_pending_join_requests.py` returns 0 (DIP)
    - `Backend/app/infrastructure/database/models/project_join_request.py` contains `__tablename__ = "project_join_requests"` AND `ondelete="CASCADE"` AND `ondelete="SET NULL"`
    - `Backend/alembic/versions/006_phase14_admin_panel.py` contains `_table_exists("project_join_requests")` AND `op.create_table` AND `ix_project_join_requests_status_created`
    - `Backend/alembic/versions/006_phase14_admin_panel.py` does NOT contain RBAC table creation: `grep -c "roles\\|permissions" Backend/alembic/versions/006_phase14_admin_panel.py` returns 0 (D-A2)
    - `Backend/app/api/v1/admin_join_requests.py` contains 4 handler decorators (grep `@router.(get|post)` returns ≥4) AND each handler has `Depends(require_admin)` OR `Depends(require_project_transition_authority)`
    - `Backend/app/api/dependencies.py` ends with `get_project_join_request_repo  # noqa: F401`
    - `cd Backend && python -m pytest -q tests/integration/test_create_join_request.py` exits 0 (test count > 0)
    - `cd Backend && python -m pytest -q tests/integration/test_approve_join_request.py` exits 0 (test count > 0)
    - Atomic rollback test in test_approve_join_request.py asserts status flips back to "pending" when team_repo raises (grep test file for `pending` after `add_member` mock raise)
  </acceptance_criteria>
  <done>
    Backend ProjectJoinRequest vertical slice complete. Domain entity + ABC are pure (DIP enforced). Migration 006 idempotent + RBAC-table-free. SqlAlchemy model has correct FK semantics (CASCADE for project, SET NULL for users). 4 use cases + 4 router endpoints honor Clean Architecture. Approve use case orchestrates status update + team_repo.add_member + audit emission with atomic rollback intent. Integration tests with in-memory fakes assert happy path AND rollback semantics. Wave 2 plans can now consume the join-request endpoints from the frontend.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Backend admin user/audit/stats/summary slices — DTOs / 9 use cases / 4 routers / Backend config TTL / 4 integration tests / register routers in main.py + alembic upgrade head</name>
  <files>
    Backend/app/domain/repositories/audit_repository.py,
    Backend/app/infrastructure/database/repositories/audit_repo.py,
    Backend/app/application/dtos/admin_user_dtos.py,
    Backend/app/application/dtos/admin_audit_dtos.py,
    Backend/app/application/dtos/admin_stats_dtos.py,
    Backend/app/application/use_cases/invite_user.py,
    Backend/app/application/use_cases/bulk_invite_user.py,
    Backend/app/application/use_cases/deactivate_user.py,
    Backend/app/application/use_cases/reset_user_password.py,
    Backend/app/application/use_cases/change_user_role.py,
    Backend/app/application/use_cases/bulk_action_user.py,
    Backend/app/application/use_cases/get_global_audit.py,
    Backend/app/application/use_cases/get_admin_stats.py,
    Backend/app/application/use_cases/generate_admin_summary_pdf.py,
    Backend/app/api/v1/admin_users.py,
    Backend/app/api/v1/admin_audit.py,
    Backend/app/api/v1/admin_stats.py,
    Backend/app/api/v1/admin_summary.py,
    Backend/app/api/main.py,
    Backend/app/core/config.py,
    Backend/tests/integration/test_admin_users_crud.py,
    Backend/tests/integration/test_admin_audit_get_global.py,
    Backend/tests/integration/test_admin_stats.py,
    Backend/tests/integration/test_generate_admin_summary_pdf.py
  </files>
  <read_first>
    Backend/app/domain/repositories/audit_repository.py (existing 11 abstract methods — extend with get_global_audit AFTER existing block),
    Backend/app/infrastructure/database/repositories/audit_repo.py lines 70-110 (create_with_metadata signature + extra_metadata["key"].astext JSONB syntax) AND lines 199-275 (get_global_activity full signature + projection block to clone for get_global_audit),
    Backend/app/application/dtos/auth_dtos.py (EmailStr + Pydantic password validators existing pattern),
    Backend/app/application/dtos/activity_dtos.py (ActivityItemDTO snake_case shape with metadata: Optional[Dict] field nullable),
    Backend/app/application/dtos/chart_dtos.py (composite payload pattern reference for AdminStatsResponseDTO),
    Backend/app/application/use_cases/request_password_reset.py (token creation + email send analog for invite_user + reset_user_password),
    Backend/app/application/use_cases/get_user_summary.py (asyncio.gather of 3 independent reads — analog for get_admin_stats),
    Backend/app/application/use_cases/manage_teams.py (member-add + audit emission pattern),
    Backend/app/application/use_cases/manage_projects.py (single field update + audit emit pattern),
    Backend/app/application/use_cases/update_user_profile.py (single field flip pattern for deactivate_user),
    Backend/app/api/v1/admin_settings.py (verbatim 33-line router shape — every handler Depends(require_admin)),
    Backend/app/api/v1/activity.py (admin endpoint pattern + Optional date Query + paginated DTO),
    Backend/app/api/v1/reports.py lines 180-256 (PDF StreamingResponse + Content-Disposition + rate-limit decorator),
    Backend/app/api/main.py lines 156-180 (existing router include block — append 4 NEW admin routers after admin_settings_router_module),
    Backend/app/core/config.py (current Settings shape — extend with INVITE_TOKEN_TTL_DAYS = 7),
    Backend/tests/integration/test_user_activity.py lines 31-100 (FakePrivacyFilteredAuditRepo — in-memory fakes pattern),
    Backend/tests/integration/test_activity.py lines 13-60 (authenticated_client(role="admin") fixture + admin/member/anonymous status code matrix),
    Backend/tests/integration/test_charts.py (composite read with multiple sub-queries pattern for test_admin_stats),
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-PATTERNS.md "Plan 14-01 — Backend Application/API/Tests" lines 36-83,
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-RESEARCH.md §Pattern 5 lines 750-807 (verbatim admin_audit.py router skeleton) AND §Pitfall 6 lines 916-924 (50k cap pattern) AND §Pitfall 7-8 lines 925-944 (JSONB syntax + extra_metadata column-attr mismatch) AND §Don't Hand-Roll lines 822-833,
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-CONTEXT.md decisions D-A6, D-A7, D-A8, D-B2 through D-B8, D-W3, D-X1 through D-X4, D-Z2,
    CLAUDE.md §2 + §4 (Clean Architecture + DIP)
  </read_first>
  <behavior>
    - audit_repository.py ABC EXTENDED with get_global_audit(date_from, date_to, actor_id, action_prefix, limit=50, offset=0) → Tuple[List[dict], int, bool] — third element is `truncated` flag (True when actual count > 50000).
    - audit_repo.py impl extends with get_global_audit. WHERE clauses applied dynamically. action_prefix uses `AuditLogModel.action.like(f"{prefix}%")`. Cap items returned at 50000; total = min(actual_count, 50000); truncated = actual_count > 50000.
    - admin_user_dtos.py: InviteUserRequestDTO (email EmailStr, role Literal["Admin","Project Manager","Member"], name Optional str, team_id Optional int), InviteUserResponseDTO (user_id int, email str, invite_token_expires_at datetime), BulkInviteRowDTO (email EmailStr, name Optional str, role Literal[...]), BulkInviteRequestDTO (rows: List[BulkInviteRowDTO] = Field(max_items=500) — D-B4 server-side cap), BulkInviteResponseDTO (successful: List[InviteUserResponseDTO], failed: List[{row_number, email, errors}]), RoleChangeRequestDTO (role: Literal[...]), BulkActionRequestDTO (user_ids: List[int] = Field(max_items=500), action: Literal["deactivate","role_change"], payload: Optional[dict]), DeactivateRequestDTO (no body — toggle).
    - admin_audit_dtos.py: AdminAuditItemDTO mirrors ActivityItemDTO shape (entity_type, action, field_name, old_value, new_value, user_id, user_name, timestamp, metadata: Optional[Dict[str, Any]]). AdminAuditResponseDTO {items: List[AdminAuditItemDTO], total: int, truncated: bool}.
    - admin_stats_dtos.py: ActiveUsersTrendPointDTO {date: date, count: int}, ProjectVelocityDTO {project_id, key, name, progress: float, velocity_history: List[float]}, AdminStatsResponseDTO {active_users_trend: List[ActiveUsersTrendPointDTO], methodology_distribution: Dict[str, int], project_velocities: List[ProjectVelocityDTO]}.
    - 9 use cases pure DIP — NO sqlalchemy imports. invite_user creates User(is_active=False) + random hashed password + PasswordResetToken (TTL from config.INVITE_TOKEN_TTL_DAYS) + Phase 5 invite email + audit user.invited. bulk_invite iterates rows, calls invite_user per row inside try/except, returns {successful, failed}. deactivate_user toggles users.is_active + audit user.deactivated/activated. reset_user_password mirrors request_password_reset (24h TTL) + audit auth.password_reset_requested with metadata.requested_by_admin_id. change_user_role validates role enum + updates users.role_id via roles.name lookup + audit user.role_changed. bulk_action_user per-user transaction (D-B7) + per-user audit. get_global_audit wraps repo + returns AdminAuditResponseDTO with truncated flag. get_admin_stats asyncio.gather 3 reads (active_users_trend / methodology_distribution / project_velocities top-30) → AdminStatsResponseDTO. generate_admin_summary_pdf reuses Phase 12 fpdf2 (composes 1-page summary: user count + delta, project count, top 5 most-active projects, top 5 most-active users) → BytesIO.
    - admin_users.py router: 7 endpoints, each `Depends(require_admin)`. POST /admin/users (invite), PATCH /admin/users/{id}/role, PATCH /admin/users/{id}/deactivate, POST /admin/users/{id}/password-reset, POST /admin/users/bulk-invite, POST /admin/users/bulk-action, GET /admin/users.csv (StreamingResponse + UTF-8 BOM + Content-Disposition).
    - admin_audit.py router: 2 endpoints. GET /admin/audit (limit ge=1 le=100 default 50, offset ge=0 default 0) → AdminAuditResponseDTO. GET /admin/audit.json → StreamingResponse JSON-array (max 50k rows hard cap server-side).
    - admin_stats.py router: 1 endpoint GET /admin/stats → AdminStatsResponseDTO (require_admin).
    - admin_summary.py router: 1 endpoint GET /admin/summary.pdf → StreamingResponse application/pdf + rate-limited @limiter.limit("1/30seconds") (D-B6).
    - main.py: append 5 router includes after admin_settings_router_module — admin_join_requests (from Task 2), admin_users, admin_audit, admin_stats, admin_summary — all with prefix /api/v1 and tag "Admin".
    - core/config.py: add `INVITE_TOKEN_TTL_DAYS: int = 7` field to Settings class (Pydantic BaseSettings reads from env; default 7 — see RESEARCH §Runtime State Inventory line 850).
    - 4 integration tests use in-memory fakes:
      - test_admin_users_crud.py: 7 endpoints × 3 access classes (admin-200, member-403, anonymous-401). Bulk-invite happy path + bad-CSV row mix (3 valid + 2 invalid → assert successful=3 failed=2).
      - test_admin_audit_get_global.py: filter by date_from/date_to, actor_id, action_prefix → assert hits. **Pitfall 6**: seed > 50k rows in fake repo → assert response.truncated=True AND total=50000. **Pitfall 7**: seed audit row with extra_metadata={"project_id": 5}, filter on project_id → assert hit count > 0.
      - test_admin_stats.py: assert composite shape — 3 keys + correct types + project_velocities ≤ 30.
      - test_generate_admin_summary_pdf.py: assert response Content-Type application/pdf + body starts with %PDF + Content-Disposition contains attachment.
    - Run `cd Backend && alembic upgrade head` AFTER migration file is written (Task 2) AND before integration tests in this task run. (Task 2 creates the migration; this task is BLOCKING-gated to apply it.)
  </behavior>
  <action>
    Step 1 — Extend audit_repository.py ABC. Add the new abstract method AFTER the existing block (around line 90+, after get_global_activity):
    ```python
    @abstractmethod
    async def get_global_audit(
        self,
        date_from: Optional[datetime] = None,
        date_to: Optional[datetime] = None,
        actor_id: Optional[int] = None,
        action_prefix: Optional[str] = None,
        limit: int = 50,
        offset: int = 0,
    ) -> Tuple[List[dict], int, bool]:
        """Admin-wide audit retrieval. Returns (items, total, truncated).
        truncated=True when actual count > 50000 (D-Z2 50k hard cap)."""
        ...
    ```

    Step 2 — Extend audit_repo.py impl with get_global_audit. Mirror get_global_activity (lines 199-255) for projection block. Apply WHERE clauses dynamically using the existing `astext` JSONB syntax for metadata filters (line 71 reference). Build separate count_query without LIMIT/OFFSET. After fetching count, compute `truncated = (count > 50000); capped_total = min(count, 50000); items = items[:50000]`.

    Step 3 — Write all 3 DTO files (admin_user_dtos.py, admin_audit_dtos.py, admin_stats_dtos.py) per behavior contract above. Use Pydantic v2 syntax: `Field(max_items=500)` for list caps; `Literal["Admin","Project Manager","Member"]` for role enum.

    Step 4 — Write all 9 use cases. Strict DIP: each takes interface params via __init__, NO sqlalchemy imports. invite_user creates User(is_active=False) with bcrypt-hashed random password (use existing security.hash_password from app.api.deps.security) + PasswordResetToken via password_reset_repo.create with expires_at = now + timedelta(days=settings.INVITE_TOKEN_TTL_DAYS) + email_service.send_invite(email, set_password_url with token) + audit_repo.create_with_metadata("user", new_user.id, "invited", admin.id, metadata={"user_id": new_user.id, "user_email": email, "target_role": role, "requested_by_admin_id": admin.id}).

    For bulk_invite_user, iterate rows with per-row try/except calling invite_user.execute. Collect (row_number, email, errors_list) for failures. Return BulkInviteResponseDTO. Audit row per successful invite (NOT per batch — D-A8 / D-B7).

    For get_admin_stats:
    ```python
    async def execute(self) -> AdminStatsResponseDTO:
        trend, methodology, velocities = await asyncio.gather(
            self.audit_repo.active_users_trend(days=30),
            self.project_repo.methodology_distribution(),
            self._fetch_velocities_top_30(),
        )
        return AdminStatsResponseDTO(
            active_users_trend=trend,
            methodology_distribution=methodology,
            project_velocities=velocities,
        )
    ```
    The _fetch_velocities_top_30 helper iterates project_repo.list_recent_projects(limit=30) + per-project get_iteration aggregation (Phase 13 GetProjectIterationUseCase reuse). Document scaling cliff in code comment per D-X2.

    For generate_admin_summary_pdf: copy Phase 12 fpdf2 imports + UNICODE_FONT + A4 layout from Backend/app/api/v1/reports.py lines 180-247. Compose 1-page admin summary per CONTEXT D-B6 / Discretion: section 1 (User counts: total, +N this month, role split), section 2 (Active project count + total project count), section 3 (Top 5 most-active projects by audit_log entry count last 30d), section 4 (Top 5 most-active users by audit_log entry count last 30d). Returns BytesIO.

    Step 5 — Write 4 admin routers. Mirror admin_settings.py shape exactly. CSV export uses StreamingResponse with UTF-8 BOM (`io.BytesIO(b"\\xef\\xbb\\xbf" + csv_bytes)`). PDF endpoint uses `@limiter.limit("1/30seconds")` from slowapi (existing in reports.py).

    Step 6 — Update Backend/app/api/main.py. Append after the existing admin_settings router include:
    ```python
    from app.api.v1 import admin_join_requests, admin_users, admin_audit, admin_stats, admin_summary
    api_router.include_router(admin_join_requests.router, prefix="/api/v1", tags=["Admin"])
    api_router.include_router(admin_users.router, prefix="/api/v1", tags=["Admin"])
    api_router.include_router(admin_audit.router, prefix="/api/v1", tags=["Admin"])
    api_router.include_router(admin_stats.router, prefix="/api/v1", tags=["Admin"])
    api_router.include_router(admin_summary.router, prefix="/api/v1", tags=["Admin"])
    ```

    Step 7 — Extend Backend/app/core/config.py Settings class. Add INVITE_TOKEN_TTL_DAYS: int = 7.

    Step 8 — RUN MIGRATION (BLOCKING). After all files in Tasks 2 + 3 are written, run:
    ```bash
    cd Backend && alembic upgrade head
    ```
    Verify: `cd Backend && python -c "from sqlalchemy import inspect; from app.infrastructure.database.database import sync_engine; print('project_join_requests' in inspect(sync_engine).get_table_names())"` returns `True` OR `psql -d "$DATABASE_URL" -c "\\d project_join_requests"` returns the table definition. This is the equivalent of the TS schema-push gate per planning context. Without alembic upgrade head, integration tests in this task and Plan 14-12 (E2E) would fail because the table does not exist.

    Step 9 — Write 4 integration tests:
    - test_admin_users_crud.py: parametrize 7 endpoints. For each, run as admin (assert 200), member (assert 403), anonymous (assert 401). Add bulk-invite happy + mixed (3 valid + 2 invalid) test asserting BulkInviteResponseDTO.successful length 3 and failed length 2.
    - test_admin_audit_get_global.py: feed in-memory FakeAuditRepository with 60000 rows. Assert get_global_audit response has truncated=True AND total == 50000. Test filter scenarios: date range slice, actor_id slice, action_prefix slice ("task." returning task.* rows only). Test JSONB filter via metadata["project_id"].astext when seeded fake supports it (skip if fake doesn't honor JSONB).
    - test_admin_stats.py: feed minimal fake project + audit data. Assert response keys ["active_users_trend","methodology_distribution","project_velocities"] all present, types match, project_velocities length ≤ 30.
    - test_generate_admin_summary_pdf.py: HTTP GET /api/v1/admin/summary.pdf as admin. Assert response.headers["content-type"] starts with "application/pdf"; response.headers["content-disposition"] contains "attachment"; response.content[:4] == b"%PDF".
  </action>
  <verify>
    <automated>cd Backend && alembic upgrade head 2>&1 | tail -5 && python -m pytest -q tests/integration/test_admin_users_crud.py tests/integration/test_admin_audit_get_global.py tests/integration/test_admin_stats.py tests/integration/test_generate_admin_summary_pdf.py 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd Backend && alembic upgrade head` exits 0
    - `psql -c "\\d project_join_requests"` returns table definition (or `python -c "from sqlalchemy import inspect; from app.infrastructure.database.database import sync_engine; assert 'project_join_requests' in inspect(sync_engine).get_table_names()"` exits 0)
    - `Backend/app/domain/repositories/audit_repository.py` contains `async def get_global_audit` (grep returns 1)
    - `Backend/app/infrastructure/database/repositories/audit_repo.py` contains `truncated = (` AND `min(count, 50000)` OR equivalent 50k cap logic
    - `grep -c "import sqlalchemy\|from app.infrastructure" Backend/app/application/use_cases/invite_user.py Backend/app/application/use_cases/bulk_invite_user.py Backend/app/application/use_cases/deactivate_user.py Backend/app/application/use_cases/reset_user_password.py Backend/app/application/use_cases/change_user_role.py Backend/app/application/use_cases/bulk_action_user.py Backend/app/application/use_cases/get_global_audit.py Backend/app/application/use_cases/get_admin_stats.py Backend/app/application/use_cases/generate_admin_summary_pdf.py` returns 0
    - `Backend/app/application/dtos/admin_user_dtos.py` contains `Field(max_items=500)` AND `Literal["Admin"` (D-B4 server-side cap)
    - `Backend/app/api/v1/admin_users.py` has 7 handlers (grep `@router.(get|post|patch)` returns 7) AND every handler line includes `Depends(require_admin)`
    - `Backend/app/api/v1/admin_audit.py` has 2 handlers AND `limit: int = Query(default=50, ge=1, le=100)` AND `Depends(require_admin)`
    - `Backend/app/api/v1/admin_summary.py` contains `@limiter.limit("1/30seconds")` (D-B6)
    - `Backend/app/api/v1/admin_users.py` GET /admin/users.csv contains `\\xef\\xbb\\xbf` UTF-8 BOM literal
    - `Backend/app/api/main.py` includes 5 NEW admin routers (grep `include_router.*admin_(join_requests|users|audit|stats|summary)` returns 5)
    - `Backend/app/core/config.py` contains `INVITE_TOKEN_TTL_DAYS` (grep returns 1)
    - `cd Backend && python -m pytest -q tests/integration/test_admin_users_crud.py` exits 0; test class includes admin-200/member-403/anonymous-401 assertions for each endpoint
    - `cd Backend && python -m pytest -q tests/integration/test_admin_audit_get_global.py` exits 0; truncated=True assertion for >50k case present
    - `cd Backend && python -m pytest -q tests/integration/test_admin_stats.py` exits 0
    - `cd Backend && python -m pytest -q tests/integration/test_generate_admin_summary_pdf.py` exits 0
  </acceptance_criteria>
  <done>
    Backend admin user/audit/stats/summary endpoints fully wired. 9 use cases honor Clean Architecture DIP. Audit repo extended with get_global_audit + 50k truncated flag (Pitfall 6). PDF endpoint reuses Phase 12 fpdf2 with 30s rate limit. CSV export ships UTF-8 BOM. Bulk invite enforces 500-row server cap. Admin routers registered in main.py. Backend config has INVITE_TOKEN_TTL_DAYS. **Alembic migration 006 has been applied (alembic upgrade head exited 0)** — project_join_requests table exists in DB. 4 integration test files cover happy paths + access matrix (admin/member/anonymous) + Pitfall 6 (50k cap) + Pitfall 7 (JSONB filter syntax).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Frontend admin services + 14 hooks (TanStack Query staleTime + optimistic update on approve/reject) + service unit tests</name>
  <files>
    Frontend2/services/admin-join-request-service.ts,
    Frontend2/services/admin-user-service.ts,
    Frontend2/services/admin-audit-service.ts,
    Frontend2/services/admin-stats-service.ts,
    Frontend2/hooks/use-pending-join-requests.ts,
    Frontend2/hooks/use-approve-join-request.ts,
    Frontend2/hooks/use-reject-join-request.ts,
    Frontend2/hooks/use-admin-users.ts,
    Frontend2/hooks/use-invite-user.ts,
    Frontend2/hooks/use-bulk-invite.ts,
    Frontend2/hooks/use-deactivate-user.ts,
    Frontend2/hooks/use-reset-password.ts,
    Frontend2/hooks/use-change-role.ts,
    Frontend2/hooks/use-bulk-action.ts,
    Frontend2/hooks/use-admin-audit.ts,
    Frontend2/hooks/use-admin-stats.ts,
    Frontend2/services/admin-join-request-service.test.ts,
    Frontend2/hooks/use-approve-join-request.test.tsx
  </files>
  <read_first>
    Frontend2/lib/api-client.ts (apiClient axios wrapper),
    Frontend2/services/profile-service.ts (snake → camel mapper pattern lines 56-71 + service object lines 116-166 — verbatim shape donor for admin-* services),
    Frontend2/services/activity-service.ts (buildActivityQuery manual-pair builder + paginated response — analog for admin-audit-service),
    Frontend2/services/chart-service.ts (composite payload reader + snake → camel for nested objects — analog for admin-stats-service),
    Frontend2/hooks/use-projects.ts lines 6-69 (useProjects + useProjectMembers + useUpdateProjectStatus + useCreateProject — analog for admin hooks),
    Frontend2/hooks/use-user-summary.ts (minimal useQuery + staleTime — analog for use-pending-join-requests),
    Frontend2/hooks/use-user-activity.ts (filter param pattern + refetchOnWindowFocus — analog for use-admin-audit),
    Frontend2/components/toast/* (useToast hook usage),
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-PATTERNS.md "Plan 14-01 — Frontend Services + Hooks" lines 86-104 + Shared S3, S4 lines 320-368,
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-RESEARCH.md §Common Op 1 lines 970-1031 (verbatim adminJoinRequestService + usePendingJoinRequests) + §Pattern 4 lines 689-747 (verbatim useInviteUser),
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-CONTEXT.md decisions D-W1 (staleTime 30s admin / 60s stats), D-W2 (optimistic updates limited to bulk + approve/reject), D-W3 (CSV via server-side endpoint)
  </read_first>
  <behavior>
    - 4 services follow profile-service.ts pattern. Snake_case backend DTOs are mapped to camelCase domain shapes via inline mapper helpers. EXCEPTION: audit_log.metadata payload stays snake_case all the way to activity-row.tsx (Pitfall 2, S3 caveat) — DO NOT camelCase the audit metadata dictionary.
    - admin-join-request-service: listPending(limit, offset) → {items: PendingJoinRequest[], total}; approve(id) → void; reject(id) → void.
    - admin-user-service: invite(req), bulkInvite(req), deactivate(id), resetPassword(id), changeRole(id, role), bulkAction({user_ids, action, payload}), list({role, status, q, limit, offset}), exportCsv() (returns "/api/v1/admin/users.csv" string for downloadCsv to consume — NO data fetched in browser).
    - admin-audit-service: list(filter: AdminAuditFilter) → {items: AdminAuditItem[], total, truncated}; exportJsonUrl(filter): string returns "/api/v1/admin/audit.json?from=...&to=...&actor_id=...&action_prefix=...".
    - admin-stats-service: getStats() returns {activeUsersTrend, methodologyDistribution, projectVelocities} with snake → camel applied to top-level keys + nested project_velocities[].velocity_history etc.
    - 14 hooks follow use-projects.ts + Phase 13 use-user-* patterns:
      - usePendingJoinRequests(limit=5): useQuery, queryKey ["admin","join-requests","pending",{limit}], staleTime 30000, refetchOnWindowFocus true (D-W1).
      - useApproveJoinRequest: useMutation with **optimistic update** (D-W2): onMutate snapshots current ["admin","join-requests","pending"] cache, removes the approved request from items + decrements total. onError reverts to snapshot. onSettled invalidates ["admin","join-requests"]. Toast on success "Talep onaylandı"; on error "Onaylama başarısız: {detail}".
      - useRejectJoinRequest: same shape (optimistic remove from list).
      - useAdminUsers(filter): useQuery, queryKey ["admin","users",filter], staleTime 30000.
      - useInviteUser: useMutation, onSuccess invalidate ["admin","users"] + Toast "Davet gönderildi: {email}". onError Toast "Davet gönderilemedi: {detail}".
      - useBulkInvite: useMutation returning BulkInviteResponse {successful, failed}; onSuccess invalidate ["admin","users"] (caller renders summary modal).
      - useDeactivateUser: useMutation, onSettled invalidate ["admin","users"], Toast "Kullanıcı devre dışı bırakıldı: {email}".
      - useResetPassword: useMutation, onSuccess Toast "Şifre sıfırlama linki gönderildi: {email}".
      - useChangeRole: useMutation, onSettled invalidate ["admin","users"]. Single-user is confirmation toast; bulk-role-change goes through useBulkAction.
      - useBulkAction: useMutation, **optimistic update** for deactivate (set is_active=false on each user in cache), onError revert. Toast summary "{success_count}/{total} kullanıcı güncellendi".
      - useAdminAudit(filter): useQuery, queryKey ["admin","audit",filter], staleTime 30000, refetchOnWindowFocus true.
      - useAdminStats: useQuery, queryKey ["admin","stats"], staleTime 60000 (D-W1 stats), refetchOnWindowFocus true (D-B3 Phase 13 carry-forward).
    - 2 representative tests:
      - admin-join-request-service.test.ts: vitest mock apiClient. Test listPending happy path returns mapped PendingJoinRequest with requestedBy/targetUser camelCase. Test approve calls POST with correct path.
      - use-approve-join-request.test.tsx: RTL with QueryClient + ToastProvider mocks. Optimistic update test: pre-populate cache with 3 pending; trigger mutate; assert cache immediately has 2 + total decremented; mock failure; assert revert to 3.
  </behavior>
  <action>
    Step 1 — Write services. Use RESEARCH §Common Op 1 + §Pattern 4 verbatim shapes. Each service exports a single object adminXService with methods. Snake → camel applied at the boundary — internal mapper functions like mapPending/mapInvite (private to file).

    For admin-join-request-service.ts copy RESEARCH §Common Op 1 lines 970-1016 verbatim. Add note above the service object: "audit_log.metadata payload stays snake_case all the way to activity-row.tsx (Pitfall 2) — DO NOT add a camelCase mapper for audit metadata."

    For admin-audit-service.ts: include `truncated: boolean` in response shape mapping.

    For admin-user-service.ts exportCsv: simply returns the URL string `/api/v1/admin/users.csv?` + URLSearchParams of current filter — caller wraps in csv-export.downloadCsv(). NO axios call, NO blob handling.

    Step 2 — Write 14 hooks. Each is a thin wrapper. Standard mutation pattern from S4 PATTERNS.md lines 350-365 + Toast.

    For useApproveJoinRequest implement optimistic update verbatim:
    ```typescript
    export function useApproveJoinRequest() {
      const qc = useQueryClient()
      const { showToast } = useToast()
      return useMutation({
        mutationFn: adminJoinRequestService.approve,
        onMutate: async (id: number) => {
          await qc.cancelQueries({ queryKey: ["admin", "join-requests", "pending"] })
          const snapshot = qc.getQueryData<{ items: PendingJoinRequest[]; total: number }>(
            ["admin", "join-requests", "pending", { limit: 5 }]
          )
          if (snapshot) {
            qc.setQueryData(["admin", "join-requests", "pending", { limit: 5 }], {
              items: snapshot.items.filter(r => r.id !== id),
              total: snapshot.total - 1,
            })
          }
          return { snapshot }
        },
        onError: (err: any, _id, ctx) => {
          if (ctx?.snapshot) {
            qc.setQueryData(["admin", "join-requests", "pending", { limit: 5 }], ctx.snapshot)
          }
          const msg = err?.response?.data?.detail || "Onaylama başarısız"
          showToast({ tone: "danger", message: msg })
        },
        onSettled: () => qc.invalidateQueries({ queryKey: ["admin", "join-requests"] }),
        onSuccess: () => showToast({ tone: "success", message: "Talep onaylandı" }),
      })
    }
    ```

    Repeat for useRejectJoinRequest with appropriate Toast text.

    Step 3 — Write tests:
    - admin-join-request-service.test.ts: vi.mock("@/lib/api-client"), assert apiClient.get called with "/admin/join-requests" + correct params; assert mapPending output shape (requestedBy / targetUser camelCase but created_at stays snake).
    - use-approve-join-request.test.tsx: render with QueryClientProvider + ToastProvider. Pre-populate cache with 3 items. Trigger hook.mutate(2). Assert immediate cache contains 2 items (item id 2 removed). Mock apiClient.post to reject. Assert cache reverts to 3 items + Toast.danger called.
  </action>
  <verify>
    <automated>cd Frontend2 && npm run test -- --run admin-join-request-service.test.ts use-approve-join-request.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 4 service files exist; each exports `adminXService` constant
    - `Frontend2/services/admin-join-request-service.ts` defines `mapPending` AND uses `requestedBy: d.requested_by` (camelCase mapping enforced)
    - `Frontend2/services/admin-audit-service.ts` includes `truncated` in mapped response shape (grep returns 1)
    - `Frontend2/services/admin-user-service.ts` `exportCsv` returns a string URL — does NOT call apiClient (grep `apiClient` inside exportCsv function body returns 0; grep `URLSearchParams` returns ≥1)
    - All 14 hook files exist
    - `Frontend2/hooks/use-approve-join-request.ts` contains `onMutate` AND `onError` AND `cancelQueries` AND `setQueryData` (optimistic update enforced — D-W2)
    - `Frontend2/hooks/use-reject-join-request.ts` contains `onMutate` AND `setQueryData` (optimistic — D-W2)
    - `Frontend2/hooks/use-pending-join-requests.ts` contains `staleTime: 30 * 1000` OR `staleTime: 30000` (D-W1)
    - `Frontend2/hooks/use-admin-stats.ts` contains `staleTime: 60 * 1000` OR `staleTime: 60000` (D-W1 stats)
    - `Frontend2/hooks/use-admin-users.ts` contains `queryKey: ["admin", "users"` (correct cache key)
    - `cd Frontend2 && npm run test -- --run admin-join-request-service.test.ts` exits 0; mapPending output assertion passes
    - `cd Frontend2 && npm run test -- --run use-approve-join-request.test.tsx` exits 0; optimistic update + revert-on-error tests pass
  </acceptance_criteria>
  <done>
    4 admin services map snake → camel correctly (with documented exception for audit_log.metadata staying snake). 14 hooks ship with TanStack Query staleTime contracts (30s admin / 60s stats per D-W1). Approve + Reject + BulkAction hooks implement optimistic updates with onError revert per D-W2. Service test asserts mapper produces correct camelCase shape. Hook test asserts optimistic update applies immediately and reverts on error. Wave 2 plans can now consume hooks without re-deriving cache keys or staleTime values.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Backend API | All admin endpoints accept JWT in Authorization header; require_admin gate on every handler |
| Backend → Database | SQLAlchemy session via DI; FK ON DELETE semantics (CASCADE for project_id, SET NULL for users) preserve historical join request rows |
| User → SMTP | Phase 5 invite + reset emails sent through SMTP; email content does NOT include passwords (only token-bearing set-password URL) |
| Admin → audit_log writes | All admin mutating use cases emit audit_log entries with metadata enrichment per D-D2 |
| CSV upload → backend | Bulk invite max 500 rows enforced in DTO + per-row Pydantic validation |
| PDF generation → admin endpoint | Rate-limited 30s per user (D-B6, Phase 12 D-58 reuse) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | E (Elevation of Privilege) | Backend admin endpoints (admin_join_requests/users/audit/stats/summary) | mitigate | Every handler in 5 new admin router files MUST include `Depends(require_admin)` (existing dep at Backend/app/api/deps/auth.py:53). Grep gate in acceptance criteria asserts presence on every handler. Plan 14-12 E2E spec asserts 403 for non-admin on each endpoint. |
| T-14-01-02 | E (Elevation of Privilege) | PM-side join-request creation `POST /projects/{id}/join-requests` | mitigate | Uses `Depends(require_project_transition_authority)` (Phase 9 D-15) — only PMs of the project + admins may create join requests. |
| T-14-01-03 | I (Information Disclosure) | Audit JSON export endpoint `/admin/audit.json` | mitigate | Server-side 50k row hard cap (D-Z2 / D-B8). StreamingResponse with Content-Disposition. Honors current filter — no unbounded export. |
| T-14-01-04 | I (Information Disclosure) | Audit metadata `comment_excerpt` field | mitigate | 160-char hard cap with ellipsis enforced in Plan 14-09 (consumed by this plan's audit_repo extension). Comment table remains source of truth — never store full body in audit_log (D-D2). |
| T-14-01-05 | T (Tampering) | Bulk invite CSV upload | mitigate | papaparse client-side parse + Pydantic BulkInviteRowDTO server-side validation (Pitfall 5). Both layers validate email regex, role enum, name length. Single source of truth = Pydantic DTO. 500-row Field(max_items=500) DoS guard enforced server-side. |
| T-14-01-06 | D (Denial of Service) | PDF summary endpoint | mitigate | `@limiter.limit("1/30seconds")` per Phase 12 D-58 reuse; on rate-limit hit returns 429. |
| T-14-01-07 | D (Denial of Service) | Admin stats endpoint | mitigate | velocity_history capped at top-30 projects by recent activity (D-X4); active_users_trend capped at 30-day window. |
| T-14-01-08 | T (Tampering) | Approve join-request atomic intent | mitigate | If team_repo.add_member fails non-IntegrityError, ApproveJoinRequestUseCase rolls back status to "pending" — atomic intent test in test_approve_join_request.py asserts this. |
| T-14-01-09 | R (Repudiation) | Admin user CRUD operations | mitigate | Every admin write op (invite/deactivate/reset/role-change/bulk-action) emits audit_log entry with `requested_by_admin_id` in metadata (D-A6 + D-D2). Per-user audit row for bulk operations (D-B7) — no batch-only entries. |
| T-14-01-10 | I (Information Disclosure) | extra_metadata JSONB column-attr mismatch (Pitfall 7-8) | mitigate | All consumers MUST use Python attr `extra_metadata` (DB column literally `metadata`). Always use `["key"].astext` for JSONB filters. Documented in code comments + integration test asserts non-zero hit count via JSONB filter. |
| T-14-01-11 | E (Elevation of Privilege) | papaparse CSV injection (formula injection) | accept | Bulk invite endpoint creates User entities only (no CSV re-export at server). Email/role/name fields are validated. CSV injection scenario applies to re-export to Excel — Users CSV export ships UTF-8 BOM but field values are derived from DB (admin already trusted source). NO leading `=`, `+`, `-`, `@` filter applied — accepted risk for v2.0. |
</threat_model>

<verification>
- All 4 tasks pass automated verify commands.
- `cd Frontend2 && npm run lint` exits 0 OR existing lint warnings only (no NEW lint errors).
- `cd Backend && python -m pytest -q tests/integration/test_create_join_request.py tests/integration/test_approve_join_request.py tests/integration/test_admin_users_crud.py tests/integration/test_admin_audit_get_global.py tests/integration/test_admin_stats.py tests/integration/test_generate_admin_summary_pdf.py` reports all green.
- `cd Backend && alembic upgrade head` exits 0 AND `project_join_requests` table appears in DB.
- `cd Frontend2 && npm run test -- --run nav-tabs.test.tsx confirm-dialog.test.tsx admin-join-request-service.test.ts use-approve-join-request.test.tsx` all green.
- Snapshot of every D-XX (D-A1, D-A2 deferred, D-A6, D-A7, D-A8, D-B2-B8, D-W1-W3, D-X1-X4, D-Z2) addressed by an artifact in this plan.
- Threat T-14-01-01 through T-14-01-11 mitigated/accepted with linked artifacts.
- VALIDATION.md per-task verification map populated with rows for tasks 1-4 (set in Plan 14-12 finalization step or here).
</verification>

<success_criteria>
- papaparse@^5.5.3 installed; ConfirmDialog accepts tone prop; NavTabs + Modal primitives exist with tests; admin lib utilities (csv-parse / csv-export / permissions-static / audit-field-labels) ship.
- Backend ProjectJoinRequest vertical slice complete (entity / repo / migration 006 / use cases / router / tests). DIP enforced.
- Backend admin user/audit/stats/summary slices complete (DTOs / 9 use cases / 4 routers / config TTL / 4 integration tests). DIP enforced. Audit 50k cap (Pitfall 6) returns truncated flag.
- **alembic upgrade head executed** — project_join_requests table exists in dev DB.
- 4 admin services + 14 hooks ship with documented snake-vs-camel boundary; optimistic updates implemented for approve/reject/bulk per D-W2.
- All commits made before Wave 2 plans start.
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-01-SUMMARY.md` documenting:
- All NEW files created
- All EXTENDED files (ConfirmDialog tone, audit_repository ABC + impl, dependencies.py shim, main.py router includes, core/config.py)
- Test commands that proved each must-have truth
- Wave 0 deliverables for 14-VALIDATION.md (mark each Wave 0 checkbox satisfied)
- Snake_case-vs-camelCase decisions (audit_log.metadata stays snake; admin-* services camelCase domain shapes)
- Migration 006 applied confirmation
- Hand-off notes for Wave 2 plans (consumer hooks ready, primitive barrel updated, no further infra setup needed in 14-02..14-08)
</output>
