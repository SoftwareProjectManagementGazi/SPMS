---
phase: 14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - Frontend2/app/(shell)/admin/users/page.tsx
  - Frontend2/components/admin/users/users-toolbar.tsx
  - Frontend2/components/admin/users/users-table.tsx
  - Frontend2/components/admin/users/user-row.tsx
  - Frontend2/components/admin/users/user-row-actions.tsx
  - Frontend2/components/admin/users/user-bulk-bar.tsx
  - Frontend2/components/admin/users/add-user-modal.tsx
  - Frontend2/components/admin/users/bulk-invite-modal.tsx
  - Frontend2/components/admin/users/users-table.test.tsx
  - Frontend2/components/admin/users/add-user-modal.test.tsx
  - Frontend2/components/admin/users/bulk-invite-modal.test.tsx
  - Frontend2/lib/i18n/admin-users-keys.ts
autonomous: true
requirements:
  - D-00
  - D-A6
  - D-B1
  - D-B2
  - D-B3
  - D-B4
  - D-B7
  - D-W2
  - D-W3
  - D-C5
user_setup: []

must_haves:
  truths:
    - "/admin/users renders the Kullanıcılar tab with toolbar (search + role filter SegmentedControl + Bulk Invite + CSV + Add User buttons), users table, and bulk-action sticky bar when selections exist"
    - "Users table columns match prototype line 170 verbatim grid: 28px(checkbox) / 40px(avatar) / 2fr(name) / 2fr(email) / 130px(role badge) / 1fr(projects) / 100px(last seen) / 90px(status badge) / 28px(MoreH)"
    - "Per-row MoreH menu has 5 items per UI-SPEC §Surface C: Şifre sıfırla / Rolü değiştir (submenu Admin/PM/Member) / Devre dışı bırak (or Tekrar aktif et if is_active=false) / Sil (red-tinted)"
    - "Add User modal has 3 fields (email, role select, name optional) and on submit calls useInviteUser; Pydantic-mirror client validation matches backend (Pitfall 5)"
    - "Bulk Invite modal accepts CSV file (papaparse via lib/admin/csv-parse.ts), shows preview table with per-row validation status, 500-row hard cap (D-B4) enforced client-side, submit triggers useBulkInvite + final summary modal"
    - "User selection state managed via useState<number[]>; bulk-select toolbar (UserBulkBar) shows '{N} seçili' + Bulk Deactivate + Bulk Role Change + Vazgeç buttons (D-B7)"
    - "ConfirmDialog tone='danger' used for Devre dışı bırak + Sil + Bulk deactivate (D-B7); tone='warning' used for Bulk role change with disruptive impact"
    - "CSV export button triggers downloadCsv('/api/v1/admin/users.csv?' + URLSearchParams of current filter) — server-side per D-W3, NO client-side CSV assembly"
    - "Filter state (search, role) persisted to localStorage key 'spms.admin.users.filter' per Phase 11 D-21 pattern (D-C5)"
  artifacts:
    - path: "Frontend2/app/(shell)/admin/users/page.tsx"
      provides: "Kullanıcılar sub-route page"
      contains: "UsersToolbar"
    - path: "Frontend2/components/admin/users/users-table.tsx"
      provides: "Sortable user table with bulk-select column"
      exports: ["UsersTable"]
    - path: "Frontend2/components/admin/users/add-user-modal.tsx"
      provides: "Email-invite modal (D-B2)"
      exports: ["AddUserModal"]
    - path: "Frontend2/components/admin/users/bulk-invite-modal.tsx"
      provides: "CSV bulk-invite modal with preview + summary"
      exports: ["BulkInviteModal"]
  key_links:
    - from: "Frontend2/app/(shell)/admin/users/page.tsx"
      to: "Frontend2/hooks/use-admin-users.ts"
      via: "useAdminUsers(filter)"
      pattern: "useAdminUsers"
    - from: "Frontend2/components/admin/users/bulk-invite-modal.tsx"
      to: "Frontend2/lib/admin/csv-parse.ts parseBulkInviteCsv"
      via: "papaparse wrapper"
      pattern: "parseBulkInviteCsv"
    - from: "Frontend2/components/admin/users/users-toolbar.tsx"
      to: "Frontend2/lib/admin/csv-export.ts downloadCsv"
      via: "anchor download trigger for /api/v1/admin/users.csv"
      pattern: "downloadCsv"

tags:
  - admin-panel
  - users-tab
  - bulk-invite
  - csv
  - papaparse
  - frontend2
---

<objective>
Build the `/admin/users` (Kullanıcılar) tab end-to-end: full users table with prototype-verbatim grid, per-row MoreH actions menu (Reset password / Change role submenu / Deactivate / Delete), bulk-select with sticky action bar, Add User modal (email-invite flow), Bulk Invite modal (CSV via papaparse with preview + per-row validation + commit-or-skip submission), CSV export trigger. Wires to Plan 14-01 hooks (useAdminUsers, useInviteUser, useBulkInvite, useDeactivateUser, useResetPassword, useChangeRole, useBulkAction), Plan 14-01 lib utilities (csv-parse, csv-export), and CONSUMES the shared MoreMenu primitive built in Plan 14-01 (Frontend2/components/admin/shared/more-menu.tsx — see Plan 14-01 Task 1 Step 6.5).

Purpose: Users tab is the most-action-dense admin surface (~15 prototype actions, 5 MoreH items per row, 3 modals). Plan 14-01 has stood up the service+hook layer; this plan delivers the consumer UI with bulk-action ergonomics + CSV happy/error paths covered.

Output: 1 NEW route page + 7 NEW Users-tab components (more-menu.tsx is CONSUMED from Plan 14-01) + 1 EXTENDED admin-keys.ts + 3 NEW RTL tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-CONTEXT.md
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-RESEARCH.md
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-PATTERNS.md
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-UI-SPEC.md
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-01-SUMMARY.md
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-02-SUMMARY.md
@New_Frontend/src/pages/admin.jsx

<interfaces>
From Frontend2/lib/admin/csv-parse.ts (Plan 14-01):
```typescript
export interface BulkInviteRow {
  email: string
  name: string
  role: "Admin" | "Project Manager" | "Member"
}
export function parseBulkInviteCsv(file: File): Promise<{
  rows: BulkInviteRow[]
  errors: { row_number: number; message: string }[]
}>
```

From Frontend2/lib/admin/csv-export.ts (Plan 14-01):
```typescript
export function downloadCsv(url: string, filename?: string): void
```

From Frontend2/hooks (Plan 14-01):
- useAdminUsers(filter): UseQueryResult<{items: AdminUser[], total: number}>
- useInviteUser(): UseMutationResult<InviteUserResponse, Error, InviteUserRequest>
- useBulkInvite(): UseMutationResult<BulkInviteResponse, Error, BulkInviteRequest>
- useDeactivateUser(): UseMutationResult<void, Error, number>
- useResetPassword(): UseMutationResult<void, Error, number>
- useChangeRole(): UseMutationResult<void, Error, {id: number, role: string}>
- useBulkAction(): UseMutationResult<BulkActionResponse, Error, BulkActionRequest>

From New_Frontend/src/pages/admin.jsx lines 152-200 (verbatim AdminUsers grid):
```
gridTemplateColumns: 28px 40px 2fr 2fr 130px 1fr 100px 90px 28px
columns: checkbox / avatar / name / email / role badge / projects / last seen / status badge / MoreH
```

From Frontend2/components/admin/shared/more-menu.tsx (Plan 14-01 — SHARED primitive, IMPORT do not rebuild):
```typescript
export interface MoreMenuItem { id: string; label: string; icon?: React.ReactNode; onClick: () => void; disabled?: boolean; destructive?: boolean }
export function MoreMenu(props: { items: MoreMenuItem[]; trigger?: React.ReactNode }): JSX.Element
// Click-outside dismiss + ESC + destructive-item color already wired in Plan 14-01.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build UsersTable + UsersToolbar + UserRow + UserRowActions + UserBulkBar + page composition (consumes shared MoreMenu from Plan 14-01)</name>
  <files>
    Frontend2/app/(shell)/admin/users/page.tsx,
    Frontend2/components/admin/users/users-toolbar.tsx,
    Frontend2/components/admin/users/users-table.tsx,
    Frontend2/components/admin/users/user-row.tsx,
    Frontend2/components/admin/users/user-row-actions.tsx,
    Frontend2/components/admin/users/user-bulk-bar.tsx,
    Frontend2/components/admin/users/users-table.test.tsx,
    Frontend2/lib/i18n/admin-users-keys.ts
  </files>
  <read_first>
    Frontend2/components/admin/shared/more-menu.tsx (Plan 14-01 — IMPORT-FROM source for the per-row MoreH dropdown; do NOT rebuild, just `import { MoreMenu, type MoreMenuItem } from "@/components/admin/shared/more-menu"`),
    Frontend2/components/dashboard/portfolio-table.tsx (existing table pattern — for UsersTable analog),
    Frontend2/components/primitives/segmented-control.tsx (role filter UI),
    Frontend2/components/primitives/avatar.tsx + avatar-stack.tsx,
    Frontend2/components/primitives/badge.tsx (role + status badges),
    Frontend2/components/primitives/input.tsx (search input),
    Frontend2/components/primitives/button.tsx,
    Frontend2/components/primitives/data-state.tsx,
    Frontend2/components/projects/confirm-dialog.tsx (extended with tone in Plan 14-01),
    Frontend2/hooks/use-admin-users.ts (Plan 14-01),
    Frontend2/hooks/use-deactivate-user.ts + use-reset-password.ts + use-change-role.ts + use-bulk-action.ts,
    Frontend2/hooks/use-local-storage-pref.ts (existing — Phase 11 D-21 pattern for filter persistence),
    Frontend2/lib/activity-date-format.ts (formatRelativeTime),
    Frontend2/lib/admin/csv-export.ts (Plan 14-01 — downloadCsv),
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-PATTERNS.md "Plan 14-03" lines 138-149 + S3 lines 320-341,
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-UI-SPEC.md §Surface C lines 335-368 (full TR/EN copy),
    New_Frontend/src/pages/admin.jsx lines 152-208 (verbatim AdminUsers JSX — port to TS)
  </read_first>
  <behavior>
    - more-menu.tsx (CONSUMED from Plan 14-01 — see 14-01 Task 1 Step 6.5; this plan only IMPORTS via `@/components/admin/shared/more-menu`).
    - app/(shell)/admin/users/page.tsx ("use client"): reads filter from useLocalStoragePref<{search, role}>("spms.admin.users.filter", {search:"", role:"all"}). Compose <UsersToolbar/> + <UsersTable/> + <UserBulkBar/> + <AddUserModal/> + <BulkInviteModal/> (modals from Task 2). Bulk selection state via useState<number[]> selectedIds. Modal open state via useState<"add"|"bulk-invite"|null>.
    - users-toolbar.tsx: search Input (placeholder "Kullanıcı ara…"), role filter SegmentedControl ([Tümü, Admin, Project Manager, Member]), 3 buttons: CSV (variant="ghost", icon Download), Toplu davet (variant="ghost", icon Upload), Kullanıcı ekle (variant="primary", icon Plus). Search updates filter via setFilter. Role filter updates filter.role. CSV button calls `downloadCsv(\`/api/v1/admin/users.csv?\${new URLSearchParams(filter).toString()}\`)`. Bulk invite button calls onOpenBulkInvite. Add user button calls onOpenAddUser.
    - users-table.tsx: read useAdminUsers(filter). DataState fallback. When data: render header row (column labels per UI-SPEC) + body rows mapped to <UserRow/>. Header checkbox toggles all selections; row checkboxes toggle individual selection. Pagination at bottom (Sayfa N / M + N kullanıcı per UI-SPEC §Surface C).
    - user-row.tsx: renders one row in the prototype grid. Checkbox + Avatar + name (fontWeight 500 per UI-SPEC) + email + role Badge (tone per role: Admin=danger, PM=warning, Member=neutral) + projects (count or AvatarStack of project leads — pick count for v2.0) + last seen (formatRelativeTime) + status Badge (Aktif=success, Pasif=neutral) + MoreH trigger (opens UserRowActions menu).
    - user-row-actions.tsx: uses MoreMenu. 5 items per UI-SPEC §Surface C:
      - Şifre sıfırla → useResetPassword.mutate(user.id) (no confirm — direct trigger per UI-SPEC line 205)
      - Rolü değiştir → submenu (or popover) with 3 sub-items Admin/PM/Member, each calling useChangeRole.mutate({id, role}) (no confirm)
      - Devre dışı bırak (when user.is_active=true) OR Tekrar aktif et (when is_active=false) → opens ConfirmDialog tone="danger" with body matching UI-SPEC line 203; on confirm calls useDeactivateUser.mutate(user.id)
      - Sil → opens ConfirmDialog tone="danger" with body matching UI-SPEC line 204; on confirm calls deleteUser API (NOTE: D-A6 lists deactivate but UI-SPEC §Surface C also lists Sil — for v2.0 implement Delete via the same useBulkAction with action="delete" on a single user OR add a useDeleteUser hook in this plan referencing the existing soft-delete user endpoint if one exists; if no backend delete-user endpoint exists, this menu item shows a Toast "Silme v2.1'de aktif olacak" — researcher to confirm. Default: SOFT-DISABLE this item with tooltip "v2.1'de aktif olacak" since CONTEXT D-A6 only enumerates POST/PATCH endpoints, no DELETE. Plan 14-12 UAT will verify expectation alignment.)
    - user-bulk-bar.tsx: renders only when selectedIds.length > 0. Sticky bottom bar (or top-of-table — pick top per prototype). "{N} seçili" label + Bulk Deactivate Button (tone="danger") + Bulk Role Change dropdown (Admin/PM/Member options) + Vazgeç (clears selection). Bulk Deactivate opens ConfirmDialog tone="danger" with body matching UI-SPEC line 207 ("{N} kullanıcı devre dışı bırakılsın mı?" + lists first 5 names + "+ {N-5} more"). Bulk Role Change → no confirm, direct PATCH via useBulkAction({user_ids: selectedIds, action: "role_change", payload: {role}}). Toast summary on response.
    - users-table.test.tsx (RTL): 4 cases.
      1. Loading: useAdminUsers returns isLoading=true → DataState loading rendered.
      2. Empty: returns items=[] → empty state rendered.
      3. Renders 3 mock users: assert table has 3 rows + each row has Avatar + email + role Badge.
      4. Bulk select: click checkbox on row 1 → UserBulkBar appears + shows "1 seçili".
    - APPEND new keys to Frontend2/lib/i18n/admin-users-keys.ts for ALL strings in UI-SPEC §Surface C (table column labels + bulk strings + MoreH menu items + empty states + status badges).
  </behavior>
  <action>
    Step 1 — IMPORT shared MoreMenu from Plan 14-01: `import { MoreMenu, type MoreMenuItem } from "@/components/admin/shared/more-menu"`. Do NOT rebuild this primitive — it ships in Wave 0 (Plan 14-01 Task 1 Step 6.5) with click-outside dismiss + destructive-item styling already wired. UsersTable's per-row action menu wires consumers around it.

    Step 2 — Write users-toolbar.tsx. Use SegmentedControl for role filter:
    ```tsx
    <SegmentedControl
      options={[
        { id: "all", label: adminT("admin.users.filter_all", lang) },
        { id: "Admin", label: "Admin" },
        { id: "Project Manager", label: "Project Manager" },
        { id: "Member", label: "Member" },
      ]}
      active={filter.role}
      onChange={(role) => setFilter({...filter, role})}
    />
    ```

    Step 3 — Write users-table.tsx. Grid template: `28px 40px 2fr 2fr 130px 1fr 100px 90px 28px` per UI-SPEC §Spacing line 84. Header row uses Label-uppercase typography (11px, fontWeight 600, text-transform uppercase, letter-spacing 0.4 per UI-SPEC §Typography). Each row maps to <UserRow user={u} selected={...} onToggleSelect={...}/>.

    Step 4 — Write user-row.tsx + user-row-actions.tsx. Row layout matches grid template. Status badge uses tone="success" for Aktif, tone="neutral" for Pasif. Role badge uses Phase 8 Badge: tone="danger" for Admin, tone="warning" for PM, tone="neutral" for Member.

    Step 5 — Write user-bulk-bar.tsx. Sticky position at top of table when visible. Hide when selectedIds.length === 0. Bulk Deactivate triggers ConfirmDialog with tone="danger" + body containing first 5 user names from selectedIds.

    Step 6 — Write app/(shell)/admin/users/page.tsx. Compose all child components. Manage modal state + selection state. localStorage filter persistence via useLocalStoragePref. Render <AddUserModal open={modal === "add"} onClose={() => setModal(null)}/> + <BulkInviteModal open={modal === "bulk-invite"} onClose={() => setModal(null)}/> (these modals are built in Task 2).

    Step 7 — Write users-table.test.tsx (RTL with vi.mock for hooks). 4 cases per behavior contract.

    Step 8 — APPEND keys to Frontend2/lib/i18n/admin-users-keys.ts. All admin.users.* keys per UI-SPEC §Surface C: search_placeholder, filter_all, csv_button, bulk_invite_button, add_user_button, table_col_name, table_col_email, table_col_role, table_col_projects, table_col_last_seen, table_col_status, status_active, status_inactive, last_seen_now, pagination_caption, pagination_page, more_reset_password, more_change_role, more_change_role_admin/.pm/.member, more_deactivate, more_reactivate, more_delete, bulk_selected, bulk_deactivate, bulk_role_change, bulk_cancel, empty_no_match, empty_no_users.
  </action>
  <verify>
    <automated>cd Frontend2 && npm run test -- --run users-table.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `Frontend2/app/(shell)/admin/users/page.tsx` exists AND is `"use client"` AND imports useLocalStoragePref AND uses key `spms.admin.users.filter`
    - `Frontend2/components/admin/users/users-toolbar.tsx` calls `downloadCsv` AND uses URLSearchParams of filter (D-W3 server-side, NO client-side blob)
    - users-toolbar.tsx renders 4-option SegmentedControl (Tümü/Admin/PM/Member)
    - `Frontend2/components/admin/users/users-table.tsx` uses gridTemplateColumns matching `28px 40px 2fr 2fr 130px 1fr 100px 90px 28px` (grep the literal values)
    - `Frontend2/components/admin/users/user-row-actions.tsx` defines 5 MoreMenuItem entries (grep `id:` count inside the items array ≥ 5)
    - user-row-actions.tsx uses ConfirmDialog with `tone="danger"` for Devre dışı bırak + Sil
    - `Frontend2/components/admin/users/user-bulk-bar.tsx` renders only when `selectedIds.length > 0` (grep `selectedIds.length` returns ≥1)
    - user-bulk-bar.tsx Bulk Deactivate uses ConfirmDialog with tone="danger" AND body lists first 5 selected names
    - `Frontend2/lib/i18n/admin-users-keys.ts` extended with at least 25 new admin.users.* keys (grep `admin.users.` returns ≥25)
    - users-table.test.tsx exists AND tests loading + empty + 3-row render + bulk select cases (grep test for `useAdminUsers` mock + `selectedIds`)
    - `cd Frontend2 && npm run test -- --run users-table.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    UsersTable matches prototype grid verbatim, consuming the shared MoreMenu primitive built in Plan 14-01 (no per-plan rebuild). Per-row MoreH ships 5 actions with proper ConfirmDialog tones. Bulk-select sticky bar exposes Bulk Deactivate (danger confirm with first-5-names preview) + Bulk Role Change (no-confirm). CSV export triggers server endpoint via downloadCsv (D-W3). Filter state persists to localStorage. RTL test covers loading/empty/3-row/bulk-select cases.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build AddUserModal (single email-invite) + BulkInviteModal (CSV preview + per-row validation + commit-or-skip submit + final summary modal) + 2 modal RTL tests</name>
  <files>
    Frontend2/components/admin/users/add-user-modal.tsx,
    Frontend2/components/admin/users/bulk-invite-modal.tsx,
    Frontend2/components/admin/users/add-user-modal.test.tsx,
    Frontend2/components/admin/users/bulk-invite-modal.test.tsx
  </files>
  <read_first>
    Frontend2/components/primitives/modal.tsx (Plan 14-01 — Modal + ModalHeader/Body/Footer slots),
    Frontend2/components/primitives/input.tsx (existing),
    Frontend2/components/primitives/button.tsx (existing),
    Frontend2/components/primitives/badge.tsx (per-row validation status badges in CSV preview),
    Frontend2/components/primitives/data-state.tsx (loading/error states for submit),
    Frontend2/lib/admin/csv-parse.ts (Plan 14-01 — parseBulkInviteCsv shape),
    Frontend2/hooks/use-invite-user.ts (Plan 14-01),
    Frontend2/hooks/use-bulk-invite.ts (Plan 14-01),
    Frontend2/components/lifecycle/__tests__/* (Phase 12 RTL test pattern with vi.mock for hooks — copy structure),
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-UI-SPEC.md §Modal Anatomy Add User (line 88) + Bulk Invite (line 89) + Surface C bulk strings (lines 339-368) + the Toplu davet > 500 satır AlertBanner (line 215),
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-RESEARCH.md §Pitfall 5 lines 905-913 (client-vs-server validation symmetry),
    .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-CONTEXT.md decisions D-B2 (email invite) + D-B4 (CSV format)
  </read_first>
  <behavior>
    - AddUserModal: Modal width 480 (UI-SPEC §Spacing line 89). ModalHeader "Kullanıcı ekle". ModalBody: form with 3 fields:
      - Email (Input type="email" required, validates RFC 5322 lax matching backend Pydantic EmailStr — same regex as csv-parse.ts to mirror Pitfall 5)
      - Role (native <select> OR custom dropdown with 3 options Admin/PM/Member)
      - Name (Input optional, max 100 chars)
      Validation errors shown inline below each field. Submit button: "Davet gönder" (primary, disabled when validation fails OR while mutation pending). Cancel button: "Vazgeç" (ghost). On submit calls useInviteUser.mutate({email, role, name}) → on success closes modal + clears form (Toast handled by hook). On error: error message rendered above form (in addition to Toast from hook).
    - BulkInviteModal: Modal width 560 (UI-SPEC §Spacing line 89). 4-step wizard inside modal body:
      - Step 1 — File picker: "<input type='file' accept='.csv'>" + drag-drop area. Below: "CSV format: email,name,role (3 columns; header row optional). Max 500 satır."
      - Step 2 — Preview: after file selected, calls parseBulkInviteCsv(file) → renders preview table with rows (max 100 rows shown; "+{N-100} more" if larger). Each row has Avatar (initials from name) + email + role + per-row validation Badge (tone="success" for valid, tone="danger" for invalid with tooltip showing error). Summary line: "{N_valid} geçerli / {N_invalid} hatalı / {N_total} toplam".
      - Step 3 — 500-row warning: if rows.length > 500, render AlertBanner tone="warning" body matching UI-SPEC line 215 ("Maksimum 500 satır işlenebilir. CSV {N} satır içeriyor; ilk 500 satır işlenecek mi?") + primary CTA "İlk 500'ü İşle".
      - Step 4 — Submit: button "Davetleri gönder ({N_valid})". On click: useBulkInvite.mutate({rows: validRows.slice(0, 500)}) with submit-button spinner state + "{N} davet gönderiliyor..." text per CONTEXT Discretion. On response: render final summary modal (a second Modal OR a state inside same modal): "Toplu davet sonucu: {N_successful} başarılı, {N_failed} başarısız." Lists failed rows with reason. Buttons: "Başarısızları indir (CSV)" (downloadCsv with failures dataURI) + "Kapat".
    - Both modals close on ESC, click-outside, and Cancel button.
    - add-user-modal.test.tsx (RTL): 4 cases.
      1. Renders 3 form fields + Submit + Cancel.
      2. Empty email submit → validation error "Email gerekli".
      3. Invalid email "foo" → validation error.
      4. Valid submit → useInviteUser.mutate called with correct payload.
    - bulk-invite-modal.test.tsx (RTL): 3 cases.
      1. File select with valid CSV (3 rows) → preview shows 3 rows with success badges + summary "3 geçerli / 0 hatalı".
      2. File select with mixed CSV (2 valid, 1 invalid email) → preview shows 3 rows with per-row badges + summary.
      3. File select with 600 rows → 500-row warning AlertBanner appears + primary CTA "İlk 500'ü İşle" enabled.
  </behavior>
  <action>
    Step 1 — Write add-user-modal.tsx. Use Modal width=480. Form state via useState({email:"", role:"Member", name:""}). Inline validation:
    ```typescript
    const emailValid = /^[^\s@]+@[^\s@]+\.[^\s@]+$/i.test(email)
    const nameValid = name.length <= 100
    const formValid = emailValid && nameValid
    ```
    Submit handler:
    ```typescript
    const onSubmit = () => {
      if (!formValid) return
      inviteUser.mutate({email, role, name: name || undefined}, {
        onSuccess: () => { onClose(); setEmail(""); setRole("Member"); setName("") }
      })
    }
    ```
    Render fields with labels + validation messages. Modal footer: "Vazgeç" (ghost) + "Davet gönder" (primary, disabled={!formValid || inviteUser.isPending}).

    Step 2 — Write bulk-invite-modal.tsx. State machine:
    ```typescript
    const [step, setStep] = useState<"upload"|"preview"|"submitting"|"summary">("upload")
    const [parseResult, setParseResult] = useState<{rows: BulkInviteRow[], errors: ParseError[]} | null>(null)
    const [submitResult, setSubmitResult] = useState<BulkInviteResponse | null>(null)
    ```
    On file select: setStep("preview") + parseBulkInviteCsv(file).then(setParseResult). On submit: setStep("submitting") + bulkInvite.mutate({rows: validRows.slice(0, 500)}, { onSuccess: (resp) => { setStep("summary"); setSubmitResult(resp) } }). Per-row preview table uses gridTemplateColumns: 32px 1.5fr 1fr 80px 100px (avatar/email/name/role/status badge).

    Step 3 — Tests:
    - add-user-modal.test.tsx: vi.mock("@/hooks/use-invite-user", () => ({ useInviteUser: () => ({ mutate: vi.fn(), isPending: false }) })). Render with open=true. 4 cases per behavior contract.
    - bulk-invite-modal.test.tsx: vi.mock("@/lib/admin/csv-parse"). Mock parseBulkInviteCsv to return controlled fixtures (3 valid / mixed / 600 rows). Use fireEvent.change on file input with `new File([csv], "test.csv")`. Assert preview rendering + summary line.
  </action>
  <verify>
    <automated>cd Frontend2 && npm run test -- --run add-user-modal.test.tsx bulk-invite-modal.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `Frontend2/components/admin/users/add-user-modal.tsx` uses Modal with width 480 (grep `width={480}` OR `width: 480`)
    - add-user-modal.tsx form has 3 input fields (email, role, name) — grep `<Input` AND `<select` together returns ≥3 OR equivalent
    - add-user-modal.tsx calls `useInviteUser` AND `inviteUser.mutate(`
    - add-user-modal.tsx validates email with regex matching csv-parse.ts (grep `/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/i` OR equivalent)
    - `Frontend2/components/admin/users/bulk-invite-modal.tsx` uses Modal with width 560
    - bulk-invite-modal.tsx imports `parseBulkInviteCsv` from `@/lib/admin/csv-parse`
    - bulk-invite-modal.tsx implements 4-step state machine (grep `step === "upload"` AND `step === "preview"` AND `step === "summary"` returns ≥3)
    - bulk-invite-modal.tsx renders AlertBanner tone="warning" when rows.length > 500 (grep `tone="warning"` AND `> 500`)
    - bulk-invite-modal.tsx submits at most 500 rows (grep `slice(0, 500)`)
    - add-user-modal.test.tsx tests validation + valid submit cases (grep `mutate.*toHaveBeenCalledWith` returns ≥1)
    - bulk-invite-modal.test.tsx tests preview + 500-row warning cases (grep `600` AND `İlk 500` returns ≥1 each)
    - `cd Frontend2 && npm run test -- --run add-user-modal.test.tsx bulk-invite-modal.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    AddUserModal ships 3-field form with Pydantic-mirror client validation (Pitfall 5 client-server symmetry). BulkInviteModal implements 4-step wizard (upload → preview → submit → summary) with per-row validation badges + 500-row hard cap warning + final summary. RTL tests cover validation paths + file upload paths + 500-row scenario. Wave 2 Users tab is now feature-complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → bulk invite endpoint | papaparse client-side parse + per-row Pydantic backend validation (defense in depth — Pitfall 5) |
| User → CSV upload | File API (browser-managed); papaparse handles RFC-4180 + UTF-8 BOM detection |
| Admin → bulk action | Bulk endpoint atomic per-user (D-B7); each user gets own audit_log entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | T (Tampering) | CSV bulk invite | mitigate | Client validates email regex + role enum + name length; server re-validates via Pydantic BulkInviteRowDTO. Mismatch impossible because both use the same RFC 5322 lax regex (Pitfall 5 explicit symmetry). 500-row hard cap on BOTH layers (client warning at 500; server Pydantic Field(max_items=500)). |
| T-14-03-02 | I (Information Disclosure) | CSV export endpoint URL exposed in browser history | accept | Endpoint requires admin auth (require_admin gate); URL parameters are filter-only (no PII). UTF-8 BOM ensures Excel-friendly encoding. |
| T-14-03-03 | E (Elevation of Privilege) | Bulk role change to Admin | mitigate | useBulkAction calls /admin/users/bulk-action which honors require_admin. Backend ChangeRoleUseCase + audit_log capture target_role + source_role + requested_by_admin_id. |
| T-14-03-04 | T (Tampering) | Optimistic update on bulk deactivate | mitigate | useBulkAction implements optimistic update + onError revert per Plan 14-01 D-W2. Failed mutations cannot persist client cache desync. |
| T-14-03-05 | I (Information Disclosure) | Filter state in localStorage | accept | spms.admin.users.filter contains only search string + role filter. No PII. Cleared on logout (existing app-context behavior). |
| T-14-03-06 | E (Elevation of Privilege) | Sil (Delete) menu item | mitigate (soft-disable) | If no DELETE /api/v1/admin/users/{id} endpoint exists in v2.0, the menu item is soft-disabled with tooltip "v2.1'de aktif olacak" — no UI affordance for an unbacked operation. Plan 14-12 UAT verifies expectation alignment with stakeholders. |
</threat_model>

<verification>
- All 4 RTL tests pass (users-table, add-user-modal, bulk-invite-modal — 3 files, multiple cases each).
- `cd Frontend2 && npm run build` exits 0.
- TR + EN parity for every Users tab string (admin-keys.ts has both languages for each new key).
- 500-row cap enforced on client (AlertBanner) AND server (Pydantic — Plan 14-01).
- No client-side CSV blob assembly (csv-export.ts only creates anchor; csv-parse.ts only reads upload).
</verification>

<success_criteria>
- /admin/users renders the Kullanıcılar tab with all controls functional.
- Add User modal sends single invite → user appears in table after refetch.
- Bulk Invite modal accepts CSV → preview → submit → summary modal with success/failure breakdown.
- Bulk-select toolbar appears on row checkbox; Bulk Deactivate confirms with first-5-names preview.
- CSV export downloads via server endpoint (UTF-8 BOM Excel-friendly).
- Filter persists across tab switches via localStorage.
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-03-SUMMARY.md` documenting:
- Components built (7 Users-tab files; MoreMenu CONSUMED from Plan 14-01, not built here)
- Sil menu item disposition (soft-disabled or wired — depends on backend audit)
- 500-row guard tested at client + delegated to server cap from Plan 14-01
- localStorage key spms.admin.users.filter
- admin-keys.ts current size (count after Plan 14-03 additions)
</output>
