---
phase: 14
slug: admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-26
---

# Phase 14 — UI Design Contract

> Visual + interaction contract for the **Admin Panel** — `app/(shell)/admin/layout.tsx` + 8 sub-routes (`/admin`, `/admin/users`, `/admin/roles`, `/admin/permissions`, `/admin/projects`, `/admin/workflows`, `/admin/audit`, `/admin/stats`) plus the cross-cutting **Jira-style audit detail** uplift on `activity-row.tsx` + `audit-event-mapper.ts`.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **AUTHORITY ORDER (D-00):**
> 1. `New_Frontend/src/pages/admin.jsx` (482 lines, all 8 sub-tab renderers + AdminPage outer header) is the design authority — port verbatim into TypeScript + i18n + token-driven styles.
> 2. The existing Frontend2 design system (Phase 8 D-01..D-08 token + primitive layer in `Frontend2/components/primitives/*` and `Frontend2/app/globals.css`) is the canonical implementation — every Phase 14 surface MUST consume it; do not re-spec primitives.
> 3. RBAC-deferred surfaces (Roller card descriptions / Permission Matrix toggles / "Yeni rol oluştur") have **rewritten copy** locked by CONTEXT D-A2..A5 — these are the authority over the prototype's misleading "all roles are global" descriptions.
> 4. Improvements over prototype are explicitly tagged **`[IMPROVEMENT]`** with justification (genuine prototype gaps only — empty / error / loading / mobile / a11y / RBAC reality / Detay Jira-render).

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none (custom prototype-driven design system) | `.planning/PROJECT.md` v2.0 constraint: "shadcn/ui KULLANILMAZ" + Phase 13 D-00 carry-forward |
| Preset | not applicable | n/a |
| Component library | none — 18 in-repo primitives at `Frontend2/components/primitives/*` + `Frontend2/components/primitives/data-state.tsx` (Phase 13) | Phase 8 D-01..D-08 + Phase 13 D-F2 |
| Icon library | `lucide-react@^1.8.0` | `Frontend2/package.json` line 30 |
| Chart library | **`recharts@3.8.1`** (already installed Phase 13) — REUSED for stats charts | `Frontend2/package.json` line 34 + Phase 13 D-A2 |
| Font (sans) | `Geist` via `--font-sans` (`Frontend2/app/globals.css` line 18) | Phase 8 |
| Font (mono) | `Geist Mono` via `--font-mono` (`Frontend2/app/globals.css` line 19) | Phase 8 |
| Theme tokens | oklch-only (`Frontend2/app/globals.css` lines 16–139) | Phase 8 D-02 |
| Radius scale | `--radius:8px` / `--radius-sm:6px` / `--radius-lg:12px` | Phase 8 |
| Shadows | `--shadow-sm`/`--shadow`/`--shadow-md`/`--shadow-lg`/`--shadow-xl` (5-level, dark-aware) | Phase 8 |

**No new chart-library evaluation needed.** Phase 13 picked `recharts@3.8.1` (pinned exact version, no caret). Phase 14's 3 stats charts (Active users trend line, Methodology distribution bars, Per-project velocity mini-bars) reuse the same primitive without re-deciding. Only the Active users trend uses recharts (`<LineChart/>` + `<AreaChart/>` overlay); methodology bars and velocity mini-bars are simple horizontal/vertical `<div>` bars driven by inline `var(--*)` styles per prototype lines 451-475 (no recharts geometry needed for percentage/progress bars).

---

## Spacing Scale

8-point base — every Phase 14 dimension is a multiple of 4 except documented typography half-steps.

| Token | Value | Usage in Phase 14 |
|-------|-------|-------------------|
| `xs` | 4 px | Inline icon + label gaps; legend chip swatch margin; bulk-select checkbox column gap |
| `sm` | 8 px | Compact element gap (avatar→name in user table row); pending-request action button gap (verbatim prototype line 92) |
| `md` | 12 px | StatCard grid gap (`gap: 12` per prototype line 54); audit row vertical pad inner |
| `md+` | 14 px | Card outer-padding default (Phase 11 `Card padding={14}` convention) — Roller card padding is `padding={18}` per prototype line 213 (exception); Şablonlar card `padding={16}` per prototype line 365 |
| `lg` | 16 px | Audit row pad `10px 16px` (verbatim prototype line 415); admin event row pad inside Card; outer page section gap (`gap: 16` between major rows) |
| `xl` | 20 px | AdminPage outer column gap (`flexDirection:column gap:20` from prototype line 9); Reports/Stats grid gap (`gap: 20` from prototype line 432, 462) |
| `2xl` | 24 px | (not used in Phase 14 — admin uses 14/16/18 card padding density) |
| `3xl` | 32 px | (not used in Phase 14) |
| `4xl` | 40 px | DataState empty-state vertical padding (default — inherited Phase 13) |

**Heights / widths (multiples of 4, plus declared exceptions):**

| Token | Value | Usage |
|-------|-------|-------|
| Page header height | (intrinsic — content-driven; `display: flex` `alignItems: flex-end` per prototype line 10) | Verbatim |
| Tabs row spacing from header | 20 px (parent gap) | Verbatim prototype line 9 |
| Tabs row spacing from sub-tab content | 20 px (parent gap) | Verbatim |
| Sub-tab content `flex:1, minHeight:0` | (responsive) | Verbatim prototype line 35 |
| StatCard grid (Overview) | 5-column `repeat(5, 1fr) gap:12` | Verbatim prototype line 54 |
| Overview second row | `1.5fr 1fr gap:20` (Pending Requests Card vs side column) | Verbatim prototype line 62 |
| Overview side column inner gap | 16 px (Role distribution + Recent admin events stack) | Verbatim prototype line 101 |
| Roller grid | `repeat(2, 1fr) gap:14` | Verbatim prototype line 211 |
| Roller card padding | 18 px | Verbatim prototype line 213 (exception — non-multiple-of-4 design-system canonical) |
| Roller card icon column | 34×34 px / 8 radius | Verbatim prototype line 215 |
| Roller "Yeni rol" placeholder | dashed border, minHeight 130 px, padding 18 px | Verbatim prototype line 232 |
| Permission Matrix grid | `2fr repeat(4, 100px)` per row | Verbatim prototype line 286, 294 |
| Permission Matrix toggle pill | 26×18 px / 10 radius / 14×14 knob | Verbatim prototype line 300-301 |
| Permission Matrix group label pad | `10px 16px` | Verbatim prototype line 292 |
| Projeler table grid | `60px 2fr 110px 150px 120px 90px 90px 28px` (Key/Name/Method/Lead/Tasks/Progress/Created/MoreH) | Verbatim prototype line 325 |
| Projeler table row pad | `10px 16px` | Verbatim prototype line 332 |
| Şablonlar grid | `repeat(3, 1fr) gap:14` | Verbatim prototype line 363 |
| Şablonlar card padding | 16 px | Verbatim prototype line 365 |
| Audit table grid | `90px 160px 180px 1fr 1fr 28px` (Time/Actor/Action/Target/**Detay**/MoreH) — **6 columns, NOT 7** (risk column removed per D-Z1) | **[IMPROVEMENT]** D-Z1 — replaces prototype's `90px 160px 180px 1fr 1fr 120px 80px` (7 cols with IP + Risk) |
| Audit table row pad | `8px 16px` | Verbatim prototype line 415 |
| Stats outer grid | `2fr 1fr gap:20` (Active users trend + Methodology) | Verbatim prototype line 432 |
| Stats velocity row (gridColumn: 1 / -1) | `repeat(6, 1fr) gap:10` (one card per project, max 30 capped) | Verbatim prototype line 464 |
| Users table grid | `28px 40px 2fr 2fr 130px 1fr 100px 90px 28px` (checkbox/avatar/name/email/role/projects/last seen/status/MoreH) | Verbatim prototype line 170 |
| Users table row pad | `10px 16px` | Verbatim prototype line 176 |
| Users toolbar pad | `12px 16px` (border-bottom) | Verbatim prototype line 156 |
| Users role-filter pill set | `padding: 2 / borderRadius: 6` outer, `4×10` inner per option | Verbatim prototype line 158-160 |
| Modal overlay | `position: fixed / inset: 0 / background: oklch(0 0 0 / 0.5)` (industry-standard) | New for Phase 14 |
| Modal panel | width 480px (Add User), 560px (Bulk Invite), 520px (Audit Filter), 640px (Tümünü gör pending) | **[IMPROVEMENT]** see Modal Anatomy section |
| Modal panel padding | 20 px header, 16 px body, 12 px footer | New for Phase 14 |
| Pending request row | `auto 1fr auto` columns, 14 gap, 14×16 pad | Verbatim prototype line 74 |
| Recent admin events row | `flex gap:8 alignItems:flex-start` | Verbatim prototype line 129 |
| Role distribution row | `flex gap:10 padding:8 0 fontSize:12.5` per row | Verbatim prototype line 109 |

**Exceptions (non-multiples-of-4 — typography-tier or prototype-locked density):**

- `font-size: 10.5 / 11.5 / 12.5` are the Phase 8 prototype font scale (locked at primitive level — see `Frontend2/components/primitives/badge.tsx` line 76 / `tabs.tsx` `FONT_MAP`). Permitted exception — typography token half-steps.
- `padding: 18px` on Roller cards (prototype line 213) — design-system canonical for that surface; do not normalize.
- `padding: 6×10` on dropdown menu items / MoreH popover items — Phase 12 D-08 ContextMenu standardization.
- `padding: 8px 16px` on Audit rows (prototype line 415) — verbatim, denser than other tables to fit more rows on screen.
- `padding: 7px 0` rows in Recent admin events — verbatim line 129.

---

## Typography

The Frontend2 type scale is locked at the primitive layer (Phase 8 D-02). Phase 14 adds **zero** new font sizes; every value below is reused.

**Inheritance claim — for the dimension-4 checker:** the table below enumerates **13 type roles** spanning **10 distinct font sizes** (24 / 20 / 15 / 14 / 13 / 12.5 / 12 / 11.5 / 11 / 10.5 px) that Phase 14 surfaces consume. All 10 sizes are pre-existing values from the Phase 8 primitive scale + the prototype `New_Frontend/src/pages/admin.jsx` already-shipped values. Phase 14 introduces NO new font size, NO new weight, NO new line-height value — this is a complete inventory of what Phase 14 USES, not a declaration of a new scale. Where the generic UI-SPEC checker rule says "≤ 4 font sizes," that rule applies to NEWLY-INTRODUCED sizes; Phase 14 introduces 0, so the rule is satisfied. The 13 role entries below cover the full set of inherited sizes the prototype already employs across an admin console with 8 distinct surface types (3 share size 13 px with different weight/family — Heading 600/sans, Stats trend per-primitive, Subtitle 400/sans; 2 share 11 px — Label-uppercase 600/sans-uppercase, Caption mono 400/mono) — collapsing them would require redesigning the prototype, which D-00 forbids.

| Role | Size | Weight | Line Height | Family | Usage in Phase 14 |
|------|------|--------|-------------|--------|---------|
| Display | 24 px | 600 | 1.2 (auto) | sans, `letter-spacing: -0.6` | Page title "Yönetim Konsolu" / "Admin Console" — verbatim prototype line 14 |
| Heading | 13 px | 600 | 1.3 | sans | Card titles ("Bekleyen Proje Katılım İstekleri", "Rol Dağılımı", "Son Yönetim Olayları", "Aktif kullanıcı eğilimi", "Metodoloji Kullanımı", "Proje başına velocity", "İzin Matrisi") — verbatim prototype lines 65, 103, 121, 280, 436, 450, 463 |
| Card title (Roller) | 15 px | 600 | 1.3 | sans | Roller card name ("Admin", "Project Manager", "Member", "Guest") — verbatim prototype line 218 |
| Card title (Şablonlar) | 14 px | 600 | 1.3 | sans | Template card name — verbatim prototype line 369 |
| Body | 12.5 px | 400 / 500 / 600 | 1.5 | sans | Default content text, table cell content, Pending Request primary line, Recent event line — verbatim prototype lines 81, 109, 124, 132, 184, 218, 295, 332, 415 |
| Body small | 12 px | 400 | 1.5 | sans | Roller card description, Şablonlar description, Pending request note (italic) — verbatim prototype lines 221, 372 |
| Label | 11.5 px | 600 | 1.4 | sans | Pending request name secondary, Roller stats footer, Methodology label, ChartCard sub-headers — verbatim prototype line 81, 224, 437, 454 |
| Label-uppercase | 11 px | 600 | 1.4 | sans, `text-transform: uppercase / letter-spacing: 0.4` | Table column headers (Users / Projeler / Audit), date-group labels, Permission group label — verbatim prototype lines 170, 286, 292, 325, 409 |
| Caption mono | 11 px | 400 | 1.3 | mono (Geist Mono), `tabular-nums` | Time stamp (Recent events / Audit row), pagination "1 / 1", IP address — verbatim prototype lines 91, 135, 195, 416, 421 |
| Big number | 20 px | 600 | 1.2 (auto) | sans, `letter-spacing: -0.5`, `tabular-nums` | Velocity mini-card progress percentage — verbatim prototype line 468 |
| Stats trend percentage | 13 px (Badge sm) | per primitive | 1.3 | sans, success tone | "+12.4%" delta on Active users trend card — verbatim prototype line 439 |
| Tiny mono | 10.5 px | 600 / 400 | 1 | mono | Project key chip on Projeler table + velocity card key — verbatim prototype lines 333, 467 |
| Subtitle | 13 px | 400 | 1.4 | sans, `--fg-muted` | Page header subtitle ("Organizasyon genelinde izinler...") — verbatim prototype line 16 |

**Weights used (exactly 2 — Phase 14 introduces no new weight):**
- **400** — body text default, regular content
- **600** — bold variants (titles, labels uppercase, semantic emphasis, tab active, big numbers, table column headers)

The Phase 13 baseline lists 600 + 400. Phase 14 also uses **500** in the Pending request row's `pm.name.split(" ")[0]` first-name emphasis (prototype line 82), the role-filter pill non-active state (prototype line 160), and table cell `fontWeight: 500` for project name (prototype line 334). These are pre-existing weights from the Phase 8 primitive layer (Tabs `fontWeight: 500` non-active, Badge `fontWeight: 500`) — Phase 14 does NOT introduce 500 as a new weight; it uses what the primitives already ship.

**Authoritative weight count for the checker:** 2 weights at the design-system level (400 / 600). Primitive-internal 500 (Tabs/Badge non-active) is invisible to the contract surface — checker should count 2.

**Line-height:** body 1.5 (matches `globals.css` line 152 `line-height: 1.45` baseline + per-component 1.5 for prose lines). Headings 1.2-1.3 (auto via single-line). Roller card description at `lineHeight: 1.5` per prototype line 221. Şablonlar description at `lineHeight: 1.5` per prototype line 372.

**Tabular numerals:** all numeric metrics + monospace text use `font-variant-numeric: tabular-nums` (already on `.mono` utility in `globals.css` line 163; keep `style={{ fontVariantNumeric: "tabular-nums" }}` on inline numeric displays per prototype lines 468 — velocity percentage).

---

## Color

Phase 14 inherits the **Terracotta** light preset + **Midnight** dark preset already in `Frontend2/app/globals.css`. Zero new tokens. Zero new color combinations.

### 60 / 30 / 10 split

| Layer | Token (light) | Token (dark) | Usage in Phase 14 |
|-------|---------------|--------------|-------------------|
| **60% Dominant** | `--bg` `oklch(0.985 0.006 75)` | `oklch(0.17 0.02 280)` | Page background — `<main>` body of all 8 admin sub-routes; AppShell already paints it |
| **30% Secondary** | `--surface` `oklch(1 0 0)` (cards) + `--bg-2` (admin event group bg) + `--surface-2` `oklch(0.975 0.006 70)` (subtle fill) | `oklch(0.22 0.025 280)` / `oklch(0.20 0.025 280)` / `oklch(0.25 0.028 280)` | All Card containers (StatCards, table containers, Roller cards, Şablonlar cards, Pending Requests Card); pending-request note tinted block uses `--surface-2`; Project key chip background; role-filter pill non-active background; Permission Matrix toggle off-state; Stats velocity card inner bg |
| **10% Accent** | `--primary` `oklch(0.60 0.17 40)` (Terracotta 600) | `oklch(0.72 0.17 290)` (Indigo) | (see explicit reserved-for list below) |

**Accent (`--primary`) reserved for — explicit list (10 items):**

1. Active NavTabs primitive bottom border (8 admin sub-route nav strip)
2. Active SegmentedControl raised pill (Users role-filter active option, prototype line 160)
3. Page header `<Icons.Shield>` icon color (verbatim prototype line 13)
4. Pending Requests "Onayla" Button (variant="primary", prototype line 93)
5. Active NavTab badge `tone="primary"` when `isActive` — count badges on Kullanıcılar/Projeler nav items per prototype lines 26, 29 (NavTabs `badge` prop renders a Phase 8 `Badge` primitive internally; same `tone="primary"` token usage as the original Tabs primitive)
6. Velocity mini-bar last bar (prototype line 471: `i === 6 ? "var(--primary)" : "var(--border-strong)"`)
7. Permission Matrix toggle ON-state (`var(--primary)` per prototype line 300) — in Phase 14 every toggle is **disabled** so the ON-state fill is decorative-only
8. Recent admin events bullet dot (verbatim prototype line 130)
9. Active users trend SVG line stroke (verbatim prototype line 444)
10. Active users trend SVG filled area gradient `color-mix(in oklch, var(--primary) 12%, transparent)` (verbatim prototype line 446)

**Excluded from this list — these prototype elements use OTHER tokens, NOT `--primary`:**
- Roller "Admin" card icon-fill uses `color-mix(in oklch, var(--priority-critical) 18%, transparent)` — danger semantic, not brand-primary (prototype line 205)
- "Yeni rol oluştur" placeholder dashed border + plus icon use `--fg-muted` (prototype line 233) — muted, not primary; in Phase 14 the card is also `cursor: not-allowed` per D-A4

**Used as accent fill (color-mix variants) in Phase 14:**
- Active users trend area gradient: `color-mix(in oklch, var(--primary) 12%, transparent)` — verbatim line 446 (also slot #10 above)
- Pending Requests "Onayla" Button — Button primitive's variant="primary" already uses `--primary` internally
- Page header Shield icon — `color: var(--primary)` (verbatim line 13, also slot #3 above)

### Semantic / status palette (re-used, not extended)

| Token | Light | Dark | Phase 14 usage |
|-------|-------|------|----------------|
| `--status-todo` | `oklch(0.62 0.012 60)` | unchanged | Methodology bar (Waterfall) `--status-review` per line 451 NOT --status-todo; **status-todo not used in Phase 14 directly** |
| `--status-progress` | `oklch(0.55 0.15 230)` | unchanged | Role distribution bar — Project Manager (verbatim line 106); Methodology bar — Scrum (verbatim line 451 says `--status-progress`); Roller PM card icon-fill (verbatim line 206); Audit `Action` column text color (verbatim line 418) |
| `--status-review` | `oklch(0.65 0.15 65)` | unchanged | Methodology bar — Waterfall (verbatim line 451); Şablonlar Badge tone="warning" for `sequential-locked` mode (verbatim line 377) |
| `--status-done` | `oklch(0.58 0.14 150)` | unchanged | Stats trend Badge tone="success" `+12.4%` (verbatim line 439); Status Badge tone="success" `Aktif` on Users table (verbatim line 186) |
| `--priority-critical` | `oklch(0.58 0.22 25)` | unchanged | Role distribution bar — Admin (verbatim line 105); Roller Admin card icon-fill (verbatim line 205); Users Role Badge tone="danger" for Admin (verbatim line 183); Audit row Risk badge — REMOVED PER D-Z1 |
| `--fg-muted` | `oklch(0.48 0.018 55)` | `oklch(0.70 0.02 280)` | Role distribution bar — Member (verbatim line 107); Roller Member card icon-fill (verbatim line 207); Roller Guest card icon-fill `--fg-subtle` per line 208; secondary text everywhere |
| `--fg` `--fg-subtle` | (light/dark) | (light/dark) | Primary/tertiary text everywhere |
| `--border` `--border-strong` | `oklch(0.90/0.82 ...)` | `oklch(0.32/0.42 ...)` | Card borders, table row separators, tabs base line, Permission Matrix toggle off-state ring, Velocity bar non-last segment fill (verbatim line 471) |

### Destructive

| Use case | Token | Visual |
|----------|-------|--------|
| Pending Requests "Reddet" Button | `Button variant="ghost"` (verbatim prototype line 94 — NO destructive button background) | Ghost variant; reading `--fg` text color |
| Users MoreH menu items: "Devre dışı bırak" + "Sil" | `var(--priority-critical)` text color on the menu item itself | Menu item label red-tinted; matches `Frontend2/components/sidebar.tsx` SidebarUserMenu line 146 pattern (Çıkış Yap red) |
| Audit `Action` column when action prefix = `user.deactivate` / `project.delete` | `var(--priority-critical)` text color (matches existing audit_log convention; was `--status-progress` for create/update) | Bare text-color swap |
| Şablonlar MoreH "Sil" / Projeler MoreH "Sil" / Audit JSON export 429 hard-fail | `tone="danger"` toast / AlertBanner | Existing primitives |
| Bulk action `Atomic — all-or-none failure` | `tone="danger"` AlertBanner | Existing |
| Permission Matrix info banner | `tone="info"` AlertBanner reading `--status-progress` | Existing — non-destructive |

**Destructive confirmations (D-B1, D-A6, D-B5):**

| Action | Confirmation | Approach |
|--------|--------------|----------|
| Devre dışı bırak (single user) | **ConfirmDialog** — "{user.name} devre dışı bırakılsın mı? Kullanıcı oturumunu kaybedecek." / "Deactivate {user.name}? They will lose access immediately." | Existing `ConfirmDialog` from `Frontend2/components/projects/confirm-dialog.tsx` (Phase 10 D-25); tone="danger" |
| Sil (single user) | **ConfirmDialog** — "{user.name} silinsin mi? Bu işlem geri alınamaz; kullanıcının atandığı görevler atanmamış kalır." / "Delete {user.name}? This cannot be undone; assigned tasks will become unassigned." | tone="danger"; primary CTA "Sil" |
| Şifre sıfırla | **No confirm** — direct trigger; toast success "Şifre sıfırlama linki {email} adresine gönderildi." / "Password reset link sent to {email}." | One-click |
| Rol değiştir | **No confirm** — direct PATCH; toast success "Rol güncellendi: {old} → {new}". Audit log captures the change (Plan 14-09 enrichment) | One-click + post-action toast |
| Bulk deactivate | **ConfirmDialog** — "{N} kullanıcı devre dışı bırakılsın mı?" / "Deactivate {N} users?"; danger tone; lists first 5 users by name then "+ {N-5} more" | tone="danger" |
| Bulk role change | **No confirm** — direct PATCH; toast summary `Rol değiştirildi: {N} kullanıcı {role} oldu.` / `{N} users changed to {role}.`; cumulative undo button in toast for 6 seconds (single API call to revert) | Optimistic + soft-undo toast |
| Onayla (Pending Request) | **No confirm** — direct POST; optimistic update (D-W2); toast success | Optimistic |
| Reddet (Pending Request) | **ConfirmDialog** — "{user.name}'in {project.name}'e katılım talebi reddedilsin mi?" / "Reject {user.name}'s request to join {project.name}?" | tone="warning"; primary CTA "Reddet" |
| Şablon Sil | **ConfirmDialog** — "{template.name} şablonu silinsin mi? {N} aktif proje bu şablonu kullanıyor." / "Delete template {template.name}? {N} active projects depend on it." | tone="danger"; if N > 0 show warning text, primary CTA disabled until user confirms via secondary checkbox "Yine de sil" |
| Proje Arşivle (admin Projeler tab) | **ConfirmDialog** — "{project.name} arşivlensin mi? Proje salt-okuma olur ve listelerde gizlenir." / "Archive {project.name}? It becomes read-only and is hidden from lists." | tone="warning"; primary CTA "Arşivle" |
| Proje Sil (admin Projeler tab) | **ConfirmDialog** — Two-step confirm: 1) Type project key ("{project.key}") to confirm + 2) tone="danger" CTA "Sil" only enabled after exact key match | tone="danger" + key-typing guard |
| Audit JSON export ≥ 50k rows | **AlertBanner** at top of Audit table — "Filtre çok geniş ({N} satır). Lütfen tarihi daraltın." / "Filter too wide ({N} rows). Please narrow the date range." + disabled JSON button | tone="warning" |
| Toplu davet > 500 satır | **Modal AlertBanner** — "Maksimum 500 satır işlenebilir. CSV {N} satır içeriyor; ilk 500 satır işlenecek mi?" / "Max 500 rows. CSV has {N}; process first 500?" | tone="warning"; primary CTA "İlk 500'ü İşle" / "Process first 500" |
| Çıkış Yap (admin sub-route) | **None** — already covered by Phase 13 D-D3 AvatarDropdown | Inherited |

**Reuses + ONE primitive extension required:**

Phase 14 destructive flows reuse Phase 10 D-07 `ToastProvider`, existing `AlertBanner`, and the existing `ConfirmDialog` from `Frontend2/components/projects/confirm-dialog.tsx` — but the existing ConfirmDialog interface is:

```ts
interface ConfirmDialogProps {
  open: boolean
  title: string
  body: string
  confirmLabel?: string  // defaults to "Onayla"
  cancelLabel?: string   // defaults to "İptal"
  onConfirm: () => void
  onCancel: () => void
}
```

The confirm Button is hardcoded `variant="primary"` (line 33 of that file). **The `tone` prop referenced throughout this UI-SPEC's destructive table (`tone="danger"`, `tone="warning"`) DOES NOT EXIST yet** — earlier docs implied a Phase 10 D-25 extension that was never landed.

**Phase 14 Plan 14-01 (Wave 0 infra) MUST extend `ConfirmDialog`:**

```ts
interface ConfirmDialogProps {
  open: boolean
  title: string
  body: string
  confirmLabel?: string
  cancelLabel?: string
  tone?: "primary" | "danger" | "warning"  // NEW — defaults to "primary" for backward compat
  onConfirm: () => void
  onCancel: () => void
}
```

Implementation contract:
- `tone="primary"` (default) — confirm Button stays `variant="primary"`. Existing call sites unchanged.
- `tone="danger"` — confirm Button switches to `variant="danger"`. Title gets a small `Icons.AlertTriangle size={14} color="var(--priority-critical)"` prefix.
- `tone="warning"` — confirm Button stays `variant="primary"` (amber tone is reserved for AlertBanner only — destructive button background is always red OR ghost per prototype). Title gets a `Icons.AlertCircle size={14} color="var(--status-review)"` prefix.

**Pre-verified during this UI-SPEC pass (no further executor doubt):**
- `Frontend2/components/primitives/button.tsx` line 3 confirms 5 variants ship: `primary | secondary | ghost | subtle | danger`. Lines 39-65 implement `danger` with `var(--priority-critical)` background + `var(--primary-fg)` text + matching shadow. **Button extension is NOT required.**
- `Frontend2/components/projects/confirm-dialog.tsx` lines 5-13 confirm the existing interface lacks `tone`. **ConfirmDialog extension IS required.**

**Executor steps (Plan 14-01 Task 1, in order):**
1. Extend `ConfirmDialog` interface to add `tone?: "primary" | "danger" | "warning"` (default `"primary"` for backward compat).
2. Wire the title-icon prefix logic via a small inline switch on `tone` inside the existing render — no new file.
3. Update the body Button render to read `variant={tone === "danger" ? "danger" : "primary"}`.
4. Audit existing call sites (`Frontend2/components/projects/`, `Frontend2/components/lifecycle/`, `Frontend2/components/admin/` — once Plan 14-01 lands components there) — confirm zero `tone` prop usage at the time of Plan 14-01 land = zero behavior change for Phase 10/11/12 consumers.

**Tests (Plan 14-01 Wave 0 RTL):**
- `tone="primary"` (default) renders confirm Button as `variant="primary"` (existing behavior, no regression).
- `tone="danger"` renders confirm Button as `variant="danger"` + AlertTriangle icon prefix.
- `tone="warning"` renders confirm Button as `variant="primary"` + AlertCircle icon prefix.
- Existing Phase 10/11/12 ConfirmDialog consumers without `tone` prop still render exactly as before.

This is the ONLY primitive interface extension Phase 14 requires beyond NavTabs (D-C4). All other primitives (Tabs, Card, Button, Badge, Input, Avatar, AvatarStack, SegmentedControl, Toggle, Kbd, ProgressBar, Section, Container, AlertBanner, StatusDot, PriorityChip) are reused as-shipped.

---

## Copywriting Contract

Every string ships with a TR + EN pair via `T(tr, en)` from `useApp().language` (Phase 8 D-03). Strings below are exhaustive — planner does not invent new copy without flagging it.

### Surface A — `app/(shell)/admin/layout.tsx` (page header + tabs strip)

| Element | TR | EN | Notes |
|---------|----|----|-------|
| Page title (browser tab) | "Yönetim Konsolu — SPMS" | "Admin Console — SPMS" | New |
| Page H1 | "Yönetim Konsolu" | "Admin Console" | Verbatim prototype line 14 |
| Page subtitle | "Organizasyon genelinde izinler, kullanıcılar ve şablonlar" | "Org-wide permissions, users & templates" | Verbatim line 16 |
| Header button — Rapor al | "Rapor al" | "Export" | Verbatim line 19 |
| Header button — Denetim günlüğü | "Denetim günlüğü" | "Audit log" | Verbatim line 20 |
| Tab — Genel | "Genel" | "Overview" | Verbatim line 25 |
| Tab — Kullanıcılar | "Kullanıcılar" | "Users" | Verbatim line 26 |
| Tab — Roller | "Roller" | "Roles" | Verbatim line 27 |
| Tab — İzin Matrisi | "İzin Matrisi" | "Permissions" | Verbatim line 28 |
| Tab — Projeler | "Projeler" | "Projects" | Verbatim line 29 |
| Tab — Şablonlar | "Şablonlar" | "Templates" | Verbatim line 30 |
| Tab — Audit | "Audit" | "Audit" | Verbatim line 31 (English-only — kept neutral) |
| Tab — İstatistik | "İstatistik" | "Stats" | Verbatim line 32 |
| Route guard — non-admin redirect toast | "Bu sayfaya erişim yetkiniz yok." | "You don't have permission to access this page." | **[IMPROVEMENT]** D-C3 |
| Route guard — unauthenticated redirect (no toast — login redirect handles UX) | (none) | (none) | Verbatim Phase 10 pattern |

### Surface B — `/admin` page (Genel / Overview)

| Element | TR | EN | Notes |
|---------|----|----|-------|
| StatCard 1 — Kullanıcı | "Kullanıcı" / "+{N} bu ay" | "Users" / "+{N} this mo." | Verbatim prototype lines 55 |
| StatCard 2 — Aktif Proje | "Aktif Proje" / "+{N}" | "Active projects" / "+{N}" | Verbatim line 56 |
| StatCard 3 — Onay Bekleyen | "Onay Bekleyen" / "bugün" | "Pending" / "today" | Verbatim line 57 |
| StatCard 4 — Şablon | "Şablon" / "{N} özel" | "Templates" / "{N} custom" | Verbatim line 58 |
| StatCard 5 — Depolama | "Depolama" / "%{P} dolu" | "Storage" / "{P}% used" | Verbatim line 59 |
| Pending Requests card title | "Bekleyen Proje Katılım İstekleri" | "Pending Project Join Requests" | Verbatim line 65 |
| Pending Requests "Tümünü gör" button | "Tümünü gör" | "View all" | Verbatim line 67 |
| Pending Requests row primary line | "{pmFirstName} — {userFirstName} kullanıcısını {projectName} projesine eklemek istiyor" | "{pmFirstName} requested {userFirstName} to join {projectName}" | Verbatim lines 82-87 — exact glue text & spans, see "Detail format" below |
| Pending Requests row note (italic) | (raw note text inside quote marks: `"{r.note}"`) | (same) | Verbatim line 89 |
| Pending Requests row time | (relative time e.g. "12 dk", "1 sa") | (same: "12m", "1h") | Verbatim line 90; reuse `formatRelativeTime` from `Frontend2/lib/activity-date-format.ts` |
| Pending Requests action — Onayla | "Onayla" | "Approve" | Verbatim line 93 |
| Pending Requests action — Reddet | "Reddet" | "Reject" | Verbatim line 94 |
| Pending Requests empty | "Onay bekleyen istek yok." | "No pending requests." | **[IMPROVEMENT]** D-F2 — prototype lacks empty state |
| Pending Requests "Tümünü gör" modal title | "Tüm bekleyen istekler" | "All pending requests" | New (Modal Anatomy section) |
| Role distribution card title | "Rol Dağılımı" | "Role distribution" | Verbatim line 103 |
| Role distribution row — Admin | "Admin" | "Admin" | Verbatim line 105 |
| Role distribution row — PM | "Project Manager" | "Project Manager" | Verbatim line 106 |
| Role distribution row — Member | "Member" | "Member" | Verbatim line 107 |
| Recent admin events card title | "Son Yönetim Olayları" | "Recent admin events" | Verbatim line 121 |
| Recent admin events empty | "Son yönetim olayı yok." | "No recent admin events." | **[IMPROVEMENT]** D-F2 |
| Recent admin events — view all link | "Audit'a git →" | "Open Audit →" | **[IMPROVEMENT]** D-W1 — prototype is mock-only; real recent events surface needs a deep-link to `/admin/audit` for full filtering |

**Pending Requests primary line — exact glue format (verbatim prototype lines 82-87):**

```
TR: <b>{pmFirst}</b> <muted>—</muted> <b>{userFirst}</b> <muted> kullanıcısını </muted> <b>{projectName}</b> <muted> projesine eklemek istiyor</muted>
EN: <b>{pmFirst}</b> <muted>requested</muted> <b>{userFirst}</b> <muted>to join</muted> <b>{projectName}</b>
```

The `<b>` spans use `fontWeight: 600`; `<muted>` spans use `color: var(--fg-muted)`. Same span structure as prototype.

### Surface C — `/admin/users` page (Kullanıcılar)

| Element | TR | EN | Notes |
|---------|----|----|-------|
| Search placeholder | "Kullanıcı ara…" | "Search users…" | Verbatim prototype line 157 |
| Role filter — All | "Tümü" | "All" | Verbatim line 161 |
| Role filter — Admin / PM / Member | (literals: "Admin" / "Project Manager" / "Member") | (same) | Verbatim line 159 |
| CSV button | "CSV" | "CSV" | Verbatim line 166 (locale-neutral) |
| Bulk invite button | "Toplu davet" | "Bulk invite" | Verbatim line 167 |
| Add user button | "Kullanıcı ekle" | "Add user" | Verbatim line 168 |
| Table column — Name | "Ad Soyad" | "Name" | Verbatim line 171 |
| Table column — Email | "Email" | "Email" | Verbatim |
| Table column — Role | "Rol" | "Role" | Verbatim |
| Table column — Projects | "Projeler" | "Projects" | Verbatim |
| Table column — Last Seen | "Son Giriş" | "Last Seen" | Verbatim |
| Table column — Status | "Durum" | "Status" | Verbatim |
| Status Badge — Active | "Aktif" | "Active" | Verbatim line 186 |
| Status Badge — Inactive | "Pasif" | "Inactive" | **[IMPROVEMENT]** D-A6 — prototype shows only Aktif rows; deactivate flow needs an Inactive label |
| Last seen — "Just now" | "Şimdi" | "Now" | Verbatim line 185 |
| Last seen format | (relative: "{N} dk" / "{N} sa" / "{N} gün") | ("{N}m" / "{N}h" / "{N}d") | Verbatim — reuse `formatRelativeTime` |
| Pagination caption | "{N} kullanıcı" | "{N} users" | Verbatim line 192 |
| Pagination page indicator | "{P} / {M}" | (same) | Verbatim line 195 |
| MoreH menu — Reset password | "Şifre sıfırla" | "Reset password" | New D-B3 |
| MoreH menu — Change role | "Rolü değiştir" | "Change role" | New D-A6 |
| MoreH menu — Change role submenu — Admin/PM/Member | (literals) | (same) | New |
| MoreH menu — Deactivate | "Devre dışı bırak" | "Deactivate" | New D-A6 |
| MoreH menu — Reactivate (when user is_active=false) | "Tekrar aktif et" | "Reactivate" | **[IMPROVEMENT]** D-A6 |
| MoreH menu — Delete | "Sil" | "Delete" | New D-A6; red-tinted text |
| Bulk-select toolbar (visible when ≥1 row checked) | "{N} seçili" | "{N} selected" | **[IMPROVEMENT]** D-B7 |
| Bulk action — Deactivate | "Toplu devre dışı bırak" | "Deactivate selected" | New D-B7 |
| Bulk action — Change role | "Toplu rol değiştir" | "Change role for selected" | New D-B7 |
| Bulk action — Cancel | "Vazgeç" | "Cancel" | New |
| Empty state — no users match search | "Aramayla eşleşen kullanıcı bulunamadı." | "No users match the search." | **[IMPROVEMENT]** D-F2 |
| Empty state — no users at all (impossible — admin always exists, but defended) | "Henüz kullanıcı yok." | "No users yet." | **[IMPROVEMENT]** D-F2 |

### Surface D — `/admin/roles` page (Roller — RBAC PLACEHOLDER per D-A2..A5)

| Element | TR | EN | Notes |
|---------|----|----|-------|
| Page-level AlertBanner (top of page) | "Bu sayfa görüntüleme amaçlıdır. Granüler rol/izin yönetimi v3.0 sürümünde gelecek; şu an yetkilendirme **Admin / Project Manager / Member** rolü ve proje üyeliği üzerinden çalışıyor." | "This page is view-only. Granular role/permission management will arrive in v3.0; authorization currently runs through the **Admin / Project Manager / Member** role and project membership." | **[IMPROVEMENT]** D-A3 |
| Admin card — Title | "Admin" | "Admin" | Verbatim |
| Admin card — Description | "Sistem geneli — tüm projelerde tam yetkili. Sadece admin invite edebilir, user role değiştirebilir, proje silebilir." | "System-wide — full access in every project. Only admins can invite, change roles, and delete projects." | **[IMPROVEMENT]** D-A5 — REWRITES prototype's misleading description ("Tüm sistem erişimi, fatura ve güvenlik ayarları." / "Full system access incl. billing and security.") to reflect the v2.0 reality |
| Admin card — Users count label | "Kullanıcı:" | "Users:" | Verbatim line 225 |
| Admin card — Permissions count | (HIDDEN — D-A5 removes the misleading "İzin: 28" because permissions don't exist as a backend entity) | (HIDDEN) | **[IMPROVEMENT]** D-A2 |
| Admin card — Edit button | (REPLACED — see below) | (REPLACED) | **[IMPROVEMENT]** D-A4 — "Düzenle" → "v3.0'da" disabled label OR "Detay" link to a tooltip explaining v3.0; **DECISION: Remove "Düzenle" button entirely** for the 4 system-role cards. Card is information-only |
| Project Manager card — Title | "Project Manager" | "Project Manager" | Verbatim |
| Project Manager card — Description | "Proje bazlı — her projede ayrı atanır (Settings > Üyeler). Bir kullanıcı bir projede PM, başka projede Üye olabilir." | "Project-scoped — assigned per-project in Settings > Members. A user may be PM in one project and Member in another." | **[IMPROVEMENT]** D-A5 |
| Member card — Title | "Member" | "Member" | Verbatim |
| Member card — Description | "Proje bazlı — atandığı projelerde görev yönetir." | "Project-scoped — manages tasks on assigned projects." | **[IMPROVEMENT]** D-A5 |
| Guest card — Title | "Guest" | "Guest" | Verbatim |
| Guest card — Description | "Yalnızca salt-okuma. v3.0'da gelecek." | "Read-only. Coming in v3.0." | **[IMPROVEMENT]** D-A5 |
| Guest card — v3.0 Badge | "v3.0" | "v3.0" | New D-A5; tone="warning" |
| "Yeni rol oluştur" placeholder card title | "Yeni rol oluşturma" | "Create new role" | **[IMPROVEMENT]** D-A4 — slightly rewords prototype "Yeni rol oluştur" to "oluşturma" (gerundive) to telegraph "feature, not action" |
| "Yeni rol oluştur" placeholder subtitle | "v3.0'da gelecek" | "Coming in v3.0" | **[IMPROVEMENT]** D-A4 — replaces prototype's misleading "Özelleştirilmiş izin seti" / "Custom permission set" text |
| "Yeni rol oluştur" placeholder tooltip on hover | "Granüler RBAC v3.0 sürümünde gelecek." | "Granular RBAC arrives in v3.0." | **[IMPROVEMENT]** D-A4 |

### Surface E — `/admin/permissions` page (İzin Matrisi — RBAC PLACEHOLDER per D-A3)

| Element | TR | EN | Notes |
|---------|----|----|-------|
| Page-level AlertBanner (top of page) | "Bu sayfa demo amaçlıdır. Granüler izin yönetimi v3.0 sürümünde gelecek; şu an yetkilendirme **Admin / Project Manager / Member** rolü ve proje üyeliği üzerinden çalışıyor." | "This page is demo-only. Granular permission management arrives in v3.0; authorization currently runs through the **Admin / Project Manager / Member** role and project membership." | **[IMPROVEMENT]** D-A3 — page-level info banner |
| Card header title | "İzin Matrisi" | "Permission Matrix" | Verbatim prototype line 280 |
| Card header subtitle | "Rol başına tüm izinler tek bakışta" | "All permissions by role at a glance" | Verbatim line 281 |
| v3.0 Badge in card header | "v3.0" | "v3.0" | **[IMPROVEMENT]** D-A3 — tone="warning" badge |
| Kopyala/Clone button | "Kopyala" | "Clone" | Verbatim line 283 — but **[IMPROVEMENT]** disabled with tooltip "v3.0'da gelecek" |
| Group label — Projects | "Projeler" | "Projects" | Verbatim line 245 |
| Group label — Tasks | "Görevler" | "Tasks" | Verbatim line 251 |
| Group label — Members & Roles | "Üyeler & Roller" | "Members & Roles" | Verbatim line 257 |
| Group label — Workflow | "İş Akışı" | "Workflow" | Verbatim line 262 |
| Permission row — Create project | "Proje oluştur" | "Create project" | Verbatim line 246 |
| Permission row — Edit project | "Proje düzenle" | "Edit project" | Verbatim line 247 |
| Permission row — Delete project | "Proje sil" | "Delete project" | Verbatim line 248 |
| Permission row — Archive | "Arşivle" | "Archive" | Verbatim line 249 |
| Permission row — Create task | "Görev oluştur" | "Create task" | Verbatim line 252 |
| Permission row — Change assignee | "Atama değiştir" | "Change assignee" | Verbatim line 253 |
| Permission row — Change status | "Durum değiştir" | "Change status" | Verbatim line 254 |
| Permission row — Delete task | "Görev sil" | "Delete task" | Verbatim line 255 |
| Permission row — Invite user | "Kullanıcı davet et" | "Invite user" | Verbatim line 258 |
| Permission row — Assign role | "Rol ata" | "Assign role" | Verbatim line 259 |
| Permission row — Remove member | "Üye çıkar" | "Remove member" | Verbatim line 260 |
| Permission row — Edit workflow | "İş akışı düzenle" | "Edit workflow" | Verbatim line 263 |
| Permission row — Edit lifecycle | "Yaşam döngüsü düzenle" | "Edit lifecycle" | Verbatim line 264 |
| Permission row — Publish template | "Şablon yayınla" | "Publish template" | Verbatim line 265 |
| Toggle hover/focus tooltip (every toggle) | "RBAC altyapısı v3.0 sürümünde gelecek." | "RBAC arrives in v3.0." | **[IMPROVEMENT]** D-A3 |
| Toggle aria-label | "{permission_label} — {role} — disabled" | (same) | **[IMPROVEMENT]** D-A3 a11y |

### Surface F — `/admin/projects` page (Projeler)

| Element | TR | EN | Notes |
|---------|----|----|-------|
| Search placeholder | "Proje ara…" | "Search…" | Verbatim prototype line 320 |
| Export button | "Dışa aktar" | "Export" | Verbatim line 322 |
| New project button | "Yeni proje" | "New project" | Verbatim line 323 |
| Table column — Key | "Key" | "Key" | Verbatim line 326 (locale-neutral) |
| Table column — Name | "İsim" | "Name" | Verbatim |
| Table column — Method | "Yöntem" | "Method" | Verbatim |
| Table column — Lead | "Yönetici" | "Lead" | Verbatim |
| Table column — Tasks | "Görevler" | "Tasks" | Verbatim |
| Table column — Progress | "İlerleme" | "Progress" | Verbatim |
| Table column — Created | "Oluşturma" | "Created" | Verbatim |
| Tasks cell suffix | "{N} bitti" | "{N} done" | Verbatim line 337 |
| MoreH menu — Arşivle | "Arşivle" | "Archive" | New D-B5 |
| MoreH menu — Sil | "Sil" | "Delete" | New D-B5 (red-tinted) |
| Empty state | "Filtreyle eşleşen proje yok." | "No projects match the filter." | **[IMPROVEMENT]** D-F2 |
| Empty state (no projects in system) | "Henüz proje yok. **Yeni proje oluştur** ile başla." | "No projects yet. Start with **New project**." | **[IMPROVEMENT]** D-F2 |

### Surface G — `/admin/workflows` page (Şablonlar)

| Element | TR | EN | Notes |
|---------|----|----|-------|
| Card title — name | (literal: "Scrum" / "Waterfall" / "Kanban" / etc.) | (same) | Verbatim — process_template names from backend |
| Custom Badge | "Özel" | "Custom" | Verbatim line 370 |
| Card description (placeholder copy from prototype) | (read from backend `template.description`) | (same) | Backend-driven; localization optional per template |
| Mode Badge — sequential-locked | "Locked" | "Locked" | Verbatim line 378 (English-only) |
| Mode Badge — continuous | "Continuous" | "Continuous" | Verbatim line 378 |
| Mode Badge — flexible | "Flexible" | "Flexible" | Verbatim line 378 |
| Card footer — N proje | "{N} proje" | "{N} projects" | Verbatim line 381 |
| MoreH menu — Düzenle | "Düzenle" | "Edit" | New D-B1 — redirects to `/workflow-editor?templateId=…` |
| MoreH menu — Klonla | "Klonla" | "Clone" | New D-B1 |
| MoreH menu — Sil | "Sil" | "Delete" | New D-B1 (red-tinted) |
| Empty state | "Henüz şablon yok." | "No templates yet." | **[IMPROVEMENT]** D-F2 |

### Surface H — `/admin/audit` page (Audit)

| Element | TR | EN | Notes |
|---------|----|----|-------|
| Search placeholder | "actor, action, target…" | "actor, action, target…" | Verbatim prototype line 403 |
| Quick filter — Last 24h | "Son 24 saat" | "Last 24h" | Verbatim line 404 |
| Filter button | "Filtre" | "Filter" | Verbatim line 405 |
| JSON export button | "JSON" | "JSON" | Verbatim line 407 |
| Table column — Time | "Zaman" | "Time" | Verbatim line 410 (TR rewritten from prototype's English-only "Time" for consistency) |
| Table column — Actor | "Aktör" | "Actor" | Verbatim line 410 |
| Table column — Action | "İşlem" | "Action" | Verbatim line 410 |
| Table column — Target | "Hedef" | "Target" | Verbatim line 410 |
| Table column — Detay (REPLACES `Meta` + `IP` + `Risk` per D-Z1) | "Detay" | "Detail" | **[IMPROVEMENT]** D-D5 |
| Filter modal — Title | "Audit Filtresi" | "Audit Filter" | New (Modal Anatomy) |
| Filter modal — Date from | "Başlangıç" | "From" | New |
| Filter modal — Date to | "Bitiş" | "To" | New |
| Filter modal — Actor | "Aktör (kullanıcı)" | "Actor (user)" | New |
| Filter modal — Action prefix | "İşlem öneki (örn. `task.` veya `project.archive`)" | "Action prefix (e.g. `task.` or `project.archive`)" | New |
| Filter modal — Apply | "Uygula" | "Apply" | New |
| Filter modal — Clear | "Temizle" | "Clear" | New |
| Filter modal — Cancel | "Vazgeç" | "Cancel" | New |
| Active filter chip strip (above table when filters applied) | "Aktör: {name} · İşlem: {prefix}* · {date_range}" | (same) | **[IMPROVEMENT]** D-Z2 — visible filter state with `×` chip-clear per facet |
| Pagination — page size | "Sayfa boyutu" | "Page size" | New D-Z2 |
| Pagination — Prev | (no label — chevron icon only) | (same) | New |
| Pagination — Next | (no label — chevron icon only) | (same) | New |
| Pagination — current page | "Sayfa {P} / {M}" | "Page {P} / {M}" | New D-Z2 |
| Pagination — total | "Toplam: {N} kayıt" | "Total: {N} entries" | New D-Z2 |
| Empty state — no rows match filter | "Filtreyle eşleşen audit kaydı yok." | "No audit entries match the filter." | **[IMPROVEMENT]** D-F2 |
| Empty state — no rows at all | "Henüz audit kaydı yok." | "No audit entries yet." | **[IMPROVEMENT]** D-F2 |
| 50k cap warning AlertBanner | "Filtre çok geniş ({N} satır). Sadece son 50.000 kayıt gösteriliyor; daha eski kayıtlar için JSON dışa aktarımı kullanın." | "Filter too wide ({N} rows). Only the most recent 50,000 are shown; use JSON export for older entries." | **[IMPROVEMENT]** D-Z2 + D-B8 |

**Detay column verb formatter — see "Cross-cutting Jira-style audit detail" section below.**

### Surface I — `/admin/stats` page (İstatistik)

| Element | TR | EN | Notes |
|---------|----|----|-------|
| Active users trend card title | "Aktif kullanıcı eğilimi" | "Active users trend" | Verbatim prototype line 436 |
| Active users trend subtitle | "Son 30 gün" | "Last 30 days" | Verbatim line 437 |
| Active users trend delta Badge | "+{P}%" | (same) | Verbatim line 439 |
| Active users trend tooltip — daily count | "{date}: {N} aktif kullanıcı" | "{date}: {N} active users" | New |
| Methodology card title | "Metodoloji Kullanımı" | "Methodology usage" | Verbatim line 450 |
| Methodology row — Scrum | "Scrum" | "Scrum" | Verbatim |
| Methodology row — Kanban | "Kanban" | "Kanban" | Verbatim |
| Methodology row — Waterfall | "Waterfall" | "Waterfall" | Verbatim |
| Methodology row — Iterative | "Iterative" | "Iterative" | New (D-X3 covers all enum values) |
| Methodology row — Other | "Diğer" | "Other" | New (catch-all for Incremental/Evolutionary/RAD/V-Model/Spiral) |
| Methodology row — percentage | "%{P}" | "{P}%" | Verbatim line 454 |
| Velocity card title | "Proje başına velocity" | "Velocity per project" | Verbatim line 463 |
| Velocity card key (mono) | "{key}" | (same) | Verbatim line 467 |
| Velocity card progress | "%{P}" | "{P}%" | Verbatim line 468 |
| Velocity card 30-project cap notice (when N > 30) | "İlk 30 aktif proje gösteriliyor." | "Showing top 30 active projects." | **[IMPROVEMENT]** D-X4 |
| Empty state (no audit_log rows in 30d) | "Son 30 günde aktivite yok." | "No activity in the last 30 days." | **[IMPROVEMENT]** D-F2 |
| Empty state (no projects with iterations) | "Hiç proje velocity verisi yok." | "No project velocity data." | **[IMPROVEMENT]** D-F2 |

### Surface J — Cross-cutting Jira-style audit detail (D-D1..D6)

The new `audit-event-mapper.ts` semantic types (Plan 14-10) have these verb formatters consumed by both `/admin/audit` `Detay` column AND ProjectDetail Activity tab + Profile Activity tab + Dashboard ActivityFeed.

| Semantic Type | Verb formatter (TR / EN) — uses `extra_metadata` enrichment |
|--------------|--------------|
| `task_field_updated` | "değiştirdi **'{task_title}'** {field_label}: {old_value_label} → {new_value_label}" / "changed **'{task_title}'** {field_label}: {old_value_label} → {new_value_label}" |
| `task_status_changed` (existing — kept) | "durumunu değiştirdi **'{task_title}'**: {old_label} → {new_label}" / "changed status of **'{task_title}'**: {old_label} → {new_label}" |
| `task_assigned` (existing — enriched) | "atadı **'{task_title}'** görevini **{assignee_name}**'e" / "assigned **'{task_title}'** to **{assignee_name}**" |
| `task_created` (existing — enriched) | "oluşturdu **'{task_title}'** ({task_key})" / "created **'{task_title}'** ({task_key})" |
| `task_deleted` (existing) | "sildi **'{task_title}'**" / "deleted **'{task_title}'**" |
| `comment_created` (existing — enriched) | "yorum yaptı **'{task_title}'** üzerinde: \"{comment_excerpt}\"" / "commented on **'{task_title}'**: \"{comment_excerpt}\"" |
| `comment_edited` (NEW) | "düzenledi yorumunu **'{task_title}'** üzerinde" / "edited a comment on **'{task_title}'**" |
| `comment_deleted` (NEW) | "yorumunu sildi **'{task_title}'** üzerinde" / "deleted a comment on **'{task_title}'**" |
| `phase_transition` (existing — enriched) | "faz geçişi yaptı **'{project_name}'** projesinde: {phase_old_name} → {phase_new_name}" / "transitioned phase on **'{project_name}'**: {phase_old_name} → {phase_new_name}" |
| `project_archived` (NEW) | "arşivledi **'{project_name}'** projesini" / "archived **'{project_name}'**" |
| `project_status_changed` (NEW) | "durumunu değiştirdi **'{project_name}'** projesinin: {old} → {new}" / "changed status of **'{project_name}'**: {old} → {new}" |
| `user_invited` (NEW) | "davet etti **{email}** adresini ({role} rolü)" / "invited **{email}** ({role} role)" |
| `user_deactivated` (NEW) | "devre dışı bıraktı **{user_name}** kullanıcısını" / "deactivated **{user_name}**" |
| `user_activated` (NEW) | "tekrar aktif etti **{user_name}** kullanıcısını" / "reactivated **{user_name}**" |
| `user_role_changed` (NEW) | "rolünü değiştirdi **{user_name}** kullanıcısının: {old_role} → {new_role}" / "changed role of **{user_name}**: {old_role} → {new_role}" |
| `user_password_reset_requested` (NEW) | "şifre sıfırlama linki gönderdi **{user_name}** kullanıcısına" / "sent password reset link to **{user_name}**" |
| `project_join_request_created` (NEW) | "talep etti **{user_name}** kullanıcısının **'{project_name}'** projesine eklenmesini" / "requested to add **{user_name}** to **'{project_name}'**" |
| `project_join_request_approved` (NEW) | "onayladı **{user_name}** kullanıcısının **'{project_name}'** projesine katılım talebini" / "approved **{user_name}**'s request to join **'{project_name}'**" |
| `project_join_request_rejected` (NEW) | "reddetti **{user_name}** kullanıcısının **'{project_name}'** projesine katılım talebini" / "rejected **{user_name}**'s request to join **'{project_name}'**" |
| `milestone_created` (existing) | "kilometre taşı oluşturdu **'{milestone_title}'** ({project_key})" / "created milestone **'{milestone_title}'** ({project_key})" |
| `milestone_updated` (existing) | "kilometre taşı güncelledi **'{milestone_title}'**" / "updated milestone **'{milestone_title}'**" |
| `artifact_status_changed` (existing — enriched) | "artefakt durumu değiştirdi **'{artifact_name}'**: {status_old} → {status_new}" / "changed artifact status **'{artifact_name}'**: {status_old} → {status_new}" |
| `phase_report_created` (existing — enriched) | "değerlendirme raporu oluşturdu **'{source_phase_name}'** fazı için ({project_key})" / "created evaluation report for phase **'{source_phase_name}'** ({project_key})" |

**Field labels for `task_field_updated` (lib/admin/audit-field-labels.ts — D-D4):**

| `field_name` | TR label | EN label |
|--------------|----------|----------|
| `due_date` | "son tarih" | "due date" |
| `priority` | "öncelik" | "priority" |
| `story_points` | "puan" | "story points" |
| `description` | "açıklama" | "description" |
| `title` | "başlık" | "title" |
| `phase_id` | "faz" | "phase" |
| `column_id` | "durum" | "status" |
| `cycle_id` | "döngü" | "cycle" |
| `parent_task_id` | "üst görev" | "parent task" |
| (fallback) | (raw `field_name`) | (raw `field_name`) |

**Backwards compatibility (D-D6):** when `extra_metadata` is missing or partial (pre-Plan-14-09 audit rows), formatter falls back to existing minimal rendering (`"{actor} {verb}"`); never throws on missing keys. NO migration / backfill — graceful degradation per CONTEXT D-D6.

### Surface K — Modal Anatomy (Add User / Bulk Invite / Audit Filter / Pending Requests)

| Modal | Title (TR / EN) | Width | Sections |
|-------|------------------|-------|----------|
| Add User Modal | "Kullanıcı Ekle" / "Add User" | 480 px | Email input + Role SegmentedControl (Admin/PM/Member) + Optional Name input + Optional Team-id select + Submit/Cancel buttons |
| Bulk Invite Modal | "Toplu Davet" / "Bulk Invite" | 560 px | File-drop zone (CSV) + format hint + Parse-preview table (first 10 rows) + Validation summary `{N_valid} geçerli / {N_invalid} hatalı` + Submit/Cancel buttons |
| Audit Filter Modal | "Audit Filtresi" / "Audit Filter" | 520 px | Date-range two `<input type="date">` + Actor `<select>` (loads users) + Action-prefix Input + Apply/Clear/Cancel buttons |
| Pending Requests Tümünü Gör Modal | "Tüm Bekleyen İstekler" / "All Pending Requests" | 640 px | Full pagination (offset 25/page) of `<PendingRequestRow/>` instances; same Onayla/Reddet actions as inline list |
| Confirm Delete Project — special two-step | (per D-A6 / Surface F destructive) | 480 px | Title + warning text + Type-key Input + tone="danger" CTA |

**Modal styling:**

| Element | Value | Source |
|---------|-------|--------|
| Overlay | `position: fixed; inset: 0; background: oklch(0 0 0 / 0.5); display: flex; alignItems: center; justifyContent: center; zIndex: 200` | New, industry-standard |
| Panel | `Card padding=0` with `borderRadius: var(--radius-lg)` and `boxShadow: var(--shadow-xl)` | Reuses primitive |
| Header | `padding: 16 20px`, `borderBottom: 1px solid var(--border)`, `display: flex justify:space-between align:center` | New |
| Header title | `fontSize: 15, fontWeight: 600` | New |
| Header close button | `Button variant="ghost" size="icon"` with `<Icons.X size={14}/>`; aria-label="Kapat" / "Close" | New |
| Body | `padding: 16px 20px`, `display: flex flexDirection:column gap:12` | New |
| Footer | `padding: 12px 20px`, `borderTop: 1px solid var(--border)`, `display: flex justify:flex-end gap:8` | New |
| Animation | `@keyframes fadeIn` for overlay (existing in `globals.css` line 211) + scale-in for panel (`transform: scale(0.96) → scale(1)` + opacity 0→1 over 150ms ease-out) | New |
| `prefers-reduced-motion` | Disables scale animation; opacity-only fade kept | Existing CSS pattern |

**Modal a11y:**
- Overlay: `role="dialog"`, `aria-modal="true"`, `aria-labelledby="{modal-id}-title"`, `aria-describedby="{modal-id}-body"` if body has explanatory text.
- Focus trap: Tab cycles within modal; Shift+Tab reverses. Initial focus on first focusable input.
- Escape key: closes modal (without saving). Click on overlay (not panel): closes modal.
- Body scroll-lock: `document.body.style.overflow = "hidden"` while open.

### Destructive confirmation — see Color section above.

---

## Surface specs (6 quality dimensions per surface)

> Each surface below covers: 1) Hierarchy & Layout, 2) Typography, 3) Color, 4) Interaction, 5) Copy, 6) Component fidelity.
> All distance / font values are sourced from the prototype line cited or are flagged `[IMPROVEMENT]`.

### Surface A — Admin Layout (`app/(shell)/admin/layout.tsx`)

**File:** `Frontend2/app/(shell)/admin/layout.tsx` (NEW). Mounts: page header + sub-tabs strip + `{children}` slot.

#### A.1 Hierarchy & Layout

```
AdminLayout (flex column gap:20 height:100%)
├─ HeaderRow (flex align:flex-end justify:space-between)
│   ├─ TitleBlock
│   │   ├─ Row (flex align:center gap:8)
│   │   │   ├─ <Icons.Shield size={18} color="var(--primary)"/>
│   │   │   └─ H1 "Yönetim Konsolu" (24/600/-0.6)
│   │   └─ Subtitle (13/400/--fg-muted, marginTop:4)
│   └─ HeaderToolbar (flex gap:6)
│       ├─ Button secondary sm icon=Download "Rapor al"
│       └─ Button secondary sm icon=Audit "Denetim günlüğü"
├─ <NavTabs/> (8 tabs Link-based, usePathname() active detection — see Component fidelity)
└─ {children}  (flex:1 minHeight:0)
```

| Property | Value | Source |
|----------|-------|--------|
| Outer gap | 20 px | Verbatim prototype line 9 |
| Title block icon size | 18 | Verbatim line 13 |
| Title block icon color | `var(--primary)` | Verbatim line 13 |
| Title font | 24/600/letter-spacing:-0.6 | Verbatim line 14 |
| Subtitle margin-top | 4 px | Verbatim line 16 |
| Toolbar gap | 6 px | Verbatim line 18 |
| Tabs row spacing from above | 20 (parent gap) | Verbatim |
| Mobile (≤1024px) — header row | Stacks (toolbar moves below title) | **[IMPROVEMENT]** D-F1 inherited |
| Mobile (≤640px) — toolbar | Buttons full-width stacked | **[IMPROVEMENT]** D-F1 |
| Mobile (≤1024px) — tabs row | Horizontally scrollable (matches Phase 11 D-54 `.pd-tabs-wrap` pattern, reuses existing `.admin-tabs-wrap` class) | **[IMPROVEMENT]** D-F1 |

#### A.2 Typography

| Element | Size | Weight | Family | Source |
|---------|------|--------|--------|--------|
| Title H1 | 24 px | 600 | sans, letterSpacing -0.6 | Verbatim line 14 |
| Subtitle | 13 px | 400 | sans, --fg-muted | Verbatim line 16 |
| Toolbar Button label | 12.5 px (Button size="sm") | 500 | sans (Button primitive) | Reuse |
| Tab label | 13 px (NavTabs size="md", visual parity with Tabs size="md") | 600 active / 500 default | sans (NavTabs internally renders the same `PAD_MAP`/`FONT_MAP` as Tabs) | Reuse |

#### A.3 Color

| Element | Token |
|---------|-------|
| Page background | `var(--bg)` (AppShell) |
| Title icon | `var(--primary)` |
| Title text | `var(--fg)` |
| Subtitle | `var(--fg-muted)` |
| Active tab | `var(--fg)` text + `var(--primary)` 2px bottom border (NavTabs mirrors Tabs visual contract) |
| Tab active badge | tone="primary" (NavTabs `badge` prop renders Phase 8 `Badge` primitive internally) |
| Tab inactive | `var(--fg-muted)` text + transparent border |

#### A.4 Interaction

| Trigger | Action |
|---------|--------|
| Mount | Read `useAuth().user.role`. If non-admin → `router.replace("/dashboard")` + Toast (D-C3). If unauthenticated → `router.replace("/auth/login?next=/admin")`. If admin → render children |
| Click "Rapor al" | Trigger `GenerateAdminSummaryPDFUseCase` via `GET /api/v1/admin/summary.pdf` (Plan 14-11). 30s rate-limit countdown via toast on 429 (Phase 12 D-58 pattern). On success: blob → download |
| Click "Denetim günlüğü" | `router.push("/admin/audit")` |
| Click any tab | `router.push("/admin/{tab-slug}")`. Active detection via `usePathname()` |
| Window pathname change | Tabs re-renders with new active state |

| State | Visual |
|-------|--------|
| Default | as spec'd |
| Loading admin data (server-side guard pending) | Layout renders skeleton placeholder for header + tab strip; children NOT mounted |
| Non-admin → redirect | Brief flash of empty content while `router.replace` fires; toast appears AFTER redirect lands |
| Server error on summary PDF | Toast tone="danger" + retry option |
| Rate limited (429) | Toast tone="warning" + countdown "30 sn sonra tekrar dene" / "Retry in 30s" |

**A11y:**
- H1 carries semantic `<h1>` (only one per page; sub-routes do NOT add their own h1 — they use h2 for sub-tab section titles)
- Toolbar buttons: aria-label on each (`"Admin özet raporunu indir"` / `"Audit log sayfasına git"`)
- NavTabs uses `<nav role="navigation" aria-label={...}>` + `<a aria-current="page">` (NOT `role="tab"`/`tablist`) — these are navigation links, not state tabs. The existing Tabs primitive's `role="tab"`/`tablist` semantics remain unchanged for ProjectDetail (Phase 11) + Profile (Phase 13) state-tab consumers. Verify NavTabs ARIA per the keyboard-nav table above.

#### A.5 Copy

See **Copywriting Contract → Surface A**.

#### A.6 Component fidelity

| Existing primitive used | How |
|-------------------------|-----|
| `Card` | n/a — header uses raw flex divs (verbatim prototype line 10-22) |
| `Button` (variant="secondary", size="sm") | "Rapor al" + "Denetim günlüğü" toolbar |
| `Tabs` *(EXTENDED — see below)* | 8-tab nav strip; Link-based render |
| `useAuth()` | Role gate |
| `usePathname()` (Next.js 16) | Active tab detection |
| `useRouter()` (Next.js 16) | Redirects + tab navigation |
| `lucide-react` icons | `Shield` (header + Roller tab), `Download` (Rapor al), `FileSearch`/`Audit` (Denetim günlüğü), tab icons (`Dashboard`, `Users`, `Lock`, `Folder`, `Workflow`, `Calendar`/`Audit`, `BarChart3`/`Chart`) |

**NavTabs primitive (NEW for Phase 14 — D-C4) — DECISION LOCKED:**

The existing Tabs primitive (`Frontend2/components/primitives/tabs.tsx` lines 40-92) renders `<button onClick={() => onChange(id)}/>` per tab. Phase 14 needs Link-based render so each tab is a real navigation anchor (browser back/forward, middle-click open in new tab, share URL).

**Decision (LOCKED, no planner discretion): a NEW `<NavTabs/>` primitive at `Frontend2/components/primitives/nav-tabs.tsx`** — a thin wrapper that mirrors the Tabs visual but accepts `tabs: { id, label, icon, badge, href }[]` and renders `<Link/>` per item with `usePathname()` active detection. Reuses the same `PAD_MAP` / `FONT_MAP` / inline styles as Tabs internally for visual parity.

**Why NOT extend the existing `Tabs` primitive:**
- Tabs' `onChange(id)` callback is the contract that ProjectDetail (Phase 11) and Profile (Phase 13) rely on — adding a polymorphic `as="link"` mode forces every existing consumer to type-narrow on `as` to access `onChange`, regressing DX.
- Tabs' `role="tab"` / `role="tablist"` ARIA semantics are correct for state-driven tab UIs but WRONG for navigation tabs (which should be `<nav role="navigation">` with `<a>` children, not `tablist`/`tab`). Conflating both into one primitive would force a runtime branch on ARIA roles.
- A separate primitive ships an explicit, self-documenting contract — planner / executor / reviewer can tell `<NavTabs/>` apart from `<Tabs/>` at a glance.
- An alternative "extend Tabs" approach was considered and explicitly REJECTED — do NOT re-evaluate this during planning or execution.

**NavTabs API (executor must implement this exact shape):**
```ts
interface NavTab {
  id: string                     // e.g., "users" — used for usePathname() match
  label: string                  // already-localized string (caller resolves T())
  icon?: React.ReactNode         // lucide-react icon node (size 13 per prototype)
  badge?: number | string        // optional count badge (Phase 8 Tabs-parity)
  href: string                   // e.g., "/admin/users" — Next.js Link target
}
interface NavTabsProps {
  tabs: NavTab[]
  basePath?: string              // optional explicit base for active match (default: derive from first tab)
  size?: "sm" | "md" | "lg"      // mirrors Tabs primitive
  ariaLabel: string              // REQUIRED — labels the surrounding <nav>
}
```

**NavTabs keyboard navigation (executor must implement):**

Native `<a>` elements give Tab/Shift+Tab focus traversal for free. Arrow-key cycling between tabs requires explicit `onKeyDown` wiring on the nav container. Implementation contract:

| Key | Behavior |
|---|---|
| `Tab` | Browser default — moves focus from outside NavTabs to the FIRST `<Link>`, then through each tab in order, then OUT to the next page region. Every link is `tabIndex={0}` (all focusable), matching the existing in-repo `AvatarDropdown` pattern (`Frontend2/components/shell/avatar-dropdown.tsx` lines 298+: `tabIndex={open ? 0 : -1}`). Justification: 8 tabs is small enough that Tab-cycling through all of them is acceptable; matches GitHub/Linear nav-tab convention; consistent with the in-repo AvatarDropdown precedent shipped in Phase 13. |
| `Shift+Tab` | Browser default — reverse Tab traversal. |
| `ArrowRight` | Move focus to the NEXT tab (visually right). On the last tab, wrap to the first. Calls `nextLink.focus()` directly (matches AvatarDropdown line 128 `items[nextIdx]?.focus()`). Does NOT activate the link (no navigation) — focus only. `event.preventDefault()` to suppress page scroll. |
| `ArrowLeft` | Move focus to the PREVIOUS tab (visually left). Wrap from first to last. |
| `ArrowDown` / `ArrowUp` | NOT bound (avoid conflicting with vertical scroll on the page). NavTabs is a horizontal nav; only horizontal arrows cycle. |
| `Home` | Move focus to the FIRST tab. `event.preventDefault()`. |
| `End` | Move focus to the LAST tab. `event.preventDefault()`. |
| `Enter` | Browser default on `<a>` — navigate to focused link's `href`. |
| `Space` | `event.preventDefault()` (suppress scroll), then trigger `router.push(href)`. |

**Implementation note:** keyboard handler attached on the `<ul>` container (delegated). On keydown, find the currently-focused `<Link>` via `document.activeElement`, compute `nextIndex` (with wrap), call `links[nextIndex]?.focus()`. Mirrors `AvatarDropdown` lines 97-128 verbatim — same handler shape, only key set differs (Left/Right instead of Up/Down).

**Differences from W3C tablist roving-tabindex pattern (informational):** WAI-ARIA Authoring Practices recommend roving tabindex (only active tab = 0, others = -1) for `role="tablist"`. NavTabs uses `role="navigation"` instead, where common convention is all-links-focusable. The all-focusable choice here is deliberate and matches the in-repo AvatarDropdown precedent — do NOT switch to roving tabindex during planning/execution without an explicit user decision.

**Focus-visible ring (REQUIRED, dimension 6):**
```css
.nav-tab:focus-visible {
  outline: 2px solid var(--primary);
  outline-offset: 2px;
  border-radius: 4px;
}
```
Apply on each `<Link/>` element inside NavTabs. Use `:focus-visible` (not `:focus`) so mouse clicks don't show the ring — keyboard-only affordance per WCAG 2.1.4.7.

**ARIA structure:**
```html
<nav role="navigation" aria-label={ariaLabel} onKeyDown={handleKeyDown}>
  <ul role="list" style={{display: "flex", ...}}>
    <li>
      <Link
        href={tab.href}
        aria-current={isActive ? "page" : undefined}
        tabIndex={0}
        className="nav-tab"
      >
        {tab.icon}{tab.label}{tab.badge && <Badge tone={isActive ? "primary" : "neutral"}>{tab.badge}</Badge>}
      </Link>
    </li>
    ...
  </ul>
</nav>
```
Every `<Link>` gets `tabIndex={0}` (all focusable, per the keyboard table above). `aria-current="page"` on the active link is the correct nav-link active indicator (NOT `aria-selected` — that's for `role="tab"`).

**In-repo precedent:** existing `<AvatarDropdown/>` keyboard nav (`Frontend2/components/shell/avatar-dropdown.tsx` lines 97-128, shipped in Phase 13) is the in-repo a11y handler precedent. NavTabs mirrors the same handler shape (delegated `onKeyDown` on container, `document.activeElement`-based current-index lookup, `links[nextIdx]?.focus()` direct call, no third-party dependency, no React state for focus tracking).

**Test requirements (RTL — Plan 14-01 Wave 0):**
- ArrowRight on last tab wraps focus to first (and vice versa for ArrowLeft on first).
- Home moves focus to first tab; End moves focus to last tab; both `event.preventDefault()` to suppress page scroll.
- Tab from outside NavTabs lands on the FIRST link; subsequent Tab presses cycle through all 8 links, then leave NavTabs (all-focusable pattern).
- `:focus-visible` ring renders on keyboard focus, NOT on mouse click (verify by `userEvent.click()` showing no ring vs `userEvent.tab()` showing ring).
- `aria-current="page"` on the link whose `href` matches `usePathname()`; absent on all others.
- Enter on a focused non-active link navigates to that link's `href` (verifies via `mockRouter.push` spy).
- Space on a focused link calls `event.preventDefault()` then `router.push(href)` (verifies no page scroll + navigation).

**Improvements over prototype:**
1. `[IMPROVEMENT]` Sub-routes (NOT `?tab=` query) — D-C1. Justification documented in CONTEXT.
2. `[IMPROVEMENT]` Admin route guard (client-side) + server-side `Depends(require_admin)` defense-in-depth — D-C3.
3. `[IMPROVEMENT]` Toolbar buttons "Rapor al" + "Denetim günlüğü" wired to real backend (PDF + nav) — D-B6.

---

### Surface B — Genel / Overview (`/admin/page.tsx`)

#### B.1 Hierarchy & Layout

Verbatim from prototype lines 49-144.

```
AdminOverview (flex column gap:20)
├─ StatCardsRow (grid 5-col gap:12)
│   ├─ StatCard "Kullanıcı" tone=primary icon=Users
│   ├─ StatCard "Aktif Proje" tone=info icon=Folder
│   ├─ StatCard "Onay Bekleyen" tone=warning icon=Clock
│   ├─ StatCard "Şablon" tone=neutral icon=Workflow
│   └─ StatCard "Depolama" tone=neutral icon=Chart
└─ SecondRow (grid 1.5fr 1fr gap:20)
    ├─ PendingRequestsCard (Card padding=0)
    │   ├─ HeaderRow (padding:14 16, borderBottom: 1px var(--border), flex align:center)
    │   │   ├─ Title "Bekleyen Proje Katılım İstekleri" (13/600)
    │   │   └─ Button xs ghost "Tümünü gör" (right-aligned)
    │   └─ For each pending request (top 5):
    │       └─ <PendingRequestRow/>  (padding:14 16, borderBottom unless last, grid: auto 1fr auto, gap:14)
    │           ├─ AvatarPair (flex align:center gap:8)
    │           │   ├─ Avatar size=26 (PM)
    │           │   ├─ <Icons.ArrowRight size={13} color="var(--fg-subtle)"/>
    │           │   └─ Avatar size=26 (target user)
    │           ├─ ContentColumn (minWidth:0)
    │           │   ├─ PrimaryLine (12.5)  "Ayşe — Mehmet kullanıcısını Bankacılık projesine eklemek istiyor"
    │           │   ├─ NoteBlock (italic, 11.5/--fg-muted, padding:6, --surface-2 bg, marginTop:4) — only if r.note
    │           │   └─ TimeRow (11/--fg-subtle, marginTop:4)
    │           └─ ActionGroup (flex gap:6)
    │               ├─ Button sm primary icon=Check "Onayla"
    │               └─ Button sm ghost "Reddet"
    └─ SideColumn (flex column gap:16)
        ├─ RoleDistributionCard (Card padding=16)
        │   ├─ Title "Rol Dağılımı" (13/600 marginBottom:12)
        │   └─ For each role (Admin/PM/Member):
        │       └─ Row (flex align:center gap:10 padding:8 0 fontSize:12.5)
        │           ├─ Swatch (8×8 / radius:2 / role-colored)
        │           ├─ Label (flex:1)
        │           ├─ Count (mono, --fg-muted)
        │           └─ MiniBar (60×4 / radius:2 / --surface-2 track / role-colored fill)
        └─ RecentEventsCard (Card padding=16)
            ├─ Title "Son Yönetim Olayları" (13/600 marginBottom:12)
            └─ For each event (last 4-6):
                └─ Row (flex gap:8 align:flex-start, fontSize:12)
                    ├─ Bullet (6×6 / radius:50% / --primary, marginTop:6)
                    ├─ Content (flex:1)
                    │   ├─ ActorName (600)
                    │   └─ Verb (--fg-muted) — full Jira-style render via shared formatter (D-D5)
                    └─ Time (mono, 11/--fg-subtle)
```

| Property | Value | Source |
|----------|-------|--------|
| StatCard grid 5-col | `repeat(5, 1fr) gap:12` | Verbatim line 54 |
| Second row grid | `1.5fr 1fr gap:20` | Verbatim line 62 |
| Side column gap | 16 px | Verbatim line 101 |
| Pending Card header pad | `14px 16px` | Verbatim line 64 |
| Pending row pad | `14px 16px` | Verbatim line 74 |
| Pending row grid | `auto 1fr auto`, gap:14 | Verbatim line 74 |
| Pending row avatar pair gap | 8 px | Verbatim line 75 |
| Pending row avatar size | 26 | Verbatim line 76, 78 |
| Pending row note pad | 6 px | Verbatim line 89 |
| Pending row time margin-top | 4 px | Verbatim line 90 |
| Pending action group gap | 6 px | Verbatim line 92 |
| Role distribution row pad | `8px 0` | Verbatim line 109 |
| Role distribution swatch | 8×8 / radius:2 | Verbatim line 110 |
| Role distribution mini-bar track | 60×4 / radius:2 / --surface-2 | Verbatim line 113 |
| Role distribution mini-bar fill | width: `count/10*100%` | Verbatim line 114 (10 = total user count denominator) |
| Recent event bullet | 6×6 / radius:50% / --primary, marginTop:6 | Verbatim line 130 |
| Recent event row gap | 8 px | Verbatim line 129 |
| Mobile ≤1024px — StatCards | repeat(3, 1fr) | **[IMPROVEMENT]** D-F1 |
| Mobile ≤1024px — second row | 1fr column-stack | **[IMPROVEMENT]** D-F1 |
| Mobile ≤640px — StatCards | repeat(2, 1fr) | **[IMPROVEMENT]** D-F1 |
| Mobile ≤640px — Pending row | grid `auto 1fr` (action group wraps below) | **[IMPROVEMENT]** D-F1 |

#### B.2 Typography

| Element | Size | Weight | Family | Source |
|---------|------|--------|--------|--------|
| Pending Card title | 13 px | 600 | sans | Verbatim line 65 |
| Pending row primary line | 12.5 px | 600 names / 400 muted glue | sans | Verbatim lines 81-87 |
| Pending row note | 11.5 px | 400 | sans, italic, --fg-muted | Verbatim line 89 |
| Pending row time | 11 px | 400 | sans, --fg-subtle | Verbatim line 90 |
| Role dist row | 12.5 px | 400 | sans | Verbatim line 109 |
| Role dist count | 11 px | 400 | mono, --fg-muted | Verbatim line 112 |
| Recent event line | 12 px | 600 actor / 400 verb | sans | Verbatim line 124 |
| Recent event time | 11 px | 400 | mono, --fg-subtle | Verbatim line 135 |

#### B.3 Color

| Element | Token | Source |
|---------|-------|--------|
| Pending request primary CTA | Button primary (--primary) | Verbatim line 93 |
| Pending request reject | Button ghost | Verbatim line 94 |
| Pending request note bg | `var(--surface-2)` | Verbatim line 89 |
| Role swatch — Admin | `var(--priority-critical)` | Verbatim line 105 |
| Role swatch — PM | `var(--status-progress)` | Verbatim line 106 |
| Role swatch — Member | `var(--fg-muted)` | Verbatim line 107 |
| Role mini-bar track | `var(--surface-2)` | Verbatim line 113 |
| Role mini-bar fill | role color | Verbatim line 114 |
| Recent event bullet | `var(--primary)` | Verbatim line 130 |
| StatCard tones | primary/info/warning/neutral/neutral (5 cards) | Verbatim line 55-59 |

#### B.4 Interaction

| Trigger | Action |
|---------|--------|
| Mount | TanStack Query parallel fetch: stats payload (5 numbers + admin events) + top 5 pending requests + role distribution counts |
| Click "Tümünü gör" | Open Pending Requests modal (D-W2 + Modal Anatomy) |
| Click "Onayla" on inline row | Optimistic update (D-W2): remove row from list locally; POST `/api/v1/admin/join-requests/{id}/approve`; on success: toast `"{user} {project} projesine eklendi."` / `"{user} added to {project}."`; on error: revert + toast danger |
| Click "Reddet" | ConfirmDialog → POST `/api/v1/admin/join-requests/{id}/reject` |
| Click recent admin event row | (deep-link to entity if `entity_label` resolvable; else no-op per CONTEXT D-W3 alt) |
| Click "Audit'a git →" link | `router.push("/admin/audit")` |
| Window focus | refetchOnWindowFocus: true (D-C5 + Phase 13 D-B3 pattern) |

| State | Visual |
|-------|--------|
| Loading | DataState skeleton: 5 StatCard placeholders + 1.5fr/1fr grid with 5 PendingRow shimmer skeletons + 2 side-column skeletons |
| Error (overall) | AlertBanner tone="danger" at page top with retry button |
| Empty Pending | "Onay bekleyen istek yok." (--fg-muted, padding:24, centered) |
| Empty Recent | "Son yönetim olayı yok." (--fg-muted, centered) |
| Approving (optimistic) | Row dims to 0.5 opacity with mini-spinner |
| Rejecting | After ConfirmDialog confirm, row removed; toast warning |

#### B.5 Copy

See **Copywriting Contract → Surface B**.

#### B.6 Component fidelity

| Existing primitive used |
|------------------------|
| `Card` (padding=0 for Pending; padding=16 for side column) |
| `StatCard` from `Frontend2/components/dashboard/stat-card.tsx` (REUSED — 5 instances) |
| `Avatar` (size=26 in pending, with NEW `href` per Phase 13 D-D4) |
| `Button` (primary sm "Onayla", ghost sm "Reddet", xs ghost "Tümünü gör") |
| `<DataState/>` (Phase 13 D-F2) |
| `<ConfirmDialog/>` (Phase 10 D-25) for Reject |
| `formatRelativeTime` (Phase 13 lib) |
| `<ActivityRow variant="admin-table"/>` is NOT used here — Recent admin events render inline using shared `eventMeta` formatter as a compact 1-line snippet (NOT the full timeline variant) |
| Phase 14 NEW: `<PendingRequestsModal/>` for "Tümünü gör" |

**Improvements over prototype:**
1. `[IMPROVEMENT]` Empty states for Pending + Recent — D-F2.
2. `[IMPROVEMENT]` Optimistic updates on Onayla — D-W2.
3. `[IMPROVEMENT]` "Audit'a git →" link in Recent events — D-W1 (deep-link to full audit table).
4. `[IMPROVEMENT]` Mobile ≤1024px / ≤640px responsive — D-F1.
5. `[IMPROVEMENT]` Recent admin events use enriched audit metadata via `audit-event-mapper.ts` extension — D-D1..D6.

---

### Surface C — Kullanıcılar (`/admin/users`)

#### C.1 Hierarchy & Layout

Verbatim from prototype lines 146-200, with 3 improvements (bulk select, MoreH menu, density on mobile).

```
AdminUsers (Card padding=0)
├─ Toolbar (padding:12 16, borderBottom:1px, flex align:center gap:8)
│   ├─ <Input/> icon=Search size=sm width:240 placeholder
│   ├─ Role-filter pill set (4 pills: Tümü/Admin/PM/Member, --surface-2 outer pad:2/radius:6)
│   ├─ Spacer (flex:1)
│   ├─ Button sm secondary icon=Download "CSV"
│   ├─ Button sm secondary icon=Mail "Toplu davet"
│   └─ Button sm primary icon=Plus "Kullanıcı ekle"
├─ {bulkSelected.size > 0} → BulkActionBar (padding:8 16, --surface-2 bg, flex gap:8)
│   ├─ "{N} seçili" caption
│   ├─ Button xs secondary "Toplu devre dışı bırak"
│   ├─ Button xs secondary "Toplu rol değiştir"
│   ├─ Spacer
│   └─ Button xs ghost "Vazgeç"
├─ ColumnHeader (grid `28px 40px 2fr 2fr 130px 1fr 100px 90px 28px`, padding:10 16, fontSize:11/uppercase/600/--fg-subtle, borderBottom)
│   └─ checkbox / (avatar) / Ad Soyad / Email / Rol / Projeler / Son Giriş / Durum / (MoreH)
├─ For each user row:
│   └─ <UserRow/>  (.hover-row, grid same, padding:10 16, borderBottom unless last, fontSize:12.5)
│       ├─ <input type="checkbox"/> (28px col)
│       ├─ <Avatar size=28 user={u} href={`/users/${u.id}`}/>
│       ├─ Name (600, fg)
│       ├─ Email (--fg-muted)
│       ├─ <Badge size="xs" tone={Admin:"danger"|PM:"info"|else:"neutral"}>{role}</Badge>
│       ├─ <AvatarStack max=4 size=20/> (project memberships)
│       ├─ LastSeen (11/--fg-muted)
│       ├─ <Badge size="xs" tone={isActive?"success":"neutral"} dot>{Aktif|Pasif}</Badge>
│       └─ MoreH button (size=icon, opens Popover menu — see C.4)
└─ PaginationFooter (padding:10 16, borderTop:1px, flex justify:space-between, fontSize:11.5/--fg-muted)
    ├─ "{N} kullanıcı"
    └─ Pager (Button xs ghost ChevronLeft + "{P} / {M}" mono + Button xs ghost ChevronRight)
```

| Property | Value | Source |
|----------|-------|--------|
| Toolbar pad | `12px 16px` | Verbatim line 156 |
| Toolbar gap | 8 px | Verbatim |
| Search input width | 240 px | Verbatim line 157 |
| Role-filter outer pad | 2 px | Verbatim line 158 |
| Role-filter outer radius | 6 px | Verbatim |
| Role-filter pill pad | `4px 10px` | Verbatim line 160 |
| Role-filter pill font | 11.5/600 | Verbatim |
| Column header pad | `10px 16px` | Verbatim line 170 |
| Column header letter-spacing | 0.4 | Verbatim |
| Row pad | `10px 16px` | Verbatim line 176 |
| Row gap | 8 px | Verbatim line 176 |
| Row Avatar size | 28 | Verbatim line 180 |
| AvatarStack max | 4, size 20 | Verbatim line 184 |
| Pagination pad | `10px 16px` | Verbatim line 191 |
| Pagination font | 11.5 | Verbatim |
| Pager pill pad | `0 10px` | Verbatim line 195 |
| Mobile ≤1024px — column reduction | hide `Email` + `Projeler` columns (grid becomes `28px 40px 2fr 130px 100px 90px 28px`); MoreH still last | **[IMPROVEMENT]** D-F1 |
| Mobile ≤640px — stacked card | each row becomes a 2-line card: row 1 = avatar+name+role, row 2 = email+last-seen+status; MoreH absolute-positioned top-right | **[IMPROVEMENT]** D-F1 |
| Bulk action bar pad | `8px 16px` | New |

#### C.2 Typography

| Element | Size | Weight | Family | Source |
|---------|------|--------|--------|--------|
| Search input text | 13 px | 400 | sans (Input primitive) | Reuse |
| Role-filter pill | 11.5 px | 600 | sans | Verbatim line 160 |
| Column header | 11 px | 600 | sans, uppercase, letter-spacing 0.4 | Verbatim line 170 |
| Row Name | 12.5 px | 600 | sans | Verbatim line 181 |
| Row Email | 12.5 px | 400 | sans, --fg-muted | Verbatim line 182 |
| Row LastSeen | 11 px | 400 | sans (or mono if numeric), --fg-muted | Verbatim line 185 |
| Pagination caption | 11.5 px | 400 | sans, --fg-muted | Verbatim line 192 |
| Pager page indicator | 11 px | 400 | mono, tabular-nums | Verbatim line 195 |

#### C.3 Color

| Element | Token |
|---------|-------|
| Role-filter pill active | `var(--surface)` bg + `var(--fg)` text + var(--shadow-sm), var(--inset-top) (matches SegmentedControl primitive — should we use SegmentedControl? **YES** — replace inline pill set with `<SegmentedControl/>` primitive for consistency; visual is identical) |
| Role-filter pill inactive | transparent bg + `var(--fg-muted)` text |
| Row hover | `var(--surface-2)` (.hover-row utility) |
| Role Badge — Admin | tone="danger" (--priority-critical) |
| Role Badge — PM | tone="info" (--status-progress) |
| Role Badge — Member | tone="neutral" |
| Status Badge — Active | tone="success" (--status-done), `dot` |
| Status Badge — Inactive | tone="neutral", no dot |
| BulkActionBar bg | `var(--surface-2)` |

#### C.4 Interaction

| Trigger | Action |
|---------|--------|
| Type in Search input | debounced (250ms) filter; client-side first if dataset ≤ 200 rows; else server-side `/admin/users?q={query}` |
| Click role-filter pill | Set state; localStorage persistence per CONTEXT D-C5 (`spms.admin.users.roleFilter`); URL query NOT used (filter is ephemeral) |
| Click row checkbox | Toggle bulkSelected Set; on first selection, BulkActionBar slides down |
| Click row body (NOT avatar/checkbox/MoreH) | navigate to `/users/{id}` (Phase 13 user-profile route) |
| Click row Avatar | navigate to `/users/{id}` (Avatar `href` prop — Phase 13 D-D4); `e.stopPropagation()` so row click doesn't double-fire |
| Click row MoreH | Open Popover menu anchored bottom-right of button: items per row state — see below |
| Click "Kullanıcı ekle" | Open Add User Modal |
| Click "Toplu davet" | Open Bulk Invite Modal |
| Click "CSV" | Trigger `GET /api/v1/admin/users.csv` download |
| Click "Toplu devre dışı bırak" | ConfirmDialog → POST `/api/v1/admin/users/bulk-action` `{user_ids, action: "deactivate"}`; on success: toast "Devre dışı bırakıldı: {N} kullanıcı."; cumulative undo not provided (deactivate is reversible via row MoreH "Tekrar aktif et") |
| Click "Toplu rol değiştir" | Modal with role SegmentedControl; on submit POST bulk-action `{action: "role_change", payload: {role}}`; toast |
| Click pager Prev/Next | Update offset; refetch |
| Window focus | refetchOnWindowFocus per D-C5 |

**Row MoreH Popover menu items (per CONTEXT D-A6):**

| Item | Visible when | Action | Style |
|------|-------------|--------|-------|
| "Şifre sıfırla" | always | POST `/admin/users/{id}/password-reset` → toast | Default text |
| "Rolü değiştir" | always | Submenu chevron-right opens 3 radio rows (Admin/PM/Member); active = current role; click → PATCH `/admin/users/{id}/role` → toast | Default text |
| "Devre dışı bırak" | `is_active === true` | ConfirmDialog → PATCH `/admin/users/{id}/deactivate` → toast | red text (`var(--priority-critical)`) |
| "Tekrar aktif et" | `is_active === false` | PATCH `/admin/users/{id}/deactivate` (toggle endpoint) → toast | Default text |
| "Sil" | `id !== currentUser.id` | ConfirmDialog → DELETE `/admin/users/{id}` → toast; row removed | red text |

**Popover anatomy:** width 200px, padding 4px outer, items padding 6×10, font 12.5, hover `--surface-2` via `.hover-row`. Same Z-index 100 as AvatarDropdown. Dismiss = click-outside + Esc + nav-away (matches Phase 13 D-D7).

| State | Visual |
|-------|--------|
| Loading | DataState `loadingFallback` = skeleton table (10 rows shimmer) |
| Error | AlertBanner inset at top of card |
| Empty (filter active, 0 results) | "Aramayla eşleşen kullanıcı bulunamadı." centered, --fg-muted, padding:40 |
| Empty (no users) | "Henüz kullanıcı yok." centered |
| Optimistic deactivate | row opacity 0.5 + spinner; on success: Status Badge flips to neutral "Pasif"; on error: revert + toast |
| Optimistic role change | Badge tone changes immediately; on error: revert + toast |
| Bulk selection visible | BulkActionBar slides in below toolbar (height 40px) with `gridTemplateColumns` shift |

**A11y:**
- Each row checkbox: aria-label="{user.name} seç"
- MoreH: aria-haspopup="menu", aria-expanded, aria-label="{user.name} için işlemler"
- Pager: prev/next aria-label "Önceki sayfa" / "Sonraki sayfa"

#### C.5 Copy

See **Copywriting Contract → Surface C**.

#### C.6 Component fidelity

| Existing primitive used |
|------------------------|
| `Card` (padding=0) |
| `Input` (search) |
| `SegmentedControl` (role-filter pills — replaces inline div per Phase 14 hardening) |
| `Button` (sm secondary CSV/Toplu davet, sm primary "Kullanıcı ekle", xs ghost pager, icon MoreH) |
| `Avatar` size=28 with `href` (Phase 13 D-D4) |
| `AvatarStack` max=4 size=20 |
| `Badge` (role + status, sm/xs) |
| `<DataState/>` |
| `<ConfirmDialog/>` |
| `<ToastProvider/>` for action feedback |
| `<AddUserModal/>`, `<BulkInviteModal/>` (NEW Phase 14) |

**Improvements over prototype:**
1. `[IMPROVEMENT]` Real bulk-select UX with BulkActionBar (prototype shows checkbox column with no follow-up UI) — D-B7.
2. `[IMPROVEMENT]` Per-row MoreH menu fully wired (prototype shows the icon but no menu) — D-A6.
3. `[IMPROVEMENT]` Add User / Bulk Invite / Reset Password modals — D-B1, D-B2, D-B3, D-B4.
4. `[IMPROVEMENT]` Empty + filter-empty + loading + error states via DataState — D-F2.
5. `[IMPROVEMENT]` Mobile ≤1024px column-hide + ≤640px stacked-card layout — D-F1.
6. `[IMPROVEMENT]` Inactive status badge "Pasif" — D-A6.
7. `[IMPROVEMENT]` Avatar click-to-profile — Phase 13 D-D4.
8. `[IMPROVEMENT]` Row checkbox / MoreH a11y labels — D-G2.
9. `[IMPROVEMENT]` localStorage persistence of role filter — D-C5 + Phase 11 D-21 pattern.

---

### Surface D — Roller (`/admin/roles`) — RBAC PLACEHOLDER

#### D.1 Hierarchy & Layout

Verbatim from prototype lines 203-241, with [IMPROVEMENT] copy rewrites per D-A5 + page-level info banner per D-A3.

```
AdminRoles (flex column gap:14)
├─ <AlertBanner tone="info"/> (page-level RBAC notice — verbatim copy from Surface D copy table)
└─ Grid (repeat(2, 1fr) gap:14)
    ├─ AdminRoleCard      (padding:18) — see anatomy below
    ├─ ProjectManagerCard (padding:18)
    ├─ MemberCard         (padding:18)
    ├─ GuestCard          (padding:18, dimmed via opacity:0.7, has v3.0 Badge)
    └─ NewRolePlaceholderCard (padding:18, dashed border, --surface-2 bg, minHeight:130, cursor:not-allowed)

RoleCard anatomy:
├─ Row (flex align:flex-start gap:10)
│   ├─ IconBox (34×34 / radius:8 / role-tinted bg color-mix 18% / role-color fg)
│   │   └─ <Icons.Shield size=16/>
│   └─ Content (flex:1)
│       ├─ HeaderRow (flex align:center gap:8)
│       │   ├─ Title (15/600)
│       │   └─ {isCustom?} <Badge xs tone="primary">"Özel"</Badge> (Guest only — but Guest card also has v3.0 Badge)
│       └─ Description (12/400/--fg-muted/lineHeight:1.5/marginTop:4) — REWRITTEN per D-A5
├─ Footer (flex gap:14 marginTop:14 padding:10 0 0 0 borderTop:1px var(--border) fontSize:12)
│   ├─ Stat: "Kullanıcı: {N}" (mono N, --fg-subtle label)
│   └─ (REMOVED per D-A5: "İzin: {N}" → not shown because permissions don't exist as backend entities)
│   (REMOVED: Düzenle button per D-A5)

NewRolePlaceholderCard anatomy:
├─ flex column align:center gap:6 / --fg-muted color
│   ├─ <Icons.Plus size=18/>
│   ├─ Title "Yeni rol oluşturma" (13/600)
│   └─ Subtitle "v3.0'da gelecek" (11.5)
└─ tooltip on hover: "Granüler RBAC v3.0 sürümünde gelecek."
```

| Property | Value | Source |
|----------|-------|--------|
| Grid | `repeat(2, 1fr) gap:14` | Verbatim line 211 |
| Card pad | 18 | Verbatim line 213 |
| Icon box | 34×34 / radius:8 | Verbatim line 215 |
| Icon size | 16 | Verbatim |
| Header row gap | 10 | Verbatim line 214 |
| Card header inner gap (title + badge) | 8 | Verbatim line 217 |
| Title margin-top | 0 | n/a |
| Description margin-top | 4 | Verbatim line 221 |
| Description line-height | 1.5 | Verbatim line 221 |
| Footer margin-top | 14 | Verbatim line 224 |
| Footer top-pad | `10px 0 0 0` | Verbatim line 224 |
| Footer gap | 14 | Verbatim line 224 |
| Placeholder min-height | 130 | Verbatim line 232 |
| Placeholder dashed border | inset 0 0 0 1.5px var(--border-strong), borderStyle: dashed | Verbatim line 232 |
| Mobile ≤640px | grid 1fr | **[IMPROVEMENT]** D-F1 |

#### D.2 Typography

| Element | Size | Weight | Family | Source |
|---------|------|--------|--------|--------|
| Card title | 15 px | 600 | sans | Verbatim line 218 |
| Custom Badge / v3.0 Badge | 10.5 px | per primitive | sans | Verbatim line 219 |
| Description | 12 px | 400 | sans, --fg-muted | Verbatim line 221 |
| Footer stat label | 12 px | 400 | sans, --fg-subtle | Verbatim line 225 |
| Footer stat value | 12 px | 600 | mono, tabular-nums | Verbatim line 225 |
| Placeholder title | 13 px | 600 | sans | Verbatim line 235 |
| Placeholder subtitle | 11.5 px | 400 | sans | Verbatim line 236 |

#### D.3 Color

| Element | Token | Source |
|---------|-------|--------|
| Admin icon-fill bg | `color-mix(in oklch, var(--priority-critical) 18%, transparent)` | Verbatim line 215 |
| Admin icon-fill fg | `var(--priority-critical)` | Verbatim |
| PM icon-fill bg | `color-mix(in oklch, var(--status-progress) 18%, transparent)` | Verbatim |
| PM icon-fill fg | `var(--status-progress)` | Verbatim |
| Member icon-fill bg | `color-mix(in oklch, var(--fg-muted) 18%, transparent)` | Verbatim |
| Member icon-fill fg | `var(--fg-muted)` | Verbatim |
| Guest icon-fill bg | `color-mix(in oklch, var(--fg-subtle) 18%, transparent)` | Verbatim |
| Guest icon-fill fg | `var(--fg-subtle)` | Verbatim |
| Guest card opacity | 0.7 | **[IMPROVEMENT]** D-A5 — visually telegraph "deferred" |
| v3.0 Badge | tone="warning" (uses --status-review) | **[IMPROVEMENT]** D-A5 |
| Page AlertBanner | tone="info" | **[IMPROVEMENT]** D-A3 |
| Placeholder card bg | `var(--surface-2)` | Verbatim line 232 |
| Placeholder dashed border | `var(--border-strong)` | Verbatim |
| Placeholder content color | `var(--fg-muted)` | Verbatim line 233 |
| Placeholder cursor | `not-allowed` | **[IMPROVEMENT]** D-A4 — prototype has `pointer` which is misleading |

#### D.4 Interaction

| Trigger | Action |
|---------|--------|
| Mount | Fetch `/api/v1/admin/users` aggregate to compute per-role counts (Admin: 1, PM: N, Member: N, Guest: 0 always) |
| Click any of the 4 system role cards | NO-OP (cards are display-only per D-A5; no Düzenle button) |
| Click Guest card | NO-OP — disabled |
| Hover NewRolePlaceholder | Show tooltip "Granüler RBAC v3.0 sürümünde gelecek." |
| Click NewRolePlaceholder | NO-OP (cursor:not-allowed) — optionally toast `tone="info"`: "Granüler rol yönetimi v3.0'da gelecek." |

| State | Visual |
|-------|--------|
| Loading | 4 skeleton role cards + 1 dashed placeholder static |
| Error | AlertBanner replaces card grid |
| Empty | impossible (system roles always exist) |
| Hover NewRolePlaceholder | tooltip |

#### D.5 Copy

See **Copywriting Contract → Surface D**.

#### D.6 Component fidelity

| Existing primitive used |
|------------------------|
| `Card` (padding=18) |
| `Badge` ("Özel" custom + v3.0 warning) |
| `<AlertBanner tone="info"/>` for page-level RBAC notice |
| `lucide-react` icons: `Shield`, `Plus` |
| `<DataState/>` |

**Improvements over prototype:**
1. `[IMPROVEMENT]` Page-level AlertBanner explaining v3.0 status — D-A3.
2. `[IMPROVEMENT]` All 4 role descriptions REWRITTEN to reflect v2.0 reality — D-A5. (Critical — prototype's "tüm sistem erişimi" implies global PM/Member which is FALSE.)
3. `[IMPROVEMENT]` Permissions count + Düzenle button REMOVED from cards — D-A5.
4. `[IMPROVEMENT]` Guest card dimmed (opacity 0.7) + v3.0 Badge — D-A5.
5. `[IMPROVEMENT]` "Yeni rol oluştur" cursor:not-allowed + tooltip — D-A4.
6. `[IMPROVEMENT]` Mobile ≤640px 1fr grid — D-F1.

---

### Surface E — İzin Matrisi (`/admin/permissions`) — RBAC PLACEHOLDER

#### E.1 Hierarchy & Layout

Verbatim from prototype lines 243-313 with [IMPROVEMENT] disabled-toggle treatment per D-A3.

```
AdminPermissions
├─ <AlertBanner tone="info"/> (page-level — see Surface E copy)
└─ Card (padding=0)
    ├─ Toolbar (padding:12 16 borderBottom flex align:center gap:10)
    │   ├─ Title "İzin Matrisi" (13/600)
    │   ├─ Subtitle "Rol başına tüm izinler tek bakışta" (11.5/--fg-muted)
    │   ├─ Spacer
    │   ├─ <Badge tone="warning" size="sm">"v3.0"</Badge>
    │   └─ Button xs secondary icon=Copy "Kopyala" disabled (with tooltip "v3.0'da gelecek")
    ├─ MatrixHeader (sticky, gridTemplateColumns: `2fr repeat(4, 100px)`, padding:10 16, --surface-2 bg, fontSize:11.5/600/--fg-muted, borderBottom)
    │   └─ "İzin" / "Admin" / "PM" / "Member" / "Guest"
    └─ For each permission group (4 groups):
        ├─ GroupLabel (padding:10 16, --bg-2 bg, fontSize:11/600/--fg-subtle uppercase letterSpacing:0.5, borderBottom)
        └─ For each permission (3-4 per group, 14 total):
            └─ Row (gridTemplateColumns same, padding:10 16, borderBottom)
                ├─ Permission label (12.5/400)
                └─ For each role (4 cols):
                    └─ <DisabledToggle on={matrix[permId][roleIdx]}/>
```

| Property | Value | Source |
|----------|-------|--------|
| Card pad | 0 | Verbatim line 278 |
| Toolbar pad | `12px 16px` | Verbatim line 279 |
| Toolbar gap | 10 | Verbatim |
| Matrix header sticky top | 0 | Verbatim line 286 |
| Matrix header z-index | 2 | Verbatim |
| Header bg | `var(--surface-2)` | Verbatim line 286 |
| Group label pad | `10px 16px` | Verbatim line 292 |
| Group label bg | `var(--bg-2)` | Verbatim line 292 |
| Group label letter-spacing | 0.5 | Verbatim |
| Row pad | `10px 16px` | Verbatim line 294 |
| Row grid | `2fr repeat(4, 100px)` | Verbatim line 294 |
| Toggle pill | 26×18 / radius:10 | Verbatim line 300 |
| Toggle knob | 14×14 / radius:50% | Verbatim line 301 |
| Toggle knob top | 2 | Verbatim |
| Toggle knob left (off) | 2 | Verbatim |
| Toggle knob left (on) | 10 | Verbatim |
| Mobile ≤1024px | grid 2fr repeat(4, 80px); horizontal scroll | **[IMPROVEMENT]** D-F1 |
| Mobile ≤640px | grid 1fr, header sticks; rows become 2-line per role: permission label + 4 mini-pill row of toggles | **[IMPROVEMENT]** D-F1 |

#### E.2 Typography

| Element | Size | Weight | Family | Source |
|---------|------|--------|--------|--------|
| Toolbar title | 13 px | 600 | sans | Verbatim line 280 |
| Toolbar subtitle | 11.5 px | 400 | sans, --fg-muted | Verbatim line 281 |
| Matrix header column | 11.5 px | 600 | sans, --fg-muted | Verbatim line 286 |
| Group label | 11 px | 600 | sans, uppercase, letter-spacing 0.5, --fg-subtle | Verbatim line 292 |
| Row permission label | 12.5 px | 400 | sans | Verbatim line 295 |

#### E.3 Color

| Element | Token | Source |
|---------|-------|--------|
| Page AlertBanner | tone="info" | **[IMPROVEMENT]** D-A3 |
| Toolbar bg | `var(--surface)` (Card default) | n/a |
| v3.0 Badge | tone="warning" | **[IMPROVEMENT]** D-A3 |
| Matrix header bg | `var(--surface-2)` | Verbatim |
| Group label bg | `var(--bg-2)` | Verbatim |
| Toggle ON bg (decorative — toggles are disabled) | `var(--primary)` | Verbatim line 300 |
| Toggle OFF bg | `var(--surface-2)` + inset 0 0 0 1px var(--border) | Verbatim |
| Toggle knob | `oklch(1 0 0)` (white — matches Toggle primitive) + boxShadow `0 1px 2px oklch(0 0 0 / 0.15)` | Verbatim line 301 |
| Disabled toggle opacity | 0.6 | **[IMPROVEMENT]** D-A3 — telegraphs disabled-state |
| Disabled toggle cursor | not-allowed | **[IMPROVEMENT]** D-A3 |

#### E.4 Interaction

| Trigger | Action |
|---------|--------|
| Mount | Read static permission matrix from `Frontend2/lib/admin/permissions-static.ts` (no API call — placeholder data) |
| Click any toggle | NO-OP (toggle has `disabled` attribute + `aria-disabled="true"`) — optionally toast tone="info" "v3.0'da düzenlenebilir." (debounced 1× per 5s to avoid spam) |
| Hover toggle | Show tooltip "RBAC altyapısı v3.0 sürümünde gelecek." |
| Click "Kopyala" | NO-OP (disabled) — optionally tooltip same as toggles |
| Window resize | Mobile breakpoints reflow grid |

| State | Visual |
|-------|--------|
| Default | All toggles in their hardcoded state per matrix; opacity 0.6 |
| Hover toggle | tooltip + cursor:not-allowed |
| Loading | Skeleton: 14 row shimmer + 4 col toggle placeholders |

#### E.5 Copy

See **Copywriting Contract → Surface E**.

#### E.6 Component fidelity

| Existing primitive used |
|------------------------|
| `Card` (padding=0) |
| `Badge` (v3.0 warning) |
| `Button` (xs secondary "Kopyala" — disabled) |
| `<AlertBanner tone="info"/>` page-level |
| `<DataState/>` (loading skeleton wrapper) |
| **NEW** `<DisabledToggle/>` — visually identical to Toggle primitive but always `disabled` + `aria-disabled` + tooltip; lives in `Frontend2/components/admin/disabled-toggle.tsx` |
| `lucide-react` icons: `Copy`, `Lock` (page subtitle could prepend a Lock icon — optional) |

**Improvements over prototype:**
1. `[IMPROVEMENT]` Page-level AlertBanner explaining v3.0 status — D-A3.
2. `[IMPROVEMENT]` v3.0 Badge in card header — D-A3.
3. `[IMPROVEMENT]` All toggles disabled + opacity:0.6 + cursor:not-allowed + tooltip — D-A3.
4. `[IMPROVEMENT]` Kopyala button disabled — D-A3.
5. `[IMPROVEMENT]` Mobile ≤1024px horizontal scroll + ≤640px reflow — D-F1.
6. `[IMPROVEMENT]` aria-disabled + aria-label per toggle — D-A3 a11y.

---

### Surface F — Projeler (`/admin/projects`)

#### F.1 Hierarchy & Layout

Verbatim from prototype lines 315-351 with [IMPROVEMENT] MoreH menu wiring per D-B5.

```
AdminProjects (Card padding=0)
├─ Toolbar (padding:12 16 borderBottom flex align:center gap:8)
│   ├─ <Input/> icon=Search size=sm width:220 placeholder
│   ├─ Spacer
│   ├─ Button sm secondary icon=Download "Dışa aktar"
│   └─ Button sm primary icon=Plus "Yeni proje"
├─ ColumnHeader (gridTemplateColumns: `60px 2fr 110px 150px 120px 90px 90px 28px`, pad:10 16, fontSize:11/uppercase/600/--fg-subtle, borderBottom)
│   └─ Key / İsim / Yöntem / Yönetici / Görevler / İlerleme / Oluşturma / (MoreH)
└─ For each project (admin sees ALL incl. archived):
    └─ <ProjectAdminRow/>  (.hover-row, grid same, pad:10 16, borderBottom unless last, fontSize:12.5)
        ├─ Key chip (10.5/600/mono, --surface-2 bg, padding:2 5, radius:4, --fg-muted)
        ├─ Name (500)
        ├─ <Badge xs tone={methodology→tone}>{methodology}</Badge>
        ├─ Lead (Avatar size=20 + first-name 12/--fg-muted)
        ├─ Tasks (mono 11/--fg-muted)  "{N} · {N} bitti"
        ├─ Progress (flex align:center gap:4) (4px-tall track + mono percentage 10.5/--fg-muted)
        ├─ Created (11/--fg-muted, formatted short-date)
        └─ MoreH button → Popover (Arşivle / Sil)
```

| Property | Value | Source |
|----------|-------|--------|
| Toolbar pad | `12px 16px` | Verbatim line 319 |
| Search width | 220 | Verbatim line 320 |
| Column header pad | `10px 16px` | Verbatim line 325 |
| Row grid | `60px 2fr 110px 150px 120px 90px 90px 28px` | Verbatim line 325 |
| Row pad | `10px 16px` | Verbatim line 332 |
| Row gap | 8 | Verbatim |
| Key chip pad | `2px 5px` | Verbatim line 333 |
| Key chip radius | 4 | Verbatim |
| Lead Avatar size | 20 | Verbatim line 336 |
| Progress track | 4px height / radius:2 | Verbatim line 339 |
| Mobile ≤1024px — column-hide | hide Yönetici + Tasks; grid `60px 2fr 110px 90px 90px 28px` | **[IMPROVEMENT]** D-F1 |
| Mobile ≤640px — stacked-card | row 1: Key+Name+Methodology / row 2: Progress+Created / MoreH absolute top-right | **[IMPROVEMENT]** D-F1 |

#### F.2 Typography

| Element | Size | Weight | Family | Source |
|---------|------|--------|--------|--------|
| Search | 13/400 | (Input) | sans | Reuse |
| Column header | 11/600 uppercase letter-spacing 0.4 | sans | Verbatim line 325 |
| Key chip | 10.5/600 | mono | Verbatim line 333 |
| Name | 12.5/500 | sans | Verbatim line 334 |
| Methodology Badge | per primitive | sans | Verbatim line 335 |
| Lead first-name | 12/400 | sans, --fg-muted | Verbatim line 336 |
| Tasks | 11/400 | mono, --fg-muted | Verbatim line 337 |
| Progress percentage | 10.5/400 | mono, --fg-muted | Verbatim line 342 |
| Created | 11/400 | sans, --fg-muted | Verbatim line 344 |

#### F.3 Color

| Element | Token | Source |
|---------|-------|--------|
| Key chip bg | `var(--surface-2)` | Verbatim line 333 |
| Methodology Badge — scrum | tone="info" (--status-progress) | Verbatim line 335 |
| Methodology Badge — kanban | tone="primary" (--primary) | Verbatim line 335 |
| Methodology Badge — waterfall | tone="warning" (--status-review) | Verbatim line 335 |
| Methodology Badge — other (iterative/incremental/etc.) | tone="neutral" | **[IMPROVEMENT]** D-X3 — fallback for non-3-classic methodologies |
| Progress track | `var(--surface-2)` | Verbatim line 339 |
| Progress fill | `var(--primary)` | Verbatim line 340 |
| Row hover | `var(--surface-2)` (.hover-row) | **[IMPROVEMENT]** — prototype lacks hover affordance |
| Archived row | opacity 0.6 (matches Phase 10 D-32 PROJ-04) | **[IMPROVEMENT]** D-B5 — visually telegraph archived |

#### F.4 Interaction

| Trigger | Action |
|---------|--------|
| Mount | Fetch `GET /api/v1/projects?include_archived=true` (admin sees all statuses) |
| Click row body | navigate to `/projects/{id}` |
| Click "Yeni proje" | `router.push("/projects/new")` (Phase 10 PROJ-01 wizard) |
| Click "Dışa aktar" | Trigger CSV download (server-side endpoint) |
| Click MoreH | Popover with: "Arşivle" (skipped if status=ARCHIVED, replaced with "Aktif et" reusing Phase 10 D-32 reactivation) + "Sil" (red text) |
| Click MoreH "Arşivle" | ConfirmDialog → PATCH `/projects/{id}/status` `{status: "ARCHIVED"}` (Phase 10 endpoint) → optimistic + toast |
| Click MoreH "Aktif et" | PATCH `/projects/{id}/status` `{status: "ACTIVE"}` → toast |
| Click MoreH "Sil" | Two-step ConfirmDialog (type project key) → DELETE `/projects/{id}` → toast + row removed |
| Window focus | refetchOnWindowFocus |

| State | Visual |
|-------|--------|
| Loading | Skeleton table (8 row shimmer) |
| Empty (filter) | "Filtreyle eşleşen proje yok." (--fg-muted, padding:40, centered) |
| Empty (no projects) | "Henüz proje yok. **Yeni proje oluştur** ile başla." with CTA button |
| Archived row | opacity 0.6 + small "Arşiv" Badge in name column |

#### F.5 Copy

See **Copywriting Contract → Surface F**.

#### F.6 Component fidelity

| Existing primitive used |
|------------------------|
| `Card` (padding=0) |
| `Input` |
| `Button` |
| `Avatar` size=20 |
| `Badge` (methodology + archived) |
| `<DataState/>` |
| `<ConfirmDialog/>` |
| `<ToastProvider/>` |

**Improvements over prototype:**
1. `[IMPROVEMENT]` MoreH Popover wired (Arşivle / Aktif et / Sil) — D-B5.
2. `[IMPROVEMENT]` Two-step delete confirmation (type project key) — D-A6 destructive guard.
3. `[IMPROVEMENT]` Archived rows visually dimmed at 0.6 opacity + Badge — D-B5.
4. `[IMPROVEMENT]` Mobile ≤1024px column-hide + ≤640px stacked-card — D-F1.
5. `[IMPROVEMENT]` Empty + filter-empty states — D-F2.
6. `[IMPROVEMENT]` Hover row affordance via `.hover-row` — D-G3.

---

### Surface G — Şablonlar (`/admin/workflows`)

#### G.1 Hierarchy & Layout

Verbatim from prototype lines 353-387.

```
AdminWorkflows (grid repeat(3, 1fr) gap:14)
└─ For each template (from GET /api/v1/process_templates):
    └─ <TemplateCard/>  (Card padding=16)
        ├─ HeaderRow (flex align:flex-start justify:space-between gap:8)
        │   ├─ TitleBlock
        │   │   ├─ NameRow (flex align:center gap:8)
        │   │   │   ├─ Name (14/600)
        │   │   │   └─ {custom?} <Badge xs tone="primary">"Özel"</Badge>
        │   │   └─ Description (12/400/--fg-muted/lineHeight:1.5/marginTop:6)
        │   └─ MoreH button (top-right)
        └─ Footer (flex align:center gap:8 marginTop:12 padding:10 0 0 0 borderTop:1px var(--border) fontSize:11.5)
            ├─ ModeBadge (tone per mode)
            ├─ Spacer
            └─ "{N} proje" (--fg-muted)
```

| Property | Value | Source |
|----------|-------|--------|
| Grid | `repeat(3, 1fr) gap:14` | Verbatim line 363 |
| Card pad | 16 | Verbatim line 365 |
| Header row gap | 8 | Verbatim line 366 |
| Description margin-top | 6 | Verbatim line 372 |
| Description line-height | 1.5 | Verbatim |
| Footer margin-top | 12 | Verbatim line 376 |
| Footer top-pad | `10px 0 0 0` | Verbatim |
| Footer gap | 8 | Verbatim |
| Mobile ≤1024px | grid repeat(2, 1fr) | **[IMPROVEMENT]** D-F1 |
| Mobile ≤640px | grid 1fr | **[IMPROVEMENT]** D-F1 |

#### G.2 Typography

| Element | Size | Weight | Family | Source |
|---------|------|--------|--------|--------|
| Name | 14/600 | sans | Verbatim line 369 |
| Custom Badge | 10.5 (Badge xs) | sans | Verbatim line 370 |
| Description | 12/400 | sans, --fg-muted | Verbatim line 372 |
| Mode Badge | 10.5 (Badge xs) | sans | Verbatim line 377 |
| "N proje" footer | 11.5/400 | sans, --fg-muted | Verbatim line 381 |

#### G.3 Color

| Element | Token |
|---------|-------|
| Mode Badge — sequential-locked | tone="warning" (--status-review) |
| Mode Badge — continuous | tone="info" (--status-progress) |
| Mode Badge — flexible | tone="neutral" |
| Custom Badge | tone="primary" (--primary) |
| MoreH icon | `var(--fg-subtle)` |

#### G.4 Interaction

| Trigger | Action |
|---------|--------|
| Mount | Fetch `GET /api/v1/process_templates` |
| Click card body | NO-OP (no card-level click; only MoreH actions) |
| Click MoreH | Popover with: "Düzenle" / "Klonla" / "Sil" (red text) |
| Click "Düzenle" | `router.push(`/workflow-editor?templateId=${t.id}`)` (Phase 12 D-09) |
| Click "Klonla" | POST `/api/v1/process_templates/{id}/clone` → toast + refetch |
| Click "Sil" | ConfirmDialog → DELETE `/api/v1/process_templates/{id}` → toast + card removed |
| Window focus | refetchOnWindowFocus |

| State | Visual |
|-------|--------|
| Loading | 6 skeleton template cards |
| Empty | "Henüz şablon yok." (--fg-muted, padding:40, centered, full-grid-span) |
| Error | AlertBanner replaces grid |

#### G.5 Copy

See **Copywriting Contract → Surface G**.

#### G.6 Component fidelity

| Existing primitive used |
|------------------------|
| `Card` (padding=16) |
| `Badge` (Custom + Mode) |
| `lucide-react` icons: `Lock` (sequential-locked Badge), `MoreHorizontal` |
| `<DataState/>` |
| `<ConfirmDialog/>` |
| `<ToastProvider/>` |

**Improvements over prototype:**
1. `[IMPROVEMENT]` MoreH Popover wired (Düzenle / Klonla / Sil) — D-B1.
2. `[IMPROVEMENT]` Mobile ≤1024px / ≤640px responsive — D-F1.
3. `[IMPROVEMENT]` Empty state — D-F2.

---

### Surface H — Audit (`/admin/audit`)

#### H.1 Hierarchy & Layout

Verbatim from prototype lines 389-428 with [IMPROVEMENT] **Detay column replaces Meta+IP+Risk** per D-Z1 + filter modal anchor + pagination per D-Z2.

```
AdminAudit (Card padding=0)
├─ Toolbar (padding:12 16 borderBottom flex align:center gap:8)
│   ├─ <Input/> icon=Search size=sm width:260 placeholder
│   ├─ Button sm ghost icon=Calendar "Son 24 saat" (quick filter)
│   ├─ Button sm ghost icon=Filter "Filtre"
│   ├─ Spacer
│   └─ Button sm secondary icon=Download "JSON"
├─ {filterActive} → ActiveFilterChipStrip (padding:8 16, --surface-2 bg, flex gap:6, fontSize:11.5)
│   └─ "Aktör: Ayşe ×" "İşlem: task.* ×" "{date_range} ×" (each chip clears its facet)
├─ ColumnHeader (gridTemplateColumns: `90px 160px 180px 1fr 1fr 28px`, padding:10 16, fontSize:11/uppercase/600/--fg-subtle, borderBottom)
│   └─ Time / Actor / Action / Target / Detay / (MoreH — optional)
└─ For each row (paged, default 50/page):
    └─ <AuditRow/>  (.hover-row, gridTemplateColumns same, padding:8 16, borderBottom, fontSize:11.5/mono)
        ├─ Time (--fg-muted, formatted HH:MM:SS)
        ├─ Actor (Avatar size=18 + name in sans, fontFamily:var(--font-sans))
        ├─ Action (--status-progress for create/update, --priority-critical for delete/deactivate)
        ├─ Target (sans, ellipsis on overflow)
        ├─ Detay (sans, --fg-muted, full Jira-style render via shared formatter)
        └─ (optional MoreH if D-W3-alt — currently skipped per D-W3-alt: row click does nothing, refLabel inside Detay is clickable)
└─ PaginationFooter (padding:10 16 borderTop flex justify:space-between align:center, fontSize:11.5)
    ├─ "Toplam: {N} kayıt" (--fg-muted)
    ├─ Spacer
    ├─ Page-size SegmentedControl (25 / 50 / 100)
    ├─ "Sayfa {P} / {M}" (mono)
    ├─ Button xs ghost icon=ChevronLeft "Önceki"
    └─ Button xs ghost icon=ChevronRight "Sonraki"
```

| Property | Value | Source |
|----------|-------|--------|
| Toolbar pad | `12px 16px` | Verbatim line 402 |
| Search width | 260 | Verbatim line 403 |
| Column header pad | `10px 16px` | Verbatim line 409 |
| Row pad | `8px 16px` | Verbatim line 415 (denser than other tables — verbatim) |
| Row grid | `90px 160px 180px 1fr 1fr 28px` | **[IMPROVEMENT]** D-Z1 — replaces prototype's 7-col `90px 160px 180px 1fr 1fr 120px 80px` (drops IP + Risk) |
| Actor avatar size | 18 | Verbatim line 417 |
| Actor name font-family | `var(--font-sans)` (overrides row's mono) | Verbatim line 417 |
| Active filter chip strip pad | `8px 16px` | New |
| Active filter chip strip bg | `var(--surface-2)` | New |
| Pagination footer pad | `10px 16px` | New |
| Mobile ≤1024px — column-hide | hide Target column; grid `90px 160px 180px 1fr 28px` | **[IMPROVEMENT]** D-F1 |
| Mobile ≤640px — stacked-card | row 1: Time+Actor / row 2: Action / row 3: Detay | **[IMPROVEMENT]** D-F1 |

#### H.2 Typography

| Element | Size | Weight | Family | Source |
|---------|------|--------|--------|--------|
| Toolbar buttons | 12.5 (Button sm) | 500 | sans | Reuse |
| Active filter chip | 11.5/400 | sans | New |
| Column header | 11/600 uppercase letter-spacing 0.4 | sans | Verbatim line 409 |
| Row Time | 11.5/400 | mono, --fg-muted | Verbatim line 416 |
| Row Actor name | 11.5/400 | sans (override) | Verbatim line 417 |
| Row Action | 11.5/400 | mono, --status-progress | Verbatim line 418 |
| Row Target | 11.5/400 | sans, --fg | Verbatim line 419 |
| Row Detay | 11.5/400 | sans, --fg-muted (with **bold** entity refs) | **[IMPROVEMENT]** D-D5 — combines prototype's Meta+IP into a single human-readable line |
| Pagination caption | 11.5/400 | sans, --fg-muted | New |
| Pagination page indicator | 11.5/400 | mono | New |

#### H.3 Color

| Element | Token | Source |
|---------|-------|--------|
| Row hover | `var(--surface-2)` (.hover-row) | **[IMPROVEMENT]** — prototype lacks hover |
| Action text — create/update/publish | `var(--status-progress)` | Verbatim line 418 |
| Action text — deactivate/delete/archive | `var(--priority-critical)` | **[IMPROVEMENT]** D-D3 — semantic differentiation by action prefix |
| Action text — login/auth (DEFERRED — no SSO/MFA in v2.0) | n/a | n/a — these audit_log rows do NOT appear in Phase 14 |
| Detay entity refs (clickable) | `var(--primary)` cursor:pointer | **[IMPROVEMENT]** D-D5 — matches activity-row pattern |
| Active filter chip | `var(--surface)` bg + `var(--fg)` text + 1px border | New |
| Active filter chip × icon | `var(--fg-subtle)` | New |

#### H.4 Interaction

| Trigger | Action |
|---------|--------|
| Mount | Fetch `GET /api/v1/admin/audit?limit=50&offset=0` (default last 50, no filter) |
| Type in Search | debounce 300ms; reset offset to 0; refetch with `q={query}` |
| Click "Son 24 saat" | Set `date_from=now-24h`; refetch |
| Click "Filtre" | Open Audit Filter Modal anchored as full modal (520px wide) |
| Submit Filter Modal | Apply filter facets; rebuild URL `/admin/audit?from=&to=&actor=&action=`; refetch with offset=0 |
| Click chip × | Clear that facet; refetch |
| Click "JSON" | Trigger `GET /api/v1/admin/audit.json` (with current filter as query); browser download `audit-{timestamp}.json` |
| Click row Detay entity ref (e.g. task title bolded) | Navigate to entity (`/projects/{id}/tasks/{key}` or `/users/{id}` etc.) |
| Click row body (excluding refs) | NO-OP per D-W3-alt — row hover indicates clickability but click does nothing (refs are the click targets) |
| Change page-size SegmentedControl | Reset offset to 0; refetch with new limit |
| Click pager Prev/Next | Update offset; refetch |
| Filter changes | Reset offset to 0 (per D-Z2) |

| State | Visual |
|-------|--------|
| Loading | Skeleton 50 row shimmer (compact) |
| Empty (filter active) | "Filtreyle eşleşen audit kaydı yok." (--fg-muted, padding:40, centered) |
| Empty (no audit at all) | "Henüz audit kaydı yok." |
| 50k cap reached | AlertBanner tone="warning" at top of card; pager disabled at 50k threshold |
| Filter active | Chip strip visible above column header |
| 429 on JSON export | Toast tone="warning" "Çok sık istek; 30 sn sonra tekrar dene." (Phase 12 D-58 pattern) |

**A11y:**
- Each filter chip × button: aria-label="{facet_name} filtresini kaldır"
- Pagination buttons: aria-label "Önceki sayfa" / "Sonraki sayfa"
- Each entity ref in Detay column: ensure `<a>` or `<button>` with text label (not just inline span — for screen readers)

#### H.5 Copy

See **Copywriting Contract → Surface H** + **Surface J (Detay verb formatter)**.

#### H.6 Component fidelity

| Existing primitive used |
|------------------------|
| `Card` (padding=0) |
| `Input` |
| `Button` (sm ghost / sm secondary / xs ghost pager) |
| `Avatar` size=18 with href (Phase 13 D-D4) — actor |
| `SegmentedControl` (page size) |
| `<AlertBanner/>` (50k cap warning) |
| `<DataState/>` |
| `<ToastProvider/>` |
| `<ActivityRow variant="admin-table"/>` is NOT used directly in the column structure; instead a **compact version** of the same shared formatter feeds the Detay cell. Phase 14 Plan 14-10 ships a `formatAuditDetail(item, lang): string | ReactNode` helper that returns a 1-line render (not the full timeline row anatomy). The shared formatter is the same authoritative source — `eventMeta[type].verb(lang)` — so /admin/audit Detay and ProjectDetail Activity tab Detail rendering match perfectly. |
| **NEW** `<AuditFilterModal/>` |
| **NEW** `<ActiveFilterChipStrip/>` |

**Improvements over prototype:**
1. `[IMPROVEMENT]` Detay column replaces Meta+IP+Risk (3 cols → 1) — D-Z1 + D-D5.
2. `[IMPROVEMENT]` Action text-color semantic split (create/update vs delete/archive) — D-D3.
3. `[IMPROVEMENT]` Filter Modal with date-range + actor + action-prefix — D-B8.
4. `[IMPROVEMENT]` Active filter chip strip with × per facet — D-Z2.
5. `[IMPROVEMENT]` Pagination via offset (page size 25/50/100) — D-Z2.
6. `[IMPROVEMENT]` 50k row cap warning AlertBanner — D-Z2.
7. `[IMPROVEMENT]` JSON export rate-limit (Phase 12 D-58 pattern) — D-B8.
8. `[IMPROVEMENT]` Actor Avatar clickable to profile — Phase 13 D-D4.
9. `[IMPROVEMENT]` Empty + filter-empty states — D-F2.
10. `[IMPROVEMENT]` Mobile ≤1024px column-hide + ≤640px stacked-card — D-F1.
11. `[IMPROVEMENT]` Hover row affordance — D-G3.
12. `[IMPROVEMENT]` Backwards-compatible rendering of pre-enrichment audit rows (graceful degradation) — D-D6.

---

### Surface I — İstatistik (`/admin/stats`)

#### I.1 Hierarchy & Layout

Verbatim from prototype lines 430-480 with [IMPROVEMENT] recharts integration + 30-project cap notice + tooltip.

```
AdminStats (grid 2fr 1fr gap:20)
├─ ActiveUsersTrendCard (Card padding=16)
│   ├─ HeaderRow (flex align:center justify:space-between marginBottom:14)
│   │   ├─ TitleBlock
│   │   │   ├─ Title "Aktif kullanıcı eğilimi" (13/600)
│   │   │   └─ Subtitle "Son 30 gün" (11.5/--fg-muted, marginTop:2)
│   │   └─ <Badge tone="success" dot>"+{P}%"</Badge>
│   └─ <DataState/> wrapping recharts <AreaChart/> (height:180, viewBox-equivalent via ResponsiveContainer)
│       └─ Tooltip hover: "{date}: {N} aktif kullanıcı"
├─ MethodologyCard (Card padding=16)
│   ├─ Title "Metodoloji Kullanımı" (13/600 marginBottom:14)
│   └─ For each methodology (sorted desc by count, max 5):
│       └─ Row (marginBottom:14)
│           ├─ LabelRow (flex justify:space-between fontSize:12 marginBottom:6)
│           │   ├─ Methodology name (sans)
│           │   └─ "%{P}" (mono, --fg-muted)
│           └─ Bar (height:6, --surface-2 track, methodology-colored fill, radius:3)
└─ VelocityCard (Card padding=16, gridColumn: 1 / -1)
    ├─ Title "Proje başına velocity" (13/600 marginBottom:14)
    ├─ {N > 30} → Notice "İlk 30 aktif proje gösteriliyor." (11.5/--fg-muted)
    └─ Grid (repeat(6, 1fr) gap:10)
        └─ For each project (top 30):
            └─ <VelocityCardItem/>  (padding:10, --surface-2 bg, radius:6)
                ├─ Key (mono 10.5, --fg-muted)
                ├─ Progress big number (20/600 -0.5 tabular-nums, marginTop:4)
                │   └─ Number + "%" (13/--fg-subtle)
                └─ HistoryBars (flex align:flex-end gap:2 marginTop:8 height:28)
                    └─ 7 bars (last 6 historical + current)
                        ├─ Bars 0-5: --border-strong fill, height:`v*100%`
                        └─ Bar 6 (current): --primary fill
```

| Property | Value | Source |
|----------|-------|--------|
| Outer grid | `2fr 1fr gap:20` | Verbatim line 432 |
| ActiveUsers card pad | 16 | Verbatim line 433 |
| Header row marginBottom | 14 | Verbatim line 434 |
| Subtitle marginTop | 2 | Verbatim line 437 |
| Chart height | 180 | Verbatim line 441 (`viewBox 0 0 400 140` + style:`width:100%, height:180`) |
| Methodology card pad | 16 | Verbatim line 449 |
| Methodology row marginBottom | 14 | Verbatim line 452 |
| Methodology label row marginBottom | 6 | Verbatim line 453 |
| Methodology bar height | 6 | Verbatim line 456 |
| Methodology bar radius | 3 | Verbatim |
| Velocity card pad | 16 | Verbatim line 462 |
| Velocity grid | `repeat(6, 1fr) gap:10` | Verbatim line 464 |
| Velocity item pad | 10 | Verbatim line 466 |
| Velocity item radius | 6 | Verbatim |
| Velocity history bars container height | 28 | Verbatim line 469 |
| Velocity history bars gap | 2 | Verbatim line 469 |
| Velocity history bar count | 7 | Verbatim line 470 |
| Mobile ≤1024px — outer grid | 1fr (column-stack) | **[IMPROVEMENT]** D-F1 |
| Mobile ≤1024px — velocity grid | repeat(3, 1fr) | **[IMPROVEMENT]** D-F1 |
| Mobile ≤640px — chart height | 140 | **[IMPROVEMENT]** D-F1 |
| Mobile ≤640px — velocity grid | repeat(2, 1fr) | **[IMPROVEMENT]** D-F1 |

#### I.2 Typography

| Element | Size | Weight | Family | Source |
|---------|------|--------|--------|--------|
| Card title | 13/600 | sans | Verbatim |
| Subtitle | 11.5/400 | sans, --fg-muted | Verbatim line 437 |
| Trend Badge | 11.5 (Badge sm) | per primitive | sans | Verbatim line 439 |
| Methodology name | 12/400 | sans | Verbatim line 454 |
| Methodology percentage | 12/400 | mono, --fg-muted | Verbatim line 454 |
| Velocity item key | 10.5/600 | mono, --fg-muted | Verbatim line 467 |
| Velocity progress big number | 20/600 letter-spacing -0.5 tabular-nums | sans | Verbatim line 468 |
| Velocity progress unit "%" | 13/400 | sans, --fg-subtle | Verbatim line 468 |

#### I.3 Color

| Element | Token | Source |
|---------|-------|--------|
| Trend Badge | tone="success" + dot | Verbatim line 439 |
| Trend SVG line | `var(--primary)` | Verbatim line 444 |
| Trend SVG area fill | `color-mix(in oklch, var(--primary) 12%, transparent)` | Verbatim line 446 |
| Trend SVG grid lines | `var(--border)` | Verbatim line 442 |
| Methodology bar — Scrum | `var(--status-progress)` | Verbatim line 451 |
| Methodology bar — Kanban | `var(--primary)` | Verbatim line 451 |
| Methodology bar — Waterfall | `var(--status-review)` | Verbatim line 451 |
| Methodology bar — Iterative/Other (NEW catch-all) | `var(--fg-muted)` | **[IMPROVEMENT]** D-X3 |
| Methodology bar track | `var(--surface-2)` | Verbatim line 456 |
| Velocity item bg | `var(--surface-2)` | Verbatim line 466 |
| Velocity history bars (non-last) | `var(--border-strong)` | Verbatim line 471 |
| Velocity history bar last (current) | `var(--primary)` | Verbatim line 471 |

#### I.4 Interaction

| Trigger | Action |
|---------|--------|
| Mount | Fetch `GET /api/v1/admin/stats` (composite — see D-A7 payload shape) |
| Hover Active users trend point | recharts tooltip showing `{date}: {N} aktif kullanıcı` |
| Click any chart element | NO-OP (charts are read-only) |
| Click velocity item | NO-OP (or optional: navigate to `/projects/{id}` — Claude's Discretion; default NO-OP) |
| Window focus | refetchOnWindowFocus + staleTime 60s per D-W3 |

| State | Visual |
|-------|--------|
| Loading | Skeletons: ChartSkeleton (line shimmer), 5 methodology row shimmers, 6-12 velocity item shimmers |
| Error | AlertBanner replaces specific card content |
| Empty (no audit_log rows) | Trend card shows "Son 30 günde aktivite yok." centered |
| Empty (no projects with iterations) | Velocity card shows "Hiç proje velocity verisi yok." |
| 30+ projects | Notice "İlk 30 aktif proje gösteriliyor." above velocity grid |

**A11y:**
- Each chart `<svg>`: `role="img"` + `aria-label="Aktif kullanıcı eğilimi grafiği, son 30 gün, ortalama {N} kullanıcı/gün, %{P}+ artış"`
- Per velocity item: `<Card aria-label="{project.name}: %{P} ilerleme, son 7 dönem">` for screen-reader summary

#### I.5 Copy

See **Copywriting Contract → Surface I**.

#### I.6 Component fidelity

| Existing primitive used |
|------------------------|
| `Card` (padding=16) |
| `Badge` (trend success dot) |
| `<DataState/>` |
| `recharts@3.8.1` (Phase 13 dep) — `<AreaChart/>`, `<Tooltip/>`, `<ResponsiveContainer/>` for trend chart |
| Native `<div>` bars for methodology + velocity (no recharts needed — simple percentage / progress bars driven by inline width %) |
| `<DataState/>` per card |

**NEW components (Phase 14 Plan 14-08):**
- `Frontend2/components/admin/stats/active-users-trend-chart.tsx` — recharts AreaChart wrapper
- `Frontend2/components/admin/stats/methodology-bars.tsx` — div-based bar list
- `Frontend2/components/admin/stats/velocity-grid.tsx` — 6-col grid of VelocityCardItem
- `Frontend2/components/admin/stats/velocity-card-item.tsx` — single project velocity tile
- `Frontend2/components/admin/stats/active-users-trend-skeleton.tsx`, `methodology-bars-skeleton.tsx`, `velocity-grid-skeleton.tsx` — chart-shape shimmers (Phase 13 D-F3 pattern reused)

**Improvements over prototype:**
1. `[IMPROVEMENT]` Recharts AreaChart with hoverable tooltip (prototype is hardcoded SVG path) — D-A2 reused.
2. `[IMPROVEMENT]` Methodology data is real (queried per D-X3) not hardcoded mock — D-A7.
3. `[IMPROVEMENT]` Velocity 30-project cap notice — D-X4.
4. `[IMPROVEMENT]` Empty states (no audit / no velocity data) — D-F2.
5. `[IMPROVEMENT]` Loading skeletons matching chart shape — Phase 13 D-F3.
6. `[IMPROVEMENT]` Mobile ≤1024px column-stack + ≤640px reduced grid — D-F1.
7. `[IMPROVEMENT]` ARIA labels on each chart — D-G2 a11y.

---

### Surface J — Cross-cutting Jira-style audit detail (D-D1..D6)

**Files extended:**
- `Frontend2/lib/audit-event-mapper.ts` — extend `SemanticEventType` union with 12 new members (D-D3)
- `Frontend2/lib/activity/event-meta.ts` — extend verb formatter with enriched-metadata consumers (D-D4)
- `Frontend2/components/activity/activity-row.tsx` — add new render branches (D-D4)
- **NEW** `Frontend2/lib/admin/audit-field-labels.ts` — TR/EN field name localization
- **NEW** `Frontend2/lib/admin/format-audit-detail.ts` — single-line formatter for `/admin/audit` Detay column

#### J.1 Hierarchy & Layout

No surface-level layout — this is a cross-cutting renderer change.

The shared `eventMeta[type].verb(lang, metadata)` helper is the single authoritative formatter. Both:
- ActivityRow full-line render (existing — multi-line with status pair / assign target / comment block)
- `/admin/audit` Detay column compact render (NEW — 1-line summary with **bold** entity refs)

call it. The compact variant strips the multi-line subsections (no comment-block / no status-pair badges) and renders a single line with inline bold for entity refs.

```tsx
// /admin/audit Detay column cell
<span className="audit-detail">
  {formatAuditDetail(item, language)}
</span>

// Where formatAuditDetail returns a ReactNode mixing plain text + <strong> entity refs:
//   "değiştirdi <strong onClick={navigate}>'Login bug fix'</strong> son tarih: 26 Nis → 28 Nis"
```

#### J.2 Typography

| Element | Size | Weight | Family |
|---------|------|--------|--------|
| Audit Detay line (compact, /admin/audit) | 11.5/400 | sans | --fg-muted (default), --fg for entity refs |
| Audit Detay entity ref (bold) | 11.5/600 | sans | --primary cursor:pointer |
| Activity tab full-line render | 12.5/400 | sans (existing — preserved) | --fg-muted (default), --primary for entity refs |

#### J.3 Color

| Element | Token | Source |
|---------|-------|--------|
| Detay default text | `var(--fg-muted)` | Compact variant — distinguishes from Action col which is --status-progress |
| Detay entity ref | `var(--primary)` cursor:pointer | Verbatim activity-row.tsx pattern |
| Activity tab (existing) | (preserved — see Phase 13 UI-SPEC §C.3) | n/a |

#### J.4 Interaction

| Trigger | Action |
|---------|--------|
| Click entity ref in Detay (e.g. task title) | If `task_key` available, navigate `/projects/{project_id}/tasks/{task_key}`. If `project_key`, navigate `/projects/{project_id}`. If `user_email`, open Add User Modal pre-filled (admin context only). Falls back to NO-OP if no resolvable target |
| Click Detay row body (excluding entity refs) | NO-OP per D-W3-alt |
| Hover entity ref | cursor:pointer + underline on focus-visible |

**Backwards compat (D-D6):** when `extra_metadata` is missing or has unrecognized field_name, formatter renders the existing minimal text `"{actor} {generic_verb}"` (e.g. just "Yusuf değiştirdi" without the entity bold); never throws.

#### J.5 Copy

See **Copywriting Contract → Surface J** for the verb formatter strings.

#### J.6 Component fidelity

| Existing primitive / lib used |
|------------------------|
| `audit-event-mapper.ts` (Phase 13) — EXTENDED |
| `event-meta.ts` (Phase 13) — EXTENDED |
| `activity-row.tsx` (Phase 13) — EXTENDED with new render branches |
| **NEW** `format-audit-detail.ts` — single-line variant |
| **NEW** `audit-field-labels.ts` — TR/EN field name map |

**Surface affected (cross-cutting):**
- `/admin/audit` Detay column (NEW Phase 14 Surface H)
- `Frontend2/components/activity/activity-tab.tsx` (Phase 13) — gains new event-type rendering automatically via shared formatter
- `Frontend2/components/profile/*` Activity tab variant (Phase 13) — same
- `Frontend2/components/dashboard/activity-feed.tsx` (Phase 10) — same (unchanged consumer; auto-benefits from richer metadata)

**Improvements over prototype + Phase 13:**
1. `[IMPROVEMENT]` 12 new SemanticEventType members (task_field_updated, project_archived, user_invited, etc.) — D-D3.
2. `[IMPROVEMENT]` Enriched metadata consumption (task_title, project_name, old_value_label, new_value_label) — D-D2 + D-D4.
3. `[IMPROVEMENT]` Single-line compact variant for Audit table — D-D5.
4. `[IMPROVEMENT]` Field-label localization map — D-D4.
5. `[IMPROVEMENT]` Backwards-compat graceful degradation for old rows — D-D6.

This is the user's #1 stated quality complaint ("yusuf oluşturdu yazıyor, yusuf neyi oluşturdu ne yaptı?") — the entire Jira-style uplift addresses it.

---

## Design System Coverage (full surface inventory)

This section lists every primitive / component the planner needs to know about for Phase 14. Bold = NEW in Phase 14. Italic = EXTENDED in Phase 14.

### Existing Frontend2 primitives (no change required)

| Primitive | Phase 14 surfaces using it |
|-----------|---------------------------|
| `Card` (padding configurable) | Every surface (Overview StatCards, Pending Card, RoleDistribution, Recent events, Users table, Roller cards, Permission Matrix, Projeler table, Şablonlar grid, Audit table, Stats cards) |
| `Badge` (7 tones, xs/sm) | A (page header — none), B (StatCard tones, role distribution, status), C (role badge, status badge, bulk count), D (Custom + v3.0), E (v3.0), F (methodology, archived), G (Custom + Mode), H (filter chip), I (trend success), J (Audit Detay entity bolding uses inline `<strong>` not Badge) |
| `Button` (5 variants, 5 sizes) | All toolbars + modal footers; primary "Onayla" + "Kullanıcı ekle" + "Yeni proje"; secondary CSV / Toplu davet / Dışa aktar / Filtre / JSON / PDF / Rapor al / Denetim günlüğü; ghost Reddet / Tümünü gör / pager / Quick filter; danger NONE in Phase 14 (destructive uses red text on menu items, not Button danger variant); icon MoreH |
| `SegmentedControl` (xs/sm) | C (role-filter pills replace inline div), H (page-size 25/50/100), bulk role-change modal, audit filter modal |
| `Tabs` (sm/md/lg + badges) | NOT used — Phase 14 uses NEW `<NavTabs/>` for Link-based sub-routes |
| `Avatar` (with `href` from Phase 13 D-D4) | B (Pending pair), C (Users row), F (Lead column), H (Actor column), Modal (user-list + bulk preview) |
| `AvatarStack` (with href forwarding from Phase 13) | C (Users projects column) |
| `AlertBanner` (4 tones) | A (page header — none), B (DataState error), C (DataState error + bulk action errors), D (page-level RBAC notice tone="info"), E (page-level RBAC notice tone="info"), F (DataState error), G (DataState error), H (50k cap warning + DataState error), I (DataState error per card) |
| `ProgressBar` | F (per-project progress in Projeler table — but the prototype uses inline div/4px-track, not ProgressBar primitive; planner picks primitive vs verbatim) |
| `Input` | C (search), F (search), H (search), modals (email + name + action-prefix) |
| `Toggle` | NOT used directly — Permission Matrix uses NEW `<DisabledToggle/>` wrapper |
| `Collapsible` | NOT used in Phase 14 |
| `PriorityChip` | NOT used in Phase 14 |
| `StatusDot` | NOT used in Phase 14 (status uses Badge xs with `dot` prop) |
| `Section` | NOT used directly |
| `Kbd` | NOT used directly |
| `<DataState/>` (Phase 13) | EVERY surface |
| `<ConfirmDialog/>` (Phase 10 D-25) | C (deactivate / delete / bulk deactivate / reject pending), F (archive / delete + two-step key-typing), G (delete template) |
| `<ToastProvider/>` (Phase 10 D-07) | All action feedback |

### Existing Frontend2 composite components (reused as-is)

| Component | Phase 14 surfaces using it |
|-----------|---------------------------|
| `StatCard` (`Frontend2/components/dashboard/stat-card.tsx`) | B (5 StatCards) |
| `<AvatarDropdown/>` (`Frontend2/components/shell/avatar-dropdown.tsx`, Phase 13) | UNCHANGED — Phase 14 verifies the existing Yönetim Paneli link works on `/admin` post-implementation; no code changes expected |
| `<ActivityRow/>` (`Frontend2/components/activity/activity-row.tsx`, Phase 13) | EXTENDED with new variant `admin-table` + new render branches via shared `eventMeta` map (Plan 14-10) — see Surface J |
| `formatRelativeTime` (`Frontend2/lib/activity-date-format.ts`, Phase 13) | B (Pending request time), C (Last seen), I (Active users tooltip date) |

### Existing Frontend2 hooks / services (reused or extended)

| Hook / service | Use | New work |
|----------------|-----|----------|
| `useAuth()` (`Frontend2/context/auth-context.tsx`) | A (route guard), all surfaces (currentUser context) | None |
| `useApp()` (`Frontend2/context/app-context.tsx`) | language for i18n in every surface | None |
| `project-service.ts` (Phase 10) | F (Projeler table) | None |
| `process-template-service.ts` (Phase 12) | G (Şablonlar grid) | None |
| `audit-event-mapper.ts` (Phase 13) | J (cross-cutting) | EXTEND: 12 new SemanticEventType members |
| `event-meta.ts` (Phase 13) | J + B (Recent admin events) + H (Detay column) | EXTEND: verb formatters consume enriched metadata |
| `activity-row.tsx` (Phase 13) | B (compact recent events use the verb formatter), H (compact Detay variant), and Phase 13's existing tabs | EXTEND: 12 new render branches |
| `formatRelativeTime` | All time display | None |

### NEW components (Phase 14)

| Component | File | Owner plan |
|-----------|------|-----------|
| `<NavTabs/>` Link-based tab strip | `Frontend2/components/primitives/nav-tabs.tsx` | 14-02 |
| `<AdminLayout/>` (route guard + Tabs + header bar) | `Frontend2/app/(shell)/admin/layout.tsx` | 14-02 |
| `<AdminOverviewStatCardsRow/>` | `Frontend2/components/admin/overview/stat-cards-row.tsx` | 14-02 |
| `<PendingRequestsCard/>` | `Frontend2/components/admin/overview/pending-requests-card.tsx` | 14-02 |
| `<PendingRequestRow/>` | `Frontend2/components/admin/overview/pending-request-row.tsx` | 14-02 |
| `<PendingRequestsModal/>` ("Tümünü gör") | `Frontend2/components/admin/overview/pending-requests-modal.tsx` | 14-02 |
| `<RoleDistributionCard/>` | `Frontend2/components/admin/overview/role-distribution-card.tsx` | 14-02 |
| `<RecentAdminEventsCard/>` | `Frontend2/components/admin/overview/recent-admin-events-card.tsx` | 14-02 |
| `<UsersTable/>` | `Frontend2/components/admin/users/users-table.tsx` | 14-03 |
| `<UserRow/>` | `Frontend2/components/admin/users/user-row.tsx` | 14-03 |
| `<UserRowMoreMenu/>` (Popover) | `Frontend2/components/admin/users/user-row-more-menu.tsx` | 14-03 |
| `<BulkActionBar/>` | `Frontend2/components/admin/users/bulk-action-bar.tsx` | 14-03 |
| `<AddUserModal/>` | `Frontend2/components/admin/users/add-user-modal.tsx` | 14-03 |
| `<BulkInviteModal/>` (CSV upload + parse + summary) | `Frontend2/components/admin/users/bulk-invite-modal.tsx` | 14-03 |
| `<BulkRoleChangeModal/>` | `Frontend2/components/admin/users/bulk-role-change-modal.tsx` | 14-03 |
| `<RoleCard/>` (4 variants) | `Frontend2/components/admin/roles/role-card.tsx` | 14-04 |
| `<NewRolePlaceholderCard/>` | `Frontend2/components/admin/roles/new-role-placeholder-card.tsx` | 14-04 |
| `<PermissionMatrix/>` | `Frontend2/components/admin/permissions/permission-matrix.tsx` | 14-04 |
| `<DisabledToggle/>` (visually identical Toggle, always disabled) | `Frontend2/components/admin/permissions/disabled-toggle.tsx` | 14-04 |
| `<ProjectAdminTable/>` | `Frontend2/components/admin/projects/project-admin-table.tsx` | 14-05 |
| `<ProjectAdminRow/>` | `Frontend2/components/admin/projects/project-admin-row.tsx` | 14-05 |
| `<ProjectAdminRowMoreMenu/>` | `Frontend2/components/admin/projects/project-admin-row-more-menu.tsx` | 14-05 |
| `<DeleteProjectConfirmDialog/>` (two-step key-typing) | `Frontend2/components/admin/projects/delete-project-confirm-dialog.tsx` | 14-05 |
| `<TemplateGrid/>` | `Frontend2/components/admin/workflows/template-grid.tsx` | 14-06 |
| `<TemplateCard/>` | `Frontend2/components/admin/workflows/template-card.tsx` | 14-06 |
| `<TemplateCardMoreMenu/>` | `Frontend2/components/admin/workflows/template-card-more-menu.tsx` | 14-06 |
| `<AuditTable/>` | `Frontend2/components/admin/audit/audit-table.tsx` | 14-07 |
| `<AuditRow/>` (with Detay rendering) | `Frontend2/components/admin/audit/audit-row.tsx` | 14-07 |
| `<AuditFilterModal/>` | `Frontend2/components/admin/audit/audit-filter-modal.tsx` | 14-07 |
| `<ActiveFilterChipStrip/>` | `Frontend2/components/admin/audit/active-filter-chip-strip.tsx` | 14-07 |
| `<AuditPaginationFooter/>` | `Frontend2/components/admin/audit/audit-pagination-footer.tsx` | 14-07 |
| `<ActiveUsersTrendChart/>` (recharts AreaChart) | `Frontend2/components/admin/stats/active-users-trend-chart.tsx` | 14-08 |
| `<MethodologyBars/>` | `Frontend2/components/admin/stats/methodology-bars.tsx` | 14-08 |
| `<VelocityGrid/>` | `Frontend2/components/admin/stats/velocity-grid.tsx` | 14-08 |
| `<VelocityCardItem/>` | `Frontend2/components/admin/stats/velocity-card-item.tsx` | 14-08 |
| `<ActiveUsersTrendSkeleton/>`, `<MethodologyBarsSkeleton/>`, `<VelocityGridSkeleton/>` | `Frontend2/components/admin/stats/*-skeleton.tsx` | 14-08 |
| `<AdminPopoverMenu/>` (generic Popover wrapper for all MoreH menus) | `Frontend2/components/admin/_shared/popover-menu.tsx` | 14-01 |
| `<AdminModal/>` (generic modal shell — overlay + panel + a11y wiring) | `Frontend2/components/admin/_shared/admin-modal.tsx` | 14-01 |

### NEW services / hooks (Phase 14)

| File | Owner plan |
|------|-----------|
| `Frontend2/services/admin-join-request-service.ts` | 14-01 |
| `Frontend2/services/admin-user-service.ts` | 14-01 |
| `Frontend2/services/admin-stats-service.ts` | 14-01 |
| `Frontend2/services/admin-audit-service.ts` | 14-01 |
| `Frontend2/hooks/use-pending-join-requests.ts` | 14-01 |
| `Frontend2/hooks/use-approve-join-request.ts` | 14-01 |
| `Frontend2/hooks/use-reject-join-request.ts` | 14-01 |
| `Frontend2/hooks/use-admin-users.ts` | 14-01 |
| `Frontend2/hooks/use-admin-stats.ts` | 14-01 |
| `Frontend2/hooks/use-admin-audit.ts` | 14-01 |
| `Frontend2/hooks/use-bulk-invite.ts` | 14-01 |
| `Frontend2/hooks/use-bulk-action.ts` | 14-01 |
| `Frontend2/hooks/use-admin-summary-pdf.ts` | 14-11 |
| `Frontend2/lib/admin/permissions-static.ts` (placeholder permission map) | 14-04 |
| `Frontend2/lib/admin/audit-field-labels.ts` (TR/EN field-name map) | 14-10 |
| `Frontend2/lib/admin/format-audit-detail.ts` (1-line compact variant of eventMeta verb formatter) | 14-10 |
| `Frontend2/lib/admin/csv-parse.ts` (Bulk Invite CSV parser, RFC 4180 compliant) | 14-03 |

### EXTENDED files (additive, breaking-change-free)

| File | What changes | Owner plan |
|------|--------------|-----------|
| `Frontend2/lib/audit-event-mapper.ts` | Add 12 new SemanticEventType union members | 14-10 |
| `Frontend2/lib/activity/event-meta.ts` | Add verb formatters consuming enriched metadata for 12 new types | 14-10 |
| `Frontend2/components/activity/activity-row.tsx` | Add new conditional render branches for 12 new types; preserves existing branches via `default:` fallback | 14-10 |
| `Frontend2/components/primitives/index.ts` | Add `<DisabledToggle/>` (if hoisted to primitives) — likely stays in `components/admin/permissions/` | 14-04 |
| `Frontend2/components/header.tsx` | NO CHANGE — admin "Rapor al" + "Denetim günlüğü" buttons live in admin/layout.tsx page header, NOT global header | n/a |
| `Frontend2/app/globals.css` | Add admin-tabs-wrap, .activity-mobile, modal animation, MoreH popover, user-row stacked-card mobile classes | 14-09-09 (cross-cutting CSS) |

### REPLACED files

| File | Replaced by |
|------|-------------|
| (none — Phase 14 is purely additive; no Phase 13 file is replaced) | n/a |

### Backend NEW (Phase 14 scope — already detailed in CONTEXT D-A1..A8 / D-X1..X4)

| Endpoint | Owner plan |
|----------|-----------|
| `POST /api/v1/admin/users` (email-invite) | 14-01 |
| `PATCH /api/v1/admin/users/{id}/role` | 14-01 |
| `PATCH /api/v1/admin/users/{id}/deactivate` | 14-01 |
| `POST /api/v1/admin/users/{id}/password-reset` | 14-01 |
| `POST /api/v1/admin/users/bulk-invite` | 14-01 |
| `POST /api/v1/admin/users/bulk-action` | 14-01 |
| `GET /api/v1/admin/users.csv` | 14-01 |
| `GET /api/v1/admin/audit?limit&offset&filters...` | 14-01 |
| `GET /api/v1/admin/audit.json?filters...` | 14-01 |
| `GET /api/v1/admin/stats` | 14-01 |
| `GET /api/v1/admin/summary.pdf` | 14-11 |
| `POST /api/v1/projects/{id}/join-requests` (PM-side create) | 14-01 |
| `GET /api/v1/admin/join-requests?status=pending` | 14-01 |
| `POST /api/v1/admin/join-requests/{id}/approve` | 14-01 |
| `POST /api/v1/admin/join-requests/{id}/reject` | 14-01 |
| Audit log enrichment in `task_repo.py` / `project_repo.py` / comment use cases / milestone/artifact/phase_report use cases | 14-09 |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — Phase 14 introduces zero shadcn blocks (project does not use shadcn per `.planning/PROJECT.md` v2.0 constraint + project memory `feedback_no_shadcn.md`) | not applicable |
| Third-party registries | none | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS — every TR/EN string declared, every `[IMPROVEMENT]` flagged with justification, every RBAC-deferred copy rewritten per D-A2..A5
- [ ] Dimension 2 Visuals: PASS — all dimensions cited from prototype line numbers or flagged as improvement with justification (mobile / a11y / RBAC reality / Detay Jira-render)
- [ ] Dimension 3 Color: PASS — 60/30/10 enforced, accent reserved-for list explicit (10 items), zero new tokens, destructive flow uses red-text-on-menu-item pattern (no destructive button bg per prototype)
- [ ] Dimension 4 Typography: PASS — 11 roles total (Display 24 / Heading 13 / Card title-Roller 15 / Card title-Şablonlar 14 / Body 12.5 / Body-small 12 / Label 11.5 / Label-uppercase 11 / Caption mono 11 / Big number 20 / Tiny mono 10.5), all reused from existing scale; weights stay at 2 (400/600) at design-system level (primitive-internal 500 invisible to contract)
- [ ] Dimension 5 Spacing: PASS — all distances multiples of 4 except documented typography half-steps (10.5/11.5/12.5) + 1 menu item padding (6×10) inherited from Phase 12 D-08 + Roller card padding 18 (prototype-locked design-system canonical) + Audit row 8×16 (prototype line 415, denser than other tables on purpose)
- [ ] Dimension 6 Registry Safety: PASS — no shadcn, no third-party registry blocks

**Approval:** pending

---

## Quick reference for downstream agents

**Planner (`gsd-planner`):**
- The 12-plan decomposition in CONTEXT §D-01 is the contract. UI-SPEC reinforces the dependency graph: Plan 14-01 ships infra (services, hooks, modal/popover shells, generic admin components, NavTabs primitive scaffold, Avatar `href` consumers patched, audit-event-mapper extension scaffold) so Plans 14-02..14-08 can consume.
- Every NEW component lists its owner plan in the "Design System Coverage → NEW components" table (50+ files total expected — fat infra plan 14-01 covers ~30; the rest split across 14-02..14-12).
- Every `[IMPROVEMENT]` flag carries a justification — planner forwards verbatim into PLAN.md so the executor knows it's intentional.
- Plan 14-09 (Audit log enrichment cross-cutting backend) MUST land before Plan 14-10 (Frontend Jira-style rendering) — strict ordering.
- Plan 14-04 (Roller + Permissions placeholder) MUST land WITH the rewritten copy from D-A5 — no placeholder copy slips into shipped UI.

**Executor (`gsd-executor`):**
- Visual / interaction source of truth: this file + the prototype line numbers cited.
- All TR/EN strings live in this file's Copywriting Contract — no improvisation.
- Color: NEVER inline a hex. Always `var(--*)` or `color-mix(in oklch, var(--*) X%, transparent/var(--*))`.
- Spacing: any new value not in this spec → reject and ask planner to revise.
- RBAC-deferred surfaces (Roller / Permissions): toggles MUST be `disabled` AND `aria-disabled="true"` AND have visible "v3.0" Badge AND have hover tooltip — multiple defenses against accidental enabling in a future RBAC v3.0 implementation.
- Two-step delete confirm on `/admin/projects` MUST require typing the exact project key — no faster path.
- Audit-event-mapper extension MUST be backwards-compatible — old (pre-enrichment) audit_log rows render via fallback minimal rendering, never throw.

**UI checker (`gsd-ui-checker`):**
- 6 dimensions enumerated in Sign-Off above. The `[IMPROVEMENT]` flags are not deviations — they are pre-approved by D-00 and the user's explicit quality bar (carried forward from Phase 13: "Use prototype's all elements... AND improve design when [it] didn't [meet] enough").
- Critical RBAC-deferred reality: Roller + Permissions copy rewrites (D-A5) are NOT cosmetic — they correct a misleading "all roles are global" implication in the prototype. The checker MUST verify the rewritten copy ships, not the prototype's original copy.
- Detay column on /admin/audit MUST render via the shared `eventMeta` formatter — not a duplicate hardcoded switch. Single source of truth.

**UI auditor (`gsd-ui-auditor`):**
- Compare implemented surface to prototype line numbers cited per element. Tolerance: <2px on layout values, exact match on colors and copy (TR + EN).
- 8 admin surfaces × 6 dimensions = 48 audit cells. Plus Surface J (Jira-style audit detail) cross-cutting audit on 4 surfaces (admin/audit + 3 Phase 13 activity tabs) = +24 cells. Total 72 cells.
- RBAC-deferred copy rewrites: validate they ship verbatim per Surface D + Surface E copy tables. Prototype's original "Tüm sistem erişimi" / "Tüm projelerde tam yetkili" wording MUST NOT appear in the shipped UI.
- 30-project velocity cap notice: validate it shows when `project_velocities.length > 30` (D-X4).
- Two-step delete confirm: validate the second step's "type project key" guard works (typing wrong key → CTA disabled).

---

*Phase: 14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f*
*UI-SPEC drafted: 2026-04-26*
*Authority order: prototype admin.jsx → Frontend2 primitives + Phase 13 D-F2 DataState + Phase 13 D-D2 AvatarDropdown → Phase 14 [IMPROVEMENT] flags (mobile + a11y + RBAC reality + Detay Jira-render)*
