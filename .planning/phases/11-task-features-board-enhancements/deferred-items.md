- **[11-09 observation] `taskService.addDependency` sends `{type}` but backend DTO expects `dependency_type`** — Plan 01 service bug. Today's usage only sends `"blocks"` which matches the backend default, so the API works by accident. Fix: rename `type` → `dependency_type` in `Frontend2/services/task-service.ts` line 149. Required before the UI ever offers a non-"blocks" option or before any test writes a non-default value.
