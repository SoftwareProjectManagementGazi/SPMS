---
phase: 02-authentication-team-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Backend/tests/unit/application/test_update_user_profile.py
  - Backend/tests/unit/application/test_manage_teams.py
  - Backend/tests/unit/application/test_password_reset.py
  - Backend/tests/unit/application/test_account_lockout.py
  - Backend/tests/integration/api/test_rate_limiting.py
  - Backend/tests/integration/api/test_auth_avatar.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - SEC-01

must_haves:
  truths:
    - "All 6 stub test files are collected by pytest with zero import errors"
    - "All stub functions are marked xfail and appear in collection output"
    - "No implementation code is written yet — stubs only"
  artifacts:
    - path: "Backend/tests/unit/application/test_update_user_profile.py"
      provides: "AUTH-01 unit test stubs (profile update, email change password confirmation)"
      min_lines: 30
    - path: "Backend/tests/unit/application/test_manage_teams.py"
      provides: "AUTH-02 unit test stubs (create team, add member)"
      min_lines: 25
    - path: "Backend/tests/unit/application/test_password_reset.py"
      provides: "AUTH-03 unit test stubs (generic response, expiry, single-use)"
      min_lines: 30
    - path: "Backend/tests/unit/application/test_account_lockout.py"
      provides: "AUTH-04 unit test stubs (5 attempts, counter reset, auto-unlock)"
      min_lines: 30
    - path: "Backend/tests/integration/api/test_rate_limiting.py"
      provides: "SEC-01 integration test stubs (429 after rate limit)"
      min_lines: 20
    - path: "Backend/tests/integration/api/test_auth_avatar.py"
      provides: "AUTH-01 avatar integration test stubs"
      min_lines: 20
  key_links:
    - from: "Implementation plans 02-03, 02-04, 02-05"
      to: "Stub test files"
      via: "pytest verify commands in each plan"
      pattern: "pytest tests/unit/application/test_.*\\.py"
---

<objective>
Create Nyquist-compliant Wave 0 test scaffolds for all Phase 2 implementation plans before any production code is written.

Purpose: Every implementation task in Phase 2 needs a runnable automated verify command. These stubs satisfy the Nyquist rule — they appear in pytest collection and fail expectedly (xfail) until the real implementation makes them pass.
Output: 6 pytest stub files with xfail markers covering AUTH-01, AUTH-02, AUTH-03, AUTH-04, SEC-01.
</objective>

<execution_context>
@C:/Users/yusti/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/yusti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication-team-management/02-CONTEXT.md
@.planning/phases/02-authentication-team-management/02-VALIDATION.md

<interfaces>
<!-- Existing test pattern from Phase 1 — mirror this exactly -->

From Backend/tests/unit/application/test_register_user.py (existing pattern):
```python
import pytest
from unittest.mock import AsyncMock, MagicMock
from app.application.use_cases.register_user import RegisterUserUseCase
from app.application.dtos.auth_dtos import UserRegisterDTO

@pytest.mark.asyncio
async def test_register_user_success():
    ...

@pytest.mark.asyncio
@pytest.mark.xfail(reason="stub — not yet implemented", strict=False)
async def test_some_stub():
    assert False, "not implemented"
```

From Backend/tests/integration/api/test_auth_rbac.py (existing integration pattern):
```python
import pytest

@pytest.mark.xfail(reason="stub — not yet implemented", strict=False)
def test_some_integration_stub():
    assert False, "not implemented"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit test stubs for AUTH-01 (profile update) and AUTH-02 (team management)</name>
  <files>Backend/tests/unit/application/test_update_user_profile.py, Backend/tests/unit/application/test_manage_teams.py</files>
  <action>
Create two unit test stub files using pytest.mark.xfail (strict=False) — same pattern as existing test_register_user.py in the same directory.

**Backend/tests/unit/application/test_update_user_profile.py** — stubs covering:
- `test_update_full_name_success`: UpdateUserProfileUseCase updates full_name, returns updated user
- `test_update_email_without_current_password_raises`: raises error if email included but current_password not provided
- `test_email_change_requires_password`: if email included, current_password must match hash before update
- `test_update_avatar_path_saved`: avatar path stored as relative path (e.g. "uploads/avatars/uuid.jpg"), not absolute
- `test_update_nonexistent_user_raises`: raises 404-style exception if user_id not found

All stubs use `pytest.mark.xfail(reason="stub — AUTH-01 not yet implemented", strict=False)` and `assert False, "not implemented"`.

**Backend/tests/unit/application/test_manage_teams.py** — stubs covering:
- `test_create_team_sets_owner`: creator user_id becomes team.owner_id
- `test_add_member_success`: add_member use case adds user to team.members
- `test_add_duplicate_member_ignored_or_raises`: adding same user twice does not create duplicate membership
- `test_remove_member_success`: remove_member reduces team member count
- `test_only_owner_can_add_member`: non-owner user attempting add_member raises 403-style exception

All stubs marked xfail same pattern.

Do NOT write any implementation code. Import only stdlib and pytest in these stub files — no app imports needed since the tests are all xfail stubs that assert False.
  </action>
  <verify>
    <automated>cd "/c/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Backend" && /c/Users/yusti/anaconda3/python.exe -m pytest tests/unit/application/test_update_user_profile.py tests/unit/application/test_manage_teams.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>Both files collected by pytest with zero import errors. All stubs show as xfail in collection output. No implementation code present.</done>
</task>

<task type="auto">
  <name>Task 2: Create unit/integration test stubs for AUTH-03, AUTH-04, and SEC-01</name>
  <files>Backend/tests/unit/application/test_password_reset.py, Backend/tests/unit/application/test_account_lockout.py, Backend/tests/integration/api/test_rate_limiting.py, Backend/tests/integration/api/test_auth_avatar.py</files>
  <action>
Create four more stub files.

**Backend/tests/unit/application/test_password_reset.py** — stubs covering:
- `test_request_returns_204_for_existing_email`: request endpoint always returns 204 (no enumeration)
- `test_request_returns_204_for_nonexistent_email`: same 204 even when email not in DB
- `test_token_expiry`: token older than 30 minutes is rejected with 400
- `test_token_single_use`: token used once cannot be used again (used_at is set)
- `test_confirm_updates_password`: valid token causes password hash to be updated

All xfail stubs. No app imports.

**Backend/tests/unit/application/test_account_lockout.py** — stubs covering:
- `test_five_failed_attempts_locks_account`: 5 consecutive bad passwords → 6th attempt returns HTTP 423
- `test_counter_resets_on_success`: successful login after partial failures resets attempts to 0
- `test_auto_unlock_after_15_minutes`: after lock, account becomes accessible after 15 min (use time mock)
- `test_locked_response_includes_unlock_time`: 423 response body contains when the lock expires
- `test_correct_password_during_lockout_still_rejected`: even correct password rejected while locked

All xfail stubs. No app imports.

**Backend/tests/integration/api/test_rate_limiting.py** — stubs covering:
- `test_login_rate_limit_429_after_10_requests`: sending 11 POST /auth/login requests in sequence yields 429
- `test_register_rate_limit_429_after_5_requests`: sending 6 POST /auth/register requests yields 429

All xfail stubs. Use minimal imports (pytest only).

**Backend/tests/integration/api/test_auth_avatar.py** — stubs covering:
- `test_avatar_upload_saves_file`: POST /auth/me/avatar with valid image file returns 200 with updated avatar path
- `test_avatar_served_requires_auth`: GET /auth/avatar/{filename} without token returns 401
- `test_avatar_file_size_limit`: uploading file > 2MB returns 413

All xfail stubs. Use minimal imports (pytest only).
  </action>
  <verify>
    <automated>cd "/c/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Backend" && /c/Users/yusti/anaconda3/python.exe -m pytest tests/unit/application/test_password_reset.py tests/unit/application/test_account_lockout.py tests/integration/api/test_rate_limiting.py tests/integration/api/test_auth_avatar.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 stub files collected with zero import errors. Combined with Task 1, total Phase 2 stub count is at least 19 xfail tests. Full collection: `pytest tests/ --collect-only -q` shows no errors.</done>
</task>

</tasks>

<verification>
Run full collection after both tasks:
```
cd "/c/Users/yusti/OneDrive/Masaüstü/bitirme projesi/SPMS/Backend" && /c/Users/yusti/anaconda3/python.exe -m pytest tests/ --collect-only -q 2>&1 | tail -10
```
Expected: all 6 new stub files appear, zero import errors, all new tests are xfail.
</verification>

<success_criteria>
- 6 stub test files exist and are collected by pytest with zero errors
- All stub tests marked xfail(strict=False) — they show as expected failures, not skips
- No app imports in stub files (avoids import errors from not-yet-existing modules)
- Subsequent implementation plans can reference these files in their `<automated>` verify commands
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-team-management/02-01-SUMMARY.md`
</output>
