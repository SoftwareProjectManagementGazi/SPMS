---
phase: 9
slug: backend-schema-entities-apis
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-21
updated: 2026-04-21
---

# Phase 9 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x + pytest-asyncio (per existing Backend/tests/) |
| **Config file** | Backend/pytest.ini (existing) |
| **Quick run command** | `cd Backend && pytest tests/unit -x --tb=short` |
| **Full suite command** | `cd Backend && pytest` |
| **Estimated runtime** | ~60s unit, ~180s full (with Postgres integration) |

---

## Sampling Rate

- **After every task commit:** Run `cd Backend && pytest tests/unit -x --tb=short`
- **After every plan wave:** Run `cd Backend && pytest`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds (unit) / 180 seconds (full)

---

## Per-Task Verification Map

Populated 2026-04-21 (BLOCKER-3 fix). Each row maps a task in this phase to a requirement
ID, the threat-model entries it covers, the secure behaviour it asserts, and the automated
command(s) that prove the behaviour. Commands are lifted verbatim from each plan's
`<verify><automated>...</automated></verify>` blocks and from `09-RESEARCH.md`
§Phase Requirements → Test Map (lines 1773-1831).

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 09-01-01 | 09-01 | 0 | BACK-08 | T-09-01-01..03 | Migration 005 syntactically valid; declares upgrade/downgrade; uses idempotent helpers (`_table_exists`, `_column_exists`, `_index_exists`); creates GIN + partial UNIQUE indexes; chains revision="005_phase9" after "004_phase5" | unit (AST + grep) | `cd Backend && python -c "import ast; tree = ast.parse(open('alembic/versions/005_phase9_schema.py').read()); assert any(isinstance(n, ast.FunctionDef) and n.name == 'upgrade' for n in tree.body)"` AND `grep -c "_column_exists\|_table_exists\|_index_exists" Backend/alembic/versions/005_phase9_schema.py` AND `grep -n "down_revision = \"004_phase5\"\|revision = \"005_phase9\"" Backend/alembic/versions/005_phase9_schema.py` AND `grep -n "GIN (linked_phase_ids)\|ux_phase_reports_active\|schema_version.*1" Backend/alembic/versions/005_phase9_schema.py` | ✅ author | ⬜ pending |
| 09-01-02 | 09-01 | 0 | BACK-08 | T-09-01-04 | AuditLogModel.extra_metadata maps to DB col `metadata` (Pitfall 7); models/__init__.py imports new entity models; migration 005 idempotent + round-trip + schema_version backfill | unit + integration | `grep -n "extra_metadata.*Column.*metadata" Backend/app/infrastructure/database/models/audit_log.py` AND `cd Backend && python -c "from app.infrastructure.database.models.audit_log import AuditLogModel; col = AuditLogModel.__table__.columns['metadata']; assert col.name == 'metadata'; assert AuditLogModel.extra_metadata is not None"` AND `cd Backend && pytest tests/integration/infrastructure/test_migration_005.py -v --tb=short` | ✅ author | ⬜ pending |
| 09-02-01 | 09-02 | 0 | BACK-07 | T-09-02-02 | DI sub-modules in `app/api/deps/` resolve without circular imports; package aggregator re-exports every legacy symbol; each sub-module declares `__all__` | unit (import smoke) | `cd Backend && python -c "from app.api.deps.auth import get_current_user, require_admin, oauth2_scheme, _is_admin"` AND `cd Backend && python -c "from app.api.deps.project import get_project_repo, get_project_member; from app.api.deps.task import get_task_repo, get_task_project_member, get_dependency_repo; from app.api.deps.team import get_team_repo; from app.api.deps.audit import get_audit_repo"` AND `cd Backend && python -c "from app.api.deps import get_current_user, get_project_repo, get_task_repo, get_team_repo"` | ✅ author | ⬜ pending |
| 09-02-02 | 09-02 | 0 | BACK-07 | T-09-02-01, T-09-02-03 | Backward-compat shim preserves identity (legacy and new path return same function object); FastAPI app composes successfully; existing routers unchanged | unit | `cd Backend && python -c "from app.api.dependencies import get_current_user, require_admin, get_project_repo, get_task_repo, get_team_repo, get_audit_repo, get_notification_service, get_sprint_repo, oauth2_scheme"` AND `cd Backend && python -c "from app.api.dependencies import get_current_user as A; from app.api.deps.auth import get_current_user as B; assert A is B"` AND `cd Backend && python -c "import app.api.main; assert app.api.main.app is not None"` AND `cd Backend && pytest tests/unit/test_deps_backward_compat.py -v --tb=short` | ✅ author | ⬜ pending |
| 09-03-01 | 09-03 | 0 | (support — covers API-01, API-10, BACK-04, BACK-05, BACK-06 downstream) | T-09-03-01 | All Phase 9 domain exceptions importable; each subclasses `DomainError`; metadata fields populated correctly | unit | `cd Backend && python -c "from app.domain.exceptions import PhaseGateLockedError, CriteriaUnmetError, PhaseGateNotApplicableError, ArchivedNodeReferenceError, CrossProjectPhaseReferenceError, WorkflowValidationError, ProcessConfigSchemaError"` AND `cd Backend && python -c "from app.domain.exceptions import DomainError, PhaseGateLockedError; e = PhaseGateLockedError(42); assert isinstance(e, DomainError) and e.project_id == 42"` AND `cd Backend && pytest tests/unit/test_domain_exceptions_phase9.py -v --tb=short` | ✅ author | ⬜ pending |
| 09-03-02 | 09-03 | 0 | (support — covers test infrastructure for all Phase 9 integration tests) | T-09-03-02 | Test factories produce valid entities; `authenticated_client` fixture issues real JWTs and respects role param | unit | `ls Backend/tests/factories/` AND `cd Backend && python -c "from tests.factories.user_factory import make_user; u = make_user(); assert u.email and u.is_active"` AND `cd Backend && python -c "from tests.factories.project_factory import make_project; p = make_project(); assert p.key and p.name and p.process_config['schema_version'] == 1"` AND `cd Backend && python -c "from tests.factories.team_factory import make_team; t = make_team(owner_id=1); assert t.name and t.owner_id == 1"` AND `grep -n "authenticated_client" Backend/tests/conftest.py` | ✅ author | ⬜ pending |
| 09-04-01 | 09-04 | 1 | BACK-01, BACK-03 | T-09-04-01, T-09-04-03 | Project entity + ORM gain `status` enum + `process_template_id` + `default_artifacts/default_phase_criteria/default_workflow/cycle_label_*` on ProcessTemplate; `_normalize_process_config` resolves legacy → schema_version=1 idempotently | unit | `cd Backend && python -c "from app.domain.entities.project import Project, ProjectStatus, _normalize_process_config; assert ProjectStatus.ACTIVE.value == 'ACTIVE'"` AND `cd Backend && python -c "from app.domain.entities.project import _normalize_process_config; r = _normalize_process_config({'methodology':'SCRUM'}); assert r['schema_version'] == 1 and r['methodology_legacy'] == 'SCRUM'"` AND `cd Backend && python -c "from app.infrastructure.database.models.project import ProjectModel; assert 'status' in ProjectModel.__table__.columns and 'process_template_id' in ProjectModel.__table__.columns"` AND `cd Backend && python -c "from app.infrastructure.database.models.process_template import ProcessTemplateModel; assert all(c in ProcessTemplateModel.__table__.columns for c in ['default_artifacts','default_phase_criteria','default_workflow','cycle_label_tr','cycle_label_en'])"` AND `cd Backend && pytest tests/unit/application/test_process_config_normalizer.py tests/unit/test_project_entity.py -v --tb=short` | ✅ author | ⬜ pending |
| 09-04-02 | 09-04 | 1 | BACK-02 (also enables D-15/D-17 via Team.leader_id) | T-09-04-02, T-09-04-04 | Task.phase_id format-validated against NODE_ID_REGEX; Team.leader_id added; team_repo gains `user_leads_any_team_on_project` + `get_teams_led_by`; `require_project_transition_authority` resolves Admin/PM/TL | unit | `cd Backend && python -c "from app.domain.entities.task import Task, NODE_ID_REGEX; t = Task(project_id=1, title='T', phase_id='nd_V1StGXR8_Z'); assert NODE_ID_REGEX.match('nd_V1StGXR8_Z')"` AND `cd Backend && python -c "from app.domain.entities.team import Team; t = Team(name='T', owner_id=1, leader_id=42); assert t.leader_id == 42"` AND `cd Backend && python -c "from app.infrastructure.database.models.task import TaskModel; assert 'phase_id' in TaskModel.__table__.columns"` AND `cd Backend && python -c "from app.infrastructure.database.models.team import TeamModel; assert 'leader_id' in TeamModel.__table__.columns"` AND `cd Backend && python -c "from app.api.deps.auth import require_project_transition_authority"` AND `cd Backend && python -c "from app.infrastructure.database.repositories.team_repo import SqlAlchemyTeamRepository; assert hasattr(SqlAlchemyTeamRepository, 'user_leads_any_team_on_project') and hasattr(SqlAlchemyTeamRepository, 'get_teams_led_by')"` AND `cd Backend && pytest tests/unit/test_task_entity.py tests/unit/test_team_entity.py -v --tb=short` | ✅ author | ⬜ pending |
| 09-05-01 | 09-05 | 1 | BACK-04 | T-09-05-01, T-09-05-02 | Milestone entity + ORM model + repo; `linked_phase_ids` GIN-indexed; `list_by_phase` exposed on repo; DI factory in `deps/milestone.py` | unit | `cd Backend && python -c "from app.domain.entities.milestone import Milestone, MilestoneStatus; m = Milestone(project_id=1, name='M1'); assert m.status == MilestoneStatus.PENDING"` AND `cd Backend && python -c "from app.infrastructure.database.models.milestone import MilestoneModel; assert MilestoneModel.__tablename__ == 'milestones' and 'linked_phase_ids' in MilestoneModel.__table__.columns"` AND `cd Backend && python -c "from app.domain.repositories.milestone_repository import IMilestoneRepository; from app.infrastructure.database.repositories.milestone_repo import SqlAlchemyMilestoneRepository; assert hasattr(SqlAlchemyMilestoneRepository, 'list_by_phase')"` AND `cd Backend && python -c "from app.api.deps.milestone import get_milestone_repo; from app.api.dependencies import get_milestone_repo as legacy; assert get_milestone_repo is legacy"` AND `cd Backend && pytest tests/unit/test_milestone_entity.py -v --tb=short` | ✅ author | ⬜ pending |
| 09-05-02 | 09-05 | 1 | BACK-04 | T-09-05-03 | Milestone DTOs validate phase_id format; use cases (CRUD) compose; `linked_phase_ids` invalid format raises ValidationError | unit | `cd Backend && python -c "from app.application.dtos.milestone_dtos import MilestoneCreateDTO, MilestoneUpdateDTO, MilestoneResponseDTO; from app.application.use_cases.manage_milestones import CreateMilestoneUseCase, UpdateMilestoneUseCase, DeleteMilestoneUseCase, ListMilestonesUseCase, GetMilestoneUseCase"` AND `cd Backend && python -c "from app.application.dtos.milestone_dtos import MilestoneCreateDTO; dto = MilestoneCreateDTO(project_id=1, name='M', linked_phase_ids=['nd_a1b2c3d4e5']); assert dto.linked_phase_ids == ['nd_a1b2c3d4e5']"` AND `cd Backend && pytest tests/unit/application/test_manage_milestones.py -v --tb=short` | ✅ author | ⬜ pending |
| 09-06-01 | 09-06 | 1 | BACK-05 | T-09-06-01, T-09-06-02 | Artifact entity + ORM + repo; `file_id` nullable column present; DI factory wired (legacy & new path identical) | unit | `cd Backend && python -c "from app.domain.entities.artifact import Artifact, ArtifactStatus; a = Artifact(project_id=1, name='A'); assert a.status == ArtifactStatus.NOT_CREATED"` AND `cd Backend && python -c "from app.infrastructure.database.models.artifact import ArtifactModel; assert ArtifactModel.__tablename__ == 'artifacts' and 'file_id' in ArtifactModel.__table__.columns"` AND `cd Backend && python -c "from app.api.deps.artifact import get_artifact_repo; from app.api.dependencies import get_artifact_repo as legacy; assert get_artifact_repo is legacy"` AND `cd Backend && pytest tests/unit/test_artifact_entity.py -v --tb=short` | ✅ author | ⬜ pending |
| 09-06-02 | 09-06 | 1 | BACK-05 | T-09-06-03, T-09-06-04 | ArtifactSeeder runs in same tx as project creation (D-29 rollback on failure); CreateProjectUseCase wires seeder; UpdateArtifactByAssigneeDTO excludes `assignee_id` (D-36) | unit | `cd Backend && python -c "from app.application.services.artifact_seeder import ArtifactSeeder; from app.application.use_cases.manage_artifacts import CreateArtifactUseCase, UpdateArtifactByAssigneeUseCase, UpdateArtifactByManagerUseCase, DeleteArtifactUseCase, ListArtifactsUseCase, GetArtifactUseCase"` AND `cd Backend && python -c "from app.application.dtos.artifact_dtos import ArtifactUpdateByAssigneeDTO; dto = ArtifactUpdateByAssigneeDTO(); assert 'assignee_id' not in dto.model_fields_set and 'assignee_id' not in type(dto).model_fields"` AND `cd Backend && pytest tests/unit/application/test_artifact_seeder.py tests/unit/application/test_manage_artifacts.py -v --tb=short` AND `grep -n "artifact_repo\|ArtifactSeeder" Backend/app/application/use_cases/manage_projects.py` | ✅ author | ⬜ pending |
| 09-07-01 | 09-07 | 1 | BACK-06 | T-09-07-01, T-09-07-02 | PhaseReport entity defaults cycle_number=1, revision=1; ORM table + `completed_tasks_notes` JSONB column; audit_repo gains `count_phase_transitions` for cycle inference; DI factory identity-mapped | unit | `cd Backend && python -c "from app.domain.entities.phase_report import PhaseReport; r = PhaseReport(project_id=1, phase_id='nd_V1StGXR8_Z'); assert r.cycle_number == 1 and r.revision == 1"` AND `cd Backend && python -c "from app.infrastructure.database.models.phase_report import PhaseReportModel; assert PhaseReportModel.__tablename__ == 'phase_reports' and 'completed_tasks_notes' in PhaseReportModel.__table__.columns"` AND `cd Backend && python -c "from app.infrastructure.database.repositories.audit_repo import SqlAlchemyAuditRepository; assert hasattr(SqlAlchemyAuditRepository, 'count_phase_transitions')"` AND `cd Backend && python -c "from app.api.deps.phase_report import get_phase_report_repo; from app.api.dependencies import get_phase_report_repo as legacy; assert get_phase_report_repo is legacy"` AND `cd Backend && pytest tests/unit/test_phase_report_entity.py -v --tb=short` | ✅ author | ⬜ pending |
| 09-07-02 | 09-07 | 1 | BACK-06 | T-09-07-03, T-09-07-04 | PhaseReport DTOs + use cases compose; `render_pdf` returns bytes starting with `%PDF` (smoke); cycle_number auto-calc + revision auto-increment | unit | `cd Backend && python -c "from app.application.dtos.phase_report_dtos import PhaseReportCreateDTO, PhaseReportUpdateDTO, PhaseReportResponseDTO"` AND `cd Backend && python -c "from app.application.use_cases.manage_phase_reports import CreatePhaseReportUseCase, UpdatePhaseReportUseCase, DeletePhaseReportUseCase, ListPhaseReportsUseCase, GetPhaseReportUseCase"` AND `cd Backend && python -c "from app.application.services.phase_report_pdf import render_pdf; from app.domain.entities.phase_report import PhaseReport; r = PhaseReport(project_id=1, phase_id='nd_a1b2c3d4e5'); b = render_pdf(r, 'P', 'F'); assert b.startswith(b'%PDF')"` AND `cd Backend && pytest tests/unit/application/test_manage_phase_reports.py tests/unit/application/test_phase_report_pdf.py -v --tb=short` | ✅ author | ⬜ pending |
| 09-08-01 | 09-08 | 2 | API-01, API-10 | T-09-08-01..06 | Idempotency cache (D-50/D-53) provides rate-limit + key→response map; WorkflowConfig Pydantic enforces D-55 business rules incl. cycle detection (Kahn's algo, feedback exempt); `acquire_project_lock_or_fail` uses `pg_try_advisory_xact_lock` with 63-bit mask; ExecutePhaseTransitionUseCase orchestrates 7-step D-01..D-12 flow incl. CriteriaUnmetError/PhaseGateLockedError | unit | `cd Backend && python -c "from app.application.services.idempotency_cache import check_rate_limit, lookup, store, record_request, reset_for_tests; reset_for_tests(); assert check_rate_limit(1,1) is None; record_request(1,1); assert check_rate_limit(1,1) is not None"` AND `cd Backend && python -c "from app.application.dtos.workflow_dtos import WorkflowConfig; wf = WorkflowConfig(mode='flexible', nodes=[{'id':'nd_a1b2c3d4e5','name':'N','x':0,'y':0,'color':'#888'}], edges=[], groups=[])"` AND `cd Backend && python -c "from app.application.services.phase_gate_service import acquire_project_lock_or_fail; from app.application.use_cases.execute_phase_transition import ExecutePhaseTransitionUseCase; from app.application.dtos.phase_transition_dtos import PhaseTransitionRequestDTO, PhaseTransitionResponseDTO"` AND `cd Backend && pytest tests/unit/application/test_idempotency_cache.py tests/unit/application/test_workflow_validation.py tests/unit/application/test_phase_gate_use_case.py -v --tb=short` | ✅ author | ⬜ pending |
| 09-08-02 | 09-08 | 2 | API-01, API-10 | T-09-08-07..10 | POST /projects/{id}/phase-transitions returns 200/409/422/400/429 per matrix; idempotency-key cached response on retry; mounted in main.py | integration | `cd Backend && python -c "from app.api.v1.phase_transitions import router; assert router.routes"` AND `grep -n "phase_transitions" Backend/app/api/main.py` AND `cd Backend && python -c "import app.api.main; assert any('phase-transitions' in str(r.path) for r in app.api.main.app.routes)"` AND `cd Backend && pytest tests/integration/api/test_phase_transitions_api.py tests/integration/api/test_workflow_validation_api.py -v --tb=short` | ✅ author | ⬜ pending |
| 09-09-01 | 09-09 | 2 | API-02, API-03 | T-09-09-01..04, T-09-09-07, T-09-09-08 | audit_repo gains denormalized `get_project_activity` (LEFT JOIN users for user_name/avatar) + `get_recent_by_user`; project_repo gains `list_by_member_and_status` + `count_by_member`; task_repo gains `count_active_by_assignee` + `count_completed_since`; GetUserSummaryUseCase uses `asyncio.gather` for 3-parallel queries; `include_archived=true` adds ARCHIVED to status set (D-49) | unit | `cd Backend && python -c "from app.infrastructure.database.repositories.audit_repo import SqlAlchemyAuditRepository; assert hasattr(SqlAlchemyAuditRepository, 'get_project_activity') and hasattr(SqlAlchemyAuditRepository, 'get_recent_by_user')"` AND `cd Backend && python -c "from app.infrastructure.database.repositories.project_repo import SqlAlchemyProjectRepository; assert hasattr(SqlAlchemyProjectRepository, 'list_by_member_and_status') and hasattr(SqlAlchemyProjectRepository, 'count_by_member')"` AND `cd Backend && python -c "from app.infrastructure.database.repositories.task_repo import SqlAlchemyTaskRepository; assert hasattr(SqlAlchemyTaskRepository, 'count_active_by_assignee') and hasattr(SqlAlchemyTaskRepository, 'count_completed_since')"` AND `cd Backend && python -c "from app.application.use_cases.get_user_summary import GetUserSummaryUseCase; from app.application.use_cases.get_project_activity import GetProjectActivityUseCase; from app.application.use_cases.manage_teams import SetTeamLeaderUseCase, GetLedTeamsUseCase"` AND `cd Backend && pytest tests/unit/application/test_get_user_summary.py -v --tb=short` | ✅ author | ⬜ pending |
| 09-09-02 | 09-09 | 2 | API-02, API-03 (also D-17 led-teams) | T-09-09-01, T-09-09-03, T-09-09-05, T-09-09-06 | GET /projects/{id}/activity returns `{items, total}` with type[]/user_id/date_from/date_to/limit/offset filters; GET /users/{id}/summary returns `{stats, projects, recent_activity}` with `?include_archived`; PATCH /teams/{id}/leader Admin-only with `extra="forbid"` rejecting unknown keys; GET /users/me/led-teams returns teams + project_ids | integration | `cd Backend && python -c "from app.api.v1.activity import router as r1; from app.api.v1.users import router as r2; print('activity', len(r1.routes), 'users', len(r2.routes))"` AND `grep -n "activity\|users_router\|from app.api.v1 import users" Backend/app/api/main.py` AND `cd Backend && python -c "import app.api.main; paths = [str(r.path) for r in app.api.main.app.routes]; assert any('/activity' in p for p in paths); assert any('/summary' in p for p in paths); assert any('led-teams' in p for p in paths)"` AND `cd Backend && pytest tests/integration/api/test_activity_api.py tests/integration/api/test_user_summary_api.py tests/integration/api/test_teams_leader_api.py -v --tb=short` | ✅ author | ⬜ pending |
| 09-10-01 | 09-10 | 2 | API-07, API-08, API-09 | T-09-10-01..03 | Milestone/Artifact/PhaseReport CRUD routers gated by inline RPTA-equivalent check (Admin OR project.manager_id OR team-leader); `_is_admin` imported from BACK-07 path `app.api.deps.auth` (WARNING-4 fix); Artifact PATCH split into `/mine` (assignee) vs `/{id}` (manager); PDF export returns `application/pdf` + StreamingResponse + per-user 30s rate limit (429 + Retry-After) | integration | `cd Backend && python -c "from app.api.v1 import milestones, artifacts, phase_reports"` AND `cd Backend && python -c "import app.api.main; paths = [str(r.path) for r in app.api.main.app.routes]; assert any('/milestones' in p for p in paths) and any('/artifacts' in p for p in paths) and any('phase-reports' in p for p in paths) and any('/pdf' in p for p in paths)"` AND `cd Backend && pytest tests/integration/api/test_milestones_api.py tests/integration/api/test_artifacts_api.py tests/integration/api/test_phase_reports_api.py -v --tb=short` | ✅ author | ⬜ pending |
| 09-10-02 | 09-10 | 2 | API-04, API-05, API-06 (+D-44 apply-template) | T-09-10-04..08 | GET /projects?status=X delegates to `list_by_status`; GET /tasks/project/{id}?phase_id=X delegates to `list_by_project_and_phase`; PATCH/DELETE /projects/{id}/phase-criteria; PATCH validates phase_id against `process_config.workflow.nodes` (excluding archived) and returns 400 INVALID_PHASE_ID on miss (WARNING-3 / D-19); POST /process-templates/{id}/apply uses per-project advisory lock with bounded 5s wait + partial success `{applied:[], failed:[{project_id, error}]}` | integration | `grep -n "status.*Query\|list_by_status\|phase_id.*Query\|phase-criteria\|/apply" Backend/app/api/v1/projects.py Backend/app/api/v1/tasks.py Backend/app/api/v1/process_templates.py` AND `cd Backend && python -c "import app.api.main; paths = [str(r.path) for r in app.api.main.app.routes]; assert any('phase-criteria' in p for p in paths); assert any('/apply' in p for p in paths)"` AND `cd Backend && pytest tests/integration/api/test_projects_api_phase9.py tests/integration/api/test_tasks_api_phase9.py tests/integration/api/test_process_templates_api_phase9.py -v --tb=short` | ✅ author | ⬜ pending |

*Status legend: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Coverage:** 20 tasks across 10 plans, 3 waves (W0=6, W1=8, W2=6). Every requirement ID
[BACK-01..08, API-01..10] from `requirements:` frontmatter is covered by at least one row.
The two 09-03 support tasks have no requirement of their own but provide infrastructure
(domain exceptions in 09-03-01; test factories + `authenticated_client` fixture in
09-03-02) that downstream rows depend on.

---

## Wave 0 Requirements

Wave 0 establishes shared test infrastructure before feature implementation:

- [x] `Backend/tests/conftest.py` — shared async fixtures (db_session, test_user, test_project, test_team, test_process_template, clean_db) — **planned in 09-03-02**
- [x] `Backend/tests/integration/conftest.py` — Postgres-backed fixtures (test_engine, test_async_session, alembic upgrade to 005) — **planned in 09-03-02**
- [x] `Backend/tests/unit/test_process_config_normalizer.py` — stubs for BACK-07 schema_version normalizer — **planned in 09-04-01 (Wave 1, but stub OK because 09-04 is the only consumer)**
- [x] `Backend/tests/unit/test_workflow_config_validator.py` — stubs for API-10 Pydantic workflow validation — **delivered as `tests/unit/application/test_workflow_validation.py` in 09-08-01**
- [x] `Backend/tests/integration/test_migration_005.py` — idempotency check (run migration twice) — **delivered in 09-01-02**
- [x] `Backend/tests/integration/test_advisory_lock.py` — stub for D-02 pg_advisory_xact_lock concurrency test — **delivered as `tests/integration/api/test_phase_transitions_api.py::test_concurrent_409` in 09-08-02**
- [x] `Backend/tests/factories/` — test data factories for new entities (Milestone, Artifact, PhaseReport) — **planned in 09-03-02**

All Wave 0 deliverables are accounted for in plans 09-01, 09-02, 09-03 (Wave 0) and tested
by their `<automated>` blocks. Wave 1+ work depends on Wave 0 fixtures landing first
(enforced by `depends_on` chain in plan frontmatter).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| PDF visual correctness (fonts, layout) | BACK-06 / API-09 PDF export | Visual fidelity cannot be asserted reliably in CI | Run full suite, open generated `test-output-phase-report.pdf`, verify header/sections/tables render cleanly in Adobe Reader + Preview + browser PDF viewer |
| Turkish locale rendering in PDF | BACK-06 / API-09 | fpdf2 Unicode font embedding visual check | Render report with Turkish fields ("Tasarım", "Kodlama"), confirm no `?` boxes, correct diacritics |
| `process_config` schema_version normalizer backward compat (legacy row) | BACK-07 | Requires pre-v2.0 production snapshot data | Load pg_dump from v1.0 staging, verify normalizer resolves `schema_version=1` default on read, no data loss |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (every row in Per-Task Verification Map carries at least one automated command lifted from the plan)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every task has unit OR integration command)
- [x] Wave 0 covers all MISSING references (all stub deliverables traced to a Wave 0 plan)
- [x] No watch-mode flags
- [x] Feedback latency < 60s (unit) / 180s (full)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ready for executor handoff
