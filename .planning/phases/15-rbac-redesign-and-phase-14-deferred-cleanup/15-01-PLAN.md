---
id: 15-01
phase: 15
plan: 01
type: execute
wave: 0
depends_on: []
requirements: [TIDY-04]
files_modified:
  - Frontend2/components/workflow-editor/editor-page.test.tsx
  - Frontend2/components/workflow-editor/selection-panel.test.tsx
  - Frontend2/components/workflow-editor/workflow-canvas.test.tsx
  - Frontend2/components/workflow-editor/phase-edge.test.tsx
  - Frontend2/components/workflow-editor/use-transition-authority.test.tsx
  - Frontend2/components/lifecycle/milestones-subtab.test.tsx
  - Frontend2/lib/api-client.test.ts
autonomous: true
must_haves:
  truths:
    - "TIDY-04: All 19 previously-failing workflow-editor tests pass after the harness fix (per CONTEXT D-4.1)"
    - "vi.mock(\"@xyflow/react\", ...) blocks include ReactFlowProvider passthrough + useReactFlow stub everywhere they appear"
    - "Position type drift in phase-edge.test.tsx is resolved (mock returns object with Top/Right/Bottom/Left string literals)"
    - "use-transition-authority.test.tsx UseQueryResult shape uses TanStack v5 fields (isPending, not isLoading)"
    - "milestones-subtab.test.tsx spread-arg fixture replaced with explicit shape props (TanStack v5 useQuery return)"
    - "lib/api-client.test.ts compiles cleanly under tsc --noEmit"
    - "cd Frontend2 && npx vitest run components/workflow-editor/ components/lifecycle/milestones-subtab.test.tsx lib/api-client.test.ts exits 0"
  artifacts:
    - path: "Frontend2/components/workflow-editor/editor-page.test.tsx"
      provides: "Extended vi.mock block with ReactFlowProvider + useReactFlow"
      contains: "ReactFlowProvider"
    - path: "Frontend2/components/workflow-editor/use-transition-authority.test.tsx"
      provides: "Test fixture using TanStack v5 UseQueryResult shape"
      contains: "isPending"
  key_links:
    - from: "Frontend2/components/workflow-editor/editor-page.test.tsx"
      to: "@xyflow/react"
      via: "vi.mock factory"
      pattern: "vi\\.mock\\(.@xyflow/react"
verification_command: "cd Frontend2 && npx vitest run components/workflow-editor/ components/lifecycle/milestones-subtab.test.tsx lib/api-client.test.ts"
---

<objective>
Plan 15-01 — Wave 0a TIDY-04: workflow-editor harness fix.

Restore green-baseline for the 19 pre-existing workflow-editor + neighbor test failures by extending the @xyflow/react vi.mock block with the missing `ReactFlowProvider` passthrough and `useReactFlow` stub, plus fixing 4 TS-error/contract-drift test files (phase-edge Position drift, use-transition-authority TanStack v5 UseQueryResult cast, milestones-subtab spread-arg fixture, lib/api-client TS error).

**Purpose:** Phase 15 RBAC plans (15-04+) need a green Frontend2 vitest baseline so regressions are detectable. Without TIDY-04, every "is this green?" answer is ambiguous (per CONTEXT D-4.6 + RESEARCH Summary line 9).

**Output:** 19 previously-failing workflow-editor tests green; full Frontend2 vitest run clean for the workflow-editor surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-rbac-redesign-and-phase-14-deferred-cleanup/15-CONTEXT.md
@.planning/phases/15-rbac-redesign-and-phase-14-deferred-cleanup/15-RESEARCH.md
@.planning/phases/15-rbac-redesign-and-phase-14-deferred-cleanup/15-PATTERNS.md
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/deferred-items.md

<interfaces>
<!-- Production code shape executor must respect (already shipped, do NOT change) -->

From Frontend2/components/workflow-editor/workflow-canvas-inner.tsx:127-134:
```typescript
// Production code wraps <ReactFlowProvider> already; only the test mocks bypass it.
return (
  <ReactFlowProvider>
    <ReactFlowInner ... />
  </ReactFlowProvider>
)
```

From Frontend2/components/workflow-editor/editor-page.test.tsx:83-111 (CURRENT mock — INCOMPLETE):
```typescript
vi.mock("@xyflow/react", () => ({
  ReactFlow: ({ children, onNodesChange, onNodeDragStart, onNodeDrag, onNodeDragStop }) => {
    capturedHandlers.onNodesChange = onNodesChange
    capturedHandlers.onNodeDragStart = onNodeDragStart
    capturedHandlers.onNodeDrag = onNodeDrag
    capturedHandlers.onNodeDragStop = onNodeDragStop
    return <div data-testid="reactflow">{children}</div>
  },
  Background: () => <div data-testid="bg" />,
  MiniMap: () => <div data-testid="minimap" />,
  Controls: () => <div data-testid="controls" />,
  Handle: () => null,
  Position: { Top: "top", Right: "right", Bottom: "bottom", Left: "left" },
  BaseEdge: () => null,
  EdgeLabelRenderer: () => null,
  getBezierPath: () => ["", 0, 0, 0, 0],
}))
```

The TWO missing keys (per RESEARCH Pattern 6 + Pitfall 3) are `ReactFlowProvider` and `useReactFlow`.

TanStack Query v5 `UseQueryResult` shape (per RESEARCH Pitfall 20):
- v4: `{data, isLoading, isError, error, isSuccess, refetch, ...}`
- v5: `{data, isPending, isError, error, isSuccess, refetch, ...}` — `isLoading` REMOVED for new queries; `isPending` is canonical.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend @xyflow/react vi.mock with ReactFlowProvider + useReactFlow + fix Position type drift across 3 workflow-editor test files</name>
  <files>Frontend2/components/workflow-editor/editor-page.test.tsx, Frontend2/components/workflow-editor/selection-panel.test.tsx, Frontend2/components/workflow-editor/workflow-canvas.test.tsx, Frontend2/components/workflow-editor/phase-edge.test.tsx</files>

  <read_first>
    - Frontend2/components/workflow-editor/editor-page.test.tsx (current vi.mock block at lines 83-111 — incomplete)
    - Frontend2/components/workflow-editor/selection-panel.test.tsx (existing vi.mock structure for parity)
    - Frontend2/components/workflow-editor/workflow-canvas.test.tsx (readOnly forwarding tests; verify mock block presence)
    - Frontend2/components/workflow-editor/phase-edge.test.tsx (current Position usage — likely uses enum import)
    - Frontend2/components/workflow-editor/workflow-canvas-inner.tsx (lines 127-134 — production ReactFlowProvider mount; do NOT modify)
    - .planning/phases/15-rbac-redesign-and-phase-14-deferred-cleanup/15-RESEARCH.md (Pattern 6 ReactFlowProvider Test Wrapper section)
    - .planning/phases/15-rbac-redesign-and-phase-14-deferred-cleanup/15-PATTERNS.md (§18 vitest mock extension)
  </read_first>

  <action>
**1.1 — editor-page.test.tsx (lines 83-111, extend the vi.mock block):**

Add TWO new keys to the existing `vi.mock("@xyflow/react", () => ({ ... }))` factory return object — placed BEFORE `Background:` for visibility:

```typescript
ReactFlowProvider: ({ children }: { children: React.ReactNode }) => (
  <div data-testid="rf-provider">{children}</div>
),
useReactFlow: () => ({
  zoomIn: vi.fn(),
  zoomOut: vi.fn(),
  fitView: vi.fn(),
}),
```

Keep all existing keys (`ReactFlow`, `Background`, `MiniMap`, `Controls`, `Handle`, `Position`, `BaseEdge`, `EdgeLabelRenderer`, `getBezierPath`) UNCHANGED.

**1.2 — selection-panel.test.tsx (Test 5 fix):**

If selection-panel.test.tsx contains its own `vi.mock("@xyflow/react", ...)`, apply the same two-key extension. If it imports/reuses editor-page.test.tsx's mock via `vi.hoisted` or shared mock factory, no edit is needed. Read first to determine. Per CONTEXT D-4.1: "selection-panel.test.tsx Test 5 fix" — likely a Provider-related crash inside one specific test.

**1.3 — workflow-canvas.test.tsx (2 readOnly tests):**

Apply the same vi.mock extension (ReactFlowProvider + useReactFlow). Plus: verify the readOnly prop is forwarded through the mock — if the mock's `ReactFlow` component drops `readOnly`, capture it like the existing handler captures (`capturedHandlers.readOnly = readOnly`).

**1.4 — phase-edge.test.tsx (Position type drift):**

Per RESEARCH Pitfall + PATTERNS §18: the `vi.mock("@xyflow/react")` returns `Position` as a plain object `{ Top: "top", Right: "right", Bottom: "bottom", Left: "left" }`. Production code typed as `Position` enum. Test uses `Position.Top` which under v12 mock returns the string `"top"`.

The fix: ensure the mock object literal `Position: { Top: "top", Right: "right", Bottom: "bottom", Left: "left" }` is present in phase-edge.test.tsx mock (mirror the editor-page mock). If the test asserts via type comparison rather than string, cast in the test: `Position.Top as unknown as Position` OR import the actual `Position` enum from @xyflow/react in the test file (not from mock).

**Quality bar (D-00):** Do NOT introduce new imports or refactor unrelated tests. Surgical edits only. Each file diff should be ≤ 20 lines net.
  </action>

  <verify>
    <automated>cd Frontend2 && npx vitest run components/workflow-editor/editor-page.test.tsx components/workflow-editor/selection-panel.test.tsx components/workflow-editor/workflow-canvas.test.tsx components/workflow-editor/phase-edge.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `Frontend2/components/workflow-editor/editor-page.test.tsx` contains the literal string `ReactFlowProvider:`
    - `Frontend2/components/workflow-editor/editor-page.test.tsx` contains the literal string `useReactFlow:`
    - `Frontend2/components/workflow-editor/editor-page.test.tsx` contains the literal string `data-testid="rf-provider"`
    - `cd Frontend2 && npx vitest run components/workflow-editor/editor-page.test.tsx` exits 0
    - `cd Frontend2 && npx vitest run components/workflow-editor/selection-panel.test.tsx` exits 0
    - `cd Frontend2 && npx vitest run components/workflow-editor/workflow-canvas.test.tsx` exits 0
    - `cd Frontend2 && npx vitest run components/workflow-editor/phase-edge.test.tsx` exits 0
    - Production file `Frontend2/components/workflow-editor/workflow-canvas-inner.tsx` is NOT in the diff (`git diff --stat HEAD~1` shows only test files)
  </acceptance_criteria>

  <done>
    - 16 editor-page tests + 1 selection-panel Test 5 + 2 workflow-canvas readOnly tests + phase-edge tests all green
    - vi.mock block in editor-page.test.tsx contains ReactFlowProvider and useReactFlow keys
    - No production code changed (verified via git diff)
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix UseQueryResult v5 cast + spread-arg fixture + lib/api-client TS error</name>
  <files>Frontend2/components/workflow-editor/use-transition-authority.test.tsx, Frontend2/components/lifecycle/milestones-subtab.test.tsx, Frontend2/lib/api-client.test.ts</files>

  <read_first>
    - Frontend2/components/workflow-editor/use-transition-authority.test.tsx (existing mock with isLoading)
    - Frontend2/hooks/use-transition-authority.ts (production hook — confirms TanStack v5 import)
    - Frontend2/components/lifecycle/milestones-subtab.test.tsx (existing fixture with `{...mockUseQuery}` spread pattern)
    - Frontend2/hooks/use-milestones.ts (return shape; verify v5 fields)
    - Frontend2/lib/api-client.test.ts (current TS error — likely missing import or wrong type)
    - Frontend2/lib/api-client.ts (production module under test; do NOT modify)
    - .planning/phases/15-rbac-redesign-and-phase-14-deferred-cleanup/15-RESEARCH.md (Pitfall 20 UseQueryResult cast)
    - .planning/phases/15-rbac-redesign-and-phase-14-deferred-cleanup/15-PATTERNS.md (§19 UseQueryResult v5 cast)
  </read_first>

  <action>
**2.1 — use-transition-authority.test.tsx (UseQueryResult v5 cast):**

TanStack Query v5 changed `UseQueryResult` shape — `isLoading` was removed for new queries; `isPending` is canonical. Locate every mock that returns `{ data, isLoading: false, ... }` and rename `isLoading` → `isPending`. If type-error mentions missing `isPending` field, add the explicit v5 minimal shape:

```typescript
{
  data: <whatever>,
  isPending: false,
  isError: false,
  isSuccess: true,
  error: null,
  // refetch, status, fetchStatus etc. NOT required if test does not consume them
}
```

If tests use `as UseQueryResult<T>` casts, replace with `as unknown as UseQueryResult<T>` to satisfy TypeScript without a full v5 interface dump. Reference: TanStack v5 `useQuery` docs (linked in RESEARCH Sources).

**2.2 — milestones-subtab.test.tsx (spread-arg fixture):**

The TanStack v5 hook return shape change broke fixture spreading. Find every `{...mockUseQuery}` or `{ ...mockResult }` pattern and replace with explicit destructuring:

```typescript
// BEFORE (v4 spread relied on isLoading present):
const mockResult = { data: [...], isLoading: false }
;(useMilestones as Mock).mockReturnValue({ ...mockResult })

// AFTER (v5 explicit):
;(useMilestones as Mock).mockReturnValue({
  data: [...],
  isPending: false,
  isError: false,
  isSuccess: true,
  error: null,
})
```

If multiple test cases share the fixture, define a helper at top of file:

```typescript
const v5Result = <T,>(data: T) => ({
  data,
  isPending: false,
  isError: false,
  isSuccess: true,
  error: null,
})
```

**2.3 — lib/api-client.test.ts (TS error):**

Read the file first to identify the exact TS error. Common shapes per RESEARCH §19:
- Missing import for a re-typed function/class
- Axios v1.x AxiosError vs v0.x Error
- TanStack v5 retry function signature change

Apply minimal-surgery fix. If it's a missing import, add it. If it's a type mismatch, cast or update the test to match production type. Do NOT modify production `lib/api-client.ts`.

**Quality bar (D-00):** Avoid touching test logic; only fix type-shape compatibility. Each file diff ≤ 30 lines net.
  </action>

  <verify>
    <automated>cd Frontend2 && npx vitest run components/workflow-editor/use-transition-authority.test.tsx components/lifecycle/milestones-subtab.test.tsx lib/api-client.test.ts && cd Frontend2 && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `Frontend2/components/workflow-editor/use-transition-authority.test.tsx` contains the literal string `isPending`
    - `Frontend2/components/workflow-editor/use-transition-authority.test.tsx` does NOT contain the literal string `isLoading: false` (replaced with `isPending: false`)
    - `Frontend2/components/lifecycle/milestones-subtab.test.tsx` does NOT contain `...mockUseQuery` style spread for hook returns (use explicit shape)
    - `cd Frontend2 && npx vitest run components/workflow-editor/use-transition-authority.test.tsx` exits 0
    - `cd Frontend2 && npx vitest run components/lifecycle/milestones-subtab.test.tsx` exits 0
    - `cd Frontend2 && npx vitest run lib/api-client.test.ts` exits 0
    - `cd Frontend2 && npx tsc --noEmit` exits 0 (no remaining TS errors in these files)
    - Production files `Frontend2/lib/api-client.ts`, `Frontend2/hooks/use-transition-authority.ts` are NOT in the diff
  </acceptance_criteria>

  <done>
    - All 3 files pass tests
    - tsc --noEmit clean for these files
    - 19 originally-failing tests (16 editor-page + 1 selection-panel + 2 workflow-canvas) ALL green together with Task 1
    - No production code changed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/CI test runner → vitest harness | Test mocks substitute for production providers; bypassing the production Provider can leak un-mocked behavior into other tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | DoS (Last-admin lockout) | n/a (test infra) | accept | Out of scope for Plan 15-01; tracked under Plan 15-05/15-11. |
| Test-01 | Tampering | vi.mock factory in editor-page.test.tsx | mitigate | Mock is module-scoped (vitest hoisting). New keys (ReactFlowProvider, useReactFlow) shadow real exports inside test bundle ONLY; production bundle untouched. Verified by acceptance criterion "production file is NOT in the diff". |
| Test-02 | Information disclosure | mocks bleeding into adjacent test files | accept | Each test file owns its `vi.mock` block; no cross-file leak. milestones-subtab/lib/api-client/use-transition-authority each scoped. |
</threat_model>

<verification>
- 19 originally-failing tests now pass (acceptance criterion in each task)
- TanStack v5 type errors resolved (`tsc --noEmit` clean)
- Zero production-code drift (`git diff --stat HEAD~1` shows only test files)
</verification>

<success_criteria>
- TIDY-04 baseline RESTORED: `cd Frontend2 && npx vitest run components/workflow-editor/ components/lifecycle/milestones-subtab.test.tsx lib/api-client.test.ts` exits 0
- No production source modified during this plan (lockstep with deferred-items.md scope)
- Plan 15-04+ can now detect their own regressions against this clean Frontend2 vitest baseline
</success_criteria>

<output>
After completion, create `.planning/phases/15-rbac-redesign-and-phase-14-deferred-cleanup/15-01-SUMMARY.md` per the get-shit-done summary template.
</output>
