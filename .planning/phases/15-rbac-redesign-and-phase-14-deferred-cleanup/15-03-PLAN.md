---
id: 15-03
phase: 15
plan: 03
type: execute
wave: 0
depends_on: []
requirements: [TIDY-01]
files_modified:
  - Frontend2/components/primitives/stat-card.tsx
  - .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/deferred-items.md
autonomous: true
must_haves:
  truths:
    - "TIDY-01: cd Frontend2 && npm run build exits 0 with no StatCard tone enum drift errors (per CONTEXT D-4.5)"
    - "Plan 14-18 Cluster F's StatCard tone fix is verified — either confirmed closed OR fixed forward in this plan"
    - "deferred-items.md TIDY-01 entry marked 'closed by Plan 14-18 Cluster F + verified by Plan 15-03' OR 'fixed forward in Plan 15-03'"
    - "Phase 15 RBAC plans (15-04+) start from a green baseline build"
  artifacts:
    - path: ".planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/deferred-items.md"
      provides: "Closure annotation for TIDY-01"
      contains: "TIDY-01"
  key_links:
    - from: ".planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/deferred-items.md"
      to: "Frontend2/components/primitives/stat-card.tsx"
      via: "TIDY-01 closure annotation"
      pattern: "TIDY-01"
verification_command: "cd Frontend2 && npm run build"
---

<objective>
Plan 15-03 — Wave 0c TIDY-01: StatCard tone enum drift verify-and-close + Phase 15 baseline gate.

Phase 14 14-18 Cluster F most likely already closed the StatCard `tone="warning"` build error at `app/(shell)/reports/page.tsx:158`. Plan 15-03 verifies this with `npm run build` smoke test:

1. **If build green:** Mark TIDY-01 as closed in deferred-items.md with reference to Plan 14-18.
2. **If build red:** Fix forward — extend StatCard tone enum to include `'warning'` OR rename `tone="neutral"`/`tone="danger"` per CONTEXT D-4.5.

**Purpose:** Lock the Phase 15 starting baseline. After Plans 15-01 (vitest harness) + 15-02 (pytest backend) + 15-03 (build smoke), every subsequent RBAC plan can detect its own regressions cleanly.

**Output:** `npm run build` green; TIDY-01 explicitly closed in deferred-items.md; Phase 15 ready to enter Wave 1 RBAC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-rbac-redesign-and-phase-14-deferred-cleanup/15-CONTEXT.md
@.planning/phases/15-rbac-redesign-and-phase-14-deferred-cleanup/15-RESEARCH.md
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/deferred-items.md
@.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-18-PLAN.md

<interfaces>
<!-- Existing StatCard tone enum — DO NOT change without verification -->

From Frontend2/components/primitives/stat-card.tsx (typical shape, current):
```typescript
type StatCardTone = "neutral" | "danger" | "warning" | "success" | ...
// (read the actual enum to confirm — TIDY-01 may already be closed)
```

If `'warning'` is already in the enum, build is green and Plan 15-03 just records the closure. Otherwise, the original Phase 13 P08 drift is still open.

Source-of-truth file: `app/(shell)/reports/page.tsx:158` consumer site of `tone="warning"`.

deferred-items.md TIDY-01 line (current state, verify before edit):
```
TIDY-01: Frontend StatCard tone="warning" build error
File: Frontend2/app/(shell)/reports/page.tsx:158
Symptom: ts(2322) "warning" not assignable to type ...
Status: pending Phase 15 P03 verify-and-close
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run npm run build smoke + close TIDY-01 (verify Plan 14-18 closure OR fix forward)</name>
  <files>Frontend2/components/primitives/stat-card.tsx, .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/deferred-items.md</files>

  <read_first>
    - Frontend2/components/primitives/stat-card.tsx (current tone enum literal type definition)
    - Frontend2/app/(shell)/reports/page.tsx (line 158 — the original tone="warning" consumer site)
    - .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/14-18-PLAN.md (Cluster F — check whether StatCard tone fix was bundled)
    - .planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/deferred-items.md (TIDY-01 current entry text)
    - .planning/phases/15-rbac-redesign-and-phase-14-deferred-cleanup/15-CONTEXT.md (D-4.5 verify-and-close decision)
  </read_first>

  <action>
**1.1 — Run smoke build:**

```bash
cd Frontend2 && npm run build
```

**1.2A — If build PASSES (Plan 14-18 already closed TIDY-01):**

(a) **DO NOT modify** `stat-card.tsx` or `reports/page.tsx` — closure path A (zero change).

(b) **Update deferred-items.md** TIDY-01 entry — Edit the existing TIDY-01 block to read:

```
TIDY-01: Frontend StatCard tone="warning" build error
File: Frontend2/app/(shell)/reports/page.tsx:158 (originally)
Symptom: ts(2322) "warning" not assignable to type ...
Status: CLOSED by Plan 14-18 Cluster F (verified by Plan 15-03 — npm run build green on 2026-04-29)
```

**1.2B — If build FAILS with `tone="warning"` ts(2322) error:**

Two options per CONTEXT D-4.5 — pick (a) extend enum (preferred — `warning` is a valid product tone used by 11+ existing StatCard call sites per CONTEXT note "zaten 11 dosya warning kullanıyor"):

(a) **Extend StatCard tone enum** in `Frontend2/components/primitives/stat-card.tsx`:

```typescript
// BEFORE (suspected — adjust based on Read):
type StatCardTone = "neutral" | "danger" | "success"

// AFTER:
type StatCardTone = "neutral" | "danger" | "warning" | "success"
```

If the StatCard component has tone-driven styling (className map, color tokens), add a `'warning'` branch using existing `--warning` oklch token (consistent with primitive tone conventions in Frontend2/components/primitives/badge.tsx and similar).

(b) **OR rename consumer** `reports/page.tsx:158` from `tone="warning"` → `tone="neutral"` if the visual semantics align (tone="warning" in the original code might have been a Phase 13 drift). Read line 158 context to decide.

After either fix, update `deferred-items.md`:
```
TIDY-01: Frontend StatCard tone="warning" build error
Status: CLOSED by Plan 15-03 (StatCard tone enum extended with 'warning' / consumer renamed; npm run build green on 2026-04-29)
```

**1.3 — Final smoke:**

Re-run `cd Frontend2 && npm run build` to confirm exit 0. If still red, iterate — but the fix is bounded (tone enum is a 5-line change).

**Quality bar (D-00):** Path A (verify-only) is preferred. If forced to fix forward, the fix is mechanical (enum extension). NO new components, NO design changes, NO Tone styling additions beyond the minimum needed for build.
  </action>

  <verify>
    <automated>cd Frontend2 && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `cd Frontend2 && npm run build` exits 0 (TypeScript compile + Next.js build green)
    - `.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/deferred-items.md` contains the literal string `TIDY-01` AND one of: `CLOSED by Plan 14-18` OR `CLOSED by Plan 15-03`
    - `.planning/phases/14-admin-panel-prototype-taki-admin-y-netim-paneli-sayfas-n-n-f/deferred-items.md` contains the literal string `2026-04-29` (closure date stamp)
    - If StatCard was modified: `Frontend2/components/primitives/stat-card.tsx` contains the literal string `"warning"` in the tone enum AND build green
    - If StatCard was NOT modified: `git diff Frontend2/components/primitives/stat-card.tsx HEAD~1` returns empty for stat-card.tsx (path A)
  </acceptance_criteria>

  <done>
    - npm run build exits 0
    - TIDY-01 marked CLOSED in deferred-items.md with verify reference
    - Phase 15 baseline locked (vitest green from 15-01 + pytest green from 15-02 + build green from 15-03)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev/CI build pipeline → Next.js TypeScript compile | TS enum drift can mask production bugs; gate on enum literal correctness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-16 | Tampering (StatCard tone re-introduction during RBAC modal work) | not yet applicable | mitigate | Plan 15-11 RBAC modals consuming StatCard MUST verify build green before commit. Plan 15-03 establishes the green-baseline contract. |
| Build-01 | DoS (build pipeline silently warns instead of erroring) | tsconfig strict mode | accept | Frontend2 tsconfig already strict; ts(2322) is a hard error (build halts). |
</threat_model>

<verification>
- npm run build exits 0
- TIDY-01 closure documented (path A or B)
</verification>

<success_criteria>
- Phase 15 baseline ready: Frontend2 vitest green (15-01) + Backend pytest green (15-02) + Frontend2 build green (15-03)
- Wave 1 RBAC plans (15-04+) have a clean substrate for regression detection
</success_criteria>

<output>
After completion, create `.planning/phases/15-rbac-redesign-and-phase-14-deferred-cleanup/15-03-SUMMARY.md` per the get-shit-done summary template.
</output>
