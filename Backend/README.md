Backend For Software Project Management System

## How to Run

1. Navigate to the `Backend` directory:
   ```bash
   cd Backend
   ```

2. Run the server using uvicorn:
   ```bash
   uvicorn app.api.main:app --reload
   ```

The API will be available at `http://127.0.0.1:8000`.
